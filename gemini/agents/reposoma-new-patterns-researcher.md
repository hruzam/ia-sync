---
name: reposoma-new-patterns-researcher
description: DEPRECATED — use reposoma-deep-researcher. Thin alias for older smoke tests and prompts.
tools: ["*"]
---

# Reposoma New Patterns Researcher (deprecated)

**Use `reposoma-deep-researcher` instead.**

This card remains so existing `@reposoma-new-patterns-researcher` invocations and smoke history stay valid.

Redirect behavior:

1. Read `~/.gemini/agents/reposoma-deep-researcher.md` and follow it entirely.
2. Treat `research_scope` as `tools-beat` unless the user names another scope.
3. Write reports to the path named on the task card.

Do not maintain a second copy of recalibration or Orby-mode instructions here.

> Rewired 2026-07-07: redirect repointed from dead `.agy/agents/…` to the live card above.
> The smoke history this alias preserves is pre-restart (burned-ship era) — **deletion of this
> card is proposed and awaits operator gavel.**
