---
what: Cartan's first cross-frame orientation pass over reposoma, ia-sync, and Nablarva
state: OBSERVATION — deploy-inert; wiring evidence, not registry canon
verified: 2026-08-08
host: office
next:
  - operator decides whether Nablarva receives a logical registry beacon and row
  - test Cartan instruction discovery from a fresh Codex session in each root
  - design the Codex buffering-cycle skill after the orientation wiring is proven
---

# Observation — temple map transfer

## Question

Can a Codex-native participant follow the existing Claude-shaped temple sources without
creating a parallel vault, and can it locate the correct project map from the supplied
roots?

## Sources followed

- `reposoma/AGENTS.md` — temple saddle and seat routing
- `reposoma/temple/README.md` — two-door entry and scoped-reader path
- `reposoma/temple/decisions/{0003,0004,0008,0009}-*.md` — machine provenance,
  logical registry, propagation, and machine-layer namespace
- `reposoma/registry/{README,index}.md` — logical sibling map and point-never-copy law
- `ia-sync/zsh/ai/temple-project-map.zsh` — physical path map
- `ia-sync/{AGENTS,SYNC_DISCIPLINE}.md` — compose-first deployment boundary
- `nablarva/AGENTS.md` — experimental-bed entry and promotion boundary

## Associations

| Supplied root | Frame resolved |
|---|---|
| `reposoma` | Temple/meta-repo. It owns the logical sibling registry and is itself present in the physical map. |
| `ia-sync` | Personal machine-layer composer. It is intentionally reached from temple canon through decisions 0003/0009 rather than registered by a home-rooted path. It owns the physical map. |
| `nablarva` | Experimental project/bed. It is present as `nablarva` and `nablarva.devenv` in the physical map. |

## Finding

The two-map architecture transferred cleanly: `registry/index.md` answers *what is related*;
`temple-project-map.zsh` answers *where it lives on this host*. Cartan did not need a copied
Codex project registry.

One curvature point surfaced: Nablarva is known to the physical map but has no project row
or beacon in the logical registry. This may be intentional lag or missing admission; the
current evidence does not decide which. Because `registry/index.md` already contains
unrelated operator changes and registry admission is canon/gavel work, this pass records the
gap and does not edit the registry.

## Identity result

`@Cartan` is the native Codex seat: moving-frame engineering, invariant preservation without
false runtime parity, and explicit drift detection. `@Symmetry` already owns the Emmy Noether
identity, so Noether was rejected as a collision. Jacquard remains a narrower measured
implementation instrument rather than being inflated into the resident Codex identity.

## Verdict

**PASS with one flagged registry gap.** The shared temple can orient a participating Codex
seat through pointers and local runtime projection; no separate Codex vault is required.

## Fresh-session probe

After targeted deployment of `codex/AGENTS.md` to the office Codex home, one ephemeral
orientation probe was launched from the reposoma root through the canonical
`zsh/ai/codex-run.zsh` wrapper.

- **Identity transfer passed:** the fresh incarnation named its resident seat `@Cartan`
  and cited both the deployed global definition and the reposoma routing entry.
- **Full map response is unproven:** wrapper stdout contained the current nested
  `item.completed` JSON object instead of a cleanly extracted final response, so the
  remaining requested bullets were not available as a reliable test artifact.
- **Economics drift:** reported usage was 258,594 input tokens, 210,176 cached input
  tokens, 7,080 output tokens, and 5,033 reasoning-output tokens. This exceeds the
  relay contract's old 16–45K estimate and requires a dated wrapper/economics refresh
  before another nontrivial cold probe.

The correct next move is to repair and re-verify the wrapper's current JSON event parsing,
not to repeat an expensive probe against a stale observation contract.
