#!/usr/bin/env zsh
# =============================================================================
# KEYBOARD.ZSH — sync/ control panel (aliases + comments ONLY)
# =============================================================================
# Location: ~/.config/zsh/sync/keyboard.zsh
# Sourced by: config.office.zsh (right after sync/guides.zsh)
# LAW (guide-for-builder.md §Architecture rules): aliases and comments only —
# every function body lives in an engine (sync/guides.zsh), never here.
# WP4 retrofit (2026-08-05).
#
# sync-guides is a directly-callable function in sync/guides.zsh — no alias
# needed (matches the temple-mail() / piql-remote() direct-call convention).
# Nothing to alias here yet; this panel exists for the help entry point and
# as the wired-in home for future sync/ commands.
# =============================================================================

alias sync-help='_sync_help'  # this panel
