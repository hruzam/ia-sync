---
title: "guides/ — POINTER STUB (guides moved to the temple, B′ 2026-08-20)"
scope: machine-layer stub
audience: everyone
machine: both
verified: 2026-08-20
---

# guides/ — pointer stub

> **⚕ Cut on the surgical table.** This stub is deployed from `~/ia-sync`. Do not edit in
> place: cut in ia-sync, deploy outward.

## The guides moved home → `~/reposoma/raw.guides/`

**Law (B′, gaveled 2026-08-20):** executable/deployed = surgical table · knowledge/readable
= temple. Guides don't execute — they live in the temple now, one slug folder per topic
(`raw.guides/<slug>/GUIDE.md` + chapter files).

**Read them:** `/guide` (list) · `/guide <slug>` (one guide) · `/guide <slug> <chapter>`.
**Write them:** new slug folder in `raw.guides/` + frontmatter + commit reposoma same
session. Full manual: `/guide guide-writing`. **Never add a guide to this folder.**

Moved 2026-08-20 → their slugs: home/office profiles (`machine-home` · `machine-office`,
services + resource-control are machine-home chapters) · `remote-control` (rewritten,
tmux-free truth) · `gemini-seats` · `temple-mail` · `harness-check` · `tree-snapshot` ·
`regime-session`.

## What still lives HERE (by design — do not move)

| file | why it stays |
|---|---|
| `keyboard.md` | claviature grammar — engine-authoring doc, lives next to the code it governs |
| `claviature.global.spec.md` | `keys` engine design spec — same rule |
| `guide-for-builder.md` | the **LAW** for modifying zsh engines — pattern-read BEFORE writing |
| `codex-relay.contract.md` (+ metadata-scripting) | load-bearing contract for Codex relay seats |
| `sudoers.valet-php.conf` | deployable config artifact, not a guide (attachment of machine-office) |

Rationale: writer rule 4 (*trust disk over doc* → the doc that governs code edits sits
beside the code). Everything else is knowledge and belongs in the temple.
