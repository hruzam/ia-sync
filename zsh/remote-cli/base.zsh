#!/usr/bin/env zsh
# remote-cli signpost — definitions only; safe to source again.
# Loaded by system/base.zsh on both hosts. Relative paths also allow source testing.
source "${${(%):-%N}:A:h}/remote-cli.sh"
source "${${(%):-%N}:A:h}/keyboard.zsh"
