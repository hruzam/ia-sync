---
title: Meeting addendum — per-instance template (fill and paste into the thread)
scope: meeting
chapter-of: meeting
renders-protocol: v3
audience: chair fills; both voices read
machine: both
volatile: yes — one filled copy per meeting, lives in the thread, replaced every time
verified: 2026-09-19
---

# Meeting addendum — template

I sit **below** `participant.md`, never inside it. The protocol is stable and derived from
`/guide meeting`; I am filled fresh for every meeting and discarded with the thread. Slots only —
no default values, so nothing leaks from one meeting into the next. The chair fills every line
before opening; an empty slot means the meeting is not ready.

## Instance

- Date:
- Chair:
- Voices:
- Topic (one line):
- Scenario (from `participant.md` role list, e.g. 02-A / 02-B / 03 / 05 / 06 / 07):

## Roles (verbatim role line per voice — the chair reads these aloud at open)

- Role — <voice 1>:
- Role — <voice 2>:

## Context packs (what each voice was given, and nothing else)

- Context pack — <voice 1>:
- Context pack — <voice 2>:
- Known asymmetry between packs (state it, don't hide it):

## Floor

- Speaking order (who opens):
- Flags open for attack (e.g. `[OPEN]`, `[INFERRED]`, `[NABLA]` markers in the source docs):
- Off-limits (if anything):
- Ends when:

## Output

- Scribe / where the record lands:
- Decision expected (yes / no / none — a mapping meeting is allowed):
