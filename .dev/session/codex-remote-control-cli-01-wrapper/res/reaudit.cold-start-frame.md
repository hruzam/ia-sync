# Cold-start frame — full-process audit of the `runbook-upgrade` multisession

`class: FRAME ONLY — no findings, no conclusions live here. Authored by the 01-wrapper`
`head (trajectory, oStar) per prompt-5; the audit itself is a SISTER session with a`
`FRESH head.`

## scope

Full-process audit of the `runbook-upgrade` multisession: did the arc obey its own
RUNBOOK law (one gate, single status owner, crossed witnesses, promotion discipline),
and did the shipped fan-out chapter + GUIDE edits match what the closing VERDICT claims.

## auditor

A FRESH Oraculum incarnation — explicitly NOT any seat of the 01-wrapper session and NOT
the runbook-upgrade head; buffers of both are contaminated by authorship.

## probe

Cartan via tunnel-spawn (read-only sandbox). Brief it with "NO web search, local reads
only" — the shim's wait-window dies on research-grade turns (01-wrapper evidence,
2026-09-10).

## input

- The audited bed: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/ (RUNBOOK, STATUS,
  _bus/, res/) — read as evidence, never edited.
- This head's final report: /home/hruzam/ia-sync/.dev/session/codex-remote-control-cli-01-wrapper/STATUS.md
  (GATE PASSED block) + raw/trajectory.experience-transfer.2026-09-10.md
- The gaveled chapter: /home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md

## read order

1. runbook-upgrade RUNBOOK.md (the promise) → 2. its STATUS.md (the claim) → 3. its
_bus/ VERDICTs (the receipts) → 4. the reposoma commit named in its gate → 5. only then
the GUIDE diffs themselves.

## verdict shape

PASS / PASS-WITH-SCARS / FAIL + max 5 findings, each as path + one sentence + which law
it touches. One paragraph: what the NEXT multisession should do differently.

## what NOT to read

- `_mail/*/inbox/` anywhere (consumed surfaces — citing them is scar 3)
- dock.md files (uncanonical by flag L9)
- This 01-wrapper session's chat history or tunnel thread — the audit judges disk truth,
  not conversation.
