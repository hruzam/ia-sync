# Experience transfer — codex-remote-control-cli-01-wrapper (oStar head → next cSharp)

`from: trajectory (oStar — un-canonical cSharp: subagents in-window + Codex via tunnel)`
`gate closed: 2026-09-10 · carried to the next head by majkee, never by assumed memory`

## 1 · head_note inherited (carry verbatim)

> head_note: cSharp — this seat authored the RUNBOOK and stays live through the whole arc
> as navigator and status_owner; it may delegate every body of work and receive only
> navigation + test parts; it closes the session if it can.

oStar variant note: subagent spawns in-window + tunnel stored-thread replaced the
cross-runtime wake hub. It held. The posture is the law; the instrument is weather.

## 2 · read before authoring (absolute paths)

- /home/hruzam/ia-sync/.dev/session/codex-remote-control-cli-01-wrapper/RUNBOOK.md + STATUS.md
- /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md (four scars — free)
- /home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md (stored thread; NOT flat — v1 RUNBOOK note was stale)
- /home/hruzam/ia-sync/devices/_shared/termux-bootstrap.md + session.next.md (the JIT law)
- /home/hruzam/ia-sync/machines.json (the ONLY host-slot registry; add machine = add row)

## 3 · rules that already bit someone (counter-signed, do not re-derive)

- The classifier blocks agents writing PC `authorized_keys` — CORRECTLY (session.next.md
  predicted it). Operator pastes; prepare the exact line for him.
- Tunnel shim's wait-window dies on research-grade turns (2 interrupted turns). Brief
  Codex with "NO web search, local reads only" and it holds. Tunnel v1 candidate.
- `claude` is absent from non-interactive SSH PATH on home → absolute binaries in every
  wrapper command, no exceptions (Cartan caught the head's own law broken on bare tmux).
- Devices never pull; all trust flows PC → device; adb over the home host is
  doctrine-aligned and live (authorized 2026-09-10) but Xiaomi gates input-injection
  behind a Mi-account toggle — deliberately NOT enabled; approvals stay human.

## 4 · scars, priced

- Forgotten key passphrase cost a full rotation ceremony (keygen + operator paste on two
  PCs + verify). Price paid once: passphrase lives in operator's head; ssh-agent
  once-per-boot in bin/bed kills the retype pain that caused the forgetting.
- README round 1 shipped INVERTED battery advice ("disable No restrictions") — a
  factual-safety inversion from a clean executor. Price: review every executor claim
  against observed evidence, not spec memory.
- Counter-sign round 3: witness kept NOK-ing a real-but-priced race. Resolution that
  worked: head DECLINES with written rationale in the code, witness re-judges the ruling
  ("judge whether the accepted risk is tolerable, not whether it exists") → OK. A witness
  is not a veto; a silent ship is not a ruling.

## 5 · the one lesson

**The bed is immortal; viewports are confetti.** Every mobile-side mechanism must assume
the connection dies mid-gesture (app-switch, lock, freezer) — and that this is FINE,
because the session's life is on the host. Design reconnect as a reflex (retry-on-255,
agent-once, one-tap attach), never as an incident.
