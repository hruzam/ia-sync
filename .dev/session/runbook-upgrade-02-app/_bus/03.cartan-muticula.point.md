---
cycle: "03"
turn: "01"
from: cartan-muticula
to: trajectory-dashboard
scope: Execute the authorized fixture-only B0 Claude lane and distinguish headless hook behavior from interactive protection.
paths:
  read:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.verdict.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/cartan.b0-go.2026-09-25.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/coordination.germline-00-home.2026-09-25.md
    - /home/hruzam/reposoma/raw.guides/bus/GUIDE.md
  write:
    - /tmp/muticula-b0-claude-<unique>/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/b0-claude/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/03.trajectory-dashboard.return.md
  metadata_exception: Own presence attachment on this bed through board mark/unmark; record and detach only the returned exact ID, with a lane-private RB_STATE ledger.
gates:
  - Majkee's GO is recorded in raw/cartan.b0-go.2026-09-25.md; resolve trajectory-dashboard and RUNBOOK prompt-1 before work.
  - claude -p is B0 test instrumentation only; no product implementation or dependency on print mode.
  - No deploy.sh, global settings/trust edits, product source, shared core/store, germline changes, Git staging/commit/push or sibling-lane edits.
  - At most eleven fresh main sessions and two total bounded children for the two subagent-mode cases; no automatic retries beyond this bound.
  - Preserve other writers; publish only the assigned RETURN and evidence package, never STATUS or another receipt.
done_when:
  - Actual version, flags, effective hook layers, project trust and exact fixture path are recorded without authentication data.
  - The nine headless cases and two interactive confirmations have measured outcomes or individually evidenced limitations.
  - Held/unheld hashes, actual tool names, hook inputs/outputs and tool decisions establish whether the guard or some other permission layer caused each result.
  - The mode-aware matrix separates observed denial, allowed failure/bypass, unsupported coverage and untested behavior; it claims no concurrent reservation proof.
  - The six-field RETURN cites the durable fixture scripts/config/logs, records the exact presence cleanup and leaves no test process mutating the fixture.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/03.trajectory-dashboard.return.md
---

# Dispatch — B0 Claude

The GO is effective. This POINT is your exact scoped dispatch; it does not depend on another
approval of the already-authorized B0 packet. The other lane is cycle 04, seat harness-builder.
Independent witness: muticula-verifier, writing `03.muticula-verifier.verdict.md` after your
RETURN. Bind the RETURN to cycle 03 and this POINT; do not echo a trusted `turn:` into it.

Create a unique disposable Git fixture with one held sentinel, one unheld sentinel and
`held.txt`. Use a synchronous path-aware Edit/Write guard with an explicit two-second timeout;
record actual tool names instead of assuming a legacy list. Keep guard code and effective
fixture settings as evidence. Capture only fixture-generated inputs/outputs and relevant
settings, never credentials or unrelated session history.

Use `--model haiku`, `--permission-mode acceptEdits`, `--setting-sources project,local` and
fixture `--settings`. Allow Agent for the child test. Verify whether those flags and project
hook trust take effect in each mode. Observe normal permission outcomes rather than treating
absence of a prompt as proof that a hook denied a write. `--bare` deliberately changes hook
loading; record effective layers/flags for that case rather than assuming isolation parity.

| Case | Mode | Required observation |
|---|---|---|
| healthy held deny + unheld control | print | Guard decision, actual tool outcome and hashes of both sentinels |
| one child's held edit | print | Whether the hook fires, actual parent/child identity fields and held hash |
| guard sleeps past two-second timeout | print | Tool continuation/refusal and bytes; no assumed fail-closed behavior |
| guard exits 1 without valid JSON | print | Error/permission handling and resulting bytes |
| malformed guard output | print | Parser/error handling and resulting bytes |
| missing guard script | print | Invocation failure and resulting bytes |
| disableAllHooks | print | Whether hook execution is skipped and whether the write occurs |
| shell redirection to held path | print | Event visibility versus deliberately uncovered Edit/Write guard scope |
| --bare | print | Documented skip-hooks behavior checked against actual logs and bytes |
| healthy held deny + unheld control | interactive | Same fixture/healthy guard as print baseline; record actual TUI trigger |
| one child's held edit | interactive | Same fixture/healthy guard; actual subagent event/identity and held hash |

Reset fixture sentinels between cases and prove each before-state. Interactive confirmations
run in an isolated test pane/session operated by you or majkee; never inject prompts into an
unrelated live pane. If operator input is required, preserve the unfinished case and return
the exact required action. A mode difference is a finding. A fixture-local guard never proves
protection against arbitrary shell/editor writes or atomic cross-actor ownership.

Report to the six BUS headings with one evidence matrix in `raw/b0-claude/`. Keep fixtures until
the witness has sufficient reproducible evidence; stop only your own test processes. Announce
unexpected write-scope needs before expanding them. The interactive/headless separation and
test-only policy apply even if the probes succeed.
