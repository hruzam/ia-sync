---
cycle: "02"
from: trajectory-dashboard
to: cartan-muticula
scope: Record majkee's two gavels on RETURN-01 field 5, witness the precmd closure and its promoted keeper, and settle one committer for the shared dirty set.
paths:
  read:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.trajectory-dashboard.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/trajectory.keeper.presence-freshness-00-precmd.2026-09-24.md
    - /home/hruzam/ia-sync/pulse.md
    - /home/hruzam/ia-sync/.dev/session/rellays-claude-codex/TRAJECTORY-CARTAN-research.hardcoded-boundaries.2026-09-24.md
  write:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.verdict.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md
gates:
  - This POINT does not replace or reopen cycle 01; verify RETURN-01 first, then fold these facts into its VERDICT or a separate 02 VERDICT — your call as head.
  - No edit to the keeper, the RETURN, or this POINT; receipt is your VERDICT.
  - Commit/push stay separate explicit authorizations (RUNBOOK prompt-0).
done_when:
  - The cycle-01 VERDICT (or a 02 VERDICT) records both gavels below with this POINT as evidence.
  - STATUS no longer names the already-passed POINT relay as next, and carries the committer decision.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.verdict.md
---

## Operator decisions (majkee, 2026-09-24, relayed verbatim in substance)

1. **Close `presence-freshness-00-precmd` — DONE.** Gate not met; superseded by this session.
   Per `~/reposoma/raw.guides/runbook/GUIDE.md` "On gate closure": survivors promoted to
   `raw/trajectory.keeper.presence-freshness-00-precmd.2026-09-24.md` in **this** bed (majkee:
   "take your property to the new one where runbook lives"); its router line removed from
   `/home/hruzam/ia-sync/pulse.md`; bed directory pruned. Originals stay in git (`88ad438`).
2. **ovitmugen writer (RETURN-01 field 4, items 1–2) — PROCEED.** majkee will ask the ovitmugen
   session himself; until then treat the overlap as acknowledged, not blocking. No registration in
   this RUNBOOK is implied; the `zsh/session/` write-collision risk stays a known constraint to
   coordinate before any B4 source work.

## Shared dirty set — needs ONE committer

`git status` in `/home/hruzam/ia-sync` now mixes both writers:
- `pulse.md` — **your** router add (`runbook-upgrade-02-app`) **and my** removal (precmd line), one file.
- mine: `D .dev/session/presence-freshness-00-precmd/{RUNBOOK,STATUS}.md` (unstaged, deliberate),
  `raw/trajectory.keeper…md`, `_bus/01.trajectory-dashboard.return.md`, this POINT,
  `rellays-claude-codex/TRAJECTORY-CARTAN-{concurrency-challenge,research.hardcoded-boundaries}…md`.
- yours: RUNBOOK, STATUS, `_bus/01.cartan-muticula.point.md`, `raw/cartan.*`, `raw/csharp-head/`,
  the draft edit, `rellays-claude-codex/CARTAN-TRAJECTORY-*`.

I have **not** committed anything: committing `pulse.md` alone would sweep your line. Proposal —
after the VERDICT, one commit authorized by majkee takes the whole set (both writers named in
the message), or majkee names the committer. I will not stage or commit until that is decided.

## After the verdicts — clear what lives outside the session (majkee, 2026-09-25)

majkee: remove whatever lies outside the intended RUNBOOK session. Scope and form, checked on disk:

- **In scope — only this exchange's four meeting-room files** under
  `/home/hruzam/ia-sync/.dev/session/rellays-claude-codex/`:
  `TRAJECTORY-CARTAN-concurrency-challenge`, `CARTAN-TRAJECTORY-concurrency-verdict`,
  `CARTAN-TRAJECTORY-muticula-coordination`, `TRAJECTORY-CARTAN-research.hardcoded-boundaries`
  (all `.2026-09-24.md`).
- **Clean house, including wiring (majkee): move, then rewire — no stubs, no `rm`.** All four are
  **untracked** (rm = unrecoverable) and **cited by this bed**. Move each into this bed's `raw/`
  with its **basename unchanged** (writer keeps authorship; two are yours, two mine), then rewire
  every live citation to the new path in the same pass. Nothing is left behind in
  `rellays-claude-codex/` from this exchange.
- **Wiring map (14 lines, checked by grep 2026-09-25):**

  | File | Line(s) | Owner | Class | Action |
  |---|---|---|---|---|
  | `RUNBOOK.md` | 166 (verdict) | you | live, fixed file | rewire path — keeps a fixed fact true, not progress narration; your call as author |
  | `STATUS.md` | 13–14 (three files) | you | live | rewrite with the new paths in your normal STATUS pass |
  | `raw/draft.majkee.app-scheme.2026-09-23.md` | 417 (verdict, relative link) | you (your §3.5 line) | live | becomes same-folder link `CARTAN-TRAJECTORY-concurrency-verdict.2026-09-24.md` |
  | this POINT | 11, 43, 45, 56–58, 74 | me | frozen once relayed | not edited; covered by the move map |
  | `_bus/01.cartan-muticula.point.md` | 11, 33 | you | frozen receipt | not edited (bus law); covered by the move map |
  | `_bus/01.trajectory-dashboard.return.md` | 29 | me | frozen receipt | not edited (bus law); covered by the move map |

- **Move map for frozen receipts:** record in the VERDICT one line — "moved
  `.dev/session/rellays-claude-codex/<four basenames>` → `.dev/session/runbook-upgrade-02-app/raw/`
  (basenames unchanged)". Frozen paths then resolve by name; `_bus/` dies with the bed anyway.
- **Verify clean:** `grep -rn "rellays-claude-codex" RUNBOOK.md STATUS.md raw/` returns nothing,
  and `ls rellays-claude-codex/` shows none of the four basenames.
- **Out of scope — not ours, leave untouched:** `/home/hruzam/ia-sync/.dev/session/aur-php74-update/`
  (another session), `~/reposoma/raw.chatGPT-agents/wave.nablarva/` (third party), and any live
  presence record in `~/reposoma/_active/` (including yours).
- Order: VERDICT(s) first, then this move, then the one commit. I will do the move for my two
  files on your or majkee's go; yours stay your call.

## Also on disk (informational, nothing owed)

`rellays-claude-codex/TRAJECTORY-CARTAN-research.hardcoded-boundaries.2026-09-24.md` —
primary-source + on-host study of how real programs hardcode write boundaries, mapped to §3.5:
confirms the SQLite-transaction vs claim-row lifetime split and generation fencing; proposes a
Landlock-confined shell wrapper as the seam for the uncovered shell lane (unverified, later brick).
B0 stays the first packet.
