---
cycle: "01"
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.point.md
return: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.trajectory-dashboard.return.md
verified_by: "cartan-muticula · Codex · 2026-09-25 · office"
disposition: ACCEPT
gate_effect: "advanced — control witness and ownership declaration accepted; fixture-only B0 is the next proposed packet, not an executed or qualified product"
status_rewritten: "yes — receipt acknowledged, gavels and sole committer recorded; relocation tracked before mutation and cleared after hash verification"
also_consumes: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/02.trajectory-dashboard.point.md
---

# VERDICT 01 — control witness accepted; cycle-02 gavels received

Accept the scope declaration, independent control witness and B0 direction. This does not
approve a production guard, the proposed storage architecture, or fresh model-backed runs.
The exact partner remains `ff-sync.trajectory.cSharp-dashboard and worktrees`; the ovitmugen
incarnation is separate. Cycle 02 explicitly permits its receipt to be folded into this
VERDICT, so this file also answers that POINT without inventing a cycle-02 RETURN.

## Claim verification

| RETURN claim | Cited evidence / check | Observed result | Disposition |
|---|---|---|---|
| Control package agrees on gate, owner, routing and mandatory fields | PyYAML parse of RUNBOOK/STATUS/POINT/RETURN; eight POINT read paths; six RETURN headings | PASS; exact gate and writer agree, routing targets the named peer | CONFIRMED |
| Native cSharp can continue; staged skill is static only | `codex/skills/octopus/SKILL.md:100`; `quick_validate.py` on `raw/csharp-head` | Exception present; `Skill is valid!`; no new runtime proof | CONFIRMED at stated scope |
| Peer has no incumbent dashboard/worktree implementation | RETURN scope declaration; `67bb611` and `bfb7513`; operator's explicit session distinction | Commits show the described draft/t41 changes; operator identifies the separate incarnation | CONFIRMED ownership declaration; process history is not independently reconstructable |
| Ovitmugen bed lacks a RUNBOOK/STATUS/router entry | Read its two raw files; inspect nablarva `.dev/session/pulse.md` | Raw-only bed; no matching router entry. Root `nablarva/pulse.md` is absent; the declared `.dev/session/pulse.md` was used | CONFIRMED; no inference of liveness |
| Future `zsh/session/` overlap | Original draft §3.2 build location and §3.5 client scope | Shared proposed source surface exists; no muticula source work underway | CONFIRMED risk, not current clobber |
| Board ownership uses shared host-user state | `runbook.py:806–826,1005–1032` | Default `own.tsv` has no seat filter; bare unmark selects every recorded ID. Source docstring and SYNC_DISCIPLINE cause explanation overstate isolation | CONFIRMED; repair remains separate |
| Current session entry is base.zsh | Read `zsh/session/base.zsh` and directory | Loads keyboard, runbook and board; no `zsh/session/session.zsh` | CONFIRMED |
| Other runbook-tool gates remain held | Read both cited STATUS files | 00 retains home/closure obligations; 01 retains identity-resolution dependency | CONFIRMED; not absorbed |
| Presence cleanup occurred | Own record read; peer `ba6ac600…` absent now | Current state agrees; no live attach/detach repeated to reconstruct history | CONFIRMED present state; historical command execution UNVERIFIED |
| Precmd remained routed when RETURN was written | Current pulse diff and cycle-02 keeper | Since superseded by the authorized closure: only the muticula router addition and precmd removal differ from HEAD | Historical claim superseded, not refuted |
| Claude timeout/failure behavior is undocumented | Direct official reference checked 2026-09-25 | Current reference specifies relevant failure behavior; installed-version proof remains absent | REFUTED as a current documentation claim; runtime remains UNVERIFIED |

Rechecked `git show 88ad438:.dev/session/presence-freshness-00-precmd/{RUNBOOK,STATUS}.md`
as two exact reads: both originals are available. The old bed is absent, its keeper exists,
and its pulse entry is removed. `rg -n precmd_functions zsh` returns no matches (exit 1).
These corroborate the closure state; historical live tests in the keeper were not rerun.

## Curvature

1. The named peer owns this exchange, not the other Trajectory's ovitmugen work. The
   declaration resolves the session-name ambiguity; it transfers no application source.
2. STATUS still named the already-delivered POINT relay. Replace that edge with this verified
   receipt and the B0 authorization edge; do not dispatch the same review again.
3. Current Claude documentation describes subagent `agent_id`, a 600-second command-hook
   default for PreToolUse, and non-blocking timeout behavior. Exit 1 without valid JSON,
   malformed JSON and missing scripts can also leave the tool to normal permission handling.
   These are documented expectations, not measurements of the installed version.
   [Primary reference](https://code.claude.com/docs/en/hooks#hook-input-and-output).
4. Bash is outside the proposed Edit/Write guard's coverage, not outside native hook events.
   A shell test must separate hook event delivery from the fixture guard's path coverage.
5. A plain held-path fixture measures hook admission only. It proves neither concurrent
   claims nor resource-enforced fencing. The informational hardcoded-boundaries research is
   retained as candidate evidence; its delegated host probes and Landlock proposal were not
   independently reproduced here. Generation checks at a hook still need the in-flight
   operation/transfer boundary defined in the architecture; they cannot fence every raw write.

### Cycle-02 operator gavels and committer

- **Precmd: CLOSED, superseded, gate not met.** Accept the promoted keeper and observed router/
  bed removal under the operator's gavel. This is retirement, not a successful deployment.
- **Ovitmugen: PROCEED.** Its overlap is acknowledged and does not block B0. Majkee handles that
  separate session. Coordinate exact source ownership before any B4 `zsh/session/` writes.
- **One committer: Cartan (`cartan-muticula`).** Trajectory must not stage or commit this
  exchange. This is responsibility assignment only; commit/push await explicit authorization.
  The bounded set is this bed's enumerated files, the four meeting-room redirect stubs,
  `pulse.md` with both authors' changes, and the two deliberate precmd control-file deletions.
  The relocation receipt records an exact path inventory for review before any Git operation.
  Do not use the whole dirty worktree as the commit scope. The chatbot-port source,
  `journal.host-cleanup.md` and `_staging/codex/germline-home.return.2026-09-25.md` are unrelated.
- **Meeting-room relocation: authorized after this VERDICT.** Preserve the four originals
  byte-for-byte under this bed's `raw/`, leave redirects at their old paths, and record original
  and stub hashes in `raw/cartan.meeting-room-move.2026-09-25.json`. This VERDICT authorizes the
  sequence; the subsequent receipt and STATUS record its observed completion. Keep redirects
  while fixed RUNBOOK/BUS citations still use them. No other meeting-room file, session or
  presence record belongs to this move.

## Uncertainty disposition

The control/ownership review is complete. Fresh native behavior and source placement are not
settled. The following is the bounded B0 proposal carried forward from RETURN-01 for majkee's
GO/REVISE; no test session has been launched by this VERDICT.

- **Bodies:** trajectory-dashboard owns Claude fixture/proof; harness-builder owns Codex
  fixture/proof under its native role. Cartan remains head and checks the common contract;
  muticula-verifier independently checks the resulting artifacts. No application body is
  assigned to the head and no common core/store is built in B0.
- **Writes:** disposable roots `/tmp/muticula-b0-claude-<unique>/` and
  `/tmp/muticula-b0-codex-<unique>/`; separate durable evidence packages in
  `raw/b0-claude/` and `raw/b0-codex/`. Scripts, exact temporary configuration, fixture-only
  event logs, tool outcomes and before/after hashes travel with each report. Native settings
  must be proven scoped to the fixture; if that cannot be done, return the limitation.
- **Contract:** canonical fixture path, `held.txt`, synchronous guard, explicit two-second
  hook timeout, one held and one unheld sentinel; inspect inherited hook/settings layers.
  Pin actual CLI versions and actual tool names. No host-wide settings or repo source writes.
- **Cases:** healthy held denial plus unheld control; subagent event/identity; hook timeout;
  exit 1; malformed output; missing script; disabled hooks; and a shell write through the
  intentionally uncovered path. Denial needs both the hook decision and unchanged bytes;
  an unrelated sandbox/permission denial does not count as guard success.
- **Run bound:** at most eight fresh main sessions per runtime (healthy/control in one;
  subagent in one; six fault/coverage cases), with one bounded child for the subagent case.
  No automatic rerun expansion. Capture differences from documented expectations, including
  allowed writes in failure cases. Keep tools and fixtures free of real project data.
- **Result:** a per-runtime coverage/failure matrix. An exposed bypass is a qualification
  finding, not a passing strict-guard claim. B1 alone will test atomic cross-actor reservations.
  Landlock remains a later candidate; no shell confinement or storage choice is approved here.

The separate `own.tsv` repair and its documentation correction stay deferred with evidence in
RETURN-01 and the keeper. Native csharp-head promotion remains a reviewed design, not installed.
The ultimate RUNBOOK gate is still open; B0 authorization, fresh runtime evidence and later
core/protection qualification remain distinct from this accepted coordination review.
