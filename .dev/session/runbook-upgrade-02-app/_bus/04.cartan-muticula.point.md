---
cycle: "04"
turn: "01"
from: cartan-muticula
to: harness-builder
scope: Execute the authorized fixture-only B0 Codex lane using native hooks, with interactive healthy/control and child evidence or an explicit capability limit.
paths:
  read:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.verdict.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/cartan.b0-go.2026-09-25.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/coordination.germline-00-home.2026-09-25.md
    - /home/hruzam/reposoma/raw.guides/bus/GUIDE.md
  write:
    - /tmp/muticula-b0-codex-<unique>/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/b0-codex/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/04.harness-builder.return.md
  metadata_exception: Own presence attachment on this bed through board mark/unmark; record and detach only the returned exact ID, with a lane-private RB_STATE ledger.
gates:
  - Majkee's fixture-only GO is recorded in raw/cartan.b0-go.2026-09-25.md; resolve harness-builder and RUNBOOK prompt-4 before work.
  - Use codex-harness and current official native documentation; no Claude-shaped emulation of Codex hook semantics.
  - No deploy.sh, global settings/trust edits, product source, shared core/store, germline changes, Git staging/commit/push or sibling-lane edits.
  - Never repurpose HOME or CODEX_HOME; isolate through supported process/fixture settings and report a limitation if isolation cannot be proved.
  - At most eight fresh main CLI sessions and one bounded child for the subagent case; no automatic rerun expansion or unassigned delegates.
  - Preserve other writers; publish only the assigned RETURN and evidence package, never STATUS or another receipt.
done_when:
  - Installed version, model, native config/flags, trust and effective hook layers are evidenced; no credentials or global hook configuration enter the evidence package.
  - Native real-trigger results cover the eight cases below or name a separately evidenced limitation for each missing case.
  - Healthy/control and child results are interactive or explicitly unqualified for interactive product use.
  - Fixture hashes, actual hook event inputs/outputs and tool results distinguish guard denial from sandbox/permission denial and unsupported coverage.
  - The six-field RETURN cites the durable fixture scripts/config/logs and matrix, records exact presence cleanup and leaves no test process mutating the fixture.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/04.harness-builder.return.md
---

# Dispatch — B0 Codex

The GO is effective. Apply the shared B0 contract natively. The other lane is cycle 03,
trajectory-dashboard; do not invoke Claude or enter its fixture/evidence scope. Independent
witness: muticula-verifier, writing `04.muticula-verifier.verdict.md` after your RETURN.
Bind the RETURN to cycle 04 and this POINT; do not echo a trusted `turn:` into it.

First resolve the actual CLI and its native hook configuration/isolation capabilities from
local source/help and fetched official documentation. Do not author or deploy a global skill,
hook, config or trust change. A needed capability which cannot be isolated is a qualification
limit, not permission to modify the user's runtime. Existing authentication may be used without
copying secrets into fixtures or evidence. Pin the actual supported model used for small probes.

Create a unique Git fixture with held/unheld sentinels and `held.txt`. Use a synchronous guard,
two-second timeout and actual supported mutation tool names. Use only fixture-local/process
settings. Keep executable scripts, sanitized configuration and real event/result logs in
`raw/b0-codex/`; retain reproducible before/after hashes. Do not simulate the events and label
them as actual CLI evidence.

Eight cases: (1) healthy held denial plus unheld control; (2) one child's held edit and its
actual identity/event scope; (3) hook timeout; (4) exit 1 without a valid decision; (5) malformed
output; (6) missing script; (7) native disabled-hooks/bypass setting, if supported; (8) shell
redirection intentionally outside the path-aware edit guard. Use native equivalents where
they exist and mark absent equivalents unsupported. Distinguish event coverage from enforced
path coverage, including shell/session continuation that receives no new pre-tool event.

Cases 1–2 need fresh interactive Codex sessions for the intended interactive product. A private
PTY/test session is within this fixture assignment; never control an unrelated live terminal
or weaken policy to get a PASS. Cases 3–8 may use native noninteractive CLI instrumentation;
record that mode explicitly. If interactive automation or child invocation cannot be safely
established, return the precise operator action/limit with the evidence already gathered.

Mark presence on this bed under your seat with a lane-private owner ledger and detach only
your returned ID. Stop only your own test processes. A guard failure/bypass remains a finding;
no B0 result proves atomic reservations, strict all-writer coverage or product readiness.
