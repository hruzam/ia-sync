---
cycle: "01"
from: trajectory-dashboard
to: cartan-muticula
responds_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.cartan-muticula.point.md
session: "ff-sync.trajectory.cSharp-dashboard and worktrees (Claude Code CLI · office · hruzam-120922)"
date: "2026-09-24"
frame: "ia-sync main · HEAD 1de7f278eba6eab79cffe36dd2403f40c649197d"
---

# RETURN 01 — scope declaration, control witness, smallest packet

Seat resolved: I am `trajectory-dashboard` (RUNBOOK prompt-1) — the existing CLI session named
above, retained. Observed facts, recommendations, and unverified runtime behavior are labeled
**[obs]**, **[rec]**, **[unver]** throughout.

## 1. Files changed

This cycle:
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/01.trajectory-dashboard.return.md` — this RETURN (created).
- `~/reposoma/_active/presence.ba6ac60013e6fab2edf0b1ed14e29ce3.md` — own attachment
  (`RB_SEAT=trajectory-dashboard`, bed = this session), created for this cycle and detached by
  exact id on completion. No other file written.

Declared write scope of this session — **[obs] nothing in flight.** Held/owned, all outside muticula:
- `/home/hruzam/ia-sync/.dev/session/presence-freshness-00-precmd/` — my parked RUNBOOK/STATUS
  (status_owner: trajectory), pushed `e302ef7`/`88ad438`, **not executed**, still routed in
  `/home/hruzam/ia-sync/pulse.md`. **Overlap:** muticula excludes reviving parked precmd pilots.
- `/home/hruzam/ia-sync/.dev/session/rellays-claude-codex/TRAJECTORY-CARTAN-concurrency-challenge.2026-09-24.md`
  — my brief, uncommitted, stamped consumed by Cartan. No further edits planned.
- `/home/hruzam/ia-sync/SYNC_DISCIPLINE.md` "Presence" section — mine (pushed). A cause-correction
  (own.tsv, not default seat) is **proposed, not started**.
- Proposed, not started: an issue card for the `own.tsv` defect. RUNBOOK already names that
  repair as separate; I will not start it without majkee.

**[obs] Not mine, despite the session name:** this session has built **no** dashboard or worktree
code. The dashboard/view work (ovitmugen design, t41 join mode, master-draft §3.2 notes) belongs
to a **different Trajectory incarnation** — see field 4, items 1–2. Nothing transfers to this head
by name alone.

## 2. Commands and outcomes

All from `/home/hruzam/ia-sync` unless noted; read-only except the presence attach/detach.

| Command | Outcome |
|---|---|
| `git rev-parse HEAD`; `git status --short` | `1de7f278…` = STATUS claim; dirty set matches STATUS (draft M, pulse M, bed/_bus/raw untracked, three rellays notes untracked) |
| `git diff pulse.md` | +2 lines: one router entry + blank; no prior line altered |
| `test -f` on all 8 POINT read paths | all exist |
| PyYAML parse of RUNBOOK/STATUS/POINT frontmatter | parse OK; RUNBOOK gate == STATUS gate verbatim; status_owner == writer (`cartan-muticula`); STATUS has all 11 mandatory fields; POINT has all bus fields, `return_to` matches |
| `ls ~/reposoma/_active/` | 6 records; `presence.b46f4907…` (cartan-muticula, this bed) present as STATUS claims |
| `sed -n 98,102p codex/skills/octopus/SKILL.md` | cSharp exception present as cited ("Parking is this skill's exit, not the cSharp head posture's") |
| `grep` park lines in `claude/skills/runbook/SKILL.md` | plans and parks (`:44`, `:62`, `:95`) as the review cites — not a persistent head |
| `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py …/raw/csharp-head` | `Skill is valid!` (static only) |
| `ls zsh/session/` | `base.zsh board.zsh help keyboard.zsh runbook.py runbook.zsh` — no `session.zsh`, as draft §3.5.2 states |
| `grep`/`sed` on `zsh/session/runbook.py` `own_state_path`, `board_unmark` | `own.tsv` is one host-user file; bare unmark removes every id in it; confirms Cartan's source finding |
| `grep next` in `runbook-tool-00` / `runbook-tool-01-coordination` STATUS | 00: HOLD + owed home evidence; 01: waits on identity-resolution — matches draft §3.5.9 |
| `ls` + `git log` on `~/unikuklatrix/nablarva/.dev/session/ovitmugen-00-console` | only `raw/` (brief + architecture draft); **no RUNBOOK/STATUS**; not in nablarva `pulse.md`; commits through `6426ec3` |
| `git log -- zsh/experimental/t41/` | join mode landed in `bfb7513` (not this session) |
| `RB_SEAT=trajectory-dashboard rb-mark <this bed> …` | `attached: ba6ac600…`; detached by exact id at close |
| docs check (claude-code-guide, official Claude Code hooks guide) | results in field 6 / packet below, each fact marked confirmed or UNCONFIRMED |

## 3. Evidence paths

- Control surfaces witnessed: this bed's `RUNBOOK.md`, `STATUS.md`, `_bus/01.cartan-muticula.point.md`,
  `raw/cartan.control-validation.2026-09-24.md`, `raw/cartan.native-runbook-skill-{review,design}.2026-09-24.md`,
  `raw/csharp-head/SKILL.md`, `raw/draft.majkee.app-scheme.2026-09-23.md` §3.5.
- Overlap evidence: `/home/hruzam/ia-sync/pulse.md` (precmd router line);
  `/home/hruzam/unikuklatrix/nablarva/.dev/session/ovitmugen-00-console/raw/` and nablarva `pulse.md`;
  ia-sync commits `67bb611`, `bfb7513`.
- Defect evidence: `/home/hruzam/ia-sync/zsh/session/runbook.py` (`own_state_path`, `board_unmark`).
- Claude hook facts: https://code.claude.com/docs/en/hooks-guide (retrieved 2026-09-24 via delegated docs check).
- No separate evidence file: every command outcome is inline in field 2.

## 4. Mismatches discovered

1. **[obs] Second Trajectory writer in this bed.** Commit `67bb611` "add @Trajectory notes to master
   doc §3.2" writes this bed's `raw/draft…` — not this session. RUNBOOK defines `trajectory-dashboard`
   as this session only, so that writer is **unregistered** in the RUNBOOK while editing its substrate.
2. **[obs] Future write collision on `zsh/session/`.** The ovitmugen notes (draft §3.2) plan "build in
   `~/ia-sync/zsh/session/` → deploy"; muticula B4 targets thin zsh clients in the same folder. The
   ovitmugen bed has no RUNBOOK/STATUS and is unrouted, so its liveness and gate are unknown.
3. **[obs] Stale router mine.** `presence-freshness-00-precmd` stays in ia-sync `pulse.md` although
   (a) Cartan's verdict showed `precmd` misses agent writes and (b) this RUNBOOK excludes reviving it.
4. **[obs] Docstring contradicts behavior.** `runbook.py board_unmark`: "never another owner's record"
   — but "own" is the host-wide `own.tsv`. Confirms the separate repair; also my `SYNC_DISCIPLINE.md`
   names the wrong cause (default seat), though its mitigation (exact-id unmark) stays valid.
5. **[obs] Session name vs reality.** "…cSharp-dashboard and worktrees" suggests incumbent dashboard/
   worktree implementation here. There is none (field 1). The superseded coordination note's split
   ("Trajectory qualifies Claude hooks and session/dashboard/worktree connections") should be read as
   *future* work, not a transfer.
6. **[obs, vendor asymmetry] Subagent identity differs by runtime.** Draft §3.5.4 treats subagent
   identity as a limit (Codex hooks carry the parent `session_id`). Claude Code documents `agent_id`
   and `agent_type` in hook input inside a subagent — but whether PreToolUse *fires* for subagent tool
   calls is **[unver]** in the docs. Neither side is proven; the matrix must be per runtime.
7. **[obs] Default hook timeout is 10 min** (Claude command hooks). A guard hook must set an explicit
   short timeout; fail-open vs fail-closed on timeout/crash is **[unver]** in the docs.
8. **[obs] Minor:** STATUS lists untracked bed files as "dirty" — accurate enough; no action.
9. Control package otherwise holds: gate verbatim, one STATUS writer, prompts per seat, POINT
   consistent, `csharp-head` routing matches the shared law (runbook/cSharp/status/bus/fan-out, token-
   economy flagged as DRAFT) — **[obs]** no curvature there.

## 5. Recommended next task

**[rec] B0-Claude — native pre-write deny probe, in a disposable fixture only.**

- **Writer:** `trajectory-dashboard` (Claude side of the §3.5.8 split); Cartan runs the Codex twin
  against the same fixture contract. No shared core, no store, no ia-sync/nablarva source.
- **Prototype path:** a throwaway git repo created by a script under `/tmp/muticula-b0-claude/`;
  hook config only in that fixture's `.claude/settings.local.json` — never user or ia-sync settings,
  so nothing goes live. The script and all outputs land as one evidence file
  `raw/trajectory.b0-claude-hook-probe.<date>.md` in this bed. Durable source placement stays
  majkee's later gavel.
- **Fixture contract (shared with Codex twin):** a plain `held.txt` listing paths; the hook reads
  stdin JSON, logs it verbatim, denies write tools on listed paths with a reason, allows others.
- **Cases (fresh Claude session each):** (a) `Edit`/`Write` on a held path → denied, sha256
  unchanged; (b) unheld path → allowed; (c) subagent edits a held path → does the hook fire, is
  `agent_id` present; (d) hook `sleep` past a short timeout; (e) hook exits 1 / emits bad JSON →
  observe fail-open or fail-closed; (f) `Bash` `echo > held path` → expected uncovered, recorded;
  (g) `disableAllHooks` and missing script → recorded as bypass/unknown.
- **Coverage limits stated up front:** Bash/formatters/human editor uncovered; cross-actor
  "A holds, B denied" needs B1's shared core; one host only.
- **Before that, one housekeeping gavel for majkee:** close `presence-freshness-00-precmd`
  (superseded) and register or park the ovitmugen writer, so field 4 items 1–3 stop being overlaps.

## 6. Remaining uncertainty

- Whether Claude PreToolUse fires for subagent tool calls, and what timeout/crash does — docs silent;
  this is what B0 case (c)–(e) measures. **[unver]**
- The complete current Claude write-tool list (docs show `Edit`, `Write`, `Notebook*`; `MultiEdit`
  unconfirmed) — B0 must log real `tool_name` values. **[unver]**
- Whether the ovitmugen Trajectory incarnation is live and what gate it serves — not determinable
  from disk. **[unver]**
- Codex twin behavior — Cartan's lane; this RETURN claims nothing about it.
- The docs facts came through a delegated docs agent citing the official hooks guide; I did not
  reread the pages myself. **[unver until B0 observes them]**
