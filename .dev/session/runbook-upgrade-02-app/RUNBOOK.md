# RUNBOOK: runbook-upgrade-02-app

```yaml
goal: >-
  Build and qualify the first bounded muticula concurrency brick for independent projects,
  using the existing session tools as clients while preserving nablarva's wider design choices.
gate: >-
  Majkee records GO or STOP on an independently witnessed muticula v0 qualification package
  covering unique writer binding, atomic file/subtree reservations, pre-write refusal in fresh
  Claude and Codex sessions, explicit release/recovery, and truthful coverage and integration boundaries.
participant_0: [cartan-muticula, {brand: openai, model: operator-selected, effort: operator-selected}, {host: office, instrument: resident, role: cSharp navigator and sole STATUS owner}]
participant_1: [trajectory-dashboard, {brand: anthropic, model: operator-selected, effort: operator-selected}, {host: office, instrument: mail, role: standing peer witness and Claude/session integration partner}]
participant_2: [muticula-builder, {brand: openai, model: gpt-5.6-terra, effort: high}, {host: office, instrument: resident, role: global implementer used only within an exact delegated scope}]
participant_3: [muticula-verifier, {brand: openai, model: gpt-5.6-sol, effort: high}, {host: office, instrument: resident, role: global verifier; independent acceptance checks; never fixes}]
participant_4: [harness-builder, {brand: openai, model: gpt-5.6-sol, effort: xhigh}, {host: office, instrument: resident, role: native RUNBOOK skill comparison and bounded primitive proposal}]
participant_5: [majkee, {brand: human, model: none, effort: none}, {host: office, role: operator transport and gavel}]
status_owner: cartan-muticula
head_note: >-
  cSharp — this seat authored the RUNBOOK and stays live through the whole arc as navigator
  and status_owner; it may delegate every body of work and receive only navigation + test
  parts; it closes the session if it can.
schema_note: >-
  runbook/GUIDE.md verified 2026-09-17; status/GUIDE.md and bus/GUIDE.md verified 2026-09-05;
  csharp-head-protocol.md gaveled 2026-09-04. Participant aliases are local to this session.
```

## Why this session exists

Several writers can share files and a Git index without noticing each other. Majkee's
briefing named muticula and explored claims, hooks, worktrees and dashboard coordination.
This converts that substrate into one bounded session. The existing directory is reused;
the wider animal's wrapper remains in nablarva and has its own convergence maintainer.

## Fixed scope and authoring boundaries

- Scope: a same-host muticula v0, reusable across projects and usable without a RUNBOOK bed.
  The first brick exposes a CLI/read API and qualifies the native hooks; it specifies the
  dashboard/worktree seams without building a whole UI, broker, or worktree orchestrator.
- Existing foundation: `/home/hruzam/ia-sync/zsh/session/base.zsh`, `runbook.{zsh,py}` and
  `board.zsh`. `RB_ROOT` is a browsing default, not the identity of protected resources.
- Architecture substrate: `/home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/draft.majkee.app-scheme.2026-09-23.md`, §3.5.
  Its technical choices remain candidates until explicitly resolved. Native reuse/retirement
  check is part of the first bounded packet; vendor hooks and worktrees are existing components.
- Cartan owns navigation, directives, STATUS and the router entry. Bodies of implementation
  are delegated whole; exact writer, source paths, fixtures and proof are fixed in each POINT.
- Prototype source placement under nablarva and its first exact write scope require the
  coordinated packet and majkee's gavel before creation; no source location is silently inferred.
- `trajectory-dashboard` means the existing `ff-sync.trajectory.cSharp-dashboard and worktrees`
  CLI session. Its current work remains its own. This head does not replace its other gates.

## prompt-0 — cartan-muticula (persistent cSharp)

```text
You are Cartan, session seat cartan-muticula and cSharp head.
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
and /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md,
then /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md,
/home/hruzam/reposoma/raw.guides/status/GUIDE.md and /home/hruzam/reposoma/raw.guides/bus/GUIDE.md.
Read the current next/recovery edge before issuing work. Resolve native skill parity using
/home/hruzam/ia-sync/claude/skills/runbook/SKILL.md and /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md;
the cSharp protocol keeps this head live after the authoring skill exits.
Coordinate incumbent scopes with Trajectory through numbered POINT/RETURN/VERDICT files under
/home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/. The operator carries absolute paths.
Do not dispatch the same body twice. Before implementation, give majkee one concrete bounded
packet: target resources/tools, prototype location, sole core writer, fixture cases, coverage
limits and evidence needed. Then delegate approved bodies; ordinary bounded code uses the
global implementer, native primitive work harness_builder, independent checks verifier.
Inspect cited artifacts and record dispositions; never self-confirm the product gate.
Keep STATUS current before uncertain mutations and after verification. Keep RUNBOOK fixed.
At closure, preserve evidence, list every raw keeper in a promotion manifest, and leave the
cSharp experience transfer. Commit/push/deploy/prune remain separate explicit authorizations.
```

## prompt-1 — trajectory-dashboard (standing peer)

```text
You are Trajectory, seat trajectory-dashboard for this exchange; retain your current session.
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
and /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md,
/home/hruzam/ia-sync/HANDSHAKE.md, and the exact POINT relayed by majkee from
/home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/_bus/.
Report overlapping active work before accepting a body. Write only the exact RETURN path and
additional artifact scope named by that POINT. Use the six RETURN headings in
/home/hruzam/reposoma/raw.guides/bus/GUIDE.md. A RETURN is receipt; do not edit another writer's POINT.
Witness Cartan's control-state claims against files. Recommend the smallest Claude/session
integration brick and its proof. Do not rewrite STATUS, the master wrapper, or shared code
without an explicit scoped assignment. Coordinate source ownership before parallel writing.
```

## Shared contract for bounded native participants

Each native participant reads the RUNBOOK, current STATUS and its global role contract.
The invoking POINT or delegation brief specifies exact read/write paths and acceptance proof.
Participants preserve unrelated edits: they are not alone and must never revert another writer.
Returns name files, commands, evidence, mismatches, one recommendation and uncertainty.
An implementer never confirms its own gate; a verifier never fixes. No participant commits,
pushes, deploys or operates cross-runtime transport through this assignment.

## prompt-2 — muticula-builder

```text
You are muticula-builder, using the global implementer role.
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
and /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md.
Follow the shared native-participant contract and exact invoking assignment.
Implement only the named body and report its verification evidence to cartan-muticula.
```

## prompt-3 — muticula-verifier

```text
You are muticula-verifier, using the global verifier role.
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
and /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md.
Follow the shared native-participant contract and exact invoking assignment.
Independently test acceptance claims against artifacts. Return findings; never fix the product.
```

## prompt-4 — harness-builder

```text
You are harness-builder, using the global harness_builder role and codex-harness skill.
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/RUNBOOK.md
and /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md.
Follow the shared native-participant contract and exact invoking assignment.
Compare existing native primitives first. Propose the smallest missing behavior before
authoring deployable source; a raw skill draft is design evidence, not an installed skill.
```

## prompt-5 — majkee

```text
Read /home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/STATUS.md for the current edge.
Carry the exact POINT path to the named existing CLI session and return its receipt path.
Record architectural choices and the final GO/STOP explicitly; Cartan records the current state.
```

## Known constraints and destructive holds

- Presence stays advisory; its law, native thread-writer locks and other sessions' records are
  outside implementation scope. Never use bare `rb-unmark`; detach only the owned exact ID.
- The shared board `own.tsv` defect is a separate repair, not muticula's identity mechanism.
- Hooks, storage choice, claim granularity and source layout are not locked by the raw draft.
  No live hook installation, paid/model probe, deploy or cross-host write without its actual gavel.
- **Majkee's policy, 2026-09-25:** `claude -p` / headless print mode is permitted only as a
  disposable B0 test instrument. No muticula product build, adapter, script or later brick
  may invoke or depend on it. B0 test harnesses stay in the fixture/evidence scope and must
  never be promoted into a product dependency. This is an operator constraint, not a claim
  that vendor policy or interactive/headless behavior has been independently established.
- B0 qualifies protection for interactive sessions. Keep headless and interactive evidence
  distinct; confirm healthy held denial plus unheld control, and the subagent case, in the
  interactive runtime. A mode difference is a finding, not an interchangeable result.
- No `deploy.sh` invocation during B0 or germline construction. Fixture-local experiments
  must not ship the concurrent germline/chatbot-port changes or alter global runtime settings.
- No automatic lease expiry, stale-owner takeover, raw shell coverage guess, dirty-tree copying,
  implicit subagent identity, or UI-driven lifetime ownership. Unsupported coverage is explicit.
- Preserve `/home/hruzam/unikuklatrix/nablarva/.dev/session/AGENTS.PROJECT-DESIGN.md` for its
  assigned convergence maintainer. Product canon, other RUNBOOK gates and home config are not ours.

## Acceptance evidence

- A witnessed scope/ownership packet, dated native-capability check and explicit coverage matrix.
- Reproducible simultaneous acquisition, subtree overlap, disjoint-scope, repeated-request,
  stale-baseline, interrupted-operation and release/handoff checks in disposable fixtures.
- Fresh Claude and Codex real-trigger evidence: one writer holds a resource; the other's covered
  mutation is denied before bytes change, in both directions; unknown/broken guard cases are exposed.
- Two unrelated project roots, a task without a bed, and a closed dashboard do not break the core.
  Git-index/deployment sharing and worktree isolation limits have explicit tested or untested dispositions.
- Independent verification of artifacts and majkee's recorded GO or STOP. A STOP preserves the
  evidence and names the missing capability; file existence alone never satisfies the gate.

## References and exclusions

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` and its cSharp/cross-vendor/token-economy chapters.
- `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` only if two full CLI branches are opened.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade-02-app/raw/CARTAN-TRAJECTORY-concurrency-verdict.2026-09-24.md` — initial evidence.
- `/home/hruzam/unikuklatrix/nablarva/.dev/session/flag.md` and `PROJECT.yaml` — later prototype placement and locks.
- Excluded: rebuilding the whole animal/IDE, editing the convergence wrapper, shipping ovitmugen,
  reviving the parked precmd/coordination pilots, automatic worktree management, cross-host service,
  canon promotion or production rollout. These do not become hidden dependencies of local v0.
