```yaml
cycle: 01
from: @atlas-ui · head (session fc.repos-atlas-fable, office)
to: @atlas-ui · fold (session fc.repos-atlas-opus.polyp-medusa, office)
scope: Attach to the shared gate roster-reform-01-triad and complete your card's Batch C inside it — medusa→@Flight, polyp→@Vara (WIDE, not temple-locked), promote /track-run.
paths:
  read:  ~/ia-sync/.dev/session/roster-reform-01-triad/RUNBOOK.md   # your part = prompt-1
         ~/ia-sync/.dev/session/roster-reform-01-triad/STATUS.md
         ~/reposoma/_cold-start/archive/CS.roster-trim-fold.2026-09-02.md   # your card, unchanged
  write: ~/ia-sync/claude/agents/flight.md
         ~/ia-sync/claude/agents/vara.md
         ~/ia-sync/claude/skills/track-run/SKILL.md   # promotion target, does not exist yet
  never: ~/reposoma/pulse.atlas.md   # head holds the pen — deliver ledger text in your RETURN
         ~/ia-sync/claude/agents/houston.md · oraculum.md
         ~/ia-sync/claude/skills/runbook/ · ~/reposoma/raw.guides/runbook/
gates:
  - no write-side git, deploy.sh, rm, cp — operator or @Delta only; everything STAGED
  - the planning-head pointer inside flight.md is the NAME `/runbook` — point, never describe how RUNBOOKs are built (guide + skill own that)
  - nothing duplicated against ~/ia-sync/codex/skills/{medusa,polyp} — role parity via the shared guides, Claude-native expression
  - mode-branch shape of Flight/Vara = @majkee gavel BEFORE editing any live seat (your card's GATE FIRST stands)
done_when:
  - first reply: (a) both files read · (b) your exclusive scope named back · (c) "none" or a list of UNSAVED edits held in your context to any file outside your scope from before this POINT
  - then: flight.md + vara.md folded on the table, /track-run promoted, RETURN written
return_to: ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/01.fold.return.md
```

RETURN = six fields per `~/reposoma/raw.guides/bus/GUIDE.md` + a seventh block
`## pulse-ledger text` — the OPEN LEDGER line and dated entry you want in `pulse.atlas.md`,
verbatim; the head folds it in unchanged. Holds discovered mid-work → mail
`~/reposoma/_mail/atlas/inbox/atlas-fold.<topic>.<date>.md`, do not wait for the RETURN.
