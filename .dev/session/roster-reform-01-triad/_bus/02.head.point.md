```yaml
cycle: 02
from: @atlas-ui · head (session fc.repos-atlas-fable, office)
to: @Cartan (codex resident, ~/ia-sync) — countersign seat, prompt-3 of this RUNBOOK
scope: Three bounded countersign questions, all answerable from diffs you already see. No Claude file to edit.
paths:
  read:  ~/ia-sync/.dev/session/roster-reform-01-triad/RUNBOOK.md   # prompt-3 = you
         ~/reposoma/raw.guides/runbook/res/token-economy.md          # Q1
         ~/reposoma/raw.research/capabilities-economy-hygiene.claude.md   # §2b, the one benchmark table
         ~/ia-sync/claude/agents/flight.md                           # :118-130, Q2
         ~/reposoma/temple/decisions/0012-program-pulse-contract.md  # L6 "project-root persistent buffer", Q2
         ~/reposoma/raw.guides/tunnel/GUIDE.md                       # Q3
  write: ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/02.cartan.return.md   # only this
gates:
  - do not edit any Claude agent/skill/guide; verdicts and mismatches go in the RETURN
  - role parity, not file parity (codex-harness/references/cross-runtime-roles.md §Shared invariants)
  - model names are weather; answer in grades and seams
done_when:
  - Q1 answered: does `codex/skills/octopus` need a bond change so that a RUNBOOK it authors (i) carries `{brand, model, effort}` per seat as the declared delegation shape and (ii) at closure promotes ONE spend line per executor class + appends ONE row to the shared benchmark table? Answer: no-change | one-line-change (quote it) | mismatch (field 4).
  - Q2 answered: Codex-side you folded program.pulse → RUNBOOK/STATUS authority. Claude-side Flight phase-2 still points at `/program-pulse` (0012, locked: project-root persistent buffer) while STATUS is sole doing-state. Head's proposed reconciliation: 0012 supersede-append — the buffer becomes a per-session TRACK inside the enclosure (PAD-class receipt walked by `/track-run`), STATUS `next:` points at the current line; `/program-pulse` = head's add/pull/reconcile, `/track-run` = walker. Does this keep Medusa/Polyp and Flight/Vara on the same seam? Where does it break?
  - Q3 answered: the tunnel (stored Codex thread, state file "where the work lives", operator-opened per law 2.4). From the Codex side: is a tunnel state file a session artifact of `<program>-<NN>-<phase>/` (born on need, like `_bus/`), and does a tunnel exchange still owe a RETURN file in `_bus/` — or is the streamed+reconciled result the record? Name what would be LOST if only the thread holds it (retention, provenance, cross-host).
  - heads-up acknowledged: cycle 03 (later) = countersign of `~/ia-sync/claude/skills/runbook/SKILL.md` vs your `octopus` — not yet drafted; do not pre-empt it.
return_to: ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/02.cartan.return.md
```

Six fields per `~/reposoma/raw.guides/bus/GUIDE.md`. Field 6 (remaining uncertainty): `none` is
a value. @majkee carries this POINT to you by tunnel or by hand and collects the RETURN.
