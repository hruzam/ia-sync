# RETURN — 01 · fold → head

Batch C authored on the surgical table. All STAGED — no write-side git, no `deploy.sh`, no
`rm`/`cp` run by me. @majkee gaveled the shape live (2026-09-03): Flight first · the five with
Atlas's leans · Vara V-B Edit-only, checkpoint via Edit-append, folded close to POLYP.

## 1. Files changed

- `~/ia-sync/claude/agents/flight.md` — **3 edits (WIDE fold, temple role kept):**
  (i) description gains the *venue* dimension (temple/tactical vs project-session mode);
  (ii) new `## Project-session mode — the working-head fold (Medusa, folded home)` — phase spine
  0–7 pointing at `/project-read` · `/buffering-cycle` · `/program-pulse` · project `/session-handoff`
  (never inlined), Assay-gate-per-kraken KEPT, dispatch discipline, "I do not do it all alone",
  observed-not-imagined line, and the planning-head pointed by NAME only: `/runbook`;
  (iii) `## Who I spawn` Execution line — @Vara re-described as a pre-routed **walker**, not a classifier.
- `~/ia-sync/claude/agents/vara.md` — **full reshape → POLYP V-B.** `Write` removed from `tools`
  (now Read/Grep/Glob/Edit/Bash/Agent); MANNED/UNMANNED added; case-switch (track → `/track-run`,
  PAD → the PAD guide law); classifier role RETIRED; checkpoint = Edit-append only (new-file =
  flag the head); dispatch boundary (spawn exactly the line-named kraken); `## Runbook-check —
  DEFERRED` pointer (not active until the runbook GUIDE lands); handoff; hard rules. haiku/high held.
- `~/ia-sync/claude/skills/track-run/SKILL.md` — **CREATED** (promoted from
  `applications-in-common/.claude/skills/track-run/`). Near-verbatim proven POLYP bus + a guide-pointer
  note (style lives in `raw.guides/{runbook,PAD,bus}`, this skill only executes it). Claude-exclusive
  per Cartan's acceptance.
- `~/reposoma/_mail/atlas/inbox/atlas-fold.scope-ack-and-gavel.2026-09-03.md` — CREATED (scope-ack +
  gavel record to the head; sanctioned fold→head channel).

## 2. Commands and outcomes

- Read-only only: `cat`/`sed`/`ls`/`wc`/`grep` for the substrate, targets, bus files, track-run bed.
- Verified `octo` gone on office (`~/.claude/skills/octo` absent; 31 live == 31 table) — @majkee
  erased it by hand (my Delta erase was auto-mode-classifier-blocked).
- No mutating command run. Everything above is on the table, not deployed.

## 3. Evidence paths

- The four changed files (§1).
- Frozen source: `~/reposoma/raw.substrate/archive/2026-09-02.{medusa,polyp,sqcr,sea-pen,podocyst}.*`.
- Promotion source: `~/www/elements-factory/applications-in-common/.claude/skills/track-run/SKILL.md`.
- Cartan confirmation: `~/reposoma/_mail/atlas/inbox/cartan.medusa-polyp-seat-transfer-accepted.2026-09-03.md`.

## 4. Mismatches discovered

1. **Cross-seat classifier language (yours to fix).** Stripping Vara's classifier: `flight.md` is
   aligned (mine); `houston.md` still spawns Vara "to classify" via its old pointer — that is your
   edit-scope (one gaveled line), not mine. Flagging for your VERDICT/action.
2. **"WIDE" applies to Flight, not Vara — by gavel, not error.** The card steered fold-WIDE
   (gain-without-loss). Flight is WIDE (temple kept + project mode gained). Vara is a **replacement**
   (V-B: classifier retired, Write dropped) because @majkee gaveled it so — POLYP's identity is
   defined by dropping the router role. Recorded so the VERDICT doesn't read Vara as an accidental
   narrowing.
3. **`/sqcr` + `/sea-pen` have no Claude home** (crossed to Codex — Cartan confirmed). Vara therefore
   points at `raw.guides/PAD/GUIDE.md` for the pad law instead of a Claude skill. Assumed law-home;
   verify against the PAD guide.
4. **Global handoff gap — resolved in context.** Flight project-mode points at the *project*
   `/session-handoff` (project-scoped per the card). It works because project-mode always runs
   in-project; no global handoff skill is needed.

## 5. Recommended next task

Verify the three table files on disk (`git diff`, read-only), write VERDICT, fold the
`## pulse-ledger text` below into `pulse.atlas.md` (tick the Octopus-family Batch C item). Then hold
Vara's **runbook-check** for the runbook GUIDE landing — it is its own later cycle (skill + the 8
detection rules @field derived from the larva.dev specimen). Deploy is your post-closure step.

## 6. Remaining uncertainty

- **Fold-now vs park-all-Vara.** I read gavel C ("vara folded close from polyp") as *fold the
  structure now, defer runbook-check*. If you read it as *park all of Vara until the guide lands*,
  revert `vara.md` to its pre-session state (no other file depends on it).
- **Vara tier.** haiku/high held; the judgment-heavy runbook-check may want sonnet-flat when built.
  Unresolved by design — rides with that build.
- **PAD-guide law-home.** `vara.md` assumes `raw.guides/PAD/GUIDE.md` is the surviving pad law;
  confirm.

## pulse-ledger text

*(OPEN LEDGER — replace the existing Octopus-family `- [ ]` line's Batch-C clause with:)*

> Batch C **FOLDED ON TABLE 2026-09-03** (staged, pre-deploy): medusa→@Flight (WIDE — temple/tactical
> kept + project-session working-head mode added, spine via pointers, `/runbook` by name, Assay-gate
> kept); polyp→@Vara (V-B POLYP — Edit-only, classifier retired, MANNED/UNMANNED, `/track-run`,
> Edit-append checkpoints, runbook-check DEFERRED to the runbook-GUIDE landing); `/track-run` promoted
> to the global table (guide-pointing). RETURN `_bus/01.fold.return.md`. OPEN: majkee deploy + prune
> stale-live (home `octo`); houston.md classifier-language line (head); Vara runbook-check = later cycle.

*(dated entry, newest-on-top:)*

> ## [2026-09-03 Octopus-family Batch C — fold on table]
> Inside gate `roster-reform-01-triad` (fold seat, under atlas-ui·head). @majkee gaveled live:
> Flight-first · V-B for Vara · Edit-append checkpoints. Drew on the surgical table (deploy-inert):
> **flight.md** — project-session mode (Medusa folded home) as a *venue* on top of MANNED/UNMANNED;
> phase spine points at shared skills, never inlines; Assay-gate-per-kraken kept (Flight's old gap);
> planning-head pointed by NAME `/runbook`; @Vara re-described as pre-routed walker. **vara.md** —
> reshaped to POLYP walker: Write dropped, Edit-only append checkpoints, classifier retired, case-switch
> track→/track-run · PAD→PAD-guide, runbook-check left as a deferred pointer (8 detection rules from
> the larva.dev specimen land with the runbook GUIDE). **skills/track-run/** — promoted from the
> applications-in-common bed, guide-pointing. Style kept in the vendor-neutral guides
> (runbook/status/PAD/bus) — anti-divergence keystone held; nothing duplicated against the Codex
> renderings. @field ran two passes (harness coverage map + runbook-shaped consolidation) — held for
> Batch D / roster-reform-02 (advisor collapse: 2-seat opus+fable deployable now, 1-seat needs #43083).
> Staged, not live: majkee deploys + prunes; houston.md classifier line is the head's.
