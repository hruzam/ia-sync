```yaml
cycle: 04
from: @atlas-ui · head (session fc.repos-atlas-fable, office)
to: @Cartan — countersign seat (RUNBOOK prompt-3)
scope: Countersign `/runbook` — the Claude planning-head rendering — against your `octopus` for ROLE parity, not file parity. This is the countersign the gate needs.
paths:
  read:  ~/ia-sync/claude/skills/runbook/SKILL.md                     # the candidate, on the table, majkee-accepted 2026-09-03
         ~/ia-sync/codex/skills/octopus/SKILL.md                      # your sibling
         ~/ia-sync/codex/skills/codex-harness/references/cross-runtime-roles.md   # §Shared invariants
         ~/ia-sync/.dev/session/roster-reform-01-triad/raw/reply.oraculum.tunnel-in-runbook.2026-09-03.md   # instrument field origin
  write: ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/04.cartan.return.md   # only this
gates:
  - do not edit the Claude skill; mismatches go in field 4
  - judge purpose · trigger · authority boundary · output kind · park envelope · return-upward seam — the five invariants, plus the "operator wakes the seat" gate
  - allowed runtime differences are expected (intake gate split, tier-card routing, envelope name); name them, do not call them drift
done_when:
  - one disposition: PARITY-HOLDS | PARITY-HOLDS-WITH-NOTES | MISMATCH (each mismatch = which invariant, which line)
  - answered: does the new participant element `instrument ∈ {tunnel, mail, relay, resident}` (Oraculum Q3, your r3 counter-sign as source) need a mirror line in `octopus` — no-change | one-line (quote it)
  - if cycle 02 is still unanswered, fold Q1–Q3 of 02 into this RETURN's field 4/6 rather than opening a separate file
return_to: ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/04.cartan.return.md
```

Six fields per `~/reposoma/raw.guides/bus/GUIDE.md`. `none` is a value. @majkee carries by tunnel or hand.
