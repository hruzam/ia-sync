# STATUS — roster-reform-01-triad

```yaml
updated:         2026-09-03 12:15 CEST
writer:          @atlas-ui · head · claude
host:            office (hruzam-120922)
worktree:        ~/ia-sync · main · 34b4412 · dirty: .dev/session/runbook-upgrade/ (untracked, majkee's move) + this folder
                 ~/reposoma · core · 241cce2 · dirty: 6 deletions (runbook raw/ + bench copy moved to ia-sync — majkee's, uncommitted)
gate:            Three re-homings exist on the surgical table — medusa→@Flight, polyp→@Vara,
                 octopus→`/runbook` skill — each cross-checked against its Codex sibling for
                 role-parity-not-file-parity, @Cartan countersigned in `_bus/`, @majkee gaveled
                 in STATUS `checkpoint:`.
checkpoint:      RUNBOOK authored (r2: no new triangulation round — raw/ IS the triangulation;
                 closing sweep added) + `_bus/01.head.point.md` written. Fold seat has written NO
                 shared file yet (verified: pulse.atlas.md mtime pre-session, ia-sync clean except
                 session dirs, session 53fedecf… zero Write/Edit). Cartan hold released 2026-09-03.
                 Economy benchmark table §2b appended to capabilities-economy-hygiene.claude.md
                 (octopus-pilot rows HARVESTED → dir ERASED by @Delta 12:40, verified gone, index
                 clean, 2 expected historical refs survive). Economy chapter + GUIDE pointer +
                 PAD/raw observation + pulse.atlas router line all on disk, uncommitted.
in_flight:       none
recovery_probe:  `cd ~/ia-sync && git status --short && ls .dev/session/roster-reform-01-triad/_bus/ 2>/dev/null`
                 → no `_bus/` = fold seat has not returned; head still in (a)/(b).
                 → `_bus/*.fold.return.md` present, no `*.verdict.md` = head owes VERDICT.
                 → `claude/skills/runbook/` modified = head reached (c); check `_bus/*.head.point.md` for (d).
                 Read-only; interprets cleanly on both hosts after `git pull`.
holds:           - pulse.atlas.md pen = head only; fold seat delivers ledger text via RETURN
                 - fold seat scope: flight.md · vara.md · track-run promotion — head never edits these
                 - head scope: skills/runbook/ · raw.guides/runbook/res/ · one GUIDE pointer line (gavel) · one houston.md line (gavel)
                 - runbook-upgrade brake: no GUIDE body amendment; pointer line subject to majkee ruling
                 - no Atlas runs write-side git / deploy.sh / rm / cp — operator only; all builds STAGED
                 - triangulation is DONE (runbook-upgrade/raw/, majkee's reading order) — no third round
                 - closing sweep (orphans + contradictions, scripts and artifacts) precedes prune
next:            @majkee pastes into session fc.repos-atlas-opus.polyp-medusa:
                 "read ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/01.head.point.md and answer its done_when first block"
expected:        `_bus/` gains nothing yet; the fold session replies in chat with (a) read · (b) scope
                 named back · (c) "none" or its list of unsaved out-of-scope edits.
```

Remaining uncertainty: whether @majkee rules the GUIDE pointer line a breach of the
runbook-upgrade brake (chapter then waits unlinked in `res/`). Whether the fold seat has
un-saved edits in its context that predate this contract — it must say so in its first reply.
