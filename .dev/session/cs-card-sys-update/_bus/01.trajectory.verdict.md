```yaml
cycle: 01
from: trajectory
kind: verdict
date: 2026-09-17
about: 01.cartan.return.md
verdict: PASS
gate_effect: >
  Codex-side build of the flat issue-card model is complete and cross-witnessed. Closes the
  phase-2 build edge. The session gate itself stays OPEN — it additionally requires the
  fresh-session behavior proofs (Claude + Codex each drop+relocate a card) and majkee's
  commit, none of which this cycle delivers.
```

# VERDICT — cycle 01: Cartan Codex flat-model rebuild → PASS

Cartan's RETURN (`01.cartan.return.md`) claimed a flat-model Codex issue-card rebuild. It was
independently verified — the claim was not taken on its word.

## Independent verification (crossed witnesses, not self-confirmed)

- **Head spot-checks:** the SHA-256 Cartan cited for the schema it read
  (`3e143e6e…3066`) matches the live `res/issue-card.md` EXACTLY — proof it built against the
  current schema, not a cached view. Only the two declared codex files changed;
  `agents/openai.yaml` parses under python (the parse Cartan honestly declined for lack of
  ruby); the old nested model / `IR.` appears only in an explicit deletion note.
- **@assay (named standing witness), 10/10 PASS, zero failures:** every flat-model RULE
  correctly expressed Codex-natively — flat vault (no open/parked/archive/reactions subtree),
  fix in the card body, the three-path fold, dual origin signal (kept ISS. name + date as
  first-seen + `origin:`, never renamed RT.), parked-as-assoc-tag (not folder/status), the
  ≥7 assoc hard floor, the sort/UNSORTED/date-vs-kind reconciliation, no self-gavel. @assay
  also ran the real YAML parser Cartan couldn't (clean) and re-confirmed the SHA independently.
  Codex `cold-start-card` skill confirmed untouched (`git diff --exit-code` = 0).

Cartan's RETURN was disciplined in exactly the way this arc rewards: it said READY-FOR-ASSAY
not "done," cited a verifiable hash, and refused to claim a parser PASS it could not run.

## One staleness closed by this cycle (not a defect in Cartan's work)

@assay flagged that GUIDE.md's "Wired surfaces" manifest still listed the Codex skill as
`[TODO — not yet in force]` / old-nested — false after this rebuild. Cartan correctly did not
touch it (reposoma canon, not a builder's file). The head updated that one manifest line to
`[PROVISIONAL]` (built + witnessed) as a meaning-preserving accuracy fix under majkee's
audit-always rule (2026-09-17). Working-tree only; majkee commits reposoma by hand.

## What this cycle does NOT close

- Not committed/deployed — majkee does all git by hand, both machines, both repos.
- Fresh-session behavior proofs (Claude + Codex each drop+relocate an issue card and a
  cold-start card) still owed for the session gate — operator drives, head does not self-wake.
- First-real-use re-audit remains a standing hook of the `[GAVELED · REVIEW-AFTER-USE]` label.

Cycle 01 closed. — @Trajectory (cSharp head)
```
