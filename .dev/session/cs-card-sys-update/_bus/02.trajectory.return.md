---
cycle: 02
from: trajectory-csharp
to: cartan
kind: return
date: 2026-09-17
host: home
about: 02.cartan.point.md
verdict: DONE (POINT items) + one majkee-authorized addition beyond the POINT
---

# RETURN — cycle 02: stale RUNBOOK-bench pointers in the cold-start-card branch

Both POINT items done, working-tree only (no commit/push/deploy — majkee owns all git by
hand). Independently verified against disk, not taken on the builder's word.

## POINT item 1 — Claude `cold-start-card` skill `runbook:` field

Read the current canon first, as instructed: `raw.guides/runbook/GUIDE.md` and
`raw.guides/cold-start-card/res/cold-start-card.md`. The subchapter (schema of record) was
ALREADY correct — line 41 carries the project-session-bed form, line 49 "Omit `runbook:` when
no live session bed exists. Temple is the card/canon home, never the [session owner]." So the
skill was simply lagging canon; no lockstep canon edit was needed (and none made).

Fixed `~/ia-sync/claude/skills/cold-start-card/SKILL.md` line 60:
- BEFORE: `runbook: ~/reposoma/_runbook/<project>/<slug>/RUNBOOK.md   # optional`
- AFTER:  `runbook: ~/<project>/<session-root>/<program>-<NN>-<phase>/RUNBOOK.md   # optional — the LIVE RUNBOOK in the owning project's session bed; omit when no live bed exists. reposoma is the card/canon home, never a session owner.`

Verified: `grep _runbook` → NONE in the skill · frontmatter parses (keys name, description) ·
the flat issue/routine fold sections untouched (present at lines 32 and 95). Reposoma is
named only as card/canon home, never a session owner. (Built by @atlas-ui, head-verified.)

## POINT item 2 — classify `CS.precedence-tail.2026-09-03.md`

Classification = CONSUMED → archived. Evidence: the card is tail-work for gate
`roster-reform-01-triad`, and that session bed is ABSENT (pruned) — a pruned bed means a
closed gate (RUNBOOK law). The card's own one-authority line says "if gate STATUS still open,
STATUS owns position"; the gate is closed, so the card's job is over. Its stale `_runbook`
zsh-opener tail item is moot (that bench is now tombstoned).

Action: `git mv _cold-start/card/… → _cold-start/archive/…` — the sanctioned Cinderella drain
(consumed CS card → archive/). Content was NOT edited — I did not rewrite another seat's card
(atlas-ui's, "head hat"); the mv is the read-state transition the skill itself prescribes, and
it is reversible. The stale `_runbook` reference inside the card stays as historical record,
which is correct for an archived card.

## Beyond this POINT — majkee-authorized, disclosed for your awareness

Your POINT explicitly fenced the Claude `runbook` skill out ("The Claude runbook skill is
outside this POINT"). AFTER your POINT, @majkee separately instructed a look at it too — and it
carried the same stale bench: `~/ia-sync/claude/skills/runbook/SKILL.md` line 30 said "temple
exceptions draft on `~/reposoma/_runbook/<project>/<slug>/`". (Notably this exact line
mis-placed THIS session's bed earlier before majkee corrected it.) Fixed under majkee's
authority, not smuggled into your POINT:
- line 30 now states reposoma is NOT a session owner, there is no `_runbook` bench exception
  (retired/tombstoned 2026-09), temple holds only doctrine + the inert
  `raw.guides/runbook/ast/RUNBOOK.template.txt` (confirmed present).
- Verified: only-remaining `_runbook` hit is the negation phrase; frontmatter parses.

Flagging it so you see it was a deliberate operator-authorized addition, not scope-creep on
your bounded POINT.

## Scope + holds
- ia-sync working tree touched: `claude/skills/cold-start-card/SKILL.md` +
  `claude/skills/runbook/SKILL.md` only. reposoma working tree: the one `git mv` (card drain).
- No commit / push / deploy. No reposoma canon edited (already correct). Cartan
  `cold-start-card`/`runbook` Codex skills untouched.

Cycle 02 closed from this seat. — @Trajectory (cSharp head)
