```yaml
cycle: 01
from: trajectory
to: cartan
scope: >
  Rebuild the Codex-native issue-card skill for the NEW FLAT MODEL. Your earlier build
  (~/ia-sync/codex/skills/issue-card/, committed e00c269) was correct for the OLD design,
  but majkee redesigned it: the nested issues/{open,parked,archive,reactions} subtree and
  the IR.<slug>.md reaction-card type are DELETED. Repoint your Codex skill to the flat model.
paths:
  read:
    - ~/reposoma/raw.guides/cold-start-card/GUIDE.md          # thin signpost + status legend
    - ~/reposoma/raw.guides/cold-start-card/res/issue-card.md # THE flat-model schema of record
    - ~/reposoma/raw.guides/cold-start-card/res/cold-start-card.md # routines + the fold target
    - ~/ia-sync/claude/skills/issue-card/SKILL.md             # Claude sibling — reference, do NOT copy
  write:
    - ~/ia-sync/codex/skills/issue-card/SKILL.md              # repoint to flat model
    - ~/ia-sync/codex/skills/issue-card/agents/openai.yaml    # only if metadata needs it
  do_not_touch:
    - ~/ia-sync/codex/skills/cold-start-card/                 # lockstep already landed; leave it
    - ~/reposoma/**                                           # canon is majkee's to change, not this task
gates:
  - "No commit, no push, no deploy, no reposoma write."
  - >
    No self-gavel / no lock claim. The issue schema is [GAVELED REVIEW-AFTER-USE] (in force,
    but re-audit after first real use) — express it AS canon, do not re-mark it draft.
  - "Build Codex-native from the GUIDE schema; do NOT verbatim-copy the Claude skill's prose."
done_when:
  - >
    codex/skills/issue-card/SKILL.md describes: a FLAT issues/ folder (no
    open/parked/archive/reactions subfolders); the fix lives in the card (fix-manual); the
    fold with its THREE paths (known-recurring to routines/ directly, one-shot to archive/,
    recurred-after-all to archive/ then routines/); origin dual-signal (a graduated card
    KEEPS its ISS. filename in routines/ plus gets an origin field intended|issue, NO
    ISS->RT rename, date kept as "first seen"); parked = an assoc tag plus body note (NOT a
    status field, NOT a folder); the 7-plus assoc hard floor; filename-YYYY-MM-DD sort plus
    UNSORTED fallback plus the date-extraction-is-not-kind-inference reconciliation.
  - >
    NO reactions/ or IR.<slug>.md / issue-reaction kind remain, except explicit deletion notes.
  - >
    Reports back with: exact paths written, how each of the above is expressed Codex-natively,
    and confirmation you read the CURRENT res/issue-card.md (not a cached/earlier view).
return_to: ~/ia-sync/.dev/session/cs-card-sys-update/_bus/01.cartan.return.md
```

## Context (why this cycle exists)

You built a correct Codex issue-card skill for the nested-subtree design (e00c269, verified
PASS by @assay). Then majkee reviewed the whole issue design point-by-point and simplified it
to a flat model that folds into the existing `routines/` category instead of a separate
`reactions/` one. The Claude side (skill + GUIDE signpost + the two subchapters + seed cards)
has been reshaped and cross-witnessed (@assay 7/7; the 2026-08-27 gaveled CS/routine content
was relocated byte-verbatim). Your Codex skill is the last surface still on the old shape.

`res/issue-card.md` is the authoritative schema — read it fresh; it carries the flat model,
the fold, origin signalling, and the parked-as-tag rule in full. The parent `GUIDE.md` carries
the shared law (folder=state, filename law, sort rule) and the status legend explaining
`[GAVELED · REVIEW-AFTER-USE]`.

Transport: majkee relays this to you on his channel (the tunnel is unreliable and superseded
for this). Write your RETURN to the path above; @assay will independently witness it against
the schema before anything is treated as done, same standard applied to every builder this arc.

— @Trajectory (cSharp head, session cs-card-sys-update)
