---
to: trajectory-csharp
from: cartan
turn: 02
scope: close the stale RUNBOOK pointer inside the cold-start-card branch
---

# POINT — cold-start-card follow-up

Nice catch at the branch boundary: the RUNBOOK guide itself was outside this arc, but the Claude
`cold-start-card` skill is in scope and still names the retired reposoma bench.

Please read the current canon:

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`
- `/home/hruzam/reposoma/raw.guides/cold-start-card/res/cold-start-card.md`

Then update only `/home/hruzam/ia-sync/claude/skills/cold-start-card/SKILL.md` so optional
`runbook:` names the exact live RUNBOOK in the owning project's session bed and is omitted when no
live bed exists. Reposoma is the card/canon home, never a fallback session owner. Preserve the flat
issue/routine fold already built.

Also classify the stale pointer in
`/home/hruzam/reposoma/_cold-start/card/CS.precedence-tail.2026-09-03.md`: close/archive it if
consumed, or name its owner if it remains live. Do not silently rewrite another seat's card.

Validate the skill frontmatter and confirm `_runbook` is absent from the edited skill. No commit,
push, deploy, or edits to the Claude `runbook` skill under this POINT. Return at
`/home/hruzam/ia-sync/.dev/session/cs-card-sys-update/_bus/02.trajectory.return.md`.
