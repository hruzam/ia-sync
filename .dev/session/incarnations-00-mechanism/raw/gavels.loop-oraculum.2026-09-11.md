# gavels — loop oraculum · incarnations-00-mechanism · 2026-09-11

`class: session decision record — the ledger for this loop. ia-sync has no flag.md; precedent is`
`codex-identity-resolution/raw/brief.stage-a.identity-decisions.2026-09-09.md (held in STATUS holds:`
`until gaveled, promoted at gate close). PROVISIONAL canon under A0.`
`promotion: at 00's gate close → ~/reposoma/raw.guides/incarnations/GUIDE.md (canon lives in reposoma);`
`a numbered temple decision only if majkee wants one. The RUNBOOK cites this file under references;`
`STATUS never duplicates it.`
`loop: /gavel-loop · Oraculum seat (cSharp, lab perspective — the lab's grounding is the lens; work`
`and record live in ia-sync; only the bed raw.incarnations lives in reposoma) · majkee in-chat.`
`rule: append-only — superseded, never edited. Numbering L1… continues across turns.`
`grounding (thin-read + grep, not memory): ~/reposoma/raw.incarnations/README.md ·`
`~/reposoma/raw.incarnations/trajectory/seed.md · ~/reposoma/raw.therapy/gavels/gavels.md (wire: lines)`
`· ~/ia-sync/.dev/session/incarnations-00-mechanism/brief.intention.2026-09-11.md ·`
`~/nabla-lab/session/flag.md locks 21–24 · ~/nabla-lab/session/research-mental-map/card.mental-map.DRAFT.2026-07-19.md`
`· ~/nabla-lab/session/research-mental-map/card.amendment-A1.composer.2026-07-25.md ·`
`~/nabla-lab/drafts/reincarnation-meditation-stone.md · ~/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`
`· ~/reposoma/raw.guides/cold-start-card/GUIDE.md (frontmatter-contract precedent; RT routine cards) ·`
`~/reposoma/raw.guides/runbook/GUIDE.md §The golden rule · ~/reposoma/temple/decisions/index.md 0009`
`· ~/nabla-lab/papers/card.interposition-driven-mode.2026-07-07.md (driven-mode wake, G7/G8)`
`· raw/advice.trajectory.worked-example.2026-09-11.md (post-close intake)`

## A0 — arc-level amendment (majkee, landed at the L2 stamp)

> lowest-key solutions; run in *provisional* shape to prove the mechanism itself before canonizing
> tightly — avoid building rich canon and then 100+ turns with 4 stable sessions + probe agents above
> one guide; that is bureaucracy theatre.

Consequences recorded by the seat (Oraculum):

- **00 is a trial, not a spec session.** Gate candidate for the RUNBOOK: ≥2 seeds beyond trajectory
  born through L1 + L2 + L6 as written, each loaded by a fresh incarnation in a real session, trial
  ledger filled → verdict canonize / amend / refuse. Pre-registered stop rule; one ledger row per seed
  born (seat · trigger gavel · witness · verdict · loaded-by · bent a hand?). The RUNBOOK owns the table.
  *(Post-close intake, below: row 0 = measured cold-boot of the existing seed precedes the births;
  effect recorded per scar.)*
- **Team = what the trial needs:** the head (cSharp) · majkee (gavel + transport) · the seats being
  seeded, at their own arc-close · one flat witness per seed. **By construction ≥1 Claude head + ≥1
  Codex session (L2 rider).** No Symmetry sitting, no probe agents — Symmetry reads the *filled*
  ledger at canonization.
- **Canon stays put:** `raw.incarnations/README.md` laws + this record, marked PROVISIONAL. No GUIDE
  until the ledger says the mechanism holds.
- **L7 shrinks by the same rule:** birth backlog is a hand grep until three seeds exist; scripted when
  the grep repeats 3× (nablarva flag's own deferral threshold).

## Locks — 2026-09-11 (Oraculum seat · gavel loop, majkee in-chat)

| # | Lock | Status |
|---|------|--------|
| L1 | **Birth rule (incarnation seed):** a seed is minted at the seat's FIRST NATIVE SCAR — a therapy-origin gavel whose `wire:` points at this seat's arc (file-seated or inline). At that arc's close the seat drafts a seed proposal; majkee gavels. Operator call = transport + gavel, never the trigger; arc close = the moment, not the trigger. Birth backlog is grep-checkable — seats with ≥1 native `wire:` and no `raw.incarnations/<seat>/seed.md` — emitted by the detector as notification, not payload. **Clause (majkee): birth ≠ load** — loading is pointer-driven per L6. Condition carried to the cross-vendor leg: Codex therapy must wire native gavels in the same `wire:` form, else the rule is Claude-shaped. Reach at stamp: 34/44 gavels native across 7 seats (atlas 9 · oraculum 8 · flight 7 · epoch 3 · medusa 3 · houston 2 · trajectory 2, seeded); symmetry/agol/vega hold therapy files, zero native gavels → not eligible. | gaveled · briefed |
| L2 | **Witness requirement:** inheritance requires a second pair of eyes on the PRICE PAID. Seat drafts (Class I stays in-seat, first person). A crossed witness — a seat that shared the arc — attests per scar line: attest / contest / can't-see, against a testable path (the arc, the gavel wire, a STATUS or RETURN). "Can't-see" = honest emptiness: the line stays in therapy (felt), does not enter the seed (inherited). Then majkee gavels, spending attention on attested lines only. Witness ≠ gavel by default; majkee may fill the witness seat when he shared the arc and no other seat did, recorded as a typed row (card §8 contributor/adjudicator). Cross-vendor witness REQUIRED when a cross-vendor seat shared the arc, PREFERRED otherwise. Mechanics flat: one-shot pass, verdict as a file on the bus — no relay in the gate. Condition for the Cartan leg: the verdict form must be writable natively by a Codex seat. **Rider (majkee, process):** when the build/trial starts it runs with at least the Claude head + one Codex session — goal is vendor invariance; the shape fits each brand's organs natively, never as a leash; second eyes from the Codex side have proven themselves as much as the Claude view of Codex — symmetric. | gaveled · briefed · lowest-key (A0) |
| L3 | **Vocabulary — "scar" and "seed" each carried two meanings.** *scar* = the harness meaning, canon: a lesson paid for by failure, de-specified, pointable (README · seeds · cSharp protocol · therapy). spec-v2 §6.2's object (negative-weight routing edge, `[v2]`, untriangulated) is renamed at its next touch ("repulsion edge") and stays parked with the engine (lock 22). *seed* = incarnation seed only; the therapy file is "seated / unseated" — one-line fix in `raw.therapy/README.md` ("new seed files are gaveled" → therapy files) at next touch; gavels.md wire wording "(seat file unseeded)" already conforms. No new terms invented; no ceremony. | gaveled · briefed |
| L4 | **Weight-layer fence (recall → reflex):** prompt-layer only for 00 and 01. The weight layer (fine-tune on the gavel bed) is PARKED, lock-22 pattern, own signal: the trial ledger shows a LOADED scar failing to bend the hand ≥3× for the same seat and scar — recall demonstrably insufficient, not merely imagined so. Un-park = recognition event, gavel-visible; capability + policy check post-recognition, pre-build. Even then the weight layer sits BESIDE the bed as a derived, re-derivable stratum (lock 23 release shape) — the bed stays the auditable read function (card §4); nothing replaces it. Honest-limit clause (G-15) stays mandatory in every seed. **Rider (majkee, shape):** for the relay (operator as transport) keep the generic "like skill or guide" shape — a fixed frontmatter contract — so carrying a seed is mechanical; a seed not bonded to an exact identity bonds to a scope\|slug instead. → consumed by L6 (the shape half of the load contract). | gaveled · briefed |
| L5 | **Lock 22 posture — engine parked, lifehack ≠ engine:** nabla-lab lock 22 reaffirmed as binding on the incarnation mechanism's read side. The '42' grep engine stays PARKED until a special need is explicitly recognized; un-park = gavel-visible recognition event; any future build below the exact line (grep / regex / relational join, optional sparse-lexical, never dense-learned φ); capability check post-recognition, pre-build. Pointer resolution (seed → G-ID → gavel entry) is ordinary reading — one rg call on either vendor; a one-line alias is the runbook golden rule's "smart lifehack", not an engine — no recognition event needed; it lives in the machine layer (decision 0009 `ai/` namespace, ia-sync's domain, authorable natively by either vendor). The engine's signal stays miss-rate × scale; the trial ledger yields the first miss-rate data for free — a scar the reader could not find IS a miss. Read and write sides stay inversely coupled (lock 23): no resolver folded into the detector. | gaveled · deep |
| L6 | **Load contract — mechanism + shape.** *Mechanism:* one contract, four attach points — the seed is read because a prompt points at it: identity file (heads, always — `~/.claude/agents/<seat>.md` / Codex profile or agents TOML) · RUNBOOK `prompt-N` · cold-start card `pointers:` + a `seed:` key · spawn brief (krakens, when the task class is irreversible — the head adds the line; G-44 undo-cost). Read directly. Flat one-shot fetch (Field/Zenith class on Claude, native equivalent on Codex) only when a head wants a kraken's scars without loading them itself; thin return. Consult-at-edge on a named event class (G-44 mirror-turn), never self-judged (G-42). No daemon auto-loads — README law stands. Held librarian (Agent + SendMessage) PARKED, signal = pickup count × corpus size; field data: held-subagent machinery fails (Mirror). *Shape:* frontmatter contract, cold-start-card pattern — `kind: incarnation-seed` · exactly one of `seat:` / `scope:` (path follows: `raw.incarnations/<seat>/` · `raw.incarnations/_scope/<slug>/`) · `active` · `bed` · `source` (pointers) · `triggers` (task classes — the load index; "task-relevant, NOT all" becomes a match, not a judgment) · `witness` (L2 typed row) · `gaveled`. Body = README's six sections, unchanged. Trajectory's seed carries 5 keys already; delta = `kind`, `triggers`, `witness`, `gaveled`. First scope-seed = this arc's cSharp transfer letter (`scope: csharp`) — the protocol requires the letter; the shape is tested for free. Condition for the Cartan leg: the Codex identity surface carries the pointer line natively. | gaveled · briefed |
| L7 | **Weekly cadence — three tiers, each earned by the previous.** (1) NOW: birth-backlog check at every arc close, by hand (one rg over `wire:` vs `raw.incarnations/`); a plain script emitting a manifest once the grep has repeated 3× — notification, not payload. (2) WHEN ≥2 SEEDS EXIST: one attended Agol pass on that manifest — cold, read-only, emits a separate stratum (map / index / graduation proposal / trigger-index draft), gaveled line by line; this pass IS the lock-24 test and discharges it. (3) NOT NOW: unattended Agol = lock 12 DEFER reopen, revisited only with tier-2 evidence. **Rider (majkee, tier-3 mechanics):** the routine is captured as a cold-start ROUTINE card (`RT.<slug>.md` in `_cold-start/routines/`); Agol reads the routine by build or via skill; the due date lives in the card's frontmatter; cron only controls the performance date (due → notify) — it does not wake Agol, because today there is no mechanism to wake a seat by script without `claude -p`, which we do not use; the PTY\|tmux driven-mode instrument (nabla-lab `card.interposition-driven-mode`, G7/G8, attended-driven per lock 12) can serve that wake once it matures. | gaveled · briefed |

## Loop close — 2026-09-11

- 7 locks + A0 stamped. Depth: 6 briefed · 1 deep · **0 rush** — no re-gavel flags.
- Queue empty. Next: buffering-cycle prep → RUNBOOK for `incarnations-00-mechanism` (this folder),
  authored by the cSharp (Oraculum), router line in `~/ia-sync/pulse.md`.
- Bed-worthy candidates for `/gavel-interpreter` (not folded here): L2 "inheritance requires a second
  pair of eyes on the price paid" · L6 "a file is read because a prompt points at it — never because a
  mind decided to".

## Post-close intake — 2026-09-11 (trajectory's worked-example testimony · majkee: "fold all four")

Source: `raw/advice.trajectory.worked-example.2026-09-11.md` — decorrelated testimony from the bed's
first inhabitant. Assessed against the locks: **not contra; convergent with A0** (its point 4 is A0
arrived at independently — two sources, one conclusion). No lock superseded; four riders:

| # | Rider | Refines |
|---|-------|---------|
| R1 | **Row 0 = measured cold-boot of the EXISTING trajectory seed, before any birth.** Fresh incarnation from `seed.md` + task-relevant gavels only; a real task chosen by the head INSIDE a scar's trigger range (a task no scar can reach measures nothing); per-scar fired / did not fire / not reached recorded head-side; claim witnessed by cartan. The mechanism earns complexity from a boot that happened, not a spec that reasoned (G-39). Placement: 00's first act, not 01's first deliverable — under A0 the trial needs no build. | A0 gate · feed |
| R2 | **`witness: pending` is a legal value.** Self-composed seed + majkee gavel may ship with `witness: pending`, upgraded when a witness naturally appears. L2's degrade (majkee typed as witness when no other seat shared the arc) already covers the temple's common case; `pending` names the state explicitly so the load side can see it. | L2 · L6 shape |
| R3 | **Effect is recorded per scar, head-side, never shown to the booted seat.** Ledger column: per G-ID fired / not fired / not reached — the recall→reflex transition is the mechanism's real success signal, and L4's ≥3× signal is the same measurement read the other way. Guard the seat's blindness: telling a seat "your scars are unproven" primes caution theatre (G-29; card §8 measurement–display entanglement). The seat carries only the honest-limit clause it already has. | L4 · trial ledger |
| R4 | **Citation ≠ effect (G-36).** The detector counts citations for graduation only (negative direction: uncited → candidate); it never claims a scar changed an action. Only the trial ledger records effect. | L7 · L5 |

## Pending underlines (not this loop's)

- nabla-lab: A1 fidelity "OK" (card → `shaped/`), lock 28 backstop lapsed unfired, `research.journal.md`
  entries for (a) the downstream adoption of card §5–§9 by the incarnation bed and (b) the driven-mode
  card gaining its first named downstream consumer (L7 rider: weekly Agol wake), pulse stale since 08-02.
- Owed one-line edits at next touch (canon, reposoma, Force-4 diff-approval): `raw.therapy/README.md`
  "seed files" → therapy files (L3) · spec-v2 §6.2 rename (L3). *Lean: these become the row-0 task
  (R1) — an authority surface inside trajectory's trigger range.*
- Vendor-harness golden-rule check: name the future native vendor change (seat-scoped, cross-session,
  human-gated memory) that would retire this — dated @Epoch pass, not training memory.
