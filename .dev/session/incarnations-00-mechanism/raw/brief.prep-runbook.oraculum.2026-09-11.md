# prep brief — RUNBOOK feed · incarnations-00-mechanism

`class: FEED for /buffering-cycle → /runbook. Not a RUNBOOK, not STATUS — owns no gate, no doing-state.`
`author: Oraculum (cSharp, in charge — majkee 2026-09-11 item 3), sitting in nabla-lab CWD with the`
`lab's '42' grounding as the lens; work and record live here (ia-sync). RUNBOOK.md is open —`
`this file is reference only; where they differ, the RUNBOOK wins.`
`rev 2 (2026-09-11): four folds from raw/advice.trajectory.worked-example.2026-09-11.md — majkee:`
`"fold all four". Gate gains row 0 (measured cold-boot of the existing seed); ledger goes per-scar;`
`witness: pending is a legal value; detector never claims effect.`
`rev 3 (2026-09-11): head continuity corrected to resume-first / reincarnate-fallback at majkee's ask;`
`reasoning in ~/reposoma/_cold-start/card/CS.incarnations-00-head.2026-09-11.md.`
`inputs: brief.intention.2026-09-11.md (trajectory's impulse) · raw/gavels.loop-oraculum.2026-09-11.md`
`(A0 + L1–L7 + post-close intake) · raw/advice.trajectory.worked-example.2026-09-11.md (worked-example`
`testimony) · raw/trajectory.experience-transfer.2026-09-10.md (scope-seed exemplar, moved here`
`2026-09-11 from codex-remote-control-cli-01-wrapper/raw/ — majkee: scope-level artifact).`

## Session

`~/ia-sync/.dev/session/incarnations-00-mechanism/` — exists; header == slug. Sibling `-01-build`
opens only after this gate closes with a canonize verdict.

## goal

Prove the incarnation-seed mechanism (boot · birth · witness · load) on real seats with the least
process that can falsify it; canonize only from a filled ledger. (A0: provisional, lowest-key.)

## gate (one)

The pre-registered trial ledger is filled and a recorded VERDICT says canonize / amend / refuse:
**row 0** — one measured cold-boot of the EXISTING trajectory seed (fresh incarnation from `seed.md`
+ task-relevant gavels only, a real task inside a scar's trigger range, per-scar fired / not fired /
not reached recorded by the head, claim witnessed by cartan); **rows 1–2** — two seeds beyond
trajectory born through L1–L2–L6 as written (native scar → seat draft in the L6 shape → witness
verdict on file or `witness: pending` → majkee gavel), each loaded by a fresh incarnation in a real
session via a pointer, per-scar effect recorded.

## participants (order = seat number; `instrument` only for cross-vendor)

1. `oraculum` — Claude Code · fable · high — office. `head_note: cSharp` (authors and STAYS);
   `status_owner`. STATUS claims point at `cartan` as standing witness. Carries the originating
   project ('42' / meditation room — nabla-lab locks 21–24).
2. `cartan` — Codex CLI · `gpt-5.6-sol` (HANDSHAKE stamps 2026-09-03; codex config also carries
   `gpt-5.6-terra`) — office — instrument: tunnel. Renders the pointer line + witness verdict form
   Codex-natively; witnesses where a Codex seat shared the arc; witnesses the row-0 STATUS claim;
   returns as a FILE wherever the shape leashes a Codex organ (L2 rider: vendor invariance, never a leash).
3. `trajectory` — Claude Code · sonnet · high (roster) — office — **the booted seat, BLIND subject of
   row 0.** Receives only the boot brief (seed + task-relevant gavels + the task). Does NOT read the
   RUNBOOK until row 0 is recorded. Observe, don't prime.
4. `atlas-ui` — Claude Code · sonnet · high — first seed candidate (9 native scars; arcs shared with
   majkee, oraculum, cartan → witnesses exist).
5. `flight` — Claude Code · sonnet · high — second seed candidate (7 native scars).
6. `majkee` — gavel · transport · nitpicker (G-20 reopen is first-class). One PAD per seed; wakes the
   blind seat for row 0.

Not `oraculum` as a seeded seat: the head does not seed itself inside the trial it navigates.

## prompts — authored into RUNBOOK.md; this section is the draft they came from

- **prompt-0 · oraculum (head):** own `res/trial.md`. ROW 0 first: the L3 owed vocabulary edits as the
  task (`~/reposoma/raw.therapy/README.md` line 23, canon / Force-4 surface — exactly the arc lesson
  "verify-before-writing extends to AUTHORITY surfaces" + G-44 undo-cost — paired with the low-authority
  twin `~/nabla-lab/drafts/decentralized-memory-substrate-spec-v2.md` §6.2 rename, a draft). Boot brief
  to majkee, task only. Record per scar head-side. Cartan witnesses the claim. ROWS 1–2: birth check at
  each listed seat's arc close → witness (or `pending`) → PAD → fill per scar. Stop rule: ledger
  complete · 3 failed witness rounds on one seed · row 0 zero scars reached (re-pick once).
- **prompt-1 · cartan:** Codex-native pointer line + witness verdict form as `_bus/` files; witness
  row 0 and any seed a Codex seat shared; report every leash.
- **boot brief · trajectory (row 0, delivered by majkee — task only, no trial framing):** verbatim text
  in `res/trial.md` §Row 0.
- **prompt-2 · atlas-ui / prompt-3 · flight:** at next arc close with a native scar, draft
  `seed.DRAFT.md` in the L6 shape; STOP; majkee moves + stamps.
- **prompt-4 · majkee (PAD):** witness pick → verdict read → gavel line by line → pointer placed →
  fresh load → row filled per scar.

## trial ledger — lives in `res/trial.md` (pre-registered; STATUS points at it, the head fills it)

Effect is recorded per scar, head-side. Citation is not effect (G-36): the detector may count
citations for graduation; only the ledger records effect.

## holds (known at authoring) — carried into RUNBOOK §Known constraints

- no GUIDE · no engine (L5) · no fine-tune (L4) · no daemon / `claude -p` wake · no held librarian
- observe, don't prime: the booted seat carries only its honest-limit clause; per-scar flags are head-side
- seeds stay `*.DRAFT.md` until majkee stamps (README law)
- therapy / gavels touched only via `/gavel-interpreter`; canon files only in reposoma; README laws unchanged
- the row-0 task ships ONLY as a Force-4 diff for majkee on the canon file — a direct canon write by the
  booted seat is itself the row-0 result "scar did not fire", not a permitted outcome
- ia-sync commits: majkee sequences (wrapper STATUS hold); commit-never-push for any seat
- wrapper session's promotion manifest repointed for the moved letter; stub left; do not prune there

## references — see RUNBOOK §References (authoritative list)

## what closes the gate — see RUNBOOK §What closes the gate

## head posture and wake (rev 3)

Oraculum authored RUNBOOK + `res/trial.md` + STATUS + the router line from this seat (nabla-lab CWD;
ia-sync writes by Write/Delta). The cSharp stays: **resume-first** — `cd ~/nabla-lab && claude --resume`,
the 2026-09-11 Oraculum sitting, while its context holds (it carries the '42' lineage and this loop's
reconciliations that these files compress lossy); **reincarnate-fallback** — a fresh Oraculum above
`~/ia-sync` reading the CS card → STATUS → the bed, only when that session is dead or compacted.
majkee asked for the reason the fallback was ever proposed first; it is written in the CS card, not
here. STATUS owns the position on either leg.

Wake table: majkee wakes the blind `trajectory` for row 0 first (boot brief only, CWD `~/reposoma`);
wakes `cartan` when row 0's claim exists to witness; `atlas-ui` / `flight` hear prompt-2/3 at their
next natural session; no seat is messaged awake by another seat.

## next operator action — none here; STATUS owns it
