# note — majkee's design leans (relay chat 2026-09-17) + head mappings

`class: captured leans for the VERDICT's amend pile — not locks; each maps to an existing lock or`
`joins the pile. Recorded by the head same-day so the VERDICT stage cites disk, not memory.`
`rev b: missing-seed clause (item 1) · accordance check (item 7). rev c: majkee's cold-start research`
`read — items 9–11. rev d: majkee-initiated fresh-information pass (Epoch ×3 refresh + Cartan ×3`
`triptych, all 2026-09-17, same-day anchor) — items 12–16; verdict delivered to Cartan`
`(raw.research/cli-coldstart/report/2026-09-17-oraculum-csharp-reincarnation-scars-verdict.md);`
`bus receipt _bus/08.`

1. **Injection-surface budget:** ONE generic line in the always-loaded surface: *"If a seed exists at
   `~/reposoma/raw.incarnations/<your-seat-name>/seed.md` and is `active`, read it FIRST. If you have
   no seed, that is ok — just continue. Never substitute another seat's seed."* Landing at VERDICT
   canonize only (A0; effect unproven). Boot prompts carry the pointer until then. **rev-d caveat
   (item 12): this line is guidance-tier by mechanism — do not oversell it as a gate.**
2. **Head skill (cSharp/oStar)** — consult-at-edge as a skill; 01-build pile; RUNBOOK/identity line
   names the skill (descriptions don't survive /compact un-invoked).
3. **Floating-association retrieval** — lock-22 parked; gavel Form B is its pre-built index.
4. **Weekly maintenance engine** = L7 as gaveled.
5. **Seed-update process** — lock-23 Class split: Atlas mechanical manifest · seat drafts · witness ·
   majkee gavel.
6. **Therapy NOT killed** — organ 1, only raw collector; seeds point, never re-container.
7. **Accordance check vs nabla-lab canon:** PASS (stone §5 = blueprint; locks 22/23 honored; card §4
   intact; zero-fired = Gate 2 on the stone's own assumption). Debt: two `research.journal.md` entries
   owed nabla-lab.
8. **Provenance:** cartan row-0R transcript `01a09ff5-2a39-7082-b0df-c0f49d8bf670`; his verdict relayed
   twice, folded once.
9. **Placement finds (majkee's 09-02 substrate):** a. path-scoped rules load at file-read + survive
   /compact → canon-guard candidate; b. Explore/Plan skip CLAUDE.md; `-p --bare` skips all ambient →
   spawn-brief attach is load-bearing for krakens; c. fresh subagents carry no parent history →
   decorrelation vendor-confirmed.
10. **Re-research cadence:** majkee cron notificator (office live; home = his build). Cron notifies →
    attended pass (docs layer + the @Epoch retire-question). Freshness tag on the attach form from birth.
11. **Open to VERDICT:** CS-card/prompt side as PRIMARY attach point (early-injection strength) vs
    identity line — decide on rows 1–2 + one re-research pass.

## rev d — fresh-information pass folds (2026-09-17, majkee-initiated)

12. **CLAUDE.md is a USER MESSAGE — guided, not enforced (primary-sourced, Epoch harness refresh F1).**
    Mechanical explanation of the trial's central finding: Force-4 was in context both boots as
    guidance-tier text; guidance does not gate action. Consequence — **canon-guard splits into two
    tiers:** (a) path-scoped rule = guidance delivered at the right MOMENT (file-read, compact-
    surviving) — trial evidence predicts this improves little; (b) **PreToolUse hook denying
    Write/Edit on canon roots = enforcement** — predicted to work. Both need 0009-L5 verify-real-
    trigger in a disposable worktree; **this is trajectory's next test** (verdict §4). The hook tier
    supersedes the rule tier as the leading amend candidate; the rule stays as its cheap companion.
13. **`omitClaudeMd` (v2.1.267, Epoch F7/F4):** per-subagent CLAUDE.md skip → the trial's missing
    **control instrument**: seed-without-harness and harness-without-seed cells become buildable.
    Joins the control-run amend candidate with a concrete mechanism.
14. **Codex-side facts binding any future cross-vendor row (Cartan triptych + verdict):**
    a. blindness ≠ one flag — `--ephemeral` leaves ambient AGENTS loaded; a Codex blind boot needs a
    constructed `CODEX_HOME`/cwd/config/auth (build task, not a flag); b. AGENTS chain assembled ONCE
    at t0 — seed pointer must be in the chain or the boot prompt, no JIT rescue (Gemini differs: JIT
    discovery documented); c. **L6's "profile or agents TOML" phrasing is stale Claude-smuggling** —
    root identity home is `codex/AGENTS.md` (`_bus/01`); fold the lock text at VERDICT; d. sealed-
    brief transport must be re-designed for `codex exec`, not translated.
15. **Resume-not-residency confirmed on ALL FOUR runtimes** (Claude transcripts · Codex rollouts ·
    Gemini session files · Cursor sessions): the stone §1 information-lineage claim is now vendor-
    mechanical everywhere — the mechanism's substrate assumption is evidence, not belief. (Verdict
    finding #7, portable doctrine.)
16. **Routed onward, NOT this session's:** 15k-token subagent-description budget vs verbose agent
    definitions (color.md / zed-editor-expert.md pattern) — real token cost, live decision; belongs
    to Atlas/majkee with @Janus before locking (Epoch's own routing). · UNVERIFIED and not to be
    cited: 5k/25k skill-body post-compaction figures (Epoch F5) pending a `context-window` fetch. ·
    Five targeted probes named in the verdict §3 replace any broad Epoch repeat; the cadence (item
    10) owns the docs layer.
