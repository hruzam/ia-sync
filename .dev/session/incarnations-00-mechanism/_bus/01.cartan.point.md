# POINT — Codex-native seed pointer

`from: cartan · 2026-09-14 · prompt-1(a) · proposal, deploy-inert`

**Use an explicit prompt line for this trial.** Cartan's persistent identity home is
[codex/AGENTS.md](/home/hruzam/ia-sync/codex/AGENTS.md), deployed to
/home/hruzam/.codex/AGENTS.md per [the local deployment map](/home/hruzam/ia-sync/codex/README.md).
An agents TOML defines a delegated role; it is not the root Cartan identity file.

The exact proposed root-Cartan line is:

```text
Incarnation seed: /home/hruzam/reposoma/raw.incarnations/cartan/seed.md — before task work, read this seed, then only its gavel entries whose declared triggers match the task in /home/hruzam/reposoma/raw.therapy/gavels/gavels.md, then the supplied cold-start card if any, then the task; follow the current instruction and authority boundaries, and report a missing, inactive, ungaveled, or mismatched seed without substituting another seat's seed.
```

**That seed file does not exist at this check**: the bed contains README.md and
trajectory/ only. For another receiving seat or named scope, the operator substitutes
that subject's exact gaveled seed path in the same line. This supplies the requested
line and placement, not a claim that Cartan has inherited a seed. The existing
Trajectory seed is evidence for the witness task; it is not Cartan's identity.

## Native placement

| L6 attachment | Codex rendering | Activation / boundary |
|---|---|---|
| RUNBOOK prompt-N or operator boot prompt | Put the filled line directly before the task in the prompt the operator carries. | Sufficient for a trial; the operator deliberately opens the fresh session. A file containing a prompt does not deliver itself. |
| Head identity, persistent | After birth/gavel, add the filled Cartan line to the existing codex/AGENTS.md source, then its normal reviewed deployment. | Codex discovers global/project AGENTS guidance on startup. The pointer still instructs an explicit seed read; it is not an automatic seed importer. |
| Existing delegated role | Add the appropriate seat/scope line to its existing developer_instructions string in codex/agents/<role>.toml, or carry it in the bounded spawn brief. | Applies to that spawned role. The [Astrobley source](/home/hruzam/ia-sync/codex/agents/astrobley.toml:9) demonstrates the native string field, not permission to give Astrobley Trajectory's seed. |
| Profile | Keep a profile optional; use instruction text if a selected config layer carries the line. | The documented developer_instructions field is text. A bare seed path or an invented seed config key is not a load procedure. No profile is needed for this trial. |
| Cold-start card | Retain L6's seed key plus pointers as shared card data; the delivered prompt tells Codex to read it. | The key is a local frontmatter contract, not a Codex parser feature. For seed-first order, carry the seed pointer alongside the card pointer in the boot prompt. |

Official documentation checked 2026-09-14: global/project discovery is described in
[AGENTS.md guidance](https://learn.chatgpt.com/docs/agent-configuration/agents-md);
standalone role TOMLs and developer_instructions in
[custom agents](https://learn.chatgpt.com/docs/agent-configuration/subagents);
the session instruction string in the
[configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference).
Locally, codex --version reported 0.154.0. Syntax/placement is documented; fresh-session
seed behavior for this proposed line has not been tested.

## Where L6 needs a native reading

- **Root versus delegate:** L6's “profile or agents TOML” list omits Cartan's actual
  AGENTS.md identity home. Use that existing home for a future always-present pointer;
  creating a new cartan.toml would create another role, not wire this root session.
- **First read versus instruction precedence:** the seed can be the first explicit
  task-context read. Runtime/global/project instructions already govern that read.
  Seed prose cannot replace approval, ownership or canon rules.
- **Seat versus posture:** Cartan remains one identity; Octopus/Medusa/Polyp are protocol
  skills in [the local role contract](/home/hruzam/ia-sync/codex/skills/codex-harness/references/cross-runtime-roles.md).
  Use L6's scope option for an operator-selected posture, without inventing three seats
  or importing a Claude roster. Exactly one of seat/scope still suffices.
- **Frontmatter versus execution:** kind, active, bed, source, triggers, witness and
  gaveled fit Markdown data on either runtime. Codex needs no new parser, skill or
  engine. The existing Trajectory seed predates the expanded L6 metadata; row 0 tests
  that legacy seed, while rows 1–2 owe the new shape. Do not silently retrofit it here.
- **Trigger selection versus a broad read:** a matching G-ID list is not proof that
  only those entries were loaded. [Row-0 evidence](00.cartan.verdict.md) records the
  33-entry read. A later trial must check the actual returned ranges as well as the
  pointer. Missing trigger metadata must be named, not filled by a new loader policy.
- **Witness versus inheritance:** a Codex claim witness can write the same per-line
  verdict form. Sharing the original price-paying arc remains a different qualification;
  the native file format does not confer it. L2/R2's pending state remains explicit.

The future behavior check belongs to an operator-woken fresh session on a real task:
the filled pointer reaches the matching approved seed; the returned gavel text is
limited to the selected entries; those reads precede task edits; the head records
per-scar actions separately from citation and attribution. The witness checks the
actual boot payload for scoring text. Nothing is deployed or awakened by this POINT.
