# raw — cSharp experience transfer, atlas-ui → Oraculum, preserved verbatim

`provenance: handed by @majkee (mobile chat, session fc-sync.oraculum.reverse-tunnel.cSharp,`
`2026-09-03) from @atlas-ui, head of the closed gate roster-reform-01-triad. A RETURN that exists`
`only in chat closes nothing (scar 2, below) — this file is the disk record. Substrate only.`

---

Oraculum — experience transfer from atlas-ui, head of the closed gate
roster-reform-01-triad (2026-09-03), handed to you by majkee. You are now
the cSharp of your own arc — majkee's board-slang, worth one sentence in
your RUNBOOK header and nowhere else for now:

  head_note: cSharp — this seat authored the RUNBOOK and stays live
  through the whole arc as navigator and status_owner; it may delegate
  every body of work and receive only navigation + test parts; it closes
  the session if it can.

(That is the whole codification. Not canon; the GUIDE body is braked.)

THE PRODUCER LAW — all live, read before authoring:
- ~/.claude/skills/runbook/SKILL.md  (/runbook — deployed today)
- ~/reposoma/raw.guides/{runbook,status,PAD,bus}/GUIDE.md
- ~/reposoma/raw.guides/runbook/res/token-economy.md
- ~/reposoma/raw.guides/runbook/res/cross-vendor-seat.md  ← you are ON
  the tunnel with Cartan; this chapter IS your exchange contract
Shape: ONE gate (write the gate: line first — cannot = not a session) ·
prompt-N per seat · absolute paths · header == folder slug ·
status_owner = you · keep every read artifact under ~150-160 lines
(majkee's bound; overflow → res/ chapter, never a longer RUNBOOK).
Note cSharp ≠ /runbook's park envelope: the skill plans-and-parks; YOU
author and STAY. Same producer shape, different exit.

TUNNEL RULES THAT BIT US (Cartan-countersigned, don't re-derive):
- instrument is FIXED in the participant tuple: [seat,{brand,model,effort},
  host,instrument]. Changing instrument mid-arc = numbered sibling session.
- two births: handle at majkee's `open --enable` (law 2.4 — only he opens);
  thread at first `send`. Handle lives beside your RUNBOOK, NEVER committed.
- ONE bus RETURN per tunnel CYCLE (open→work→close), not per send.
  Streamed==read-back proves transport only; the durable record is FILES
  (HANDSHAKE §TABLE). Close the table before prune — not on every yield.

THE FOUR SCARS FROM MY RUN — each cost something, take them free:
1. Your own STATUS is your least-verified surface. Mine implied a bus
   RETURN that never existed; Cartan's `test -f` caught it. Every claim
   you write, write as a path someone else can test — and let them.
2. A RETURN that exists only in chat closes nothing. We proved it live
   in a race: files first, then the word. Disk is the record.
3. Never cite _mail/*/inbox/ in anything durable. The mail WILL be
   consumed; ADR-GUARD will refuse your commit and it will be RIGHT.
   Durable evidence → temple/evidence/<NNNN>/ or your project's bed.
4. At closure, list EVERY raw/ keeper in an explicit promotion manifest
   before prune. I missed two POINT files; git history saved them, not I.

THE ONE LESSON ABOVE ALL: the day stayed correct not because any seat
was rigorous — every seat failed at least once, the head twice — but
because the witnesses CROSSED: peer tested files, hook blocked rot,
subordinate refused to re-dirty a closed gate. Design your gate so at
least one other seat verifies YOUR files (Cartan natively sees all
diffs on both repos — that is your standing witness; use him).
Operator wakes every seat — you never message one awake; majkee is the
transport and the gavel. Precedence collisions → temple/precedence.md
(0013): your clauses stay terminal in-domain; cross-domain goes up.

Good arc. — atlas-ui, the previous cSharp
