# RUNBOOK — reversal-tunel-00-leader

```yaml
goal:            The reverse tunnel (Cartan/Codex → named Claude seat) is proven and adopted as the
                 SAME one TABLE instrument, direction a parameter — the constitution passing its own
                 roles-swap test (forward: Oraculum built, Cartan audited; reverse: Cartan builds,
                 Oraculum audits, majkee gavels).
gate:            Both instrument siblings (01-instrument, 02-adoption) are closed with their receipts
                 promoted, and the leader's closing sweep confirms every owed edit landed — HANDSHAKE
                 r4 counter-signed, tunnel GUIDE direction section live, palette wiring deployed and
                 fresh-session verified. First in, last out.
head_note:       cSharp — this seat authored the RUNBOOK and stays live through the whole arc as
                 navigator and status_owner; it may delegate every body of work and receive only
                 navigation + test parts; it closes the session if it can.
                 (Posture canon: /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head.md —
                 gaveled by majkee 2026-09-03; carried here per its own transfer, @atlas-ui → @Oraculum.)
participant_0:   [@Oraculum, {claude, fable, high}, office]            # cSharp head — see head_note
participant_1:   [@Cartan, {codex, sol, high}, office, resident]      # architect of 01-instrument;
                                                                      # standing witness on all diffs
participant_2:   [@majkee, human, office]                             # gavel · enable (law 2.4) · PAD hands;
                                                                      # wakes every seat — no SendMessage wakes
participant_3:   [scribe, {claude, haiku, low}, office]               # PAD journaling — each sitting
                                                                      # doubles as the vara test; majkee
                                                                      # may seat a Codex/Luna scribe per sitting
status_owner:    @Oraculum        # single writer of THIS bed's STATUS.md only — never a sibling's
schema_note:     conforms to raw.guides/runbook/GUIDE.md rev 2026-08-27 + res/cross-vendor-seat.md
                 (the exchange contract with Cartan). PRECEDENT — first live program-level leader,
                 unparked by majkee gavel 2026-09-03, carried as numbered sibling 00 so the
                 flat-siblings law is bent, not broken. GUIDE amendment for the no-program-file law
                 remains OWED POST-ARC, not made here.
```

> **Read this once.** Position lives in `STATUS.md`. Each instrument sibling has its own RUNBOOK,
> its own gate, its own status_owner — this file never carries their doing-state.

---

## Why this program exists

The forward tunnel (Claude → stored Codex thread) is proven and adopted (HANDSHAKE r3, TABLE).
majkee authorized the reverse direction with the roles swapped — and the swap IS the test: if the
constitution only works when its author drives, it is not a constitution. Founding documents, both
preserved verbatim in `raw/`: Cartan's POINT (`raw/cartan.point.2026-09-03.md`) and the previous
cSharp's experience transfer (`raw/atlas-ui.experience-transfer.2026-09-03.md`). Oraculum's RETURN
(co-sign with amendments) lives at its referenced path below.

## Posture — binding on every mechanism proposed in any sibling

- **The head sequences; it never authors the instrument.** 01's RUNBOOK is Cartan's, by Blessing.
- **The golden rule binds** — canon, not copied here:
  `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` §The golden rule. This arc's own DNA:
  native `--bg` beat custom `-p` plumbing before the program opened.
- **Crossed witnesses, no self-confirmed gates:** every STATUS claim is written as a path another
  seat can test. @Cartan natively sees all diffs on both repos — he is the standing witness on the
  head's own files. Disk is the record; a RETURN that exists only in chat closes nothing.

## Sibling map (order is law; a sibling opens only when its predecessor's gate closes)

| bed | gate (one condition) | authored by |
|---|---|---|
| `reversal-tunel-00-leader` | this file's gate above | @Oraculum |
| `reversal-tunel-01-instrument` | one full reverse round-trip (Cartan→Claude-seat task, Claude→Cartan verified yield) live on office, receipts on disk — six lanes per RETURN §D, FAIL receipts kept | **@Cartan** |
| `reversal-tunel-02-adoption` | HANDSHAKE r4 counter-signed · tunnel GUIDE direction section landed (Oraculum's owed edit, RETURN §B2) · wiring promoted after fresh-session verification | @Oraculum, counter-signed by @Cartan |

Naming note: majkee's rename `reversal-tunel` (kept verbatim) supersedes the cold-start card's
suggested nablarva slug `toolbox-termbrana-04-reverse`. Beds live HERE (ia-sync — where HANDSHAKE,
the forward shim, and the wiring target live); nablarva termbrana evidence is pointed at, never moved.
No `-00-research` bed exists for this program: the wheel-check was answered by the POINT/RETURN
themselves (native `--bg` IS the vendor-harness answer) — res/research.md applies to future big scopes.

## Wiring name (head's proposal — architect may adjust with reason; majkee gavels at adoption)

- Engine: `/home/hruzam/ia-sync/zsh/ai/tunnel-claude.zsh` — exact mirror of `tunnel-codex.zsh`
  (direction named by the far end). Bodies in the engine, per control-panel law.
- Palette family: `tcc-<verb>` (`tcc-open`, `tcc-ask`, `tcc-read`, `tcc-close`) — aliases only, in
  `zsh/ai/keyboard.zsh`, grammar per `zsh/guides/keyboard.md`. Final letters confirmed at build time
  against `keys --plain` (must not land in UNSORTED, must not collide).

## prompt-0 — @Oraculum (cSharp head; owns this STATUS; stays live through the arc)

Read `/home/hruzam/ia-sync/.dev/session/reversal-tunel-00-leader/raw/cartan.point.2026-09-03.md`,
`/home/hruzam/ia-sync/.dev/session/reversal-tunel-00-leader/raw/atlas-ui.experience-transfer.2026-09-03.md`,
`/home/hruzam/ia-sync/session/rellays-calude-codex/ORACULUM-CARTAN-reverse-tunnel-return.2026-09-03.md`,
`/home/hruzam/ia-sync/HANDSHAKE.md` (§TABLE + Delivery rule),
`/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md` and `res/token-economy.md`.
Duties, in order: (a) handoff visible on disk, STATUS `next:` names majkee's wake of Cartan;
(b) when `reversal-tunel-01-instrument/RUNBOOK.md` exists, run the CHALLENGE pass (one adversarial
pass; epistemics OBSERVED/DOCUMENTED/INFERRED enforced — RETURN §C3/§B4 carry highest uncertainty;
Cartan's live observation outranks inference) into this bed's `_bus/`; (c) after majkee's gavel,
navigate — delegate every body of work, receive only navigation + test parts; (d) author
02-adoption; (e) closing sweep — promotion manifest, receipts committed before prune, then author
the experience transfer per the ritual (res/csharp-head.md §The transfer ritual). Leave last.

## prompt-1 — @Cartan (architect — woken by majkee, never by SendMessage)

You author `reversal-tunel-01-instrument` — RUNBOOK first, per
`/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`. Your own POINT is at
`/home/hruzam/ia-sync/.dev/session/reversal-tunel-00-leader/raw/cartan.point.2026-09-03.md` — your
ten invariants bind, plus the RETURN's amendments (seat-scoped Stop hook in a DEDICATED agent .md ·
dual-source reconcile · StopFailure→HOLD · opaque seat internals · stop-then-resume). Read:
`/home/hruzam/ia-sync/session/rellays-calude-codex/ORACULUM-CARTAN-reverse-tunnel-return.2026-09-03.md`
(all of A–E) · `/home/hruzam/ia-sync/HANDSHAKE.md` · `/home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md` ·
`/home/hruzam/ia-sync/zsh/ai/tunnel-codex.zsh` (the mirror you build) ·
`/home/hruzam/unikuklatrix/nablarva/toolbox/termbrana/research/evidence/t06-tunnel-v0-roundtrip.md`
(the FAIL is part of the proof — yours will be too). Distinguish OBSERVED / DOCUMENTED / INFERRED
throughout. Your RUNBOOK goes to Oraculum's CHALLENGE before any build; majkee owns every enable,
commit, deploy. You are also this program's standing witness: test the head's STATUS claims
(`test -f` the cited paths) — refuse what disk does not show.

## prompt-2 — @majkee (hands) & prompt-3 — scribe

majkee: you wake every seat deliberately after its handoff is on disk; you are the transport and
the gavel; you hold every enable (law 2.4) — no tunnel opens without you. Scribe: journal each
sitting as `pad.<N>-<scope>.md` in the active sibling's bed per
`/home/hruzam/reposoma/raw.guides/PAD/GUIDE.md` — raw output in fences, never summarized. The
sitting is itself the vara test; record where the walk stalls.

## Known constraints and destructive holds

- **No `claude -p`, no Agent SDK, no stream-json, no `ANTHROPIC_API_KEY`/Console/Bedrock/Vertex/**
  **Foundry routing — anywhere.** POINT invariants 2–4 are survival conditions, not hygiene.
- **HANDSHAKE r4 wording changes go through the counter-sign flow only** — never through any
  RUNBOOK in this program.
- **Instrument is FIXED in the participant tuple.** Changing instrument mid-arc = numbered sibling.
  Tunnel handles live beside the owning RUNBOOK, machine-local, ignored, NEVER committed; handle is
  born only at majkee's `open --enable`, thread at first `send`.
- **ONE bus RETURN per tunnel cycle (open→work→close), not per send.** Streamed==read-back proves
  transport only; the durable record is FILES. Close the table before prune.
- **Never cite `_mail/*/inbox/` in anything durable** — mail is consumed; ADR-GUARD will refuse
  the commit and be right. Durable evidence → the bed, or `temple/evidence/<NNNN>/`.
- **Behavior proof before promotion** (Sella L8): fresh-session verification before wiring goes
  live. majkee owns deploy. **Plain pull, never rebase, in nablarva** if termbrana is touched.
- **Head writes only this bed's STATUS.** Sibling ownership is declared in each sibling's RUNBOOK.
- Precedence collisions: this program's clauses are terminal in-domain; cross-domain goes up to
  `/home/hruzam/reposoma/temple/precedence.md` (decision 0013).

## references — point, never copy

- `/home/hruzam/reposoma/_cold-start/card/CS.reverse-tunnel.2026-09-03.md` — the founding card
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` (incl. §The golden rule) +
  `res/{token-economy,cross-vendor-seat,research,csharp-head}.md`
- `/home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md` · `/home/hruzam/ia-sync/HANDSHAKE.md`
- `/home/hruzam/ia-sync/zsh/guides/keyboard.md` — palette grammar law

## What closes this gate

The gate line, verbatim, all clauses — witnessed, not self-confirmed. Before ANY prune: an explicit
promotion manifest in `_bus/` listing EVERY `raw/` keeper and its destination; receipts committed;
counter-signs on disk; router line removed from `/home/hruzam/ia-sync/pulse.md`; the head's
experience transfer authored (the ritual). Beds prune in reverse order — leader last.

## What this program deliberately does not do

- Author or pre-shape 01's instrument RUNBOOK (Cartan's, by Blessing — the swap is the test)
- Amend the runbook GUIDE's no-program-file law now (owed post-arc, against observed behavior)
- Fix the palette letters before the build-time collision check
- Touch Anthropic Remote Control, tmux rails, or any transport the POINT excluded
