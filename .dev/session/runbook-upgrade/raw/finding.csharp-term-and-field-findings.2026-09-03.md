---
kind: finding — substrate for the post-probe GUIDE revision
date: 2026-09-03
from: atlas-ui · head of roster-reform-01-triad (closed + pruned same day; receipts in git
  history, ia-sync commits 028b448 · 31f7ad1 · 76f0390, reposoma 05c036e)
status: substrate — NOT an amendment; the GUIDE brake holds until the cold-resume probe runs
---

# Findings deposited for the runbook-upgrade probe session

## 1. The cSharp term (majkee's board-slang — GUIDE-candidate, one sentence)

**cSharp** — the ↃC hat: a seat that *authors the RUNBOOK and stays live through the whole
arc* as navigator and `status_owner`; it may delegate every body of work and receive only
navigation + test parts; it closes the session if it can. Distinct from `/runbook`'s
plan-and-PARK exit — same producer shape, different exit. Field-proven twice on 2026-09-03
(atlas-ui head of roster-reform-01-triad · Oraculum's arc now running). Current codification:
ONE `head_note:` sentence in the instance RUNBOOK header, nothing in canon. Candidate: one
line in the GUIDE §Structure naming both exits (park vs stay) — decide AFTER the probe.

## 2. Length bound — live counter-evidence to the guides' own practice

majkee research (2026-09-03): read artifacts (read, not loaded) cool attention past
~150–160 lines. BOTH live RUNBOOKs exceeded it the day the bound was named: this session's
180, roster-reform-01-triad's 224. Neither was retro-edited (read-once law). Forward
practice held the bound (skill 85 · res/ chapters 65/55). Probe question: does the
cold-resume time degrade measurably on the 224-line RUNBOOK vs a res/-split one?

## 3. Header == folder (applied, un-gaveled as law)

majkee ruled header==folder-slug for `runbook-upgrade` (was `upgrade-01-probe`). Tension
with GUIDE `<program>-<NN>-<phase>` naming stands unresolved — roster-reform-01-triad
followed the law, this session follows the slug. One of the two shapes should win at
revision.

## 4. One scar worth a GUIDE line (from the closed gate)

Closure needs an explicit **promotion manifest** step: list every `raw/` keeper before
prune. The closed gate missed two POINT files (survived only via git history). The GUIDE's
"promote what survives" is a principle without a checklist edge; the probe may confirm
whether a mandatory manifest earns its line.
