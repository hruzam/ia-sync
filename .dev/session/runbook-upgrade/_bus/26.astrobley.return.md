---
cycle: 26
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.cartan-csharp.point.md
written: 2026-09-09
host: office
execution_topology: existing operator-designated independent Astrobley window; direct execution with no delegation
---

# RETURN 26 — gaveled presence-board chapter folded, unstaged

## 1. Files changed

- `/home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md` — added the exact
  cycle-24 candidate after only the POINT-26 gavel-banner substitution.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — inserted the one authorized
  `res/presence-board.md` Manifest entry immediately after `res/fanout-turns.md`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.astrobley.return.md` — added
  this RETURN.

No other reposoma or ia-sync file was authored by this cycle. Both repository indexes remain empty.
No stage, commit, push, pull/rebase, reset, stash, deploy, prune, board directory/record, client edit,
tunnel, cross-host operation, VERDICT or STATUS write occurred.

## 2. Commands and outcomes

- Direct execution provenance: Majkee relayed the absolute POINT 26 path into the existing
  independent Astrobley-posture window. No `spawn_agent`, delegate, child task, other-seat wake,
  native Astrobley profile or tunnel was used.
- Frame resolution identified `office` / `hruzam-120922`. ia-sync remained at `0f52f56` on `main`
  with its pre-existing dirty work protected. Reposoma remained at
  `829a0339627985769b07ebe2d22d93238bf36930` on `core`, with only the pre-existing vault/mail dirt
  before this cut. Both indexes were empty.
- Preflight:

  ```sh
  test ! -e /home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md
  sha256sum /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
  git -C /home/hruzam/reposoma diff -- \
    raw.guides/runbook/GUIDE.md raw.guides/runbook/res/presence-board.md
  git -C /home/hruzam/reposoma diff --cached --name-only
  ```

  Exit 0. The chapter was absent, the GUIDE was clean at the required baseline SHA-256
  `1b45b8d09893f3a43f8d8c2c0a281c73b6fc94dccabc4d760ee6727dc379fac4`, the scoped
  diff was empty and the index had no entries. The managed sandbox exposed reposoma read-only;
  the POINT's explicit write authority was presented through the normal approval boundary and
  approved before either target was written.
- The chapter was prepared outside both repositories and compared with RETURN 24's sole outer
  four-backtick Markdown fence after exactly this replacement:

  ```text
  FROM: > **DRAFT — not gaveled.** This candidate has no canonical or client-binding authority.
  TO:   > **GAVELED 2026-09-09 — majkee.**
  ```

  A first prepared copy failed the pinned hash because its two inline-regex backslashes had been
  doubled during manual transfer. The exact source comparison caught this before any repository
  write. Correcting only those two extra characters produced the required hash and exact source
  equality; see field 4.
- The approved target write used:

  ```sh
  install -m 0644 /tmp/presence-board.26.md \
    /home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md && \
  install -m 0644 /tmp/runbook.GUIDE.26.md \
    /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
  ```

  Exit 0.
- Final full-file pins:

  ```text
  0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8  /home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md
  60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863  /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
  ```

  The chapter has 119 newline-terminated lines and 5,811 bytes after the shorter gavel banner.
  The GUIDE has 204 newline-terminated lines and 10,665 bytes.
- Exact verification script, created only at `/tmp/verify-point26.js` and run as
  `node /tmp/verify-point26.js`:

  ```js
  const fs = require('fs');
  const path = require('path');
  const crypto = require('crypto');
  const assert = require('assert');

  const bus = '/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus';
  const guideRoot = '/home/hruzam/reposoma/raw.guides/runbook';
  const source = fs.readFileSync(path.join(bus, '24.astrobley.return.md'), 'utf8');
  const fences = [...source.matchAll(/^````markdown\n([\s\S]*?)^````$/gm)];
  assert.equal(fences.length, 1);

  const draftBanner = '> **DRAFT — not gaveled.** This candidate has no canonical or client-binding authority.';
  const gavelBanner = '> **GAVELED 2026-09-09 — majkee.**';
  const expectedChapter = fences[0][1].replace(draftBanner, gavelBanner);
  const chapter = fs.readFileSync(path.join(guideRoot, 'res/presence-board.md'), 'utf8');
  const guide = fs.readFileSync(path.join(guideRoot, 'GUIDE.md'), 'utf8');
  const sha256 = value => crypto.createHash('sha256').update(value).digest('hex');

  assert.equal(chapter, expectedChapter);
  assert.equal(sha256(chapter), '0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8');
  assert.equal(sha256(guide), '60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863');
  assert.deepEqual(chapter.split('\n', 4), [
    '---',
    'title: Presence board — advisory cross-session attachment records',
    'chapter-of: runbook',
    '---',
  ]);
  assert(!chapter.startsWith('\ufeff'));
  assert(!chapter.includes('\r'));
  assert(!chapter.includes('\0'));
  assert(chapter.endsWith('\n'));
  assert.equal((guide.match(/^- `res\/presence-board\.md` · chapter · GAVELED 2026-09-09 — advisory cross-session attachment records and detach boundary$/gm) || []).length, 1);

  const chapterFiles = fs.readdirSync(path.join(guideRoot, 'res'))
    .filter(name => name.endsWith('.md'))
    .sort();
  const manifestFiles = [...guide.matchAll(/^- `res\/([^`]+\.md)` · chapter ·/gm)]
    .map(match => match[1])
    .sort();
  assert.deepEqual(manifestFiles, chapterFiles);

  console.log(JSON.stringify({
    result: 'PASS',
    source_fences: fences.length,
    chapter_sha256: sha256(chapter),
    chapter_lines: chapter.split('\n').length - 1,
    chapter_bytes: Buffer.byteLength(chapter),
    guide_sha256: sha256(guide),
    manifest_chapters: manifestFiles.length,
    frontmatter: 'PASS',
    framing: 'PASS',
  }));
  ```

  Exit 0:

  ```json
  {"result":"PASS","source_fences":1,"chapter_sha256":"0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8","chapter_lines":119,"chapter_bytes":5811,"guide_sha256":"60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863","manifest_chapters":6,"frontmatter":"PASS","framing":"PASS"}
  ```
- `git -C /home/hruzam/reposoma diff --check` — exit 0. Because Git does not inspect the
  untracked chapter, separate trailing-whitespace and byte-framing checks also passed: no trailing
  blanks, BOM, CR, NUL or missing final LF.
- `git -C /home/hruzam/reposoma diff -- raw.guides/runbook/GUIDE.md` shows exactly the one
  authorized Manifest insertion. `git -C /home/hruzam/reposoma status --short -- raw.guides`
  reports only:

  ```text
   M raw.guides/runbook/GUIDE.md
  ?? raw.guides/runbook/res/presence-board.md
  ```

- The five pre-existing chapters retained their before-state SHA-256 values:

  ```text
  2bb300ec96c9b1efda9f3dc97900f7bd81f4d8a78da3df13cb5d90490c81645c  cross-vendor-seat.md
  a0d6d48b7eb162fb327e3d83b5ced7a874f6d76c7c39d3800eb3f16e73bb5477  csharp-head-protocol.md
  4fb38edd3ddda0c42f0584232bc78264db198bc7c446917584c9f2d71ef45485  fanout-turns.md
  25d28ab420b54dd24b88eb4371cee91cd4717bfe5e9e1b26bb84c2a55dec265a  research.md
  52314fcf0f1c41c8f8cbf454ae6c997cbbd659b7e1547b2ab242c70891fa5237  token-economy.md
  ```

  The explicitly read unrelated BUS, guide-writing and STATUS guides also retained their captured
  hashes `4576327028c709bd2ddc5209b59d5f3e2ac1e66c352bf330a298489ae1230754`,
  `52d140a1cda7f2667426525e508e4da5ccbd8cac83e173e187b2a571f5dd9ec4`, and
  `74512734c5ee6a2b0c90d7f1a560d4d2ec3b2fc8d6d0ca23ae80ccc23fe4d445`.
  Scoped raw.guides porcelain confirms no other guide path changed.
- Final `git diff --cached --name-only` in ia-sync and
  `git -C /home/hruzam/reposoma diff --cached --name-only` both exited 0 with no output.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.cartan-csharp.point.md` — exact
  authority, source/output pins and two-file boundary.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.astrobley.return.md` — sole
  gaveled source fence, full-file SHA-256
  `304dcf8fdb8bad34daa891ceb730c2ca3ff0c61a56a4c6b266b47e4a2fa55e26`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.verdict.md` — accepted
  candidate pin and published source checker.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/25.trajectory-csharp.return.md` and
  `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/25.cartan-csharp.verdict.md` — client-fit
  assessment, optional wording proposals and their declined disposition.
- `/home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md` — installed gaveled chapter.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — installed Manifest registration.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md`, **Side documents — the manifest
  law** — chapter registration and orphan boundary.
- `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`, **What distinguishes
  it** — delegated author boundary respected; Cartan receives navigation and test evidence.
- `/tmp/verify-point26.js` — ephemeral exact verification body reproduced in field 2; it is not
  canon, a client test or durable evidence.

## 4. Mismatches discovered

1. The first temporary chapter transfer doubled the two backslashes in the inline filename regex,
   yielding SHA-256 `84ab73f7cb16a9bb2caf26a77fa07b43b66e667d70661dfc16dc07fb1ddb0aac`
   instead of the POINT pin. The source-equality assertion caught it before reposoma was written.
   Removing only the two extra backslashes produced exact candidate equality and the required
   `0bcd6c...` hash. No partial guide fold or target rollback occurred.
2. An early combined shell framing check used an `rg` alternation containing a literal NUL. That
   malformed search printed every chapter line and made the compound command exit 1 after its
   trailing-whitespace subcheck had passed. It did not change files. The explicit Node assertions
   above and a corrected shell check for final LF, BOM and CR all exited 0; no framing defect exists.
3. Reposoma was outside this session's default writable root, as POINT 26 anticipated. Normal managed
   approval granted only the exact two-file install after both outputs already matched their pins.
   No duplicate guide home or permission bypass was used.
4. No source/content conflict appeared: the chapter remained absent and the GUIDE baseline matched
   at write preflight. Reposoma's unrelated cold-start and mail dirt remained present and untouched.

## 5. Recommended next task

The original `cartan-csharp` head independently verifies this RETURN against the actual unstaged
reposoma diff, reruns proportionate checks, writes VERDICT 26 and alone reconciles STATUS. If accepted,
the two-file guide cut remains ready for the separately governed audit/gavel/commit path. Do not stage
or commit from this Astrobley window.

## 6. Remaining uncertainty

- The guide files are unstaged and uncommitted. They are local to this reposoma checkout; no home or
  remote carry is claimed.
- This RETURN and its filename do not independently prove the UI/process topology. Majkee directly
  designated and used the existing Astrobley window; Cartan remains the crossed verifier.
- The exact chapter contract is installed as gaveled prose, but no `_active/` directory, presence
  record, client parser/view, RNG operation, exclusive-create race, transport path or detach behavior
  was created or tested in cycle 26.
- The exact-source verifier tests document bytes, frontmatter/framing and Manifest coverage. It does
  not test the unbuilt client or reproduce Trajectory's omitted parser harness.
- Existing runtime P0–P6 and the separate unchanged Claude-guide proof remain outside this fold and
  unresolved under the original session gate.
