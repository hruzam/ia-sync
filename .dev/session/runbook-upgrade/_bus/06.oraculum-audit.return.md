---
cycle: 06
turn: 01
from: oraculum-audit
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/06.cartan-csharp.point.md
written: 2026-09-04
host: office
gates_held: cycle-05 RETURN not read; nothing edited; turn labels judged as experiment, not canon
---

# RETURN 06 — recovery witness for experimental coordination turn 01

## Witness verdict on the hand-written STATUS

All four required exposures hold; the deviations found are themselves evidence for the
candidate doctrine, not against the experiment.

| check | observed | verdict |
|---|---|---|
| exactly one open turn | single `turn: 01` field, STATUS-level | **PASS** |
| two unique delegated branches | cycles 05·astrobley / 06·oraculum-audit — unique cycles, unique seats, matching RUNBOOK participants | **PASS** |
| one content-sensitive `join_when:` | keys on VERDICT **contents** (`disposition` ∈ ACCEPT/STOP/BLOCKED), encodes REVISE-replaces-in-turn; not file presence | **PASS** |
| exactly one `next:` + observable `expected:` | one human-owned relay action, named plainly; expected is checkable | **PASS** |

Deviations (recorded as experiment findings, field 4 below): the delegated rows use dashboard
words (`POINT ready`, `expects <file>`) that the countersign's vocabulary bound forbids
(`awaiting: return | verdict <seat>` only), relay uncertainty sits in `in_flight:` instead of
a `carrier:` row key, `turn:` is hoisted to a STATUS-level field instead of a per-row key, and
row filenames are directory-less. None blocks recovery — the `recovery_probe:` compensates —
but the very first hand-written STATUS drifting from the bounded vocabulary on day one is the
strongest field evidence yet that the bound must be law, not advice.

## The exact cold-resume oracle — zero-context replacement head

Preconditions assumed: a fresh cSharp with no session memory, the folder path, read access,
and no reliance on the stale STATUS's self-announcing hold (a real interrupted head will not
announce itself; this oracle must not need the announcement).

**Step 0 — ground.** Verify `host:`/`worktree:` in STATUS against the machine
(`hostname -s`; `git -C /home/hruzam/ia-sync status --short --branch`). Mismatch = wrong seat
or wrong tree; stop.

**Step 1 — read STATUS as claims, not truth.** Note `turn:`, every `delegated:` row, the
`join_when:` terminal set, and every hold. STATUS is now a hypothesis to reconcile.

**Step 2 — per delegated cycle NN, classify by CONTENTS in this order.** Open each file; never
classify from `ls`:

- **POINT** `_bus/NN.cartan-csharp.point.md`: read `turn:`, `to:`, `return_to:`. Assert
  `to:` = the row's seat and `return_to:` = the expected RETURN path. Mismatch = relay or
  authoring curvature — record, do not repair.
- **RETURN** at the POINT's `return_to:`:
  - absent → branch is **POINT-only**: undispatched or in-progress. These are
    *indistinguishable by design and need no distinction* — the safe action is identical
    (confirm with carrier / wait); the file plane owes no dispatch receipt.
  - present → assert all six numbered headings exist and heading 1 (Files changed) respects
    the POINT's write boundary. Malformed = curvature; treat as unverified claims, not as a
    RETURN.
- **VERDICT** `_bus/NN.<any-seat>.verdict.md`:
  - absent (RETURN present) → branch is **RETURN-present**: the one legal action for this
    branch is verification by the head or the named witness.
  - present → read contents: assert `cycle:` = NN and `return:` path-matches the RETURN just
    read (a VERDICT whose `return:` mismatches closes nothing). Then:
    - `disposition:` ∈ {ACCEPT, STOP, BLOCKED} → branch is **VERDICT-terminal**;
    - `disposition: REVISE` → branch is **still open** — expect a corrective cycle in the
      same turn; if none exists yet, opening it is head work after the STATUS rewrite;
    - read `status_rewritten:` — in this experiment the honest value is
      `deferred: turn 01` or `no + reason`; a `yes` while the STATUS row still says
      outstanding is a hard contradiction: **disk beats STATUS, record the difference**.
  - Proportional re-check (bus-GUIDE verification rule 3): spot-check that the VERDICT's
    cited evidence paths exist before accepting the branch as terminal. Do not re-verify the
    whole RETURN; do not write a second VERDICT; never touch another seat's bus files.

**Step 3 — name the partial state.** Every branch whose disk classification exceeds its
STATUS row (here: cycle NN VERDICT-terminal while the row says outstanding) **is** the
interrupted-head gap. It is ordinary curvature, not a stall condition.

**Step 4 — one safe action: rewrite STATUS before anything else.** Full replacement snapshot:
retype only still-true rows, drop rows for terminal branches (their VERDICT is the receipt),
keep `join_when:` verbatim if any branch remains, set the single `next:` to the classification
outcome — "verify RETURN of cycle NN" if any branch is RETURN-present, else "join". No
dispatch, no new POINT, no guide edit before this rewrite lands.

**Step 5 — join if `join_when` holds.** All branches terminal → the join is the verified
STATUS replacement itself (G5B — no commit required for the join to exist; commit policy
stays repository law). Advance `checkpoint:` with the VERDICT paths as evidence, set
`delegated:` to none, write one new `next:`/`expected:`, and only then open turn 02 if
dispatching again.

**Pass criterion for the experiment:** the replacement head reaches Step 4 with cycle-05 (or
whichever branch was verdicted) classified VERDICT-terminal and the other branch correctly
classified from contents, produces exactly one rewrite and one next action, and at no point
infers anything from file existence alone or edits a witness artifact.

## 1. Files changed

none. (This RETURN only, at the POINT's `return_to:`. No guide, session authority file,
skill, runtime, deployment, or git state touched.)

## 2. Commands and outcomes

No shell commands run — read-only witness seat. Operations: reads of RUNBOOK.md, STATUS.md,
POINT 05, POINT 06; the three GUIDEs and both T2 artifacts verified in-context from cycles
02/04 (unchanged per session holds). Cycle-05 RETURN not read (gate held).

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md` — the witnessed snapshot
  (all four PASS rows; the deviations)
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md` — roster/seat match,
  status_owner, fixed fold shape
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.point.md` and
  `06.cartan-csharp.point.md` — `turn: 01` labels, `to:`/`return_to:` pairs the oracle asserts
- `raw/T2.countersign.fanout-turns.2026-09-04.md` — vocabulary bound, reconciliation-probe
  law, one-open-turn, REVISE-in-turn (the candidate doctrine being tested)
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — verification order, single-writer law
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — 03:00 standard, replacement law

## 4. Mismatches discovered

1. **Row vocabulary drift on first use:** `POINT ready` / `expects <file>` vs the candidate
   bound's `awaiting: return | verdict <seat>`. The bound survived contact with reality by
   being violated immediately — promote it from candidate to chapter law verbatim.
2. **`turn:` hoisted to STATUS level** instead of per-row. Under one-open-turn this is
   arguably the *better* shape (per-row turn is derivable and redundant); the chapter must
   pick one — my recommendation: hoisted field, rows carry `cycle · to · awaiting · carrier`.
3. **Relay uncertainty lives in `in_flight:`**, not in a row `carrier:` key. Honest but
   hybrid; candidate doctrine reserves `in_flight:` for the head's own partial action.
4. **Row filenames are directory-less** (`05.astrobley.return.md` without `_bus/`) — a fresh
   head must infer the directory; 03:00 law prefers unambiguous paths.
5. **The experiment announces itself** (hold: "joins through the deliberate interrupted-head
   probe"), mildly contaminating the recovery test; the oracle above deliberately does not
   depend on that hold.
6. `join_when:` carries a trailing action sentence ("Cartan then verifies and replaces
   STATUS") — action text belongs to `next:`; keep `join_when:` purely observable.

## 5. Recommended next task

Proceed exactly as planned: verify one branch, write its VERDICT with
`status_rewritten: deferred: turn 01`, leave STATUS pre-verdict, replace the head — and hand
the replacement head **nothing but the session folder path**. Grade it against the oracle's
pass criterion above; its divergences from the oracle are the chapter's §9 content, written
for free by the experiment.

## 6. Remaining uncertainty

Laws this live run will NOT prove, even on full success — name them in the closing VERDICT so
success is not overclaimed:

- **G1A absent-RETURN exception** — no silent branch is scheduled; `return: none` never
  exercised.
- **Late artifact after join** (deferred-names-only-open-turn, authorship tense) — no late
  arrival scheduled.
- **REVISE-inherits-turn** — proven only if Cartan actually REVISEs a branch; `join_when:`
  naming it is not exercise.
- **Turn transition** (turn 02 opening, dispatch-interleaving prohibition) — single-turn
  experiment; the later reconciliation turn may prove it, this one cannot.
- **Relay-mismatch stop-before-work** — not injected.
- **Cross-host operation** — all office; the mechanism's cross-host justification stays
  untested (cross-vendor IS exercised: codex head, claude witness).
- **Vocabulary-bound discipline over many rewrites** — one rewrite cycle only; drift was
  observed at authoring, not at retype.
- **G5B residual** narrows but does not close: head replacement is itself a crossed check of
  the join edge — the replacement head verifying the old head's position is the first
  non-self-confirmed join attestation. Worth stating in the chapter; still not a standing
  witness mechanism.
- Cycle-05's RETURN contents — unread by design; convergence is the head's join work.
