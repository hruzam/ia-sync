---
cycle: 23
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.cartan-csharp.point.md
return: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.astrobley.return.md
verified_by: cartan-csharp · 2026-09-06 · office · hruzam-120922
disposition: ACCEPT
gate_effect: advanced — exact draft is ready for Trajectory client-fit review; no canon or client binding
status_rewritten: yes
---

# VERDICT 23 — exact presence-board draft passes Cartan's check

## Claim verification

| RETURN claim | cited evidence | observed result | disposition |
|---|---|---|---|
| Sole authorized write | POINT 23 write scope; before/after porcelain; Git index | `23.astrobley.return.md` is the only new path attributable to this cycle; the pre-existing dirty set remains, and the index is empty | CONFIRMED |
| One extractable candidate with a prospective Manifest line | RETURN field 1 | One four-backtick `markdown` fence extracts the complete light-header DRAFT; the Manifest line remains outside it | CONFIRMED |
| Exact candidate bytes | RETURN field 3 | Including its final newline, the candidate is 120 newline-terminated lines and 5,850 bytes; SHA-256 `e05124138e0faade38f736869116da0d29fcbf41bc004004d059c4572c04302f` | CONFIRMED |
| Schema and example | Candidate **Record grammar** and **Valid record** | Independent Node parsing accepted the ordered eight-field example and exact `presence.<attachment_id>.md` mapping; it rejected missing timezone, invalid calendar date, and offset beyond `14:00` | CONFIRMED |
| Collision and ownership repair | Candidate **Home and attachment identity** and **Ownership and lifecycle** | Distinct 128-bit IDs select distinct filenames for same-seat, same-second and repeated-bed cases; exclusive create-and-retry handles a conflicting target without claiming mathematical uniqueness; reusable labels confer no delete right | CONFIRMED |
| Advisory, remote-path and closing boundaries | Candidate **Advisory boundary**, **Ownership and lifecycle**, and **Reading and display** | No wake, grant, lock, takeover, auto-prune or liveness authority appears; remote `~/` remains host-scoped; detach is separate from gate closure and owed handoff | CONFIRMED |
| No board/client/canon effect | filesystem and repository inspection | `/home/hruzam/reposoma/_active/` remains absent; guides, clients, RUNBOOK and prior BUS receipts were not changed by Astrobley | CONFIRMED within inspected paths |

## Curvature

The first independent byte check excluded the newline immediately before the closing outer fence and
therefore observed 5,849 bytes and a different hash. Repeating the extraction with that final newline,
which is part of the proposed chapter bytes, reproduced Astrobley's 5,850-byte hash exactly. The
reported 120-line count means 120 newline-terminated lines; a JavaScript `split("\n")` exposes a
trailing empty array element and must not be mistaken for a 121st content line.

Astrobley's direct Codex role requires six `implementation_return` fields, while the shared BUS GUIDE
defines another six-heading RETURN envelope. The artifact follows the direct role fields and records
the mismatch. This ACCEPT treats that as the selected seat's task-specific reporting envelope; it does
not amend the BUS GUIDE or establish a second general RETURN schema.

The 128-bit ID is collision-resistant, not mathematically unique. The exclusive no-replacement create
and fresh-ID retry clause is load-bearing. Client parsing details, including any stricter byte-framing
requirements beyond the written grammar, remain Trajectory's review rather than an inferred proof of
an unbuilt client.

## Uncertainty disposition

Trajectory must check the exact candidate for client fit. Majkee then accepts, revises, or rejects the
draft before any guide, Manifest, board, command or client write. The original runbook-upgrade runtime
proofs remain separate and owed. No unbuilt client, live board, other host or mathematical uniqueness
claim was tested or inferred here.
