---
cycle: 04
from: oraculum-audit
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/04.cartan-csharp.point.md
written: 2026-09-04
host: office
corpus: cycle-01 RETURN + VERDICT, cycle-02 VERDICT, four GUIDEs, runbook raw/ artifact;
  the §§10–12 candidate remains unavailable and was not inferred (gate held)
---

# RETURN 04 — crossed pass on Astrobley's exact fold

## Disposition (ranked)

**REVISE — narrow.** The chapter outline (§1–§10) and the bus/status GUIDE seams can be
adopted essentially as drafted; Astrobley's conditional-subsection placement in status/GUIDE.md
is an improvement over my own cycle-02 stub wording and satisfies my map-honesty oracle. The
required deltas are bounded: (a) the manifest is incomplete in a way the four-class taxonomy
cannot currently express, (b) two *general* laws are wrongly demoted into the conditional
chapter, (c) `verified:` is hardcoded to a date that will be wrong at gavel, (d) the
verification plan's exactly-four-file scope contradicts the fold's own consequences. Everything
else reduces to the gavel choices in field 5.

## Converged and disputed clauses

| clause | Astrobley | Oraculum 02 / countersign | status |
|---|---|---|---|
| No 4th BUS kind, no TURN file, no join-VERDICT | reject both | reject both | **CONVERGED** |
| Fan-out = STATUS `delegated:` + one `join_when:` | yes | yes | **CONVERGED** |
| One chapter `runbook/res/fanout-turns.md`, `chapter-of: runbook` | yes | yes | **CONVERGED** |
| Schema stubs/seams in owning GUIDEs, doctrine in chapter | yes (refined) | yes (cycle 02) | **CONVERGED** — Astrobley's "conditional labels subsection after the mandatory block" is the better cut; preserves the all-mandatory invariant while curing false-map omission |
| REVISE closes the cycle, does not satisfy the branch; corrective cycle inherits the turn | yes | countersign am. 1 | **CONVERGED** — Astrobley's level separation resolves the "VERDICT closes a cycle" tension cleanly |
| `deferred` names only the turn open *at authorship*; historical markers stay valid | yes | countersign am. 3, tense-implicit | **CONVERGED** — the authorship-tense refinement is correct and should enter the chapter verbatim |
| One open turn; late *arrivals* may interleave, new *dispatches* may not | yes | countersign self-contradicted (its own cycle-09 example) | **CONVERGED** — Astrobley's catch is right; VERDICT 01 confirmed |
| `recovery_probe:` reconciles VERDICT *contents*, not presence | yes | countersign | **CONVERGED** |
| Only `status_owner` rewrites; witness attests `deferred` | yes | yes | **CONVERGED** on the law — **DISPUTED** on its home (see G2) |
| `turn:` not on RETURN | no, with structural reason (verifier checks cycle→POINT binding, no trusted echo) | left open | **CONVERGED** — I accept Astrobley's reason; the open item closes, no gavel needed |
| Relay mismatch = stop-before-work; receiver validates `to:` + `return_to:` seat | yes | countersign had a weaker echo idea | **CONVERGED** — Astrobley's version supersedes the echo; drafting discretion, no gavel needed |
| Retirement trigger for the file-plane delta (§10) | yes | golden-rule law | **CONVERGED** |
| Frontmatter prepended, legacy inline labels kept in place this cut | yes | my seam 1 (preserve authority markers) | **CONVERGED** — nothing vanishes; passes my mechanical-repair oracle |
| Seat-uniqueness law lives in the chapter | chapter-only ("stay in the chapter rather than restated") | runbook/GUIDE.md (my cycle-02 §fold item 4; countersign's relocation principle) | **DISPUTED** → G2 |
| Verifier-attests/owner-rewrites law's home | chapter (§6 of outline) | runbook/GUIDE.md ("worth stating as law regardless of this proposal" — countersign) | **DISPUTED** → G2 |
| Manifest = five res/ chapters | as proposed | my cycle-02 manifest had the same omission | **REFUTED by verifier (VERDICT 01), confirmed here** — the slug also holds `raw/field.runbook-check-rules.2026-09-03.md` → G3 |
| Manifest lines carry class + role only | yes | my seam 1 wants gavel-state on the line (DRAFT/GAVELED) | **DISPUTED** → G7 |
| `verified: 2026-09-04` in the fold text | hardcoded, caveated in uncertainty | must be the actual gavel date (VERDICT 02 ruling) | **CONVERGED in principle, wrong in the artifact** — template it as `<gavel date>` |
| Absent-RETURN VERDICT (`return: none + reason`) | flagged as ungaveled, conditionally drafted | not addressed in cycle 02; assessment assumed it legal without noticing the arity break | **OPEN — the load-bearing gavel** → G1 |
| Commit-as-JOIN | demoted to repo/operator policy | assessment made the commit the receipt | **DISPUTED** → G5 |

## Check of the existing raw/ artifact

`/home/hruzam/reposoma/raw.guides/runbook/raw/field.runbook-check-rules.2026-09-03.md` read in
full. Findings:

1. **It fits none of the four manifest classes** (guide-writing L60–64). It is `.md` (not an
   attachment), carries no `chapter-of:` (not a chapter), and is not superseded (not legacy) —
   it is forward-looking *substrate*: 8 detection patterns promoted verbatim from
   roster-reform-01-triad at gate closure, feeding a future Vara runbook-check skill.
2. **Its promotion created orphan drift under already-standing law.** The manifest law was
   canonized 2026-08-20; the promotion landed 2026-09-03 into a `raw/` subfolder the
   guide-writing shape does not define at all (the shape is `GUIDE.md` + `res/`). The
   promotion header explicitly chose this home ("they graduate to the guide's raw/"), so
   relocation would break a closed session's promotion pointer.
3. **Self-referential bonus:** its own patterns 1 and 8 (ungaveled alternates; draft stage
   with no promotion note) are exactly what a gavel-state annotation on manifest lines (G7)
   would make mechanically detectable in this very slug — the runbook GUIDE's `extends:`
   lines currently name two DRAFT chapters that Astrobley's manifest would flatten to
   indistinguishable "chapter" entries.

## 1. Files changed

none. (This RETURN only, at the POINT's declared write path. No guide, session authority file,
skill, zsh, git, or deployment state touched.)

## 2. Commands and outcomes

No shell commands run — read-only seat per POINT gates. Operations: reads of the four
previously unseen artifacts named in `paths.read` (cycle-01 RETURN + VERDICT, cycle-02
VERDICT, the runbook raw/ artifact); the four GUIDEs and my own cycle-02 RETURN were verified
in-context from cycle 02. Astrobley's git/hash/tree claims (reposoma clean at `c235add`;
exactly four tracked res/ chapters) are accepted as verified by VERDICT 01, not re-run here —
this seat has no shell.

## 3. Evidence paths

- `_bus/01.astrobley.return.md` — §4 items 1–8 (pressure analysis), chapter outline, exact
  three-GUIDE cut, verification plan, §6 uncertainties
- `_bus/01.cartan-csharp.verdict.md` — manifest REFUTED row; absent-RETURN UNVERIFIED row;
  countersign self-contradiction confirmed
- `_bus/02.cartan-csharp.verdict.md` — schema-stub boundary accepted; three-seams-to-one-ruling
  disposition; legacy-normalization scope flagged UNVERIFIED
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` — L53–68 (manifest classes +
  orphan rule), L28–48 (slug shape: GUIDE.md + res/ only), L44–48 (opportunistic migration)
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — L7–8 (DRAFT vs GAVELED extends lines),
  front-matter section (participant tuple = where seat law natively lives)
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — L44–48 (single-writer law — why nobody may
  author a surrogate RETURN), L130–141 (VERDICT requires `return:` path), L156–164 (closure)
- `/home/hruzam/reposoma/raw.guides/runbook/raw/field.runbook-check-rules.2026-09-03.md` —
  header (promotion provenance), patterns 1 and 8

## 4. Mismatches discovered

1. **Astrobley's verification plan step 1 contradicts the fold's own consequences.** "Diff
   must name exactly four files" — but the manifest declaring four existing res/ files as
   class *chapter* while those files lack the class-defining `chapter-of:` marker manufactures
   an immediate doc/disk conflict (writer rule 4); curing it (G4) widens the diff, as does any
   G3 outcome. The plan must be regenerated from the gavel's outcome, not adopted as written.
2. **General laws demoted into a conditional chapter.** Seat uniqueness and
   owner-rewrites/verifier-attests bind *every* multi-seat session — two incarnations of one
   agent ambiguate in a plain two-seat session with zero turns. Hiding them in
   `fanout-turns.md` means a non-fan-out session never reads them. Contradicts the
   countersign's own relocation principle, which the session already accepted.
3. **Taxonomy gap, not mere omission:** the raw/ artifact cannot be manifest-listed under
   current law at all (see check above). Both my cycle-02 manifest and Astrobley's five-line
   manifest missed it; the fix requires a ruling, not a longer list.
4. **The bus-GUIDE cut conditionally pre-writes an ungaveled schema change** (`return: none +
   reason`). Astrobley caveats it honestly ("must be omitted if the gavel rejects"), so this
   is not smuggling — but the fold text as drafted is not gavel-neutral and must be prepared
   in two variants or templated.

## 5. Recommended next task

One bounded task: put the following choice sheet to @majkee and record the rulings in a
decision artifact before the live experiment; the fold text is then regenerated from the
rulings and the experiment runs against un-edited guides per the accepted timing brake.

- **G1 — absent-RETURN VERDICT** (the load-bearing one). **A:** allow the narrow exception —
  `return: none + <reason>`, dispositions STOP/BLOCKED only, RETURN-absence cited as the
  observed fact, any late RETURN enters a new cycle. **B:** reject — a silent branch cannot
  close; the turn cannot partially join past it. *Lean A:* under B the worker-disappears
  failure mode has no legal closure receipt — the cycle dangles forever, since single-writer
  law (bus L44) forbids a surrogate RETURN and only a VERDICT closes the edge. B leaves the
  mechanism structurally incomplete on a failure day, which is the day it exists for.
- **G2 — home of the two general laws** (seat uniqueness; owner-rewrites/verifier-attests).
  **A:** one sentence each in runbook/GUIDE.md, chapter points. **B:** chapter-only
  (Astrobley). *Lean A* — they are unconditional session law exposed by fan-out, not created
  by it.
- **G3 — the raw/ artifact.** **A:** manifest line with an honest fifth class, `substrate`
  ("promoted evidence feeding a future skill; resolver never serves it") — one-line taxonomy
  extension inside the same gavel. **B:** relocate to raw.research/temple evidence home +
  pointer stub. **C:** mark legacy. *Lean A > B > C* — A keeps the closed session's promotion
  pointer true; C is false (nothing supersedes it).
- **G4 — normalize the four existing res/ chapter headers** (`title:` + `chapter-of: runbook`
  only, no body edits). **A:** same gavel. **B:** opportunistically later. *Lean A* — the
  manifest's class claim is defined BY that marker; declaring without normalizing ships
  instant curvature.
- **G5 — join durability.** **A:** join must land as one commit (STATUS rewrite + turn
  VERDICTs). **B:** join = verified `status_owner` STATUS replacement; commit policy stays
  repo/operator policy (Astrobley). *Lean B for the experiment*, with the residual named: under
  B the join is self-confirmed, which cSharp law dislikes — the live run should record whether
  the absent commit receipt ever bites, and G5 is re-put at final gavel with that evidence.
- **G6 — metadata ruling (confirm, already drafted in VERDICT 02):** authority markers
  preserved in body, `verified:` = actual gavel date (template `<gavel date>` in all fold
  text), metadata edits ride the same gavel — no format-only exemption.
- **G7 — gavel-state annotation on manifest lines** (`· GAVELED 2026-09-03` / `· DRAFT`).
  **A:** yes. **B:** class + role only. *Lean A* — costs six words, closes my cycle-02 seam 1,
  and gives the raw/ artifact's own detection patterns (1, 8) a mechanical hook.

(Advice to the verifying seat; STATUS.next remains cartan-csharp's.)

## 6. Remaining uncertainty

- The §§10–12 candidate draft stays a recorded missing source; if it surfaces and contradicts
  a converged clause, the affected clause reopens — nothing here reconstructs it.
- G5's residual (self-confirmed join under option B) is accepted for the experiment only, not
  disposed.
- `carrier:` field vs participant-tuple instrument — deliberately NOT put to gavel; both
  audits and the countersign agree it waits for the live run.
- Astrobley's git/tree/hash claims are trusted via VERDICT 01, not independently re-run
  (no-shell seat) — an accepted operational limit.
- Whether a sixth pattern of legacy chapter debt exists beyond the four res/ headers (e.g.
  inside chapter bodies) was outside every read scope so far; G4 covers headers only.
