---
cycle: 23
from: cartan-csharp
to: astrobley
scope: Author one minimal exact-format presence-board chapter candidate inside the RETURN, for peer client-fit review before gavel
paths:
  read:
    - /home/hruzam/ia-sync/AGENTS.md
    - /home/hruzam/ia-sync/SYNC_DISCIPLINE.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/21.trajectory-csharp.point.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/21.cartan-csharp.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/22.trajectory-csharp.review.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/22.cartan-csharp.return.md
    - /home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md
    - /home/hruzam/reposoma/raw.guides/guide-writing/res/slug-directories.md
    - /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
    - /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md
    - /home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
    - /home/hruzam/reposoma/raw.guides/bus/GUIDE.md
  write:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.astrobley.return.md
gates:
  - Resolve your declared astrobley seat and return_to before work. Existing documentation writer; no new seat or native-subagent substitution.
  - Draft only. Write only the named RETURN; no guide/Manifest, board record, client, source, RUNBOOK, STATUS, pulse, journal or live-file edit. No Git mutation, deploy, push, prune, wake, tunnel or cross-host action.
  - You are not alone in this worktree. Preserve all concurrent work, especially Trajectory's zsh/session and wiring changes; do not revert, absorb or repair them.
  - This is an operator-requested follow-on candidate, not a change to the original gate or permission to reopen accepted doctrine. No general peer-head protocol or new lifecycle authority.
done_when: One six-field RETURN contains the complete small candidate chapter in one extractable Markdown fence, a proposed Manifest line, exact schema/filename/lifecycle rules, one valid example and bounded edge-case verification; no unresolved alternatives masquerade as a spec and all out-of-scope files remain untouched.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.astrobley.return.md
---

# POINT 23 — the smallest contract, not its clients

Majkee relayed Trajectory's REVIEW 22 and requested the exact-format draft for client-fit
review, then gavel. The synchronized-peer case lock and one-leaf placement are accepted.
You retain guide authorship under RUNBOOK prompt-1; this new task authorizes a candidate in
the RETURN, not installation in reposoma. Cartan verifies and routes it back to Trajectory.

## Deliverable

Under RETURN field 1, put the complete proposed `runbook/res/presence-board.md` inside one
outer four-backtick `markdown` fence (so its record example may use triple backticks).
Use the light chapter header and an explicit DRAFT / not-gaveled notice. Give the single
prospective GUIDE Manifest line outside that fence. This is review material, not a second
guide home. Target roughly 80–120 chapter lines; clarity beats padding or cryptic compression.

## Design constraints, not prewritten prose

1. Preserve RETURN 21's advisory boundary and existing closing references. Presence may inform;
   it never commands or authorizes. No wake, delivery, grant, resource lock, scheduling, takeover
   or automatic prune. Omit a new two-head law, `_closed/`, daemon and mandatory heartbeat.
2. Proposed home remains `~/reposoma/_active/`, one file per attachment. Specify ONE parse shape:
   `---` frontmatter with flat scalar keys, required/optional fields, quoting, filename grammar
   and one valid record example. Include seat, host, workspace, bed, attachment timestamp and
   optional one-line `note` for display only. No free-text policy parsing or private runtime ID.
3. Repair REVIEW 22's timestamp-only identity: two same-seat attachments in the same second and
   different workspaces with the same bed slug must not select the same destination. Prefer a
   small opaque attachment ID plus create-without-replacement; choose one exact representation,
   not a menu. Name collision/conflicting-record handling without claiming mathematical uniqueness
   or adding a registry/lock service. Identity allocation is not permission over a workspace.
4. Define single-writer ownership for the attachment, not merely the seat's name. New incarnations
   cannot delete earlier records just because labels match. Detach only one's own record;
   orphan cleanup needs explicit human authority. Readers never auto-delete or infer takeover.
5. Accept timezone-bearing ISO-8601 in the body, never mtime as age. Pin the supported syntax;
   timestamp is an attachment declaration, not liveness. Keep attachment identity/time stable.
   Prefer no refresh field in v1 unless a demonstrated need warrants it. Age thresholds remain
   client display policy; invalid, ambiguous or future/skewed evidence cannot imply activity/death.
6. Accept whole quoted `~/` pointers for workspace and bed. They are scoped to the declaring host,
   not automatically the reader's home or evidence of a local path. Bed may be outside workspace
   (this peer's ia-sync work has its bed in nablarva). No implicit remote access or path relocation.
7. Keep detach separate from gate closure. Reference GUIDE **On gate closure**, protocol receipts /
   raw-keeper manifest / transfer, and fan-out late artifacts; add only the board-detach and owed
   peer-handoff seam. Absence, stale entries and TUI unknown rows are not closure evidence.
8. State how malformed/duplicate-key or conflicted records are reported without guessing a live
   participant or hiding uncertainty. Plain-file attachment must work for both runtimes without
   `rb-mark`; shell/TUI commands, client code, a universal validator and skill edits are not deliverables.

## Proportionate verification and stop

Validate the candidate example's flat frontmatter and its exact filename/identity mapping.
Use in-memory examples for concurrent same-second attachments, repeated bed slugs in different
workspaces, foreign ownership, remote `~`, missing timezone and malformed record handling.
Report actual checks versus semantic review; do not claim unbuilt clients were tested. Keep the
commands/results in the RETURN, plus a hash of the extractable chapter bytes for later checks.
No live board test or board directory is needed. Stop after the RETURN: Cartan's independent
check → Trajectory's client-fit check → majkee's gavel, with no premature binding to the draft.
