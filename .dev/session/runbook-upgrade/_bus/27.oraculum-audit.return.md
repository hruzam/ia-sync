---
cycle: 27
from: oraculum-audit
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/27.cartan-csharp.point.md
written: 2026-09-09
host: office
gates_held: read-only witness in the operator's Oraculum window; only this RETURN written; no
  stage/commit/board/prune; gavel treated as settled; no redesign
---

# RETURN 27 — pre-commit witness of the two-file presence-board cut

## Recommendation

**ACCEPT.** The installed chapter equals RETURN 24's pinned candidate after exactly the
POINT-26 banner substitution and nothing else; the GUIDE carries exactly one new Manifest row
in the correct (alphabetical) position with the truthful `GAVELED 2026-09-09` state; the
advisory/single-owner/host-frame boundary is intact; closing law remains owned by the existing
cited canon. No concrete discrepancy blocks the commit. One pre-commit rerun is required to
close this seat's shell gap (field 6).

**Q1 — source equality and GUIDE delta (checked, my own evidence).** Line-level comparison of
the installed chapter against RETURN 24's sole outer fence held in context: every line
corresponds; the only difference is the banner (`DRAFT — not gaveled…` → `**GAVELED
2026-09-09 — majkee.**`), which matches POINT 26's substitution and POINT 27's gavel date. The
filename-grammar regex carries single backslashes (`^presence\.[0-9a-f]{32}\.md$`) — the
manual-transfer doubling recorded in VERDICT 26 is absent from the installed bytes. The GUIDE:
full re-read L1–204 against my cycle-10 audited fold (date-substituted at the 2026-09-05
commit) shows exactly one added line — the presence-board row, immediately after
`res/fanout-turns.md`, preserving the Manifest's alphabetical order, text byte-matching POINT
26's specification. All other GUIDE content — frontmatter, authority lines, both G2A
sentences, protocol paragraph, routing seam, session file set, closure and style laws — is
unchanged. `csharp-head-protocol.md` and `fanout-turns.md` pins in VERDICT 26 equal the
cycle-09 pins I hold; those chapters are untouched.

**Q2 — registration truth and boundary (checked).** Light two-field chapter header per
guide-writing law; registered once in the Manifest; one topic. The advisory boundary is
airtight in the prose: presence never commands or authorizes work, wake, delivery, permission,
scope, resource lock, schedule, takeover, prune, or access; readers never treat
presence/absence/age/damage as a behavioral gate. Single-writer ownership with human-only
orphan removal; host-frame resolution with remote `~/` as pointer only. Closing law is pointed
at, never restated or extended: RUNBOOK **On gate closure**, cSharp protocol receipts/promotion
manifest/transfer, fanout-turns **Cold recovery and late artifacts** — no new closing law, no
fourth artifact kind, no doing-state ("shared display surface, not doing-state" is explicit),
no conflict with fanout-turns' no-scheduler/no-read-state boundary.

**Q3 — blocking discrepancies: none.** Non-blocking observations in field 4.

## 1. Files changed

none. (This RETURN only, at the POINT's `return_to:`. No canon, runtime, receipt, STATUS,
RUNBOOK, or peer-bed file touched; concurrent work untouched.)

## 2. Commands and outcomes

No shell commands run — this frame has no shell (limit declared per POINT 27). My own
verification is documentary: complete fresh reads of the installed chapter (119 lines), the
full current GUIDE (204 lines), RETURN 24 (incl. the source fence), VERDICT 25, POINT 26,
RETURN 26, VERDICT 26, RUNBOOK, and the guide-writing/bus GUIDE heads; line-level source
comparison and single-delta GUIDE comparison performed in-context as described above.

Evidence classes, kept distinct:

- **My rerun:** chapter↔fence line equality; GUIDE single-row delta vs my cycle-10 fold audit;
  boundary/registration prose checks; Manifest order and state truthfulness.
- **Head evidence (not my rerun):** the two SHA-256 output pins (`0bcd6c…`, `60dbc9…`), the
  remove-row-restores-baseline hash check, `git diff --check`, porcelain scope, empty indexes,
  reposoma HEAD `829a033…`, byte-framing (BOM/CR/NUL/final-LF), and the preserved-hash sweep —
  all from RETURN 26's published script and VERDICT 26's in-memory rerun. Consistent with
  everything I can observe; not independently executed here.
- **Untested by anyone (out of this cut):** client/parser/TUI behavior, `_active/` creation,
  RNG, exclusive-create race, transport — correctly absent from the diff.

## 3. Evidence paths

- `/home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md` — installed chapter (L8
  banner; L34 regex; L101–107 closure citations; L14–20 advisory boundary)
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — L27 (the sole new row), L22–30
  (Manifest), full-file re-read
- `_bus/24.astrobley.return.md` — the gaveled source fence (L23–142)
- `_bus/26.cartan-csharp.point.md` — substitution spec, pins, two-file boundary
- `_bus/26.astrobley.return.md` + `_bus/26.cartan-csharp.verdict.md` — head-run mechanical
  checks and the manual-transfer/escape-error record relied on as head evidence
- `_bus/25.cartan-csharp.verdict.md` — declined optional wording changes (Q1/Q2), confirming
  the unchanged-contract gavel object

## 4. Mismatches discovered

1. **"The proposed home is `~/reposoma/_active/`" survives inside a GAVELED chapter** —
   intentional per POINT 26/27 (no stylistic cleanup authorized). Truthful today: no directory
   exists, the home declaration is not a board-write assignment. Note for the later purpose-fit
   audit: once a board actually exists, "proposed" becomes stale prose; that is a future
   one-word touch under its own authority, not this commit's problem.
2. **GUIDE `verified: 2026-09-05` not bumped for a 2026-09-09 content change** — intentional
   per POINT 26 and named in VERDICT 26 curvature 3. Tension with the fanout fold's G6
   precedent (metadata lands with content) is real but non-blocking: the new row carries its
   own gavel date, and re-pinning both hashes to bump one date buys nothing. Accept as-is; the
   staleness organ still sees a truthful last-full-verification date.
3. **Manual-transfer deviation + temporary escape error** — recorded in VERDICT 26; I confirm
   only that the *installed* bytes carry the single-backslash regex and full source equality.
   Per POINT 27 I do not certify the extraction as mechanical and do not reconstruct the
   producer's process from hashes.
4. **`_active/` is a reposoma-root surface, outside slug law's scope** — the chapter
   registers the *contract*, which is all guide-writing law governs; the directory itself is
   repo-level policy that begins to exist only with the separately owned client/board work.
   No registration gap; noted so nobody later reads the Manifest as covering `_active/`.

None of these blocks the commit; none is a discrepancy between the cut and its gaveled source.

## 5. Recommended next task

Cartan writes VERDICT 27 and, on ACCEPT, hands @majkee the commit step with one bounded
pre-commit rerun attached: re-hash both files against `0bcd6c…`/`60dbc9…`, confirm scoped
porcelain still shows exactly `M raw.guides/runbook/GUIDE.md` + `?? …/presence-board.md` with
empty index at reposoma HEAD `829a033…` — executed in a shelled frame immediately before
staging, closing both the freeze-to-commit window and this seat's shell gap in one stroke.

## 6. Remaining uncertainty

- Hash and repository-state claims are head evidence consistent with my documentary checks,
  not my own execution; the field-5 pre-commit rerun is the named closure.
- If either pin has moved since VERDICT 26, this RETURN evaluated a different artifact — the
  rerun detects exactly that; POINT 27's pin-drift instruction is satisfied by naming it here.
- Client, transport, board, and detach behavior remain unproven anywhere; the chapter's
  contract-only scope makes that acceptable for this commit and owed to the later
  implementation's own verification.
- The operator's later fresh Oraculum + fresh Cartan purpose-fit audit remains distinct from
  and unclaimed by this witness.
