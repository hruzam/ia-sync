---
cycle: 16
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/16.cartan-csharp.point.md
seat_validation: POINT `to: atlas-ui` and `return_to:` match my declared participant_3 seat.
carriage_note: actual carriage observed from inside remains claude-fable-5 (self-report, no
  independent transcript); fixed roster untouched, no model switch claimed.
authored: 2026-09-05 · office · resident
---

# RETURN 16 — three-file cut authored, validated, dry-run; stopped before staging

## 1. Files changed

| path | nature |
|---|---|
| `/home/hruzam/ia-sync/claude/skills/runbook/SKILL.md` | modified — exactly A (Manifest-routed law line) + B (multi-session-arc bullet) from immutable RETURN 14 |
| `/home/hruzam/ia-sync/codex/skills/octopus/SKILL.md` | modified — exactly C (Manifest-routed guide list) + D (park-vs-stay sentence) from immutable RETURN 14 |
| `/home/hruzam/ia-sync/codex/skills/guide/SKILL.md` | **new** — corrected E, byte-exact to the accepted pin (see field 2); directory created for this one file, no ancillary files, no UI metadata |
| this RETURN | the only other write |

Nothing staged, committed, deployed, pushed, pruned, spawned, or carried. RUNBOOK, STATUS,
guides, Claude guide skill, codex-harness, AGENTS files, deploy.sh, old bus artifacts, live
trees: untouched.

**E provenance — how byte-exactness was guaranteed:** E was not retyped. The single fenced
block was extracted mechanically from RETURN 15 (`awk '/^```$/{f=!f; next} f'`; the RETURN
contains exactly 2 fence lines) into scratchpad, hash-verified, then copied into place.
Process honesty: my first extraction attempt included a malformed second awk stage and
produced a wrong hash **in scratchpad only**; it was discarded and redone — no target file
ever held wrong bytes.

**The scoped diff (A–D), verbatim from `git diff`:**

```diff
--- a/claude/skills/runbook/SKILL.md
+++ b/claude/skills/runbook/SKILL.md
@@ -5,8 +5,9 @@
 I am the planning-head posture, not a seat. Any Houston-family seat (Houston · Oraculum · Flight
 MANNED) runs me on the carriage the operator chose; I never claim or check the model.
 
-**Law I execute, never restate:** `~/reposoma/raw.guides/runbook/GUIDE.md` (+ its `res/` chapters:
-token-economy · cross-vendor-seat) · `status/GUIDE.md` · `PAD/GUIDE.md` · `bus/GUIDE.md`. If this
+**Law I execute, never restate:** `~/reposoma/raw.guides/runbook/GUIDE.md` — its `## Manifest`
+names the live `res/` chapters; route by the Manifest, never by a list baked here — ·
+`status/GUIDE.md` · `PAD/GUIDE.md` · `bus/GUIDE.md`. If this
 card and a guide disagree, the guide wins. Codex sibling: `~/ia-sync/codex/skills/octopus/SKILL.md`
 — same seams, native expression; never copy it.
@@ -36,6 +37,11 @@
   `references` (point, never copy) · what closes the gate · what this session deliberately does not do.
+- **Multi-session arc?** The head seat carries `head_note:` naming its protocol — today only
+  cSharp is gaveled (authors and STAYS; `res/csharp-head-protocol.md`). A head coordinating ≥2
+  independent full CLI sessions from one decision takes `turn:` coordinates and the STATUS
+  `delegated:`/`join_when:` rows from `res/fanout-turns.md`; in-window subagent delegation
+  stays under `res/token-economy.md`. My park envelope is this skill's exit, not the cSharp's.
 - **STATUS.md** — initial: `updated · writer · host · worktree · gate (verbatim) · checkpoint ·
--- a/codex/skills/octopus/SKILL.md
+++ b/codex/skills/octopus/SKILL.md
@@ -18,11 +18,11 @@
 Before writing, resolve the repository instructions, host, worktree, project contract,
 settled locks, and current pulse. Read these shared guides completely:
 
-- `~/reposoma/raw.guides/runbook/GUIDE.md`
+- `~/reposoma/raw.guides/runbook/GUIDE.md`, then the `res/` chapters its `## Manifest` names,
+  as the session's shape makes them applicable: delegation → `token-economy`; a cross-vendor
+  participant → `cross-vendor-seat`; a multi-session head arc → `csharp-head-protocol`;
+  coordinating independent full CLI sessions from one decision → `fanout-turns`.
 - `~/reposoma/raw.guides/status/GUIDE.md`
-- `~/reposoma/raw.guides/runbook/res/token-economy.md` and
-  `~/reposoma/raw.guides/runbook/res/cross-vendor-seat.md` when delegation or a cross-vendor
-  participant makes them applicable.
@@ -86,3 +86,6 @@
 After this envelope, stop. A useful plan is not permission to execute it.
+
+Parking is this skill's exit, not the cSharp head posture's: an arc head declared by RUNBOOK
+`head_note:` stays live under `~/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`.
```

Diff stat: `claude/skills/runbook/SKILL.md | 10 ++++++++--` ·
`codex/skills/octopus/SKILL.md | 11 +++++++----` · 2 files, 15 insertions, 6 deletions;
E adds the third file whole (42 lines). E's full content = RETURN 15 field 1 fenced block,
bound by hash below, not duplicated here.

## 2. Commands and outcomes

Before-state (all matched POINT 16 §1 expectations): ia-sync HEAD `b7a2c09c…0365537`, empty
index, only the expected session/remote-control dirt; reposoma clean at
`3feba72791c1550ba993ff7d4e0d57f1bb2b3e7d`; `codex/skills/guide` and `~/.agents/skills/guide`
both ENOENT; all four relevant source hashes equal to the RETURN-14 frozen values; deployed
runbook (`27661…71f7`) and octopus (`639fd…d2ec`) equal to their sources.

| command | outcome |
|---|---|
| `python3 -B …/skill-creator/scripts/quick_validate.py` on `codex/skills/guide` · `codex/skills/octopus` · `claude/skills/runbook` | **"Skill is valid!" · exit 0, all three** |
| `sha256sum codex/skills/guide/SKILL.md` | `d31ec392b80cf9565fb1374fd1860274d43ed0dd8bcd3e70f98562f53c87b2ce` — **equals the POINT-16 pinned expected E hash** |
| `git -C ~/ia-sync diff --check` | exit 0, no whitespace errors |
| `grep -nP ' $\|\t' codex/skills/guide/SKILL.md` · `tail -c1 \| xxd` | no trailing whitespace/tabs; file ends `0a` — exactly one final newline |
| `bash deploy.sh --dry-run --codex-only` | exit 0; itemized: `cd+++++++++ guide/`, `>f+++++++++ guide/SKILL.md`, `>f.st…… octopus/SKILL.md` — nothing else content-changed |
| `bash deploy.sh --dry-run` (full) | exit 0; adds only `>f.st…… runbook/SKILL.md` under `~/.claude`; gemini/majkee/agents/commands legs itemize no content change; `.d..t` lines are rsync directory-mtime notation, not content |
| after-state: `sha256sum` deployed pair · `ls ~/.agents/skills/guide` · `git diff --cached` · `git status` · both HEADs | deployed hashes **unchanged** (before==after) · deployed guide **still absent** · index empty · only the 3 source paths changed · ia-sync `b7a2c09`, reposoma clean `3feba727` |

After-state source hashes:

```
de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9  claude/skills/runbook/SKILL.md
b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec  codex/skills/octopus/SKILL.md
d31ec392b80cf9565fb1374fd1860274d43ed0dd8bcd3e70f98562f53c87b2ce  codex/skills/guide/SKILL.md
```

Untouched-source recheck: `claude/skills/guide/SKILL.md` still `87d5a796…f6c1bb`,
`codex/skills/codex-harness/SKILL.md` still `9ffc8451…b66e12`.

## 3. Evidence paths

- Baselines applied: RETURN 14 field 1 A–D
  (`_bus/14.atlas-ui.return.md`, `18b1de1c99fff2cdd45e6c0f383c4bc365bf8744992ba26563efd8a614358d06`) ·
  corrected E (`_bus/15.atlas-ui.return.md`,
  `18f023993c14347b39876085ac9d537f565b0938bef676b9ce01ccdafea91d83`).
- Approval record: POINT 16 §"Approval and exact baseline" (majkee 2026-09-05 + Cartan
  Codex-style confirmation).
- Semantic conformance (POINT 16 step 4, checked on the diff above): park/no-execution
  clauses retained verbatim in both producers; `csharp-head-protocol.md` is the only protocol
  route written (no retired citation introduced); implicit-selection policy untouched (no
  `agents/openai.yaml`, no policy field anywhere); E is the corrected RETURN-15 text, not the
  RETURN-14 original — proven by the pinned-hash equality, which VERDICT 15 computed from the
  corrected block.

## 4. Mismatches discovered

1. `/home/hruzam/ia-sync/.codex/`: **ENOENT again in this frame** (06:2x CEST), after
   VERDICT 15 observed it empty/read-only at 05:58. The flicker continues across observers;
   still unexplained, still untouched, now recurring — per RETURN 15 field 6 it belongs in
   STATUS `holds:` for the deploy gate.
2. Dry-run output includes `.d..t……` directory-mtime rows for `guide/` and `octopus/` parents
   — rsync notation for timestamp-only directory metadata, not a content drift; disclosed,
   not repaired.
3. None otherwise: no unexpected source drift, no unrelated would-change content in either
   dry-run, concurrent codex-remote-control work untouched.

## 5. Recommended next task

Cartan verifies this RETURN against the pinned baselines (diff↔A–D anchors, E hash, validator
exits, dry-run itemization, live-tree immutability). On ACCEPT: @majkee's hands take the
remaining gates in order — stage/commit the three files on ia-sync, `bash deploy.sh`, then
the P0–P6 fresh-session proofs from RETURN 15, recording observed calls (not candidates) for
the closing VERDICT's invocation map.

## 6. Remaining uncertainty

- Everything behavioral: P0–P6 unrun; validator + dry-run prove format and targeting, never
  discovery, selection, or fresh-session conduct. The four final calls remain candidates.
- The staged-only state is live-invisible by design: until deploy, both runtimes still serve
  the before-hash cards; nothing this cycle changed what a fresh session sees today.
- The `.codex` flicker (field 4.1) — origin unknown; recommend a `holds:` line before the
  deploy gate opens.
- Commit/push policy, home-machine carry, and reposoma's 1-ahead push remain outside this
  cycle; office-local claims only.
- My process history (exact command sequence, the discarded scratchpad extraction) is
  self-reported; current on-disk state is independently checkable via every hash above.
