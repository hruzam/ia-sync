---
cycle: 17
from: cartan-csharp
to: atlas-ui
scope: Check home-side remote updates, then commit only the accepted three-file skill cut locally if the repository gates permit
paths:
  read:
    - /home/hruzam/ia-sync/AGENTS.md
    - /home/hruzam/ia-sync/SYNC_DISCIPLINE.md
    - /home/hruzam/ia-sync/HANDSHAKE.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/16.cartan-csharp.point.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/16.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/16.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/guide/SKILL.md
  write:
    - /home/hruzam/ia-sync/.git/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/17.atlas-ui.return.md
gates:
  - Validate atlas-ui seat and return_to. This is one sequential commit-only cycle, not another authoring task or fan-out turn.
  - The .git write scope permits only the scoped fetch, exact-path staging and one ordinary local commit described below; it does not grant arbitrary Git state manipulation or worktree edits.
  - Majkee approved a local commit of only the three files after ACCEPT 16; push and deployment remain held. His home-push warning is a mandatory remote check, not a push authorization.
  - You are not alone in the worktree. Preserve session-control edits, old BUS artifacts and concurrent codex-remote-control-cli work. Do not include them, the palette files, or other paths in the commit.
  - No source edits, hook bypass, amend, stash, reset, clean, pull, merge, rebase, push, live deployment, fresh agent session, spawn, wake, prune or cross-host carry. A reconciliation or permission problem means stop and report.
done_when: A six-field RETURN either proves one local commit with exactly the three frozen skill paths and unchanged protected/live state, or reports the precise remote/preflight blocker without staging; fetched remote tips, divergence, checks and remaining sync/deploy gates are explicit.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/17.atlas-ui.return.md
---

# POINT 17 — commit-only approval with home-push preflight

## Operator approval and boundary

Cartan asked for a local commit of only Claude runbook, Codex octopus and new Codex guide,
with push and deployment still held. @majkee answered `go`, then after an interruption:

> blessed, go, be aware on home side was some pushes, when push here on office side.

This authorizes the exact local commit. It does not authorize overwriting home work or changing
the previously held push/deployment scope. Recovery found no partial Git action: HEAD remains
`b7a2c09c6e335cae5a588ab41fdccc6ee0365537`, index empty, no index lock and no cycle-17 artifact.
Remote freshness has NOT been established by this head; cached origin/main is not current proof.

## Preflight before staging

1. Resolve instructions, host, branch, HEAD, index and worktree. Expected branch is `main`, HEAD
   is the full hash above, and the index is empty. Check for existing in-progress Git operations
   or active writers. Never delete a lock. Inspect applicable commit hooks; do not bypass them or
   run a hook that expands this scope into a push, deployment or unrelated source rewrite.
2. Run `git -C /home/hruzam/ia-sync fetch origin` through your normal authorized tool. Record
   the fetched `origin/main` hash and `git -C /home/hruzam/ia-sync rev-list --left-right --count HEAD...origin/main`.
   If origin/main contains commits absent locally, histories diverge, fetch fails, or local HEAD
   has changed, STOP BEFORE STAGING. Report fetched commit subjects and changed paths, especially
   overlap with this cut or protected session work. Do not pull/rebase or stash this dirty tree.
   SYNC_DISCIPLINE requires incoming work to be reconciled first; that needs its own safe scope.
3. Confirm the accepted bytes from VERDICT 16 and absence of unrelated index entries. Preserve
   before-state hashes/status for the protected session files, remote-control brief, palette and
   deployed skill files. The accepted source hashes are:

```text
de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9  claude/skills/runbook/SKILL.md
b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec  codex/skills/octopus/SKILL.md
d31ec392b80cf9565fb1374fd1860274d43ed0dd8bcd3e70f98562f53c87b2ce  codex/skills/guide/SKILL.md
```

## Exact commit, only after preflight passes

Stage only these literal paths:

```sh
git -C /home/hruzam/ia-sync add -- claude/skills/runbook/SKILL.md codex/skills/octopus/SKILL.md codex/skills/guide/SKILL.md
```

Verify the cached path set is exactly those three, each staged blob matches its pinned SHA-256,
`git diff --cached --check` passes and HEAD still equals the expected parent. Unexpected index
content means stop and disclose it; do not reset anyone's work or silently broaden the commit.

Create one ordinary local commit with subject `skills: reconcile runbook routing across Claude and Codex`
and a body recording `majkee approval 2026-09-05; runbook-upgrade cycle 17; source verification ACCEPT 16; runtime proofs pending`.
Do not amend, disable hooks or push. If the commit fails, report the exact state, including any
staged authorized files; do not turn failure into an unapproved repair operation.

Verify commit parent, subject/body, exact changed-path set, committed blob hashes, empty index,
clean status for the three skill paths and preservation of all other dirty work. Record both
repository HEADs/status and unchanged deployed files; the new deployed guide must remain absent.
Return commit identity and command evidence inside RETURN 17. No other evidence file is needed.

A local commit is not sync completion. Before any later authorized office push, fetch again and
perform the required safe pull/reconciliation under SYNC_DISCIPLINE; never force-push around home
commits. Full live deployment remains separately blocked by the unrelated palette delta in
VERDICT 16. All fresh-session proofs remain unrun.
