---
cycle: 19
from: cartan-csharp
to: atlas-ui
scope: Correct only the unverified model label in the local commit attribution; preserve the exact source tree and other message fields
paths:
  read:
    - /home/hruzam/ia-sync/AGENTS.md
    - /home/hruzam/ia-sync/SYNC_DISCIPLINE.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/18.cartan-csharp.point.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/18.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/18.cartan-csharp.verdict.md
  write:
    - /home/hruzam/ia-sync/.git/
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/19.atlas-ui.return.md
gates:
  - Prepared before majkee's reboot; not dispatched. Begin only when majkee relays this absolute POINT after Cartan reconciles STATUS.
  - Majkee explicitly authorized the model-neutral metadata-only amendment on 2026-09-05; the same message requested a reboot handoff. No push or deployment permission was added.
  - This is one corrective cycle linked to REVISE 18, not a reopened cycle 18. Validate atlas-ui seat and return_to.
  - You are not alone; preserve all dirty/session/therapy work. No source edits, staging, reset, stash, clean, rebase, merge, hook bypass, push, live deployment, fresh session, spawn or wake.
  - Only a fresh fetch and one ordinary metadata-only amendment of the exact pinned commit are granted. If your harness cannot emit model-neutral attribution, STOP before amending; do not repeat the conflicting model label.
done_when: A six-field RETURN proves one amended commit with identical tree, parent, author identity/date and message except the approved attribution replacement, or reports the exact stop and partial state; protected work is unchanged.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/19.atlas-ui.return.md
---

# POINT 19 — model-neutral attribution, no source change

## Authorization

Cartan asked: “May I authorize a metadata-only amendment to model-neutral Claude attribution,
preserving the exact source tree?” @majkee replied “1. authorized.” and asked to pause for a PC
restart. The approval survives that restart through this POINT. No operation has been performed
by authoring it; STATUS remains the sole next-action and recovery authority.

## Preflight

Require office, main, HEAD `fbd9f5e14cd2800304af73c936aed9c8c8c2ba3d`, empty index, no Git
operation/lock and no competing index writer. Inspect applicable hooks and hooksPath; do not
bypass or run a hook that broadens the write scope. Snapshot the current commit tree, sole parent,
author name/email/date, full message and committer identity. Do not silently change attribution
or authorship identities beyond the one approved message line.

Run `git -C /home/hruzam/ia-sync fetch origin`. Require origin/main still exactly
`7c4a99703d973004542e14966daf49f5b9b0e4f0` and left/right divergence `1 0`; otherwise STOP
for fresh review. Do not amend a commit now observed on origin or reconcile new incoming work
under this POINT. No force-push is authorized now or as a consequence of this amendment.

Check the current three committed/worktree source pins against POINT 18. Snapshot all current
protected dirty files and modes, including every existing BUS file, RUNBOOK/STATUS/pulse, the
remote-control brief, README/journal and both therapy files plus its staging report. Recheck live
runbook/octopus, guide absence and both palettes. Preserve any separately observed reposoma drift;
its write scope remains closed. An unexpected overlap or changed HEAD/index is a stop.

## Exact amendment

Replace exactly this line in the existing commit message:

```text
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

with:

```text
Co-Authored-By: Claude <noreply@anthropic.com>
```

Preserve every other message line, including subject, cycle-18 approval body and existing session
link. Preserve the tree, parent and author identity/date; do not use --reset-author. An ordinary
amendment's new committer timestamp and new commit hash are expected and must be reported.
Immediately before the one amendment, recheck HEAD, empty index and tree pins. Do not stage files.
If a mandatory attribution mechanism prevents this exact result, report it instead of overriding
the approval's meaning or altering unrelated runtime configuration.

## Verify and return

Verify old/new tree IDs are identical, parent remains the full pinned 7c4a997, author identity/date
is unchanged, and the message diff is exactly that one replacement. Verify committer identity,
exact three-path parent diff, source blob/mode pins, empty index and protected/live snapshots.
Record the new commit identity and exact commands/exits in RETURN 19. Any failure is a stop in
the observed state, not a second amendment, reset or unapproved repair. Cartan owns VERDICT/STATUS.

Push, palette-constrained deployment and P0–P6 remain separate later gates. This cycle neither
publishes the cut nor proves its runtime behavior.
