---
cycle: 33
from: cartan-csharp
to: atlas-ui
scope: Fresh remote preflight and one local commit of the accepted two-card cut; stop before push or deployment
paths:
  read:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/31.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/32.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/32.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/SYNC_DISCIPLINE.md
    - /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md
    - /home/hruzam/.claude/skills/runbook/SKILL.md
    - /home/hruzam/.agents/skills/octopus/SKILL.md
  write:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/33.atlas-ui.return.md
  git_operations: >-
    In /home/hruzam/ia-sync only, fetch origin; conditionally stage exactly the two
    source paths above and create one local commit. Normal Git index/object/ref writes
    for those operations only; no manual Git metadata edit or source rewrite.
gates:
  - Independent operator-opened atlas-ui window, RUNBOOK participant_3; validate the seat and return_to before work. No spawned substitute.
  - Majkee authorized fresh remote preflight and the two-file local commit on 2026-09-10. Incoming work needing reconciliation means STOP before staging, including a disjoint fast-forward.
  - No pull, merge, rebase, stash, reset, amend, force, push, live deploy, hook bypass, source edit, new model run, spawn, wake, tunnel, cross-host carry, board action or prune.
  - Protect every unrelated dirty path, prior receipt, RUNBOOK, STATUS, journal/pulse, guide, TUI, identity-resolution and remote-control surface. Never stage a directory or use git add -A, git add -u or commit -a.
done_when: >-
  One six-heading RETURN supplies the exact local commit, parent, two paths/modes and
  committed blob hashes with protected work/live copies unchanged, or a bounded STOP
  with the observed blocker and any partial Git state. Cartan verifies before acceptance.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/33.atlas-ui.return.md
---

# POINT 33 — authorized preflight and local commit

The source cut is already authored and accepted. Do not apply the patch again.
VERDICT 32 SHA-256: `cdb64268e7d5fc6f51f9bd80b4eaca8f08e1e3709ff85a0773d8ca4212af599b`.
RETURN 32 SHA-256: `8e7a040f85530c5e7b2d88e4f6fd7a5c5305442e4883dc27c01de9bcedea09e9`.

Expected branch: `main`. Expected parent:
`7347f2785e7e332df26df7cb32f5569368056c9b`.
Head observation at 2026-09-10 04:10 CEST: index empty, both source pins unchanged,
cached origin/main equals HEAD. That cached equality is NOT preflight.

Before this POINT was handed off, concurrent commit `7347f27` advanced both observed
refs from `3c94356`; its only delta is two added remote-control raw briefs, disjoint
from this cut. Cartan inspected the one-parent object and path delta; no Git mutation
was performed by this head. This POINT incorporates that already-landed parent, not
permission to accept another changed parent or to reconcile incoming work yourself.

| Path under /home/hruzam/ia-sync/ | Required source/commit SHA-256 | Mode |
|---|---|---|
| claude/skills/runbook/SKILL.md | 5a88497b37de1780b5acb2de99be164eee498c8faff1a6fc97add1b8fb5b89f9 | 100644 |
| codex/skills/octopus/SKILL.md | 9dff9c9eea11eded31e21587c33a4fad284defaab28c44753d628be8ccb147d8 | 100644 |

1. Read current instructions and STATUS. Capture host, branch, HEAD, porcelain/index,
   both source/live pairs, and a scoped preservation baseline of unrelated dirty files
   and deletions. Existing unrelated dirt is known and excluded, not cleanup authority.
   A pre-existing RETURN or changed HEAD/index requires recovery inspection, not replay.
2. Run `git -C /home/hruzam/ia-sync fetch origin`. On failure, STOP. Then inspect
   `git -C /home/hruzam/ia-sync rev-list --left-right --count HEAD...origin/main`
   and both full refs. Proceed only with `0 0`, both refs at the expected parent,
   the expected branch, no merge/rebase/cherry-pick in progress and an empty index.
   Otherwise report the actual relation and relevant incoming paths; do not reconcile.
3. Immediately before staging, recheck the exact source pins, seven-addition deltas,
   modes, unchanged HEAD and empty index. Run the two existing quick_validate.py checks
   from POINT 32 and `git -C /home/hruzam/ia-sync diff --check`. Failure means STOP,
   not repair. No further dry-run or transcript investigation is needed for this commit.
4. Stage only:

   ```sh
   git -C /home/hruzam/ia-sync add -- claude/skills/runbook/SKILL.md codex/skills/octopus/SKILL.md
   ```

   Inspect the WHOLE staged path set; it must equal the two paths, both modified,
   mode 100644. Hash each staged blob's bytes via `git cat-file` and SHA-256 against
   the table, inspect the staged diff and run staged whitespace checks. Recheck HEAD.
   Any mismatch after staging means STOP and disclose the index; do not reset it.
5. Create one local commit, with subject:
   `skills: validate runbook producer output and owner binding`
   and body: `majkee gavel 2026-09-10, session runbook-upgrade`.
   Truthful runtime-issued attribution trailers remain permitted; invent no model/session
   attribution. Do not disable hooks. If a hook changes or blocks anything, report it;
   no repair, retry, amend or widened staging is authorized by this POINT.
6. Inspect the actual commit object: exactly one expected parent, exactly two changed
   paths/modes, committed blob SHA-256 pins, subject/body and any trailers. Recheck
   empty index, clean source paths, protected-work baseline and unchanged live hashes.
   Expected relation to the freshly fetched ref is `1 0`; this is not a later remote check.
   Write RETURN 33 with exact executed commands/results, full hashes, commit metadata,
   any observed variance and remaining uncertainty. Validate its envelope and six headings.

Live copies must remain at `de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9`
(Claude runbook) and `b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec`
(Codex octopus). Stop for Cartan's independent VERDICT 33. Push, deployment and fresh
producer proofs retain separate gates; this commit proves no runtime behavior.
