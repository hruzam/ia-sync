---
cycle: 28
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/28.cartan-csharp.point.md
written: 2026-09-09
host: office
execution_topology: existing operator-designated independent Astrobley window; direct execution with no delegation
---

# RETURN 28 — exact two-file guide commit created locally

## 1. Files changed

- Reposoma Git metadata now records exactly one new local commit:
  `0f48dce792f3f34081ce0778ef181621d093f856`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/28.astrobley.return.md` — added
  this RETURN.

The guide worktree bytes were not edited in cycle 28. The two already-verified guide paths were
staged and committed exactly as pinned. No other reposoma or ia-sync path was staged or committed.
No push, pull/fetch, rebase, amend, reset, stash, clean, deploy, prune, board/client edit, tunnel,
cross-host action, hook bypass/configuration change, STATUS write or VERDICT write occurred.

## 2. Commands and outcomes

- Direct execution provenance: Majkee relayed POINT 28 into this existing independent
  Astrobley-posture window. No `spawn_agent`, delegate, child task, seat wake, native Astrobley
  profile or tunnel was used.
- Initial frame inspection found:

  ```text
  office · hruzam-120922
  ia-sync HEAD: 0f52f56cdff4274bb8e6ef6a6212b7e0cdaa921b · main · index empty
  reposoma HEAD: 829a0339627985769b07ebe2d22d93238bf36930 · core · index empty
  commits after prescribed parent: 0
  RETURN 28: absent
  ```

  Reposoma guide-scoped porcelain contained only the expected modified GUIDE and untracked chapter.
  Its unrelated deleted/archive/card/mail state and ia-sync's existing dirty work were captured for
  preservation and left outside the index.
- Effective Git configuration and hooks were inspected before staging. `core.hooksPath` and
  `commit.gpgsign` were unset, so the default `.git/hooks/` and normal unsigned policy applied.
  Configured author and committer were `hruzam <hruzam.tempos@hotmail.cz>`. The executable
  `.git/hooks/pre-commit` delegated to `${HOME}/.config/zsh/ai/adr-guard.zsh`; the executable
  `.git/hooks/post-commit` invokes the doorbell only when the new commit touches
  `temple/decisions/`. Both hooks remained enabled and unedited.
- The exact VERDICT-27 condition was coupled directly to the explicit two-path stage command:

  ```sh
  set -eu
  test "$(git -C /home/hruzam/reposoma rev-parse HEAD)" = 829a0339627985769b07ebe2d22d93238bf36930
  test -z "$(git -C /home/hruzam/reposoma diff --cached --name-only)"
  sha256sum --check <<'PINS'
  0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8  /home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md
  60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863  /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
  PINS
  commit_cut_porcelain=$(git -C /home/hruzam/reposoma status --porcelain=v1 --untracked-files=all -- raw.guides)
  test "$commit_cut_porcelain" = ' M raw.guides/runbook/GUIDE.md
  ?? raw.guides/runbook/res/presence-board.md'
  git -C /home/hruzam/reposoma diff --check
  git -C /home/hruzam/reposoma add -- raw.guides/runbook/GUIDE.md raw.guides/runbook/res/presence-board.md
  git -C /home/hruzam/reposoma status --short -- raw.guides
  ```

  Exit 0. `sha256sum --check` printed `OK` for both files; post-stage status was:

  ```text
  M  raw.guides/runbook/GUIDE.md
  A  raw.guides/runbook/res/presence-board.md
  ```

- The complete staged-index check was:

  ```sh
  set -eu
  test "$(git -C /home/hruzam/reposoma rev-parse HEAD)" = 829a0339627985769b07ebe2d22d93238bf36930
  staged_paths=$(git -C /home/hruzam/reposoma diff --cached --name-only)
  test "$staged_paths" = 'raw.guides/runbook/GUIDE.md
  raw.guides/runbook/res/presence-board.md'
  test "$(git -C /home/hruzam/reposoma show :raw.guides/runbook/GUIDE.md | sha256sum | cut -d' ' -f1)" = 60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863
  test "$(git -C /home/hruzam/reposoma show :raw.guides/runbook/res/presence-board.md | sha256sum | cut -d' ' -f1)" = 0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8
  test "$(git -C /home/hruzam/reposoma ls-files --stage -- raw.guides/runbook/GUIDE.md | cut -d' ' -f1)" = 100644
  test "$(git -C /home/hruzam/reposoma ls-files --stage -- raw.guides/runbook/res/presence-board.md | cut -d' ' -f1)" = 100644
  git -C /home/hruzam/reposoma diff --cached --check
  echo staged-check-pass
  git -C /home/hruzam/reposoma diff --cached --name-status
  git -C /home/hruzam/reposoma ls-files --stage -- raw.guides/runbook/GUIDE.md raw.guides/runbook/res/presence-board.md
  git -C /home/hruzam/reposoma show :raw.guides/runbook/GUIDE.md | sha256sum
  git -C /home/hruzam/reposoma show :raw.guides/runbook/res/presence-board.md | sha256sum
  ```

  Exit 0 with `staged-check-pass`. The staged entries were exactly:

  ```text
  M  raw.guides/runbook/GUIDE.md
  A  raw.guides/runbook/res/presence-board.md
  100644 10d1e39c9266d3b7ae5ce198f8f32386d42b0824 0  raw.guides/runbook/GUIDE.md
  100644 d85f7da6f5c4bdcdd39a038618894ed9c1ba00c0 0  raw.guides/runbook/res/presence-board.md
  60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863  GUIDE staged content
  0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8  presence-board staged content
  ```

- Immediately before the commit, the parent, exact path set, both staged SHA-256 pins, both modes
  and `git diff --cached --check` were repeated in the same shell invocation. The complete command was:

  ```sh
  set -eu
  test "$(git -C /home/hruzam/reposoma rev-parse HEAD)" = 829a0339627985769b07ebe2d22d93238bf36930
  staged_paths=$(git -C /home/hruzam/reposoma diff --cached --name-only)
  test "$staged_paths" = 'raw.guides/runbook/GUIDE.md
  raw.guides/runbook/res/presence-board.md'
  test "$(git -C /home/hruzam/reposoma show :raw.guides/runbook/GUIDE.md | sha256sum | cut -d' ' -f1)" = 60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863
  test "$(git -C /home/hruzam/reposoma show :raw.guides/runbook/res/presence-board.md | sha256sum | cut -d' ' -f1)" = 0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8
  test "$(git -C /home/hruzam/reposoma ls-files --stage -- raw.guides/runbook/GUIDE.md | cut -d' ' -f1)" = 100644
  test "$(git -C /home/hruzam/reposoma ls-files --stage -- raw.guides/runbook/res/presence-board.md | cut -d' ' -f1)" = 100644
  git -C /home/hruzam/reposoma diff --cached --check
  git -C /home/hruzam/reposoma commit -m 'runbook: add advisory presence-board contract' -m 'majkee gavel 2026-09-09, session runbook-upgrade'
  ```

  Exit 0. Normal commit output:

  ```text
  [core 0f48dce] runbook: add advisory presence-board contract
   2 files changed, 120 insertions(+)
   create mode 100644 raw.guides/runbook/res/presence-board.md
  ```

  The pre-commit hook passed without output. The guide-only commit did not match the post-commit
  hook's `temple/decisions/` branch and produced no hook error.
- The complete post-commit assertion block was:

  ```sh
  set -eu
  repo=/home/hruzam/reposoma
  head=$(git -C "$repo" rev-parse HEAD)
  test "$(git -C "$repo" rev-parse HEAD^)" = 829a0339627985769b07ebe2d22d93238bf36930
  test "$(git -C "$repo" rev-list --count 829a0339627985769b07ebe2d22d93238bf36930..HEAD)" -eq 1
  changed=$(git -C "$repo" diff-tree --no-commit-id --name-only -r HEAD)
  test "$changed" = 'raw.guides/runbook/GUIDE.md
  raw.guides/runbook/res/presence-board.md'
  test "$(git -C "$repo" show HEAD:raw.guides/runbook/GUIDE.md | sha256sum | cut -d' ' -f1)" = 60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863
  test "$(git -C "$repo" show HEAD:raw.guides/runbook/res/presence-board.md | sha256sum | cut -d' ' -f1)" = 0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8
  test "$(git -C "$repo" ls-tree HEAD -- raw.guides/runbook/GUIDE.md | cut -d' ' -f1)" = 100644
  test "$(git -C "$repo" ls-tree HEAD -- raw.guides/runbook/res/presence-board.md | cut -d' ' -f1)" = 100644
  test -z "$(git -C "$repo" diff --cached --name-only)"
  test -z "$(git -C "$repo" status --porcelain=v1 -- raw.guides/runbook/GUIDE.md raw.guides/runbook/res/presence-board.md)"
  message=$(git -C "$repo" log -1 --format='%s%n%b')
  test "$message" = 'runbook: add advisory presence-board contract
  majkee gavel 2026-09-09, session runbook-upgrade'
  echo "post-commit-check-pass HEAD=$head"
  ```

  Exit 0:

  ```text
  post-commit-check-pass HEAD=0f48dce792f3f34081ce0778ef181621d093f856
  ```

- Resulting commit identity and tree:

  ```text
  commit: 0f48dce792f3f34081ce0778ef181621d093f856
  parent: 829a0339627985769b07ebe2d22d93238bf36930
  count from parent: 1
  author: hruzam <hruzam.tempos@hotmail.cz>
  committer: hruzam <hruzam.tempos@hotmail.cz>
  subject: runbook: add advisory presence-board contract
  body: majkee gavel 2026-09-09, session runbook-upgrade
  trailers: none
  signature: none under the unchanged unset commit.gpgsign policy
  100644 blob 10d1e39c9266d3b7ae5ce198f8f32386d42b0824  raw.guides/runbook/GUIDE.md
  100644 blob d85f7da6f5c4bdcdd39a038618894ed9c1ba00c0  raw.guides/runbook/res/presence-board.md
  ```

  Committed content SHA-256 values are `60dbc953...57863` for GUIDE and
  `0bcd6c40...77b8` for the chapter, matching the full pins above.
- Post-commit reposoma is `core...origin/core [ahead 1]`, with an empty index and both guide
  worktree paths clean. The unrelated pre-existing deleted cold-start archive, untracked
  archives/cards and `_mail/toAll/inbox/zsh.stale-settings-cards-2026-09-07.md` remain present
  and uncommitted. ia-sync HEAD/index and all pre-existing dirty paths remain unchanged except
  for this new RETURN.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/28.cartan-csharp.point.md` — commit
  authority, parent, pins, message and write boundary.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/27.cartan-csharp.verdict.md` — accepted
  audit and the immediate pre-stage condition executed above.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.astrobley.return.md` and
  `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.cartan-csharp.verdict.md` — exact
  two-file fold evidence and independent verification.
- Reposoma commit `0f48dce792f3f34081ce0778ef181621d093f856` — local result with parent
  `829a0339627985769b07ebe2d22d93238bf36930`.
- Commit tree blobs `10d1e39c9266d3b7ae5ce198f8f32386d42b0824` and
  `d85f7da6f5c4bdcdd39a038618894ed9c1ba00c0` — Git-object pins for the GUIDE and chapter.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` and
  `/home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md` — clean worktree views of
  the committed blobs.
- `/home/hruzam/reposoma/.git/hooks/pre-commit` and
  `/home/hruzam/reposoma/.git/hooks/post-commit` — effective hooks inspected and left enabled.
- `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`, **What distinguishes
  it** — Astrobley executed the delegated commit body; Cartan retains independent verification.

## 4. Mismatches discovered

1. The first batched inspection wrapper contained a JavaScript syntax error outside the shell
   command and therefore executed no nested inspection or mutation. The corrected wrapper then
   established the frame above. No repository state changed during the failed wrapper.
2. No commit-state mismatch appeared: RETURN 28 was absent, reposoma had zero commits after the
   prescribed parent, its index was empty, and both worktree pins and exact two-path porcelain
   matched immediately before staging.
3. The hook/config inspection matched POINT 28. No custom `core.hooksPath`, signing override,
   attribution trailer requirement or unexpected hook behavior appeared. The commit used the
   configured human Git identity; that label is not evidence of the model or UI process that
   executed the command.
4. Reposoma is intentionally not whole-tree clean after the commit. Its unrelated vault/mail dirt
   is the preserved before-state, not part of commit `0f48dce...` and not a failed cleanup.

## 5. Recommended next task

The original `cartan-csharp` head independently verifies commit
`0f48dce792f3f34081ce0778ef181621d093f856` against POINT 28: exact parent and count, two paths,
modes, committed blob SHA-256 pins, prescribed message, empty index and preserved unrelated work.
It then writes VERDICT 28 and alone reconciles STATUS. This Astrobley window must not push or deploy.

## 6. Remaining uncertainty

- The commit is local and `core` is one commit ahead of `origin/core`; no fetch, push or remote-ref
  refresh was performed. Remote and home availability are unverified.
- Hook success is observed from the normal commit exit and absence of hook errors. No independent
  audit of all possible hook side effects or background state was requested or performed.
- The commit author/committer fields are configured Git metadata, not runtime-process or model
  provenance. Majkee's direct relay supplies the execution-topology testimony for this cycle.
- No board directory/record, client parser/view, RNG operation, exclusive-create race, transport,
  detach behavior, fresh runtime call or original P0–P6 proof was created or tested.
- Cartan's independent commit verification, the operator-owned remote carry/deployment decision,
  fresh runtime proofs, closing purpose-fit audit, final STATUS résumé and cSharp experience transfer
  remain outside this RETURN.
