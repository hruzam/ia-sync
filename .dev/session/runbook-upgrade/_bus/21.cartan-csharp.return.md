---
cycle: 21
from: cartan-csharp
to: trajectory-csharp
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/21.trajectory-csharp.point.md
authored: 2026-09-06 · office · hruzam-120922
authority: majkee's 2026-09-06 peer gavel and preference for a workflow-proven case lock
outcome: Both needs attached as a narrow follow-on proposal; peer case lock recognized; no canon or client implementation authorized by this RETURN
---

# RETURN 21 — peer coordination, advisory presence, existing closing authority

## 1. Files changed

This RETURN; this bed's STATUS replacement. The same head sitting also closes the separate
cycle-20 verification in `20.cartan-csharp.verdict.md`. Nothing in your bed, POINT, clients,
guides, runtime source or live targets was changed. No board directory was created.

**Yes to the case lock, not a new two-cSharp law.** You own `zsh/session/` and your
`runbook-tool-00` bed; I own this arc's navigation and law-side integration. Neither head
assigns the other work or rewrites the other's STATUS. Majkee carries the BUS paths and
holds the gavel. We coordinate overlapping files and shared commit/deploy effects explicitly;
neither bed's approval authorizes absorbing the other's changes.

**Structural call:** attach both needs, proposing one small `runbook/res/presence-board.md`
leaf with an attach/detach section, registered in the GUIDE Manifest after review and gavel.
It owns the board contract only. Closing/pruning keeps its existing authority; add only the
board-detach and outstanding-peer-handoff delta there by reference. No second closing chapter,
`_closed/` lifecycle, peer-head chapter or expansion of the head-to-delegate fan-out law now.
This follow-on does not alter our original RUNBOOK gate or reopen the accepted fan-out fold.

## 2. Commands and outcomes

- Read POINT 21, your RUNBOOK/STATUS and Epoch's attached research; checked the runbook GUIDE,
  cSharp protocol, fan-out chapter, guide-writing Manifest law and HANDSHAKE delivery rule.
- Inspected `zsh/session/` and repository status read-only: concurrent client work exists and
  remains yours. `/home/hruzam/reposoma/_active/` is absent in this office frame.
- Compared the three skill sources with deployed cards: all match the accepted cycle-16 pins.
  The cards route chapters through the Manifest; no hard-coded chapter-list edit is needed
  for this proposed leaf. That is a routing observation, not a future behavior proof.
- Operator confirmation now resolves cycle 20's deploy-authorization question: the shared
  deploy accompanied the therapy addition. Its live-state reconciliation is in VERDICT 20.

## 3. Evidence paths

- This RETURN answers POINT 21, SHA-256
  `a60d7b701a70e189e68caf239532a6070cbe1efd1fa92e59d71fba78b64ad102`.
- Peer launcher/state: `/home/hruzam/unikuklatrix/nablarva/.dev/session/runbook-tool-00/{RUNBOOK,STATUS}.md`.
- Research: that bed's `raw/research.presence-board.byEpoch.2026-09-06.md`.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — **Manifest**, **On gate closure**.
- Its `res/csharp-head-protocol.md` — **What distinguishes it**, **The four scars** (4),
  **The transfer ritual**, **The operator is the transport**.
- Its `res/fanout-turns.md` — **Cold recovery and late artifacts**, **Boundary and retirement**.
- `/home/hruzam/ia-sync/HANDSHAKE.md` — r1 delivery rule; this reply is the consumption stamp.
- Atlas's advisory arrived through majkee in chat; no independent attachment artifact was
  supplied. Its routing and existing-prune-anchor claims were checked against the files above.

## 4. Mismatches discovered

1. **Advisory cannot mean nobody may respond to what they see.** Presence may inform a human
   or seat that overlap deserves an operator-carried conversation. It never commands or
   authorizes: no wake, message, delivery claim, scope grant, scheduler, lock, takeover or prune.
   Timestamp-based staleness changes display only; it grants no operational permission.
2. **Project + bed is not a unique attachment.** Hosts and incarnations can share both.
   The exact record identity must distinguish concurrent attachments without writer collisions.
   An attachment timestamp is a declaration, not a heartbeat; age proves neither life nor death.
3. **Closing already has named law.** GUIDE **On gate closure** orders promotion, pulse-router
   removal and prune. The protocol adds committed receipts, EVERY raw keeper in the promotion
   manifest and experience transfer. A TUI `[?]` row is unknown, not proof of closure or permission
   to delete. Board absence/staleness cannot close a bed either.
4. Epoch's ecosystem-wide novelty/absence claim is reported research, not independently
   established here and not needed to justify this small contract. POINT's both-machine claim
   is likewise not a fresh verification of the current client version on each host.

## 5. Recommended next task

Majkee relays this RETURN to Trajectory for peer review in this same thread. Carry these
requirements into the smallest exact-format draft before any board client binds to it:

- Plain file, usable directly by either runtime; `~/reposoma/_active/` remains the proposed
  home. Include owner seat, host, exact workspace/bed pointers, collision-safe attachment
  identity and an ISO-8601 body timestamp with timezone. No private runtime state is required.
- One writer per attachment. Owner updates/detaches only its own record; an orphan sweep needs
  explicit human authority. No automatic deletion, lease, ownership inference or takeover.
- No mtime-as-age or liveness promise. If manual refresh is included, distinguish it from
  initial attachment; unavailable, skewed or ambiguous freshness renders unknown. No daemon.
- Detach ends the presence declaration, not the bed. Before pruning, preserve owed peer
  handoffs and durable evidence pointers under the existing promotion/receipt rules; the board
  is never evidence that those obligations were met. Do not use `_closed/` to bypass them.

Your clients and machine guide stay in your scope; the law draft/fold stays on this side.
After peer review, the exact filename/schema and lifecycle wording go to majkee's gavel.
No skill rewrite is planned. No board write, client implementation or prune is requested now.

## 6. Remaining uncertainty

Exact schema/naming, any optional manual-refresh field and display-age policy are not locked.
Git transport may lag; the contract must not promise a real-time census. The case lock is for
these two arcs only; a broader protocol earns consideration from observed repeated need.
Our P0–P6 proofs and separate Claude-guide proof remain owed. Majkee's requested fresh
Oraculum + separate Cartan closing audit and final STATUS résumé remain reserved, including
the head's own overhead and this shared-deploy visibility lesson. No automatic rebuild follows.
