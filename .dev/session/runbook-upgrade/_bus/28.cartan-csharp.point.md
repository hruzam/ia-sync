---
cycle: 28
from: cartan-csharp
to: astrobley
authority: majkee answered blessed on 2026-09-09 to one local reposoma commit of the exact two guide paths, with no push or deploy
scope: Stage the already-verified two-file cut, make one local commit, verify its contents and return; no source edits
git_scope:
  repository: /home/hruzam/reposoma
  branch: core
  expected_parent: 829a0339627985769b07ebe2d22d93238bf36930
  commit_count: 1
  stage_paths:
    - raw.guides/runbook/GUIDE.md
    - raw.guides/runbook/res/presence-board.md
paths:
  read:
    - /home/hruzam/reposoma/AGENTS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.cartan-csharp.point.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.astrobley.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/26.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/27.oraculum-audit.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/27.cartan-csharp.verdict.md
    - /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
    - /home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md
    - /home/hruzam/reposoma/raw.guides/bus/GUIDE.md
    - /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md
  write:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/28.astrobley.return.md
gates:
  - Execute directly in the existing operator-designated independent Astrobley window; no spawned substitute, delegation, seat wake, tunnel or cross-host operation.
  - Only the declared Git staging/commit metadata and the named RETURN may be written. Guide bytes stay unchanged. Do not edit STATUS, RUNBOOK, earlier receipts, peer work, vault/mail, runtime source or client code.
  - Preserve all unrelated work in both repositories. No ia-sync Git mutation, broad add, amend, reset, stash, clean, rebase, pull/fetch, push, deploy or prune; no hook bypass or configuration change.
  - Resolve actual permissions before Git writes through the normal managed approval boundary. Stop on a changed parent, occupied index, moved pin, overlapping work or unexpected hook behavior; do not repair the frame within this cycle.
done_when: One local commit on core has exactly the pinned parent and two approved paths/blobs, expected message, empty post-commit index, unchanged unrelated work and a canonical six-field RETURN with full evidence
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/28.astrobley.return.md
---

# POINT 28 — local two-file commit, now authorized

Majkee's **blessed** answers the explicit commit question after VERDICT 27. It authorizes
this one local commit, not remote carry, deployment, source revision or a whole-repo sweep.
The 2026-09-09 contract gavel and actual-diff audit are already settled.

## Preflight and immediate staging boundary

Resolve your direct-session seat and current frame. If a RETURN or possible completed/partial
commit already exists, inspect it and report the actual state; never blindly repeat a commit.
Capture both repositories' HEAD/index/porcelain and the protected before-state needed to show
you did not absorb or alter concurrent work. Reposoma's unrelated vault/mail dirt is expected;
“whole repository clean” is neither required nor an invitation to clean it.

Read the effective Git hooks before committing and leave them enabled. At issue, the default
pre-commit hook delegates to the live ADR guard; the post-commit hook invokes the doorbell
only for `temple/decisions/` paths, so this guide-only cut does not enter that branch. Recheck
the actual configuration and hook behavior in your frame; no notifier/config repair is scoped.

Immediately before staging, execute the complete shelled condition in VERDICT 27
**Uncertainty disposition**: exact HEAD, empty reposoma index, both SHA-256 pins and exact
two-path guide porcelain. That same-sitting execution—not our earlier check—discharges
Oraculum's condition. On any mismatch, stop before staging and return the evidence.

The approved content pins are:

```text
60dbc9537c11b325a8936f6170c15244668bec5e56dce06de99d061a60257863  raw.guides/runbook/GUIDE.md
0bcd6c408bdd96cee0d6821478e7358665c07df325e3d118a5c9f8af110677b8  raw.guides/runbook/res/presence-board.md
```

## Stage, check, commit

Stage only the explicit paths:

```sh
git -C /home/hruzam/reposoma add -- raw.guides/runbook/GUIDE.md raw.guides/runbook/res/presence-board.md
```

Before committing, require exactly those two staged paths, modes `100644`, and re-hash each
staged blob (`git cat-file` / `git show :<path>`) against the pins above. Recheck HEAD remains
the prescribed parent and run `git diff --cached --check`. If the shared index changes or a
check fails, stop and report; do not unstage/reset someone else's work or commit extra paths.

Create exactly one commit using the normal configured author:

```sh
git -C /home/hruzam/reposoma commit -m 'runbook: add advisory presence-board contract' -m 'majkee gavel 2026-09-09, session runbook-upgrade'
```

Preserve this subject and body. Do not invent identity/model/session trailers. If your runtime
requires an attribution trailer, report its literal runtime-issued form and provenance limit;
do not turn that label into a verified model claim or rewrite unrelated history. Leave hooks
and signing policy intact; a failure is evidence, not permission to bypass configuration.

## Verify and return

After the command, inspect HEAD even if the command reported an error. Verify exactly one
new commit, one parent equal to the full pin, the two intended changed paths only, both committed
blob hashes, message and modes. Check index empty, both guide worktree paths clean, and
unrelated work preserved. Report any concurrent drift honestly rather than claiming whole-repo
cleanliness. No retry/amend/reset of an unexpected commit within this scope.

Write only `28.astrobley.return.md` with exactly the six mandatory BUS RETURN headings;
put the recommendation in field 5, not an extra heading. Include exact commands/results,
full commit and parent IDs, committed blob hashes, message/trailers, the immediate pre-stage
check, preservation evidence and what remains unverified. Do not abbreviate a new checker
with an ellipsis. No board/client/RNG/race or fresh-runtime behavior proof is requested.

Cartan independently verifies the commit and alone rewrites STATUS. The original runtime
gate, operator-owned fresh proofs and closing audit remain open. Applied law: BUS **Verifying
a RETURN** and cSharp **What distinguishes it**; this POINT delegates the commit body and
retains the crossed verification boundary.
