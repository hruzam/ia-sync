---
cycle: 18
from: cartan-csharp
to: atlas-ui
scope: Receive only the reviewed disjoint home cleanup by pinned fast-forward, then make the already-approved local three-file skill commit
paths:
  read:
    - /home/hruzam/ia-sync/AGENTS.md
    - /home/hruzam/ia-sync/SYNC_DISCIPLINE.md
    - /home/hruzam/ia-sync/HANDSHAKE.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/16.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/17.cartan-csharp.point.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/17.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/17.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/guide/SKILL.md
  write:
    - /home/hruzam/ia-sync/.git/
    - /home/hruzam/ia-sync/session/rellays-calude-codex/ORACULUM-CARTAN-reverse-tunnel-return.2026-09-03.md
    - /home/hruzam/ia-sync/.dev/session/reversal-tunel-00-leader/src/ORACULUM-CARTAN-reverse-tunnel-return.2026-09-03.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/18.atlas-ui.return.md
gates:
  - Validate atlas-ui seat and return_to. One sequential reconciliation/commit cycle; no new authoring or fan-out turn.
  - The two relay paths may change only through the exact pinned Git fast-forward below; no manual edit or move. Git may create the destination src/ directory.
  - The .git scope permits fetch, that fast-forward, exact three-path staging and one ordinary local commit. It is not arbitrary Git authority.
  - Majkee's recorded local-commit blessing remains the authority. The head bounds receipt of the independently checked disjoint home move as its prerequisite; push and deployment remain held.
  - You are not alone. Preserve the therapy lane, remote-control brief, session controls and all other dirty files. Do not stage them; do not revert anyone's work.
  - No source edits, merge commit, rebase, stash, reset, clean, amend, hook bypass, force operation, push, live deployment, new session, spawn, wake, prune or cross-host send. Any unmet gate means stop and report exact partial state.
done_when: A six-field RETURN proves the pinned fast-forward and one local commit with the exact parent/path/blob/metadata checks, or reports a precise stop and any partial Git state; protected work and live targets are accounted for.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/18.atlas-ui.return.md
---

# POINT 18 — pinned receive, then the three-file local commit

## 1. Fresh preflight; no staging yet

Read the governing files above. Expected office branch is `main`, HEAD
`b7a2c09c6e335cae5a588ab41fdccc6ee0365537`, index empty. Check operation markers, Git locks,
applicable hooks including any configured hooksPath, and known competing Git writers. Never
delete a lock or bypass a hook. If exclusive index/HEAD ownership is uncertain, stop.

Run `git -C /home/hruzam/ia-sync fetch origin`. Require origin/main to be exactly
`7c4a99703d973004542e14966daf49f5b9b0e4f0` and `rev-list --left-right --count HEAD...origin/main`
to be `0 2`. A different tip, failed fetch, changed HEAD or unexpected index is a STOP before
integration/staging. Do not chase newly arrived commits under this authorization.

Recheck the full net diff: exactly the R100 move in VERDICT 17, mode 100644 and blob
`ef133346948d6eafe11c549f81937a395cf56493`. The old worktree file must match; the destination
must be absent with no symlink/path obstruction. Check all current dirty paths for exact or
ancestor/descendant overlap. Snapshot their bytes/modes and inventory, including untracked files.
New unexpected ownership/overlap is a stop, not a reason to stash or broaden this cut.

The already identified protected set includes RUNBOOK/STATUS, every prior BUS artifact, pulse,
the remote-control brief, `codex/README.md`, `journal.host-cleanup.md`,
`_staging/codex/therapy-port.2026-09-05.md` and both files under `codex/skills/therapy/`.
Snapshot deployed runbook/octopus, guide absence, both palette files and reposoma HEAD/status.
Do not use the old seven-line porcelain claim as today's inventory.

Require these frozen source SHA-256 values before proceeding:

```text
de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9  claude/skills/runbook/SKILL.md
b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec  codex/skills/octopus/SKILL.md
d31ec392b80cf9565fb1374fd1860274d43ed0dd8bcd3e70f98562f53c87b2ce  codex/skills/guide/SKILL.md
```

## 2. Receive the reviewed tree, then verify the transition

This fetch plus pinned fast-forward receives home work before the local commit, without a
rebase through dirty files or an unreviewed second fetch hidden inside pull:

```sh
git -C /home/hruzam/ia-sync merge --ff-only --no-edit 7c4a99703d973004542e14966daf49f5b9b0e4f0
```

Require HEAD exactly that hash, empty index, old path absent, destination mode/blob correct,
and all dirty source/protected bytes and modes unchanged. The relocated record remains
recoverable in Git history; report the move. No intermediate checkout or manual mkdir/mv is needed.
If verification fails, stop in the observed state; do not undo home work or repair another lane.

## 3. Commit only the accepted cut

Immediately recheck parent, empty index and pins, then:

```sh
git -C /home/hruzam/ia-sync add -- claude/skills/runbook/SKILL.md codex/skills/octopus/SKILL.md codex/skills/guide/SKILL.md
```

Verify cached paths are exactly these three, staged blobs match all three SHA-256 pins,
file modes are 100644, `git diff --cached --check` passes and parent is still 7c4a997's full hash.
Any other staged path or changed parent means stop without resetting the index.

Make one ordinary local commit with the exact subject:

`skills: reconcile runbook routing across Claude and Codex`

Its body must contain:

`majkee approval 2026-09-05; runbook-upgrade cycle 18; source verification ACCEPT 16; runtime proofs pending`

**Trailer policy:** your normal truthful `Co-Authored-By` and existing session-link attribution
may be appended. Preserve the subject and required body. Use actual attribution, not the roster's
unverified model label; invent neither an identity nor a session URL. Do not publish a transcript
or include a credential-bearing link. If a mandatory trailer cannot truthfully be supplied within
that boundary, stop before commit and disclose the constraint. Record actual final metadata in RETURN.

Verify the new commit has exactly one parent, the full pinned 7c4a997; exactly the three changed
paths and matching committed blobs/modes; required metadata; empty index; and clean status for
those three paths. Recheck all protected/live snapshots and reposoma. Disclose concurrent drift
as drift, never undo it or attribute it to your actions without evidence. No push or deploy.

## 4. Return and stop line

Write only RETURN 18 outside the bounded Git operations. Include exact commands/exits, fetched
tip, ff result, local commit identity and metadata, path/blob checks, protected-state comparisons,
and any partial state or uncertainty. A fast-forward without a commit is partial completion, not
permission to reset or retry on a new parent. Cartan alone writes VERDICT and STATUS.

The palette hold and P0–P6 remain unchanged. A later authorized push requires a fresh remote
check and safe receipt of any further home work. A later deployment needs its own exact scope
and dry-run in the then-current frame; the therapy lane does not clear this arc's runtime proofs.
