---
cycle: 23
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.cartan-csharp.point.md
written: 2026-09-06
host: office
---

# implementation_return — presence-board chapter candidate

## 1. position

The smallest viable contract is one immutable, self-identifying record per attachment. A
128-bit random attachment ID is encoded as 32 lowercase hexadecimal characters and maps exactly
to `presence.<attachment_id>.md`; exclusive creation without replacement handles the residual
collision case. This repairs timestamp-only identity without adding a registry, lock service,
heartbeat, or private runtime identifier. No architectural pushback is needed.

The complete proposed `runbook/res/presence-board.md` bytes are inside this single extractable
fence:

````markdown
---
title: Presence board — advisory cross-session attachment records
chapter-of: runbook
---

# Presence board

> **DRAFT — not gaveled.** This candidate has no canonical or client-binding authority.

Use the presence board when an active session wants to declare its attachment to a workspace so
people and clients can notice possible overlap. Attach by creating one complete record; detach by
removing it under the ownership rule below. The board is a shared display surface, not doing-state.

## Advisory boundary

Presence may inform a person or seat that coordination could help. It never commands or authorizes
work, wake, delivery, permission, scope, resource lock, schedule, takeover, prune, or workspace access.

Readers do not treat presence, absence, age, or damage as a behavioral gate. They may display a
record and its uncertainty, then leave any action to the applicable session and human authority.

## Home and attachment identity

The proposed home is `~/reposoma/_active/`, with one file per attachment. Here `~` belongs to the
host carrying the shared reposoma checkout.

Each attaching incarnation allocates 16 bytes from its operating system's cryptographic random
source and encodes them as 32 lowercase hexadecimal characters: the `attachment_id`. The filename is:

```text
presence.<attachment_id>.md
```

The filename grammar is `^presence\.[0-9a-f]{32}\.md$`. Its hexadecimal component must equal the
record's `attachment_id` byte-for-byte.

Create the target with an exclusive, create-without-replacement operation. If it exists, do not read,
replace, amend, or delete it as collision handling; allocate a fresh ID and try a new filename. This
is collision-resistant allocation with explicit conflict handling, not mathematical uniqueness.

The ID identifies only this attachment and grants no rights over a workspace, bed, seat, or record.
No registry or lock service participates. The ID and `attached_at` stay stable; v1 has no refresh.

## Record grammar

A record is UTF-8 text with one frontmatter document and no Markdown body. Exact `---` lines enclose
one flat `key: "value"` scalar per line in the order below. Blank lines, comments, nesting, lists,
multiline values, aliases, tags, duplicate keys, and unknown keys are invalid.

Every value uses ASCII double quotes and contains no double quote, backslash, carriage return, or
newline; escapes are not interpreted. The format is a small grammar, not general YAML policy parsing.

Required keys, in order:

- `schema` — exactly `presence-board/v1`.
- `attachment_id` — exactly 32 lowercase hexadecimal characters.
- `seat` — the declaring seat; 1–64 ASCII letters, digits, `.`, `_`, or `-`, starting alphanumeric.
- `host` — the declaring host label under the same 1–64 character grammar.
- `workspace` — a whole quoted pointer beginning `~/`, scoped to `host`.
- `bed` — a whole quoted pointer beginning `~/`, scoped to `host`; it may be outside `workspace`.
- `attached_at` — a calendar-valid `YYYY-MM-DDTHH:MM:SSZ` or
  `YYYY-MM-DDTHH:MM:SS±HH:MM`. Fractions and missing zones are invalid; offsets range through
  `14:00`, with `14` permitting only `00` minutes.

One optional final key is allowed:

- `note` — one line for display only. A reader never parses it as policy, state, instruction, scope,
  or authority.

Missing, reordered, duplicate, or extra keys, bad quoting, filename/ID mismatch, or conflict markers
make a record malformed. Competing contents for one filename make it conflicted. Readers report the
state, filename, and reason; they do not guess, choose, hide the row, or infer a live participant.

## Valid record

Filename: `presence.4f8c2d7a9b1e6c03d5a7f9218b4e0c6d.md`

```yaml
---
schema: "presence-board/v1"
attachment_id: "4f8c2d7a9b1e6c03d5a7f9218b4e0c6d"
seat: "astrobley"
host: "office"
workspace: "~/ia-sync"
bed: "~/ia-sync/.dev/session/runbook-upgrade"
attached_at: "2026-09-06T14:05:09+02:00"
note: "Drafting the presence-board contract"
---
```

The filename and ID map exactly. The timestamp is a declaration, not a heartbeat or liveness claim.

## Ownership and lifecycle

The incarnation that exclusively created a record is its single writer and attachment owner. Reusable
seat, host, workspace, and bed labels do not let another incarnation edit or delete it.

After complete creation, a v1 record is immutable. Its owner detaches by removing exactly the file
named by its owned ID; reattachment gets a new ID, filename, and time. Only explicit human authority
may remove an orphan. Readers never auto-delete, refresh, repair, or infer takeover.

Detaching ends only the board declaration; it neither closes the bed nor proves gate, receipt,
evidence, promotion, or transfer completion. The RUNBOOK GUIDE **On gate closure** still governs.
The cSharp protocol still requires committed receipts, EVERY raw keeper in the promotion manifest,
and experience transfer. `res/fanout-turns.md` **Cold recovery and late artifacts** still applies.

Route any owed overlap handoff through its BUS or transfer authority before the responsible session
closes. Board detachment neither satisfies that handoff nor suppresses a late artifact.

## Reading and display

Resolve `workspace` and `bed` only in the declaring `host` frame. Remote `~/` remains a quoted remote
pointer, not the reader's home, a verified local path, remote-access permission, or relocation.

Use `attached_at`, never mtime. Age thresholds and dim rendering are client policy. Old proves
neither death nor activity; invalid, ambiguous, or future/skewed renders unknown. Transport may lag,
so the board is not a real-time census.

Absence, staleness, damage, conflict, or a TUI unknown row is not closure evidence. Plain-file attach
and detach are the contract; commands, clients, universal validators, daemons, and heartbeats are out.
````

Prospective RUNBOOK GUIDE Manifest line:

```markdown
- `res/presence-board.md` · chapter · DRAFT — advisory cross-session attachment records and detach boundary
```

## 2. changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.astrobley.return.md` — added this
  draft-only RETURN containing the candidate and its evidence.
- Behavior changed: none. No chapter was installed, no board home or record was created, and no
  client was bound to the candidate.

## 3. verification

- Complete-read and frame checks: all POINT 23 read paths plus the applicable ia-sync and reposoma
  `AGENTS.md` files were readable; host signals resolved `office`; the inbox was empty; the target
  and `/home/hruzam/reposoma/_active/` were absent; the ia-sync index was empty before the cut.
- A read-only `node - <<'NODE'` harness extracted the one four-backtick fence and strictly parsed
  the embedded example. Exit 0: example and filename/ID mapping passed; the extracted chapter was
  120 lines.
- The same in-memory harness created two same-seat/same-host/same-second records with different
  IDs and two same-bed-slug records in different workspaces; all parsed and selected distinct
  filenames. It denied detach to a new incarnation with the same labels, preserved `office:~/ia-sync`
  as remote from `home`, rejected missing timezone (`attached_at`), duplicate key (`duplicate`), and
  conflict markers (`conflict marker`), and rendered future or ambiguous time as `unknown`.
- `awk`/`rg` structure checks found exactly 120 candidate lines, the six ordered role headings, one
  pair of outer fence markers, the DRAFT notice, and one proposed Manifest line. The final trailing-
  whitespace `rg` returned expected status 1 with no matches. `git diff --no-index --check /dev/null
  <RETURN>` returned expected status 1 because the file is new, with no whitespace diagnostics.
- A Node byte extraction reported 5,850 candidate bytes and the SHA-256 below. Recomputed SHA-256
  values for every POINT input matched the pre-write values. Post-write porcelain matched the
  dirty baseline plus only this RETURN; the index remained empty. Reposoma retained only its
  pre-existing cold-start-card moves.

Extractable chapter SHA-256: `e05124138e0faade38f736869116da0d29fcbf41bc004004d059c4572c04302f`

## 4. curvature

- Direct role law requires the six `implementation_return` fields (`position`, `changed`,
  `verification`, `curvature`, `untouched`, `remaining_gate`), while the standing BUS GUIDE names a
  different six-heading reporting schema. This RETURN follows the direct role contract and POINT
  23's explicit request for those exact role fields; it preserves the candidate under field 1 as
  directed.
- A cryptographically random 128-bit ID plus exclusive creation is collision-resistant, not an
  absolute uniqueness proof. The no-replacement retry rule is therefore part of the contract.
- Two initial harness wrapper attempts failed before invoking the shell because literal
  four-backtick markers terminated the JavaScript tagged template. The corrected wrapper computes
  the marker from character code 96; the candidate and target bytes were unaffected before the
  successful harness ran.

## 5. untouched

All adjacent authority and work stayed untouched: `/home/hruzam/reposoma/raw.guides/`,
`/home/hruzam/reposoma/_active/`, Trajectory's `zsh/session/` source and its bed, this bed's
RUNBOOK/STATUS and prior BUS artifacts, `pulse.md`, the host journal, runtime skills, clients, live
files, the Git index/history/remotes, deployments, tunnels, and the other host.

The guide-writing law applied here is `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md`,
especially **Chapters are the sip unit**, **GUIDE.md is the door AND the manifest**, the light
chapter-header rule, and writer rule **Point, never copy**. Its canonical home was read-only.

## 6. remaining_gate

Cartan must independently check this RETURN and exact extracted bytes. If it passes, Trajectory
still owes the client-fit review; only then may majkee gavel or reject the candidate. Guide
installation, Manifest editing, board/client work, and the original runbook-upgrade runtime proofs
remain unauthorised and unperformed.
