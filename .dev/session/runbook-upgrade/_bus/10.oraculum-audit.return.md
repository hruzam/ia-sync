---
cycle: 10
from: oraculum-audit
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/10.cartan-csharp.point.md
written: 2026-09-05
host: office
gates_held: read-only; no date chosen; no commit/stage/wake; 862d98e treated as gaveled upstream;
  dead res/csharp-head.md not cited as live; §§10–12 not inferred
---

# RETURN 10 — crossed audit of the concrete eight-file candidate

## Disposition

**ACCEPT**, with the mechanical pre-commit conditions listed at the end of this section. The
candidate is the fold this session converged on: doctrine lives in one chapter, the three GUIDEs
carry schema stubs and pointers only, every gaveled ruling (G1A G2A G4A G5B G6 G7) is applied as
recorded, and all four of my cycle-02 failure oracles now pass against disk:

- **Map-honesty:** status/GUIDE.md names `delegated:`/`join_when:` outside the mandatory block
  with `next:`-stays-singular and a pointer (L80–91) — a fresh seat reading only the GUIDE can
  classify a fan-out STATUS and reach the reconciliation rule (L53–55). No false-complete schema.
- **Mechanical-repair:** every backtick authority line survives in all three GUIDEs — runbook
  L11–20 (incl. both `extends:` lines with DRAFT/GAVELED states), bus L11–15, status L11–15
  (incl. the 2026-08-27 gavel line). Nothing vanished without a home.
- **G2A general laws** sit in runbook/GUIDE.md (seat uniqueness L124–125; owner-rewrites/
  witness-attests L127–129), one sentence each, chapter carries only operations.
- **G7 gavel states** on all five `res/` manifest lines are truthful against each chapter's own
  header: cross-vendor-seat DRAFT ✓, csharp-head-protocol GAVELED 2026-09-04 ✓ (matches its
  protocol-framing gavel line), fanout-turns GAVELED `<GAVEL-DATE>` ✓ (truthful only after
  substitution — that is the intended pre-gavel condition), research GAVELED 2026-09-03 ✓,
  token-economy DRAFT ✓. `raw/` appears exactly once as a directory line, no per-file row —
  slug-directories law honored.

**Doctrine check, chapter (`fanout-turns.md`):** one-open-turn with cycle/turn interleave
clarification (L14–17); vocabulary bound with `awaiting: return | verdict <seat>` and the
no-dashboard-words rule (L29–32); relay stop-before-work (L34–36); no trusted `turn:` echo in
RETURN (L40); deferred-names-only-the-open-turn with the authorship-tense refinement (L42–44,
L79–80); REVISE closes the cycle, not the branch, corrective cycle inherits the turn (L46–49);
narrow G1A exception with late-RETURN-new-cycle (L51–55); join = verified STATUS replacement,
commit policy separate per G5B (L59–67); contents-not-presence recovery with disk-beats-STATUS
and no-receipt-editing (L71–75); boundary + retirement trigger (L84–91). This is the doctrine
the assessment, countersign amendments 1–4, my cycles 02/04/06, and the turn-01 experiment
converged on — nothing smuggled, nothing lost.

**BUS grammar:** `<kind>` still exactly three (L35); `turn:` optional on POINT (L66) and
VERDICT (L143) only; `return: none + reason` bounded to STOP/BLOCKED in schema (L145) and law
(L156–159); closure bullet 1 extended for verified absence, closure bullet 6 routes deferral to
the chapter (L177–184); middle four closure bullets untouched. No fourth kind, no `.turn.md`,
no `carrier:`, no dead `res/csharp-head.md` citation anywhere in the eight files.

**Pre-gavel token topology:** exactly four `<GAVEL-DATE>` literals — runbook/GUIDE.md L6 +
L26, bus/GUIDE.md L6, status/GUIDE.md L6. None elsewhere; the chapter correctly carries only
the light two-field header.

**Mechanical conditions that must pass after @majkee supplies the date, before commit:**

1. One `YYYY-MM-DD` substituted in all four locations atomically; RETURN 09's deferred
   token-absence and frontmatter-structure checks (its field 6, first block) exit clean.
2. RETURN 09's final exact-state Manifest gate passes — the fanout row's date must equal the
   runbook `verified:` date by extraction, not by retyping.
3. Immediately pre-commit: rerun the eight-path porcelain check and the four `tail -n +5`
   body-hash comparisons — proving nothing crept in between the cycle-09 freeze and the commit,
   a window this shell-less audit cannot cover.
4. Commit lands metadata and substance together (G6); only then do Atlas UI and harness-builder
   wake, on their disjoint surfaces, per the RUNBOOK.

## 1. Files changed

none. (This RETURN only, at the POINT's `return_to:`. No reposoma, session authority, skill,
runtime, deployment, staging, or git state touched.)

## 2. Commands and outcomes

No shell commands run — this seat has no shell; per bus-GUIDE verification rule 3 the checks
that require one are stated below with why they cannot be rerun here. Operations performed:
complete reads of all ten reposoma files and all seven session artifacts named in `paths.read`
(RUNBOOK unchanged since my cycle-06 read per the standing hold; verified against context).

Verification method for shell-dependent claims:

- **Eight-path boundary and byte-identity of the four chapter bodies** — accepted from
  VERDICT 09's independent porcelain/hash runs, cross-checked by content: each normalized
  chapter is exactly a four-line YAML header followed by its original first `#` heading
  (line 5 in all four), and the two guide-writing files are byte-identical to the `b83026b`
  content I authored via the cycle-04 fold (compared verbatim in-context) — so no drift
  leaked into the adjacent slug. Residual risk is closed by pre-commit condition 3.
- **Chapter-equals-blueprint** — VERDICT 09's SHA-256 pair, independently corroborated by my
  line-level comparison of the disk chapter against RETURN 05's §File 4 blueprint held in
  context: no divergence found.
- **ACCEPT/REVISE assertion repair (cycle 09 curvature)** — confirmed against disk: the
  doctrine sentence wraps across chapter L52–53 exactly as RETURN 09's two-line fixed string
  states; the original one-line `rg` against the BUS file could never match. REFUTED/CONFIRMED
  rows of VERDICT 09 check out.

## 3. Evidence paths

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — L1–7, L22–29, L124–129, L133–141
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — L1–7, L35, L64–74, L141–159, L175–184
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — L1–7, L53–55, L80–91
- `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` — whole chapter (91 lines)
- `/home/hruzam/reposoma/raw.guides/runbook/res/{cross-vendor-seat,csharp-head-protocol,research,token-economy}.md`
  — four-line headers + preserved bodies from line 5
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` + `res/slug-directories.md` —
  boundary check (unmodified vs `b83026b`)
- `_bus/05.astrobley.return.md`, `07.astrobley.return.md`, `08.astrobley.return.md`,
  `09.astrobley.return.md`, `09.cartan-csharp.verdict.md` — the accepted blueprint chain and
  the freeze this audit is crossed against

## 4. Mismatches discovered

1. **Per-row `turn` vs the experiment's practice.** The candidate's row schema is
   `cycle · to · turn · awaiting`; the live turn-01 STATUS used a hoisted STATUS-level
   `turn: 01` field (and my cycle-06 RETURN leaned that way). The candidate picked per-row,
   is self-consistent, and per-row is strictly more expressive on the deferred/late edges —
   **accepted**, but note: once this gavels, the turn-01 style becomes nonstandard; the head's
   next fan-out STATUS should follow the chapter.
2. **Row-key list lives in two places** — status/GUIDE.md L82–84 and chapter L29–30 both
   enumerate the exact keys and `awaiting` values. This is within the accepted schema-stub
   boundary (VERDICT 02), but it is the one seam where a future key change must touch two
   files. Accepted low-risk curvature; not worth a third mechanism.
3. **Cosmetic:** fanout-turns.md has a blank line between frontmatter and heading; the four
   normalized chapters do not. Both shapes pass every declared check. No action.
4. **Old closure-bullet obligation relocated, not lost** — the former "confirms STATUS was
   rewritten … or explains why BLOCKED" is now carried by the `status_rewritten:` value
   vocabulary plus the chapter's owner-discharge rules. Verified equivalent; noting it so
   nobody reads the shorter bullet as weakened law.
5. **Session RUNBOOK's dead `res/csharp-head.md` references** — historical curvature already
   disposed by VERDICT 08; confirmed absent from all eight candidate files.

## 5. Recommended next task

Cartan verifies this RETURN and writes VERDICT 10; on ACCEPT, put exactly one question to
@majkee — the gavel date — then execute in order: atomic four-token substitution, RETURN 09's
deferred final-date checks, the pre-commit boundary/body-hash rerun (condition 3 above), commit
with the gavel, and only then the runtime-reconciliation POINTs to Atlas UI and harness-builder.
One bounded task; no step reordered.

## 6. Remaining uncertainty

- Shell-dependent claims (exact dirty-path set, whole-file hashes) rest on VERDICT 09's runs
  plus my content corroboration — this seat could not rerun them; pre-commit condition 3
  closes the gap at the only moment it matters.
- Laws the turn-01 run did not prove (my RETURN 06 field 6 list — G1A exercise, late-artifact
  handling, turn transition, relay mismatch, cross-host) remain unproven by experiment; the
  chapter encodes them from analysis. The closing VERDICT must not claim field proof for them.
- `carrier:` vs participant-tuple stays deferred by design; the candidate correctly adds
  neither.
- The §§10–12 candidate draft remains missing; if recovered, it is new evidence for a new
  cycle, not a reopening of this one.
- Runtime reconciliation, deployment, and fresh-session invocation proofs remain entirely
  ahead; nothing in this ACCEPT pre-authorizes them.
