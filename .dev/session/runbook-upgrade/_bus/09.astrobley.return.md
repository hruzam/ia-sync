---
cycle: 09
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/09.cartan-csharp.point.md
written: 2026-09-04
host: office
---

# RETURN 09 — exact eight-file candidate guide fold authored

## 1. Files changed

Reposoma remains at uncommitted HEAD `a47f4ba` with exactly these eight dirty paths:

1. `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — added the required GUIDE frontmatter,
   complete five-chapter plus one-`raw/` Manifest, participant-seat uniqueness law,
   `status_owner`/witness authority seam, and fan-out chapter routing seam.
2. `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — added the required GUIDE frontmatter,
   optional `turn:` coordinates to POINT and VERDICT only, narrow absent-RETURN schema and law,
   grouped-cycle STATUS disposition, and chapter pointer without adding a BUS kind.
3. `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — added the required GUIDE frontmatter,
   content-sensitive fan-out recovery seam, optional bounded `delegated:` and `join_when:` fields,
   and the singular-`next:` rule.
4. `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` — created the complete accepted
   substantive chapter for full-session fan-out, branch verification, owner join, cold recovery,
   late artifacts, and retirement boundary.
5. `/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md` — prepended only its
   four-line `chapter-of: runbook` header.
6. `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md` — prepended only its
   four-line current-protocol header, including the successors-letter role.
7. `/home/hruzam/reposoma/raw.guides/runbook/res/research.md` — prepended only its four-line
   `chapter-of: runbook` header.
8. `/home/hruzam/reposoma/raw.guides/runbook/res/token-economy.md` — prepended only its four-line
   `chapter-of: runbook` header.

This RETURN is the only ia-sync path authored by this seat:

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/09.astrobley.return.md` — records the
  actual diff, verification evidence, one verification-spec curvature, and deferred date gate.

Implementation position before the cut: apply RETURN 05's accepted prose literally, substitute
only RETURN 08's seven current-protocol corrections, and use RETURN 07's content-sensitive test
repairs. No architectural pushback was needed. The smallest coherent cut is the authorized eight
reposoma paths; `carrier:`, a fourth BUS kind, runtime behavior, and unavailable candidate-canon
sections remain outside it.

## 2. Commands and outcomes

Pre-edit resolution and hard gate:

- `cat /home/hruzam/.agents/skills/reposoma-surgical-coding/SKILL.md` — exit 0; read the required
  discipline completely before work.
- ia-sync orientation (`head -80 journal.host-cleanup.md`, `git log --oneline -5`, inbox listing,
  and `cat SYNC_DISCIPLINE.md`) — exit 0; inbox empty and existing session work identified.
- Complete reads of every path declared under POINT 09 `paths.read` — exit 0. The reposoma
  instruction chain at `/home/hruzam/reposoma/AGENTS.md` was also read. Pruned substrate and the
  unavailable candidate-canon sections were not searched for or reconstructed.
- `git status --porcelain=v1` plus `git rev-parse --short HEAD` and `git rev-parse HEAD` in
  `/home/hruzam/reposoma` — status output empty; HEAD `a47f4ba`
  (`a47f4babfef5836bae059c5830a27bbc5b0eb998`). The hard precondition passed before any edit.
- `test ! -e /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/09.astrobley.return.md` —
  exit 0 before this write.
- `sha256sum` over the four pre-existing chapters before editing — exit 0 and recorded:
  - cross-vendor-seat body: `831fbae5930db09c3589064d4af8488425661d43226b962ef0183d6cea68b580`
  - csharp-head-protocol body: `48dd8342782b32dde49c602ecf57e37dd95a916302bc51d205192a949b5064d8`
  - research body: `0fb40501d4d3ee6102d9fe7f3cb5e40cf936d16f2be9c9d1ae0dc8881f4fd28b`
  - token-economy body: `ed3952e0a43e4cf8f51407306808bd601c0480e693ab9e3575b701de7c2f30fe`

Write and review:

- The first combined `apply_patch` attempt failed validation on an exact STATUS context line and
  changed no file; immediate reposoma status and token scans were empty. The same patch was split
  by target with the observed context and applied successfully.
- `git status --short --untracked-files=all` in reposoma — exactly seven modified tracked files
  and one untracked new chapter, all eight authorized paths.
- `git diff --stat`, the tracked seven-file `git diff`, and
  `git diff --no-index -- /dev/null raw.guides/runbook/res/fanout-turns.md` — inspected the actual
  candidate. Tracked delta: 94 insertions and 5 replacements; new chapter: 91 lines.
- Blueprint chapter extraction from RETURN 05 compared by SHA-256 with the actual new chapter —
  both `4fb38edd3ddda0c42f0584232bc78264db198bc7c446917584c9f2d71ef45485`;
  the substantive chapter is byte-identical to the accepted blueprint.

Candidate-sensitive verification:

- `bash /tmp/runbook-upgrade.point09-check.sh` — final exit 0:
  `PASS head=a47f4ba dirty_paths=8 placeholders=4 bodies=4 manifest=exact bus_kinds=3`.
  The script asserts the exact dirty-path set, all four preserved-body hashes, all GUIDE/chapter
  frontmatter, exact placeholder locations, exact Manifest rows/states, single `raw/` entry,
  cycle-07 multiline clauses, POINT/VERDICT `turn:` count, three-kind BUS grammar, narrow
  absent-RETURN behavior, owner join, STATUS recovery, carrier/forbidden-kind holds, current
  cSharp protocol path, and whitespace cleanliness.
- The first harness pass exited 2 because an internal zero-match `rg -c` emitted no numeric text;
  the harness was corrected to normalize the expected no-match case to zero. No target changed.
- A second harness pass exposed the accepted blueprint's one-line ACCEPT/REVISE assertion as a
  check defect. The original command
  `rg -q 'ACCEPT and REVISE.*actual RETURN' "$bus"` exited 1. The exact accepted doctrine is in
  the chapter and wraps across two lines. This replacement exited 0 without changing prose:

  ```bash
  point09_chapter=/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
  point09_accept_revise_clause=$(printf '%s\n' \
    'the absence, records why the cycle cannot receive a RETURN, and closes no other branch. ACCEPT' \
    'and REVISE require an actual RETURN. If a RETURN later appears for a cycle closed through this')
  rg -UqF "$point09_accept_revise_clause" "$point09_chapter"
  ```

- Candidate token scan across exactly the eight reposoma targets — four occurrences total:
  three line-6 GUIDE `verified:` fields and the fanout-turns Manifest state; no other location.
- Exact Manifest comparison — exit 0; the five chapter states are cross-vendor-seat DRAFT,
  csharp-head-protocol GAVELED 2026-09-04, fanout-turns GAVELED `<GAVEL-DATE>`, research
  GAVELED 2026-09-03, and token-economy DRAFT. Exactly one `raw/` directory row exists and no
  per-file raw row exists.
- Four `tail -n +5 | sha256sum` comparisons — exit 0; each current body equals its pre-edit hash.
- Retired-path scan built from `printf '%s%s' 'res/csharp-' 'head.md'` across all eight candidate
  files — no match. No dead `res/csharp-head.md` citation entered the candidate.
- Added-line scans for `carrier:`, `§§10`, `TURN kind`, and `.turn.md`, plus whole-chapter scans
  for forbidden kinds/carrier — no match.
- `git -C /home/hruzam/reposoma diff --check` — exit 0 with no diagnostics.
- `git diff --no-index --check /dev/null <fanout-turns>` — expected status 1 with empty diagnostic
  output; the untracked chapter has no whitespace errors.
- No commit, stage, push, deploy, cross-host action, agent wake, or external-state change ran.

## 3. Evidence paths

Actual candidate and diff entry points:

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md`
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md`
- `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md`
- `/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md`
- `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`
- `/home/hruzam/reposoma/raw.guides/runbook/res/research.md`
- `/home/hruzam/reposoma/raw.guides/runbook/res/token-economy.md`

Cartan and Oraculum can inspect the complete current candidate with:

```bash
git -C /home/hruzam/reposoma status --short --untracked-files=all
git -C /home/hruzam/reposoma diff -- \
  raw.guides/runbook/GUIDE.md \
  raw.guides/bus/GUIDE.md \
  raw.guides/status/GUIDE.md \
  raw.guides/runbook/res/cross-vendor-seat.md \
  raw.guides/runbook/res/csharp-head-protocol.md \
  raw.guides/runbook/res/research.md \
  raw.guides/runbook/res/token-economy.md
git -C /home/hruzam/reposoma diff --no-index -- /dev/null \
  /home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
```

Blueprint and acceptance evidence:

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md` — accepted prose
  and original eight-file/write-pass blueprint.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.verdict.md` — limits
  cycle-07 repair to three verification defects.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/07.astrobley.return.md` and
  `07.cartan-csharp.verdict.md` — accepted multiline/state-map repairs.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/08.astrobley.return.md` and
  `08.cartan-csharp.verdict.md` — accepted seven replacements for the current cSharp protocol.
- `/tmp/runbook-upgrade.point09-check.sh` — rerunnable candidate-only check bundle used for the
  reported PASS; it is convenience evidence, not canon or a committed artifact.

Candidate whole-file SHA-256 values at RETURN time:

```text
510b87e61cd935389386e0c10d20c62749dd2f5decb773954d87ca78261eda01  raw.guides/runbook/GUIDE.md
1cc95ed8a073bda12627fdeacc5fcc2092666390b49456eeca34a19d0d3cc494  raw.guides/bus/GUIDE.md
8c485aeaff00be820c6adce33169c6f78140cb0c20954dd63b38003b78003a60  raw.guides/status/GUIDE.md
4fb38edd3ddda0c42f0584232bc78264db198bc7c446917584c9f2d71ef45485  raw.guides/runbook/res/fanout-turns.md
2bb300ec96c9b1efda9f3dc97900f7bd81f4d8a78da3df13cb5d90490c81645c  raw.guides/runbook/res/cross-vendor-seat.md
a0d6d48b7eb162fb327e3d83b5ced7a874f6d76c7c39d3800eb3f16e73bb5477  raw.guides/runbook/res/csharp-head-protocol.md
25d28ab420b54dd24b88eb4371cee91cd4717bfe5e9e1b26bb84c2a55dec265a  raw.guides/runbook/res/research.md
52314fcf0f1c41c8f8cbf454ae6c997cbbd659b7e1547b2ab242c70891fa5237  raw.guides/runbook/res/token-economy.md
```

## 4. Mismatches discovered

1. RETURN 05's original `rg -q 'ACCEPT and REVISE.*actual RETURN' "$bus"` assertion exits 1
   against the exact accepted candidate. The BUS seam narrows `none` to STOP/BLOCKED, while the
   explicit ACCEPT/REVISE sentence lives in `fanout-turns.md` and wraps between `ACCEPT` and
   `and REVISE`. The multiline fixed-string replacement recorded in field 2 exits 0. This is a
   fourth verification-spec defect, not a prose or doctrine defect; no guide text was altered to
   satisfy the stale check.
2. The fixed session RUNBOOK still contains historical `res/csharp-head.md` references, as
   already disposed in VERDICT 08. POINT 09 forbids editing session authority. The current
   reposoma candidate contains no such citation.
3. The initial combined patch's STATUS context expected a different line wrap and failed
   atomically. The subsequent context-resolved patches produced only the authorized delta. This
   is tooling curvature with no surviving content effect.
4. No sandbox or repository-write blocker occurred. The explicitly authorized reposoma targets
   accepted source-first patches directly; no alternate location or live/deployed target was used.

## 5. Recommended next task

Cartan-csharp should independently verify the eight-path uncommitted diff, all four body hashes,
the exact Manifest and placeholder topology, and the verification curvature above, then author
VERDICT 09. On ACCEPT, relay a separate actual-diff audit POINT to Oraculum. Do not supply the
gavel date, commit, or wake runtime-reconciliation seats until that crossed audit returns.

## 6. Remaining uncertainty

- The operator's final documentation-gavel date is intentionally unknown. Exactly four literal
  `<GAVEL-DATE>` placeholders remain in the candidate, and no date has been inferred from this
  session's clock.
- After @majkee supplies one `YYYY-MM-DD` date and all four substitutions are made atomically,
  rerun these deferred final-date checks before commit:

  ```bash
  repo=/home/hruzam/reposoma
  for slug in runbook bus status; do
    file="$repo/raw.guides/$slug/GUIDE.md"
    sed -n '1,7p' "$file" | rg -Uq \
      '^---\ntitle: .+\nscope: [a-z-]+\naudience: agent \+ operator \+ builder\nmachine: both\nverified: [0-9]{4}-[0-9]{2}-[0-9]{2}\n---$' || exit 1
  done
  for file in "$repo"/raw.guides/runbook/res/*.md; do
    sed -n '1,4p' "$file" | rg -Uq \
      '^---\ntitle: .+\nchapter-of: runbook\n---$' || exit 1
  done
  ! rg -nF '<GAVEL-DATE>' \
    "$repo/raw.guides/runbook/GUIDE.md" \
    "$repo/raw.guides/bus/GUIDE.md" \
    "$repo/raw.guides/status/GUIDE.md" \
    "$repo/raw.guides/runbook/res/fanout-turns.md" \
    "$repo/raw.guides/runbook/res/cross-vendor-seat.md" \
    "$repo/raw.guides/runbook/res/csharp-head-protocol.md" \
    "$repo/raw.guides/runbook/res/research.md" \
    "$repo/raw.guides/runbook/res/token-economy.md"
  ```

- The final exact-state gate must bind fanout-turns to the same numeric date carried by RUNBOOK
  frontmatter, while retaining the four other chapter states:

  ```bash
  repo=/home/hruzam/reposoma
  final_runbook="$repo/raw.guides/runbook/GUIDE.md"
  final_manifest=$(awk '/^## Manifest$/{p=1; next} p && /^## /{exit} p{print}' \
    "$final_runbook")
  final_gavel_date=$(sed -nE \
    '1,7 s/^verified: ([0-9]{4}-[0-9]{2}-[0-9]{2})$/\1/p' "$final_runbook")
  test "$(printf '%s\n' "$final_gavel_date" | wc -l)" -eq 1
  printf '%s\n' "$final_gavel_date" | rg -qx '[0-9]{4}-[0-9]{2}-[0-9]{2}'
  final_expected_chapters=$(printf '%s\n' \
    '- `res/cross-vendor-seat.md` · chapter · DRAFT — cross-vendor seat instrument, tunnel handle, and enable layers' \
    '- `res/csharp-head-protocol.md` · chapter · GAVELED 2026-09-04 — cSharp protocol: head posture, transfer ritual, and letter to successors' \
    "- \`res/fanout-turns.md\` · chapter · GAVELED $final_gavel_date — full-session fan-out, coordination join, and cold recovery" \
    '- `res/research.md` · chapter · GAVELED 2026-09-03 — big-scope pre-RUNBOOK research gate' \
    '- `res/token-economy.md` · chapter · DRAFT — delegation shape and spend record')
  final_observed_chapters=$(printf '%s\n' "$final_manifest" |
    rg '^- `res/[^`]+[.]md` · chapter · ')
  test "$final_observed_chapters" = "$final_expected_chapters"
  ```

- Oraculum's independent audit, @majkee's date/gavel and commit authority, the reposoma commit,
  runtime reconciliation, deployment, fresh-session behavior proofs, and session closure all
  remain ahead.
- `carrier:` and the unavailable candidate-canon sections remain unresolved by design. No
  carrier choice or reconstructed content entered this candidate.
