---
cycle: 31
from: cartan-csharp
to: atlas-ui
scope: Package the unchanged A/B proposal as a checked literal diff; no source authoring or repeated investigation
paths:
  read:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/STATUS.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/30.atlas-ui.return.md
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/30.cartan-csharp.verdict.md
    - /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md
    - /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md
  write:
    - /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/31.atlas-ui.return.md
gates:
  - Independent operator-opened atlas-ui window only; resolve RUNBOOK seat and return_to. No spawned substitute or other-seat write.
  - The named RETURN is the sole write. Preserve RETURN 30 and every prior receipt; this is a sequential correction, not a fan-out turn.
  - No source/live/guide/STATUS/RUNBOOK change, scratch file, installation, model run, transcript read, stage, commit, transport, deploy, wake, board action or prune.
  - Only read-only Git inspection or git apply --check is allowed; never apply the proposal to the worktree or index. In-memory/stdin extraction is sufficient.
done_when: A concise six-heading RETURN contains one literal two-file proposal diff that passes git apply --check, valid RETURN frontmatter, exact commands/results, and no new substantive change.
return_to: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/31.atlas-ui.return.md
---

# POINT 31 — mechanical finish only

VERDICT 30 confirms the substantive corrections. Do not repeat the diagnosis, transcript
analysis, evidence tables or retest design. Refer to immutable RETURN 30 and VERDICT 30.
RETURN 30's SHA-256 is
`80c6a4ad766f3ee48a5df61b99289ccf55f71b6294cc2f879f4de5ae76145f57`.

1. Put the two proposed append operations in one fenced `diff` block. Use exact existing
   source context, including soft wraps. Decode presentation-only backtick escaping from
   RETURN 30; preserve its insertion words, intended Markdown and bullet continuation.
   Do not revise semantics or author either source file.
2. Check source pins first: Claude `de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9`;
   Codex `b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec`.
   Drift means report, not reconcile. Pipe only the extracted diff to
   `git -C /home/hruzam/ia-sync apply --check --whitespace=error -`; never omit `--check`.
3. Parse the new RETURN's own frontmatter. Record the actual complete extraction/check
   commands and outcomes in field 2, with absolute paths and no placeholders. Earlier
   head-observed checks remain attributed references, not your new reruns. No need to
   rehash or read the host-local transcripts.

Keep the six sections short. Source-scope gavel, authoring, deployment and fresh producer
proofs remain separate gates. This cycle produces only the exact reviewable proposal.
