---
cycle: 03
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.cartan-csharp.point.md
written: 2026-09-04
host: office
---

# RETURN 03 — corrected documentation-fold blueprint

## 1. Files changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.astrobley.return.md` — added this
  analysis-only RETURN. It reconciles cycle 01 with Oraculum's audit and the complete observed
  runbook slug topology.
- No guide, chapter, RUNBOOK, STATUS, pulse, skill, zsh, git, deployment, or live-runtime file
  changed.

Assumptions held for this cut:

- The POINT's nine declared read paths are the complete evidence corpus for cycle 03. The fuller
  candidate draft cited elsewhere as §§10–12 was not found and is not reconstructed here.
- This cycle authors a blueprint only. Canon remains unchanged until @majkee rules the two open
  semantic choices and gavels the resulting documentation cut.
- Guide-writing's current four-class manifest law controls classification. A Markdown file does
  not become an attachment merely because it is evidence-like, and an unsuperseded substrate does
  not become legacy merely because it is old.

Success criteria met by this RETURN:

- one smallest corrected fold is specified down to file, insertion, field, and ownership boundary;
- owning GUIDEs contain honest schema stubs while the new chapter owns coordination doctrine;
- every observed runbook-slug item is named and the `raw/` artifact is classified without false
  adoption;
- the three frontmatter seams have one explicit treatment;
- the absent-RETURN choice is stated as two neutral legal branches; and
- field 6 contains only rulings that still require @majkee.

## 2. Commands and outcomes

- `sed -n '1,240p' /home/hruzam/.agents/skills/reposoma-surgical-coding/SKILL.md` — exit 0; read
  the required surgical discipline before task actions.
- `sed -n '1,280p' /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.cartan-csharp.point.md`
  — exit 0; resolved the nine-path read corpus, sole write path, gates, and done condition.
- `wc -l` over all nine POINT-declared read paths — exit 0; all paths resolved, 1,321 lines total.
- Ancestor-instruction probe over the declared ia-sync and reposoma paths — exit 0; the supplied
  `/home/hruzam/ia-sync/AGENTS.md` governs the write path and only
  `/home/hruzam/reposoma/AGENTS.md` additionally governs the reposoma evidence paths.
- `sed -n` over `/home/hruzam/reposoma/AGENTS.md` and all nine POINT-declared read paths — exit 0;
  every file was read completely. No fuller candidate draft, cycle-04 artifact, legacy chapter,
  session authority file, or unrelated source was opened.
- `pwd`, `hostname -s`, and `printenv MACHINE_NAME` — exit 0; observed
  `/home/hruzam/ia-sync`, `hruzam-120922`, and `office`.
- `git status --short --branch --untracked-files=all` in `/home/hruzam/ia-sync` before the write —
  exit 0; pre-existing changes were `RUNBOOK.md`, `codex/AGENTS.md`, `pulse.md`, `STATUS.md`, BUS
  cycles 01–04, and the unrelated `install-pkgs/maintenance/codex-subagents.2026-09-04/` artifact.
- `git -C /home/hruzam/reposoma status --short --branch --untracked-files=all` — exit 0;
  `core...origin/core`, clean.
- `find` with `-maxdepth` over the runbook, bus, and status guide slugs — exit 0; runbook contains
  `GUIDE.md`, four `res/*.md` files, and one `raw/*.md` file; bus and status each contain only
  `GUIDE.md`.
- `test ! -e /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.astrobley.return.md` before
  the write — exit 0; sole target did not exist.

Post-write verification commands and observed results:

- An initial heading probe used an over-escaped literal and exited 1 with no matches. The corrected
  `rg -n '^## [1-6][.]' <RETURN>` exited 0, and the final
  `awk '/^## [1-6][.]/ {n++} END {print n; exit(n==6?0:1)}' <RETURN>` exited 0 and printed `6`.
- `rg -n 'schema stubs, not doctrine|unregistered Markdown substrate/orphan drift|Only two
  operator rulings remain|Strict branch|STOP/BLOCKED exception branch|same @majkee gavel|fuller
  candidate draft' <RETURN>` — exit 0; all required content gates were present.
- `rg -n '[[:blank:]]+$' <RETURN>` — exit 1 with no output, the expected clean result.
- `git diff --no-index --check /dev/null <RETURN>` — exit 1 because the new file differs from
  `/dev/null`, with no diagnostic output; no whitespace errors were found.
- The first `git status --short --branch --untracked-files=all` after writing — exit 0; this RETURN
  was then the sole new post-baseline path and all pre-existing paths remained listed. A final
  status check also showed concurrently created `04.oraculum-audit.return.md`; it was not opened or
  touched. This RETURN remains the only write attributable to this seat.
- `git -C /home/hruzam/reposoma status --short --branch --untracked-files=all` — exit 0;
  `core...origin/core`, still clean.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/01.astrobley.return.md` — useful
  three-kind BUS model, exact first-pass fold, pressure cases, and the initial uncertainty list.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/01.cartan-csharp.verdict.md` — accepts the
  BUS-first direction but refutes the claimed Manifest completeness and withholds the
  absent-RETURN exception.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/02.oraculum-audit.return.md` — map-honesty
  correction, frontmatter seams, topology warning, and runtime-after-gavel boundary.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/02.cartan-csharp.verdict.md` — confirms the
  schema-stub/doctrine split, missing fuller draft, raw-path omission, and narrowed no-bulk-
  normalization scope.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` — one-topic slug, map/chapter split,
  four-class Manifest law, orphan rule, required GUIDE frontmatter, light chapter header, and
  disk-over-doc rule.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — current launcher law, inline authority
  provenance, participant schema, cSharp pointer, session file set, and missing frontmatter and
  Manifest.
- `/home/hruzam/reposoma/raw.guides/runbook/raw/field.runbook-check-rules.2026-09-03.md` — observed
  Markdown substrate with `kind: substrate`, `status: substrate`, provenance, and future skill-feed
  purpose; it is not marked `chapter-of: runbook`, attachment, or superseded legacy.
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — three-kind disk grammar, POINT/RETURN/VERDICT
  schemas, six mandatory RETURN fields, and current closure law requiring one RETURN.
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — mandatory fixed anatomy, bounded replacement,
  singular `next:`, content-sensitive 03:00 recovery, and precedent for pointing to another guide's
  authority doctrine.

### Corrected exact fold

The smallest coherent content fold remains four documentation files. It does not create a TURN
kind, put present state in BUS, duplicate fan-out doctrine in three GUIDEs, normalize existing
chapters, or wake runtime builders:

1. add `raw.guides/runbook/res/fanout-turns.md`;
2. amend `raw.guides/runbook/GUIDE.md` as the map and participant-authority seam;
3. amend `raw.guides/bus/GUIDE.md` as the POINT/VERDICT schema-of-record; and
4. amend `raw.guides/status/GUIDE.md` as the STATUS schema-of-record.

The three existing GUIDEs receive required frontmatter in the same cut. The new chapter receives
only the required light chapter header. Existing `res/*.md` headers and the raw substrate are not
bulk-normalized under this POINT.

### Frontmatter treatment — all three seams resolved

Prepend these five-key headers, substituting the date of @majkee's actual final verification/gavel
for `<GAVEL-DATE>`:

```yaml
# raw.guides/runbook/GUIDE.md
---
title: RUNBOOK builder — the session launcher
scope: runbook
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---

# raw.guides/bus/GUIDE.md
---
title: _bus builder — the session's verified multi-seat exchange
scope: bus
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---

# raw.guides/status/GUIDE.md
---
title: STATUS builder — the session's bounded recovery snapshot
scope: status
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---
```

This resolves Oraculum's three seams without inventing metadata:

1. **Authority state:** retain the existing RUNBOOK inline `extends:`, DRAFT/GAVELED, boundary,
   and revision lines verbatim below the new frontmatter. Nothing vanishes and no second authority
   field is invented.
2. **`verified:` truth:** use the final audit/gavel date, not this blueprint date and not a
   mechanical touch date. The value then means the changed document was actually verified.
3. **Canon authority:** land metadata and content together inside the same @majkee gavel. Claim no
   format-only exemption.

Do not add frontmatter to the four existing `res/*.md` files in this fold. Their missing
`chapter-of:` markers remain named legacy metadata debt, not a silently expanded migration.

### `runbook/res/fanout-turns.md` — doctrine home

Header exactly:

```yaml
---
title: cSharp fan-out turns — full-session coordination without a fourth BUS kind
chapter-of: runbook
---
```

The chapter, and only the chapter, carries these semantics:

1. use full-session fan-out only when one persistent head coordinates at least two independent CLI
   sessions; native in-window subagents remain under token-economy;
2. `cycle: NN` orders one POINT → RETURN → VERDICT exchange, while `turn: NN` groups the cycles
   opened by one head decision; exactly one turn may be open;
3. RUNBOOK fixes unique participant seats and the one head/`status_owner`; STATUS owns the open
   turn; BUS remains past-tense receipts; the human carrier is transport only;
4. the head writes all POINTs, then replaces STATUS before relay with bounded `delegated:` rows,
   one `join_when:`, a content-checking `recovery_probe:`, and one `next:`/`expected:` pair;
5. each `delegated:` row contains only `cycle`, `to`, `turn`, `awaiting`, and optional `carrier`;
   `awaiting` is `return` or `verdict <seat>`;
6. a receiver must stop before work if POINT `to:` or the seat encoded by `return_to:` differs from
   its RUNBOOK seat; the outstanding STATUS row remains true;
7. a witness verifies content, not presence, and writes the one cycle VERDICT; only
   `status_owner` replaces STATUS, while another witness may attest `deferred: turn NN`;
8. REVISE closes its cycle but not its branch; the head opens one linked corrective cycle in the
   same turn and replaces that branch's observable join obligation;
9. join is the owner's verified STATUS replacement after all current obligations are checked, not
   a fourth file kind and not necessarily a git commit;
10. late artifacts never overwrite history or receive a second same-direction file; assess new
    information in a new cycle, and author a new `deferred:` only to the then-open turn;
11. 03:00 recovery reconciles each STATUS row with POINT, RETURN if present, and VERDICT contents,
    including coordinates, seats, disposition, and `status_rewritten:`; mismatches are partial
    state, not permission to repair another writer's receipt; and
12. no scheduler, inbox, read-state, delivery claim, specialized join VERDICT, vendor syntax, or
    runtime implementation is introduced. A native durable cross-session join with equivalent
    receipts retires this file-plane delta.

The absent-RETURN paragraph is inserted according to the neutral operator branch in field 6; no
exception is presumed here.

### `bus/GUIDE.md` — schema stubs, not doctrine

Keep `<kind>` exactly `point | return | verdict`. Add after `cycle:` in the POINT schema:

```yaml
turn: <optional coordination-turn coordinate; see raw.guides/runbook/res/fanout-turns.md>
```

Add the same optional `turn:` stub after `cycle:` in the VERDICT schema. Do not add `turn:` to
RETURN; its cycle and POINT path resolve the grouping, and a worker echo would create a second
claim to reconcile.

Extend only the existing VERDICT status label:

```yaml
status_rewritten: <yes | no + reason | deferred: turn NN; see raw.guides/runbook/res/fanout-turns.md>
```

Replace the STATUS-closure bullet with:

> accounts for the STATUS edge in `status_rewritten:`; grouped-cycle deferral, owner discharge,
> and late-artifact rules live in `raw.guides/runbook/res/fanout-turns.md`.

Add one sentence after the VERDICT schema:

> `turn:` is a grouping coordinate, not a fourth BUS kind or current state; full-session fan-out
> doctrine lives in `raw.guides/runbook/res/fanout-turns.md`.

The `return:` schema stays unchanged unless @majkee selects the exception branch in field 6.

### `status/GUIDE.md` — schema stubs, not doctrine

After the mandatory fixed-fields explanation, add one small optional-schema section:

```markdown
### Optional full-session fan-out fields

`delegated:` — optional only while a full-session coordination turn is open; bounded row schema
and replacement semantics: `raw.guides/runbook/res/fanout-turns.md`.

`join_when:` — optional, exactly one while that turn is open; observable join semantics:
`raw.guides/runbook/res/fanout-turns.md`.
```

These are honest field stubs: name, optionality, cardinality, and authority pointer. The row
vocabulary, loop, and failure cases remain doctrine in the chapter. `next:` stays singular.

After the 03:00 file-presence warning, add only:

> When `delegated:` is present, `recovery_probe:` follows the content-reconciliation rule in
> `raw.guides/runbook/res/fanout-turns.md`.

### `runbook/GUIDE.md` — authority/map seam

After the participant fields, add:

> Participant seat names are unique within one RUNBOOK. A full-session fan-out receiver must match
> both POINT `to:` and the seat encoded by `return_to:` to one declared participant before work.

After the existing `head_note:` sentence, add:

> A cSharp coordinating independent full CLI sessions in one decision uses
> `res/fanout-turns.md`; ordinary in-window subagent delegation does not.

Add `## Manifest` before `## What a RUNBOOK is`, but do not claim it is final until the raw
substrate ruling in field 6 is applied. Its known entries are:

```markdown
## Manifest

- `res/cross-vendor-seat.md` · chapter · cross-vendor seat instrument, tunnel handle, and enable layers
- `res/csharp-head.md` · chapter · cSharp head posture and transfer ritual
- `res/fanout-turns.md` · chapter · cSharp full-session fan-out, coordination join, and cold recovery
- `res/research.md` · chapter · big-scope pre-RUNBOOK research gate
- `res/token-economy.md` · chapter · delegation shape and spend record
```

Do not label `raw/field.runbook-check-rules.2026-09-03.md` as one of those five chapters. Its own
frontmatter says `kind: substrate` and `status: substrate`; it lacks `chapter-of: runbook`, remains
future skill input, and is not superseded. Under current guide-writing law it is therefore an
**unregistered Markdown substrate/orphan drift**: not a chapter, not a non-Markdown attachment,
and not a legacy origin record safe to mark “superseded, do not follow.” That is its classification,
not a defense of its location. The final Manifest must apply @majkee's disposition from field 6.

## 4. Mismatches discovered

1. **Cycle 01's Manifest was incomplete.** Inspecting only `res/` missed
   `raw/field.runbook-check-rules.2026-09-03.md`. The corrected topology is four existing res
   files, one raw Markdown substrate, and the proposed fifth res chapter.
2. **The raw substrate has no legal current manifest class.** Calling it an attachment contradicts
   “attachment (non-md)”; calling it a chapter contradicts its path/header/status; calling it
   legacy falsely says it is superseded. It must be dispositioned, not renamed in prose.
3. **“GUIDEs stay maps” was incomplete language.** A GUIDE can remain a map only if it honestly
   names the fields legal in its artifact. The corrected boundary is schema stubs in BUS/STATUS,
   coordination meaning and operations in the RUNBOOK chapter.
4. **Oraculum's all-chapter frontmatter oracle exceeds the narrowed fold.** The four existing res
   files reportedly lack current light headers, but this POINT forbids bulk normalization. The
   blueprint names that pre-existing debt and does not make it a hidden fifth through eighth edit.
5. **The fuller candidate draft remains absent.** Neither the brief nor later summaries substitute
   for its cited §§10–12. No claim in this RETURN depends on reconstructed content from it.
6. **Current BUS law has no absent-RETURN VERDICT.** The two legal future branches are both
   coherent but mutually exclusive; neither is smuggled into the base schema.
7. **Frontmatter conversion need not erase provenance.** Prepending required YAML while retaining
   the inline authority block resolves the apparent home problem. The real authority edge is the
   same-cut gavel, not a format-only label.
8. **The worktree changed concurrently.** `04.oraculum-audit.return.md` appeared after this seat's
   baseline and initial post-write status. Its contents and ownership remain untouched; its presence
   is not evidence against the target-only write boundary.

## 5. Recommended next task

Have cartan-csharp present only the two field-6 rulings to @majkee, then issue one bounded drafting
POINT that instantiates the selected branches into an exact four-file proposed diff for independent
verification. The drafting POINT must not authorize runtime reconciliation, chapter-wide metadata
normalization, deployment, commit, or push.

## 6. Remaining uncertainty

Only two operator rulings remain:

1. **Absent RETURN — choose one branch neutrally.**
   - **Strict branch:** a VERDICT continues to judge one RETURN. If no RETURN exists, no VERDICT
     closes that branch; STATUS remains `awaiting: return` or records the blocking hold. Late
     material is handled when it arrives through the normal cycle history.
   - **STOP/BLOCKED exception branch:** BUS adds
     `return: <RETURN path | none + reason>` and permits `none` only for a STOP or BLOCKED
     disposition. A later RETURN does not reopen or receive a second VERDICT in that cycle; its new
     information is assessed in a new cycle.
2. **Raw substrate disposition — choose one truthful class/home before landing the Manifest.**
   - Move and verify it as a real `res/` chapter with `chapter-of: runbook` only if @majkee intends
     its detection rules to become current runbook canon.
   - Rehome it to an operator-named non-guide substrate/evidence location if it remains future
     skill input rather than runbook canon.
   - Amend guide-writing's four-class taxonomy to admit Markdown substrate only if that is a
     deliberate temple-wide architecture change; this is the broadest branch and is not implicit
     in this session.

The three frontmatter seams require no additional design ruling: preserve existing authority
lines, use the actual final gavel date, and include metadata in the same gavel. Existing chapter
metadata debt, the missing fuller draft, the later live fan-out/head-interruption test, runtime
reconciliation, and commit/deployment authority are recorded boundaries or later work—not operator
choices needed to judge this blueprint.
