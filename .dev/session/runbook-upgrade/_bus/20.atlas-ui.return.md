---
cycle: 20
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/20.cartan-csharp.point.md
seat_validation: POINT `to: atlas-ui` and `return_to:` match my declared participant_3 seat.
outcome: proposal + dry-run evidence delivered AS ORDERED, and one live-state curvature that
  outranks them — an external real deploy ran mid-cycle; live already equals the pins.
authored: 2026-09-05 (evening) · office · resident
---

# RETURN 20 — deployment preview delivered; live moved under us by another hand

## 1. Files changed

**By this seat: scratch only + this RETURN.** No live, source, git, palette, or config write.
Scratch (disposable, never truth): `/tmp/runbook-upgrade-deploy.5ks5zf/bundle/` — inventory
exactly four files, hashes: `deploy.sh` = `a4bba14a…` (**byte-identical to repo deploy.sh,
verified**) · the three accepted skills at their exact pins (`de60667b…` / `b527d48a…` /
`d31ec392…`) · plus one deliberately **empty `zsh/`** directory (see §2 — required because the
script's zsh rsync leg is unguarded under `set -e`; empty source copies nothing and
structurally excludes the palette).

**Not by this seat (field 4.1):** live targets and live palette changed mid-cycle via an
external real deploy.

### The proposed procedure (the POINT's deliverable — reviewed, dry-run-proven)

Smallest safe deployment compatible with compose-first: **therapy-precedent bundle, unchanged
deployer, full mode** — one command produces exactly the three targets:

1. **Preflight (abort on any miss):** three source pins match; live runbook = `2766115d…`,
   live octopus = `639fd06…`, `~/.agents/skills/guide` absent; bundle `deploy.sh` byte-equals
   repo `deploy.sh`; bundle inventory exactly the four files above + empty `zsh/`.
2. **Before-images:** authoritative recovery is git — old bytes live at commit `7c4a997`
   (`git show 7c4a997:claude/skills/runbook/SKILL.md` → `2766115d…`; `…:codex/skills/octopus/SKILL.md`
   → `639fd06…`). Belt: `cp -a` both live files to `~/.local/state/runbook-upgrade-backup.<date>/`
   (outside both repos; scratch is too volatile to hold rollback material).
3. **Deploy:** `bash <bundle>/deploy.sh` (no flags). Exact write scope: the three target
   files, creation of `~/.agents/skills/guide/`, directory-mtime stamps on
   `~/.claude/skills{,/runbook}`, `~/.agents/skills{,/octopus}`, `~/.config/zsh` (mtime only,
   empty source), plus two harmless WARNING prints (no machine config in bundle). The
   script's rsync legs take **no automatic backups** (only `copy_file` legs do) — hence
   step 2 is mandatory, and no `copy_file` leg fires from this bundle.
4. **Post-verify:** three live hashes == pins; both palette hashes unchanged; re-run of the
   bundle in `--dry-run` shows no remaining `>f` rows.
5. **Rollback:** runbook/octopus → restore before-images, verify `2766115d…`/`639fd06…`;
   guide → **guarded** removal only: verify `~/.agents/skills/guide` contains exactly
   `SKILL.md` at `d31ec392…` → remove file, `rmdir` — abort if anything else is present.

## 2. Commands and outcomes

| command | outcome |
|---|---|
| `mktemp -d /tmp/runbook-upgrade-deploy.XXXXXX` | `/tmp/runbook-upgrade-deploy.5ks5zf` (recorded before use) |
| preflight state (≈22:50) | sources at pins; **live runbook `2766115d…` · live octopus `639fd06…` · guide ENOENT · live palette `ee16c2ca…` ≠ source `f8c597d8…`** — the last pre-deploy observation anyone holds |
| deploy.sh audit | all content legs bundle-guarded (`[ -d/-f $REPO/… ]`); rsync flags `-a` (+`-n -i` dry), **no `--delete`**; zsh leg rsync **unguarded** → empty `zsh/` needed in bundle; `copy_file` legs auto-backup, rsync legs do not |
| bundle build + hash verify | 4 files, hashes as field 1; deployer byte-identical |
| `bash <bundle>/deploy.sh --dry-run` (22:54:55 CEST) | exit 0; content rows map **1:1 to the three targets**: `>f.st runbook/SKILL.md` (update) · `>f+++ guide/SKILL.md` under `cd+++ guide/` (create) · `>f.st octopus/SKILL.md` (update); side effects only directory-mtime rows (named above) + 2 WARNING prints; gemini/majkee/single-file legs: zero rows |
| diagnostic `bash ~/ia-sync/deploy.sh --dry-run` (≈23:1x, post-drift) | exit 0; unrelated content rows: **none** remaining (only a `.d..t ./`) — because live now equals source (see field 4) |
| diagnostic `--dry-run --codex-only` (≈23:1x) | zero itemized rows — same reason |
| post-state repeat (≈23:1x) | sources still at pins; **live runbook/octopus/guide now ALL equal the pins; guide EXISTS; live palette now `f8c597d8…` == source** |
| forensics | live skill mtimes 06:23 (= source authoring mtimes, preserved by `rsync -a` — not deploy time); **`~/.config/zsh/config.zsh.bak-2026-09-05` created 23:09 CEST** — an artifact only a real, non-dry `copy_file` leg produces |
| git | ia-sync HEAD `0f52f56` (therapy commit, published; `0 0` vs origin); reposoma untouched by this seat |

## 3. Evidence paths

- Scratch bundle + inventory hashes: field 1 (supplemental; disposable).
- Pre-deploy live hashes: field 2 preflight row — this RETURN is now the only record of the
  pre-deploy live state.
- The external deploy's artifact: `~/.config/zsh/config.zsh.bak-2026-09-05` (mtime 23:09).
- Old-byte recovery blobs: `git show 7c4a997:<path>` as in §1 step 2.

## 4. Mismatches discovered

1. **An external REAL deploy ran mid-cycle (~23:09 CEST), between my bundle dry-run
   (22:54:55) and my post-state check.** Evidence chain: pre-deploy hashes (22:50) → all
   three live targets now equal the pins and guide exists → live palette changed
   `ee16c2ca…` → `f8c597d8…` (the **palette hold is live-resolved by that deploy**, not by
   any gated decision this seat knows of) → `config.zsh.bak-2026-09-05` born 23:09, which
   only a non-dry full-repo `deploy.sh` produces. **Not this seat's action:** every deploy.sh
   invocation I ran carried `--dry-run` (exact commands above; the dry flag wires `-n -i`
   into every rsync and guards every `copy_file`). Reported, not undone, per gate. The
   plausible reading — the operator ran a full deploy by hand during the session
   interruption — is inference; the head verifies with @majkee.
2. **New concurrent dirt since POINT 20 was cut:** `zsh/nablarva/runbook.py` + `runbook.zsh`
   modified, `pulse.md` modified (the terminal-runbook-app lane, by the look of it). Untouched
   by me; snapshot in field 2's git row.
3. The RETURN's ordered deliverable (a *future* deployment plan) is now partially moot on
   office — live already matches the pins — but stands as: (a) the reviewed procedure for the
   **home machine**, (b) the rollback contract for what is now live, and (c) the record that
   the bundle dry-run predicted exactly the three content rows the external deploy then made
   real (plus its extra palette/config legs, which the bundle would have excluded).

## 5. Recommended next task

One reconciliation step before anything else: **the head confirms with @majkee whether the
23:09 full deploy was his hand.** If owned: record it in STATUS as the deploy event (its
scope was full-repo, larger than the gated three-file cut — palette and config.zsh included),
mark the palette hold resolved-by-operator, and proceed straight to the P0–P6 fresh-session
proofs (RETURN 15) — deployment verification is already satisfiable (live == pins, verified
here). If NOT owned: stop the arc and investigate the deploy path before any proof runs.
Home-side deployment, when its turn comes, has the reviewed bundle procedure in §1.

## 6. Remaining uncertainty

- **Who ran the 23:09 deploy and under what authority** — the one question that outranks
  the rest of this RETURN; everything in field 5 branches on it.
- The external deploy's full scope beyond the files I checked (gemini/majkee/zsh legs) is
  unaudited; `config.zsh` was overwritten with a backup taken — content delta unreviewed.
- P0–P6 remain unrun; live-equals-pins proves deployment, never discovery, selection, or
  behavior.
- Scratch (`/tmp/…5ks5zf`) is volatile; the procedure is reproducible from this RETURN, not
  dependent on the scratch surviving.
- My command history is self-reported; every end-state claim is re-derivable from disk and
  the named artifacts.
