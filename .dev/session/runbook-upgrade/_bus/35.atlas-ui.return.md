---
cycle: 35
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/35.cartan-csharp.point.md
seat_validation: >-
  POINT to atlas-ui and the return_to seat encoding match my declared participant_3 seat;
  one sequential proposal-only cycle, not a fan-out turn. The named RETURN is the sole write;
  no source/live/guide/agent/STATUS edit, no git mutation beyond read-only apply --check.
authored: 2026-09-10 · office · resident
frontmatter_check: >-
  python3 yaml.safe_load (6.0.3) on this file's own frontmatter returns a mapping, no error.
---

# RETURN 35 — card loaded, fan-out law still unread: no card lever for the read, one for the parser claim

Proposal only. The A/B cut is live (`5a88497b` source==live) and its mechanical surfaces now
hold; the flight-probe skipped the required fan-out read anyway. That is the finding.

## 1. Files changed

**Executed: none.** This RETURN is the only write. No source/live/guide/agent/STATUS/receipt
edit; the one embedded diff below was checked read-only with `git apply --check` and **not
applied** (card confirmed untouched, §2).

### Evidence cross-check, labeled by class

| finding | class |
|---|---|
| Claude runbook card `5a88497b` source==live; injected body matches after frontmatter removal | my rerun (§2) |
| transcript lines 1–53 hash `c2aa97bd…` | my rerun (§2) |
| final-53 6391 bytes `3b4a382b…` | head-observed (not re-extracted; boundary only re-hashed) |
| one `Skill(runbook)` call w/o args; no GUIDE/chapter read; no parser attempt | head-observed |
| both final YAML blocks parse; owner/gate strings match | head-observed (head PyYAML 6.0.3) |
| invented delegated keys + RUNBOOK turn map; no top-level `join_when`; no numbered POINT/RETURN/VERDICT; raw briefs substitute for BUS | head-observed |
| recovery combines absent/rejected + reopens reviewer without liveness reconciliation | head-observed |
| `Bash` and `Read` advertised at line 42 | public tool declaration — **not** execution evidence |
| "no parser available" claim | untested hypothesis (Bash was advertised; no attempt shown) |
| what *caused* the skip (footer / invocation form / wording) | untested — I make no causal claim |

Inaccessible model reasoning is an explicit limit, not license to infer.

### Leverage points (item 2)

- **Required reads skipped.** The card already points at the law three ways — header ("route by
  the Manifest, never by a list baked here"), §1 ("Read, don't infer"), and the §2 fan-out
  bullet ("takes `turn:` coordinates and STATUS `delegated:`/`join_when:` rows from
  `res/fanout-turns.md`"). None is an *imperative pre-authoring read-gate*; all are declarative
  pointers. This is a **missed instruction** (a soft pointer not acted on), not a **conflicting**
  one — nothing in the card tells the model *not* to read. A secondary, untested hypothesis: the
  header "Law I execute, never restate" may read as *the injected body is self-sufficient*. Tool
  advertisement (`Read` at 42) is not a read.
- **Parser unavailability asserted without a check.** Traceable to text **I authored in cycle
  32**: *"If no parser is at hand, say so — never claim a parse PASS."* That gave a claimable
  escape; the model asserted unavailability rather than attempting `import yaml` though `Bash`
  was advertised. This is a concrete card-text loophole, distinct from the read-skip.

### One intervention (item 3): one card delta + one justified no-change

**Justified NO-CHANGE for the read-skip (the headline).** A/B just proved card prose moves the
*mechanical* surfaces (YAML validity, owner binding) but not the *attention* surface: the model
skipped pointers already present. Another read-imperative is the same class as what was ignored
— low confidence, and duplicated emphasis edges toward the launcher-bloat the final audit
already owns. The correct scope is **elsewhere, named for approval, not drafted here**:
(a) the uncoached fresh producer proof must require *observable* chapter-read evidence to PASS
(a procedure gate, not card prose); (b) if a read-gate is wanted in canon, it belongs in
`raw.guides/runbook/res/csharp-head-protocol.md` as a "read before authoring fan-out" clause —
a guide/canon edit needing @majkee's ruling and a witness, **not** more skill-card law. Codex
octopus is unchanged and not presumed.

**One justified card delta — close the parser loophole I opened** (existing line, no new law,
mechanism = remove the cheap "no parser" claim by requiring an attempt first). Checked with
`git apply --check --whitespace=error` (exit 0, §2), not applied:

```diff
diff --git a/claude/skills/runbook/SKILL.md b/claude/skills/runbook/SKILL.md
--- a/claude/skills/runbook/SKILL.md
+++ b/claude/skills/runbook/SKILL.md
@@ -53,3 +53,5 @@
   never fold a typed list or map into a scalar. Shape authority stays in
-  `~/reposoma/raw.guides/status/GUIDE.md`. If no parser is at hand, say so — never claim a parse PASS.
+  `~/reposoma/raw.guides/status/GUIDE.md`. Attempt the parser first (Bash/Read are yours, e.g.
+  `python3 -c 'import yaml'`); report unavailability only after that attempt fails — never
+  assert it unchecked, and never claim a parse PASS.
 - **Router** — one line in the project's `pulse.md` (slug · gate · STATUS path). No `pulse.md` → the
```

Why not more: the semantics of validation already live in the card and `status/GUIDE.md`; this
only converts a claimable escape into a required attempt. It does not, and cannot, fix the
read-skip — that is why the read-skip gets no-change, not a second hunk.

### Discriminating tests (item 4) — specified, not run

- **Read-skip, uncoached producer proof:** `/runbook` + the fan-out toy intention, **no chapter
  or field hints**. Evidence = a tool call opening `~/reposoma/raw.guides/runbook/res/fanout-turns.md`
  *plus* correct applied shape (real `delegated:` rows, one `join_when:`, numbered POINT/RETURN/
  VERDICT, content-sensitive recovery). Skip again ⇒ the read-skip is a robust model-attention
  behavior, **not card-fixable** → final purpose-fit audit (mechanism/procedure lever).
- **Missed vs conflicting:** a **separate coached** diagnostic that explicitly says "read
  `res/fanout-turns.md` before authoring." Reads+correct ⇒ it was a missed soft pointer (a
  louder imperative *might* help); still skips ⇒ attention/routing (no card lever). The coached
  run diagnoses only; it never certifies and never feeds hints into the uncoached proof.
- **Parser clause:** with the delta, observe whether the session *attempts* `import yaml` before
  reporting. Attempt-then-truthful ⇒ the clause was the lever; still asserts unchecked ⇒
  attention, not text.
- No pass-rate quota, no automatic rerun loop. RETURN 30's six present correctness gates
  (applicable-read · YAML validity · owner binding · content-sensitive recovery · BUS grammar ·
  truthful park) stay **present** gates, not waived to the audit.

## 2. Commands and outcomes

| command | result |
|---|---|
| `sha256sum /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md /home/hruzam/.claude/skills/runbook/SKILL.md` | both `5a88497b37de1780b5acb2de99be164eee498c8faff1a6fc97add1b8fb5b89f9` — source==live (A/B deploy landed) |
| `sha256sum /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md` | `9dff9c9eea11eded31e21587c33a4fad284defaab28c44753d628be8ccb147d8` — comparison pin, unchanged |
| `head -n 53 <transcript>.jsonl \| sha256sum` | `c2aa97bda9d677b67cc6f4fbea0add17a4fe3215fbb90b5f15922443b9fe21a8` — reproduces POINT 35 pin |
| `git -C /home/hruzam/ia-sync apply --check --whitespace=error -` (parser diff on stdin) | exit 0 — applies cleanly; **not applied** |
| `git -C /home/hruzam/ia-sync status --porcelain claude/skills/runbook/SKILL.md` | empty — card untouched |
| `python3 -c "…yaml.safe_load(this RETURN frontmatter)…"` | mapping, no error (see §5 note) |

Transcript: `/home/hruzam/.claude/projects/-home-hruzam-ia-sync/39d8ed7b-7594-48af-b52f-09dc911353f6.jsonl`
(67 lines; scope 1–53 only; contents not copied).

## 3. Evidence paths

- Probe transcript (bounded prefix): path above.
- Card under diagnosis: `/home/hruzam/ia-sync/claude/skills/runbook/SKILL.md` (`5a88497b…`).
- Prior context (attributed, not reopened): RETURN 15/30, VERDICT 15, VERDICT 33; STATUS
  pointer for P5's later Astra PASS (uncontrolled vs its Sol failure).
- Candidate canon home for a read-gate (named, not edited): `raw.guides/runbook/res/csharp-head-protocol.md`.

## 4. Mismatches discovered

- **Source==live now** (`5a88497b`), where RETURN 32/33 left source≠live — the A/B deploy
  landed between cycles (operator gate). Reported, not reconciled; expected, not drift.
- No other drift; octopus comparison pin unchanged; card untouched by this cycle.
- The launcher's pre-existing unquoted `@majkee` YAML defect and STATUS bloat are **not** this
  scope and are **not** shown to be caused by flight-probe — final-audit items, per POINT 35.

## 5. Recommended next task

Cartan verifies. Disposition: (1) hold the read-skip as a **model-attention** finding for the
final fresh purpose-fit audit, with the uncoached producer proof (observable chapter-read
required to PASS) as its discriminator — no further skill-card read-imperative; (2) if @majkee
wants a canon read-gate, scope it to `csharp-head-protocol.md` with a witness, not the card;
(3) the one parser-clause delta is available as a checked diff for a later authoring cycle
under the existing source-scope/approval boundary — not authored here. Frontmatter parse of
this RETURN: `python3 -c "import yaml; yaml.safe_load(open('35.atlas-ui.return.md').read().split('---\n',2)[1])"` → mapping, no error.

## 6. Remaining uncertainty

- One sample; no causal claim on why the read was skipped, and the parser delta's effect is
  unproven — the discriminating tests decide, without a quota or rerun loop.
- Whether the read-skip is missed-instruction vs model-attention is undetermined until the
  coached-vs-uncoached tests run; my lean is attention (A/B evidence), stated as a lean.
- Inaccessible reasoning and the un-re-extracted final-53 bytes are explicit limits, not
  inferred results.
- Carriage self-reported `claude-opus-4-8` (declared tuple); no session run, no seat woken.
