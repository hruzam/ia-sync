---
cycle: 14
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/14.cartan-csharp.point.md
seat_validation: POINT `to: atlas-ui` and `return_to:` seat encoding match my declared RUNBOOK
  participant_3 seat. POINTs 12/13 acknowledged as withdrawn; no work was begun under them and
  neither POINT was ever relayed to this seat — nothing to disclose from the old dispatch.
authored: 2026-09-05 · office · resident
---

# RETURN 14 — combined Claude/Codex reconciliation proposal (one builder)

## 1. Files changed

**Executed: none of the inspected sources.** The only file written this cycle is this RETURN.
Reposoma, ia-sync sources, deployed trees, and git state are untouched.

The proposal below is **patch-ready text, NOT executed** — five items (A–E), each awaiting
@majkee's source-scope confirmation per RUNBOOK prompt-5 and the codex-harness creator loop
step 4.

### Claude runtime — proposed

**A. `/home/hruzam/ia-sync/claude/skills/runbook/SKILL.md` — replace the stale law line.**
The card enumerates two `res/` chapters (`token-economy · cross-vendor-seat`); the gaveled
guide carries five. A baked list went stale once and will again; route by the Manifest.

Replace:

```
**Law I execute, never restate:** `~/reposoma/raw.guides/runbook/GUIDE.md` (+ its `res/` chapters:
token-economy · cross-vendor-seat) · `status/GUIDE.md` · `PAD/GUIDE.md` · `bus/GUIDE.md`. If this
```

with:

```
**Law I execute, never restate:** `~/reposoma/raw.guides/runbook/GUIDE.md` — its `## Manifest`
names the live `res/` chapters; route by the Manifest, never by a list baked here — ·
`status/GUIDE.md` · `PAD/GUIDE.md` · `bus/GUIDE.md`. If this
```

**B. Same file, §2 — one new bullet** inserted between the `RUNBOOK.md` and `STATUS.md`
bullets (routes `head_note:`, the protocol, and fan-out; states the park/stay boundary):

```
- **Multi-session arc?** The head seat carries `head_note:` naming its protocol — today only
  cSharp is gaveled (authors and STAYS; `res/csharp-head-protocol.md`). A head coordinating ≥2
  independent full CLI sessions from one decision takes `turn:` coordinates and the STATUS
  `delegated:`/`join_when:` rows from `res/fanout-turns.md`; in-window subagent delegation
  stays under `res/token-economy.md`. My park envelope is this skill's exit, not the cSharp's.
```

**No-change finding — `/home/hruzam/ia-sync/claude/skills/guide/SKILL.md`.** Justification:
chapter selection already resolves `/guide <slug> <chapter>` → `raw.guides/<slug>/res/
<chapter>*.md` by filename substring, which reaches `fanout-turns.md` with no edit; the
Manifest/orphan organ already consumes the `## Manifest` the committed GUIDE now carries; the
staleness organ passes (runbook GUIDE has `verified:` without `half_life_days:` → silent).
Nothing the new chapter requires is missing. (Two pre-existing drifts observed → field 4.)

### Codex runtime — proposed

**Existence checks (commands in field 2).** `/home/hruzam/ia-sync/.codex/` does not exist —
recorded as a finding, nothing created. `/home/hruzam/ia-sync/codex/skills/` holds seven
skills (buffering, codex-harness, cold-start-card, medusa, octopus, polyp,
reposoma-surgical-coding); **none named or purposed as a guide reader** — by name (`guide`
absent from source and both deployed roots) and by purpose (codex-harness reads its own
`references/` for harness-building; openai-docs serves OpenAI official docs only; no skill
serves `~/reposoma/raw.guides/`). Reuse/extension rejected on purpose mismatch → the smallest
sufficient primitive is one new portable read-only **skill** (codex-harness selection table:
"reusable procedure or knowledge bundle").

**C. `/home/hruzam/ia-sync/codex/skills/octopus/SKILL.md` — replace the stale guide list**
(same staleness class as A). Replace:

```
- `~/reposoma/raw.guides/runbook/GUIDE.md`
- `~/reposoma/raw.guides/status/GUIDE.md`
- `~/reposoma/raw.guides/runbook/res/token-economy.md` and
  `~/reposoma/raw.guides/runbook/res/cross-vendor-seat.md` when delegation or a cross-vendor
  participant makes them applicable.
```

with:

```
- `~/reposoma/raw.guides/runbook/GUIDE.md`, then the `res/` chapters its `## Manifest` names,
  as the session's shape makes them applicable: delegation → `token-economy`; a cross-vendor
  participant → `cross-vendor-seat`; a multi-session head arc → `csharp-head-protocol`;
  coordinating independent full CLI sessions from one decision → `fanout-turns`.
- `~/reposoma/raw.guides/status/GUIDE.md`
```

**D. Same file — one boundary sentence** appended after "After this envelope, stop. A useful
plan is not permission to execute it.":

```
Parking is this skill's exit, not the cSharp head posture's: an arc head declared by RUNBOOK
`head_note:` stays live under `~/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`.
```

**E. New file `/home/hruzam/ia-sync/codex/skills/guide/SKILL.md`** — Codex-native rendering,
not a copy: the Claude card embeds slash-command grammar, tool names (Glob/Grep), B′ history,
and the 3-line writer law; this rendering is reader-only, expresses resolution as outcomes,
and points at guide-writing for writer law. Full proposed content:

```
---
name: guide
description: Read the temple's shared guides from ~/reposoma/raw.guides/ — list slugs, print one GUIDE.md, or print one res/ chapter, always verbatim. Use when a task cites a guide slug or chapter (runbook, status, bus, guide-writing, ...) or shared temple law must be consulted. Read-only; not for OpenAI product documentation (openai-docs) and not for writing guides.
---

# Guide — temple guide reader

The guides' single home is `~/reposoma/raw.guides/`: one topic = one slug folder = one
`GUIDE.md`, subchapters beside it in `res/`. Serve requested guide text verbatim — the caller
wanted the law, not a digest. This skill is read-only; it never edits, creates, or
reorganizes guides. Writer law lives in `~/reposoma/raw.guides/guide-writing/GUIDE.md`.

## Resolution

- `$guide` — list: for every `~/reposoma/raw.guides/*/GUIDE.md` report the slug, its
  frontmatter `title:` / `machine:` / `verified:` values, and the `res/` chapter basenames.
  Build the listing from those lines alone; do not read whole guide bodies.
- `$guide <slug>` — print `~/reposoma/raw.guides/<slug>/GUIDE.md` verbatim.
- `$guide <slug> <chapter>` — print the matching
  `~/reposoma/raw.guides/<slug>/res/<chapter>*.md` (substring on the chapter filename).
  Multiple matches: list them and ask. None: show that GUIDE.md `## Manifest` instead.

## Contract (gaveled: guide-writing `res/slug-directories.md`)

- Resolve only `GUIDE.md` and `res/` chapters. `raw/`, `src/`, `ast/`, `cod/` are reached by
  explicit path from a manifest line, never by this resolver.
- Frontmatter `verified:` + `half_life_days:` past expiry → prefix one stale-knowledge
  warning naming the verified date. A `PUBLISHED MIRROR` banner → state that the owning
  project is the source of truth.
- A sibling file missing from the owning `## Manifest` → append one orphan-drift warning.
```

Optional at authoring: `agents/openai.yaml` (display_name "Guide reader") via skill-creator's
generator — cosmetic, not load-bearing; omit unless @majkee wants UI metadata.

**No-change findings, Codex:** `codex-harness` (builder method, not a routing surface for
this chapter) · `codex/AGENTS.md` (skills route by description; always-on routing for an
on-demand reader violates smallest-primitive) · `deploy.sh` (new skill directory rsyncs
automatically — line 119 syncs the whole `codex/skills/` tree).

### Intended deploy targets (deploy is a later gate, not requested here)

| source (table) | deployed by `~/ia-sync/deploy.sh` to |
|---|---|
| `claude/skills/runbook/SKILL.md` | `/home/hruzam/.claude/skills/runbook/SKILL.md` |
| `codex/skills/octopus/SKILL.md` | `/home/hruzam/.agents/skills/octopus/SKILL.md` |
| `codex/skills/guide/SKILL.md` (new) | `/home/hruzam/.agents/skills/guide/SKILL.md` (new) |

### Fresh-session proof prompts (for @majkee's hands; observable pass/fail)

1. Fresh Claude: `/guide runbook fanout-turns` — PASS: verbatim chapter beginning
   `# cSharp fan-out turns` through the closing "retires this file-plane delta" paragraph.
   FAIL: slug listing, summary/digest, or not-found.
2. Fresh Claude: `/runbook` + a one-sentence toy intention, then ask *"which res chapter
   governs coordinating two independent full CLI sessions from one decision?"* — PASS: skill
   activates (refuses unearned ceremony or demands the `gate:` line) AND answers
   `res/fanout-turns.md`. FAIL: no activation, or routes to token-economy/nothing.
3. Fresh Codex: `$guide runbook fanout-turns` — PASS: verbatim chapter as in (1). FAIL: skill
   not discovered or digest. On discovery failure, run the separation probe: bare `$guide`,
   then skill-creator's eval spelling ("Use $guide at /home/hruzam/.agents/skills/guide …")
   to distinguish discovery-root failure from argument handling.
4. Fresh Codex: `$octopus` + toy intention, then ask *"which chapter names the head posture
   that stays?"* — PASS: park envelope or refusal-of-ceremony AND `csharp-head-protocol`.
   FAIL: no activation, or cites the retired `csharp-head.md`.

## 2. Commands and outcomes

| command | outcome |
|---|---|
| `git -C /home/hruzam/reposoma rev-parse HEAD` | `3feba72791c1550ba993ff7d4e0d57f1bb2b3e7d` — exactly the gaveled commit; **no later drift** |
| `git -C /home/hruzam/reposoma status --short` | empty — clean worktree |
| `find ~/ia-sync/codex -type f` · `ls ~/.agents/skills/` · `ls -a ~/.codex/skills/` | 7 portable skills; no `guide` by name anywhere; `~/.codex/skills/` holds only `.system/` |
| `find ~/ia-sync/.codex -maxdepth 3` | **ENOENT** — directory absent (finding, nothing created) |
| `diff -q` source↔deployed: octopus, guide (claude), runbook (claude) | all byte-identical — deploy current on office |
| `echo $CODEX_HOME` | unset |
| `sha256sum` (10 sources) | frozen in field 3 |
| `grep -rn "skill" ~/.codex/config.toml` | no skills-root override configured |

## 3. Evidence paths

Current source hashes (SHA-256), frozen at authoring:

```
87d5a796554c33631d463619f721c00cf398d2344c49a885e427f241f7a6c1bb  /home/hruzam/ia-sync/claude/skills/guide/SKILL.md
2766115d37e05cf0d30658ba989d384644f782f0cc9de3c5729c283d490971f7  /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md
639fd0643a607161ccfd30a7fff05069ab4e8f868485954e418db8d48b69d2ec  /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md
9ffc84515873e5be202ad6f5ab3a06c50a0c265759a809dbddefe0fa0eb66e12  /home/hruzam/ia-sync/codex/skills/codex-harness/SKILL.md
1b45b8d09893f3a43f8d8c2c0a281c73b6fc94dccabc4d760ee6727dc379fac4  /home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
4fb38edd3ddda0c42f0584232bc78264db198bc7c446917584c9f2d71ef45485  /home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
a0d6d48b7eb162fb327e3d83b5ced7a874f6d76c7c39d3800eb3f16e73bb5477  /home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md
4576327028c709bd2ddc5209b59d5f3e2ac1e66c352bf330a298489ae1230754  /home/hruzam/reposoma/raw.guides/bus/GUIDE.md
74512734c5ee6a2b0c90d7f1a560d4d2ec3b2fc8d6d0ca23ae80ccc23fe4d445  /home/hruzam/reposoma/raw.guides/status/GUIDE.md
6583aa66a7a5090a28a773663be6486a221e71208c26bf4b3cd92329c9cee932  /home/hruzam/reposoma/raw.guides/guide-writing/res/slug-directories.md
```

Doctrine consulted: `/home/hruzam/.codex/skills/.system/skill-creator/SKILL.md` (invocation
`$skill-name`, implicit selection default, naming, `$CODEX_HOME/skills` claim) ·
`/home/hruzam/.codex/skills/.system/openai-docs/SKILL.md` (docs-first order — see field 6) ·
`/home/hruzam/ia-sync/deploy.sh` lines 117–119 (codex/skills → `~/.agents/skills/`) ·
verdicts `_bus/11–13.cartan-csharp.verdict.md` · the four gaveled guide files above.

## 4. Mismatches discovered

1. **Deploy root vs documented discovery root (load-bearing for proofs 3–4).** Local
   `deploy.sh` rsyncs `codex/skills/` → `/home/hruzam/.agents/skills/`; vendor-shipped
   skill-creator documents discoverable skills at `$CODEX_HOME/skills` → `~/.codex/skills`
   (unset here). Local evidence that `~/.agents/skills/` IS discovered: all seven working
   skills exist only there, and this arc's own artifacts were authored under octopus posture.
   That is inference from use, not proof — file existence is not discovery. Proof prompt 3's
   separation probe decides it.
2. **POINT read path `/home/hruzam/ia-sync/.codex/` does not exist.** Absence recorded;
   nothing created.
3. **My carriage differs from the declared tuple.** participant_3 declares
   `{claude, claude-opus-4-8, high}`; this incarnation runs `claude-fable-5`. Seat identity
   (`atlas-ui`) matches, so per fanout-turns receiver rule I proceeded; the tuple's carriage
   claim is stale for this cycle and belongs to the operator's choice, not mine to repair.
4. **Pre-existing, NOT required by the chapter, not patched here:** the Claude guide skill's
   legacy fallback greps `raw.guides/**/*.md`, which can serve `raw/`–`src/` substrate on a
   bogus-slug substring hit — this predates and now contradicts the gaveled resolver contract
   (slug-directories: `raw/` never served). Also its "four classes" sibling wording predates
   the five-slot taxonomy (cosmetic). Reported as drift; fix belongs to a separately scoped
   cycle, not smuggled into this one.
5. **`schema_note` in this session's RUNBOOK** cites `res/csharp-head.md` — historical dead
   citation, already held in STATUS; live routing in the proposal uses
   `res/csharp-head-protocol.md` throughout.

## 5. Recommended next task

Relay this proposal to @majkee for exact source-scope confirmation of the five paths (A–E:
two Claude edits in one file · two Codex edits in one file · one new Codex file). On his
confirmation, open one new cycle authorizing atlas-ui to author A–E on the ia-sync table —
author, validate (skill-creator `quick_validate` for E, `deploy.sh --dry-run`), and stop
before deploy; deploy + the four fresh-session proofs remain @majkee's hands per prompt-5.

## 6. Remaining uncertainty

- **All invocation spellings are candidates, unrun.** `/guide runbook fanout-turns`,
  `/runbook`, `$guide …`, `$octopus` — no fresh session was opened this cycle (not
  authorized). Claude spellings rest on deployed cards in daily use; Codex spellings rest on
  vendor-shipped local documentation (`$skill-name`, implicit-selection default).
- **Codex discovery root** (field 4.1) is unproven live; the proposal survives either answer
  (source home and deploy.sh are unchanged; only the deployed path in the proof prompt would
  shift to `~/.codex/skills/` if docs win).
- **openai-docs' docs-first order was not fully executable in this seat** — no network tools
  on this carriage; current-product claims therefore carry local-documentation confidence,
  dated by the deployed Codex build, not fetched-page confidence. Labeled accordingly above.
- Whether the octopus/runbook Manifest-routing lines cause a fresh head to actually READ
  `fanout-turns.md` when coordinating (proof prompts 2 and 4 test the observable half; habit
  under load is only provable in a real fan-out arc).
- No claim is made about the home machine; all checks are office-local. Cross-host parity
  arrives only via commit + pull + deploy, all outside this cycle.
