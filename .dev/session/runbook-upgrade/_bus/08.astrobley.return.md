---
cycle: 08
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/08.cartan-csharp.point.md
written: 2026-09-04
host: office
---

# RETURN 08 — eight-file blueprint rebased on the gaveled cSharp protocol

## 1. Files changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/08.astrobley.return.md` — added this
  read-only planning correction. It rebases RETURN 05's accepted fold on reposoma `a47f4ba`
  and gaveled predecessor commit `862d98e` without editing the documentation it specifies.
- No reposoma guide, RUNBOOK, STATUS, pulse, skill, runtime, deployment, mail, cross-host, or
  git-history surface changed.

Implementation position before the cut:

- The smallest coherent correction is one filename substitution plus the semantic metadata that
  the expanded chapter now requires. The protocol chapter's latest gaveled content is dated
  2026-09-04, and its role includes the named protocol and successors letter, not only the
  original posture and transfer ritual.
- The fold remains exactly eight files. The accepted fan-out doctrine and every cycle-07
  verification repair remain unchanged except where an exact path or chapter-state row must name
  the current protocol chapter.
- No architectural pushback is needed. This RETURN neither revisits the gaveled protocol framing
  nor edits or abridges its successors chapter.

## 2. Commands and outcomes

- `cat /home/hruzam/.agents/skills/reposoma-surgical-coding/SKILL.md` — exit 0; read the required
  surgical discipline in full before authoring.
- ia-sync orientation (`head -80 journal.host-cleanup.md`, `git log --oneline -5`, inbox listing,
  and `cat SYNC_DISCIPLINE.md`) — exit 0; the inbox was empty and the existing session changes
  were preserved.
- `cat /home/hruzam/reposoma/AGENTS.md` — exit 0; resolved the instruction chain governing the
  read-only reposoma evidence paths.
- Complete reads of all fifteen paths declared by POINT 08 — exit 0. The pruned raw substrate was
  not inspected, the unavailable candidate-canon sections were not searched for, and no
  `carrier:` choice was made.
- `git -C /home/hruzam/reposoma status --porcelain=v1 --untracked-files=all` — exit 0 with no
  output before the write; reposoma was clean.
- `git -C /home/hruzam/reposoma rev-parse --short HEAD` — exit 0 and printed `a47f4ba`.
- `git -C /home/hruzam/reposoma show --stat --oneline 862d98e` and a path-bounded show of the
  RUNBOOK GUIDE and protocol chapter — exit 0; observed the gaveled rename, protocol framing,
  successors-letter expansion, and 2026-09-04 gavel without inspecting pruned substrate.
- `git -C /home/hruzam/reposoma ls-tree -r --name-only a47f4ba -- raw.guides/runbook/res` — exit
  0; listed exactly the four current pre-existing chapters, including
  `raw.guides/runbook/res/csharp-head-protocol.md` and excluding its predecessor filename.
- `sha256sum` over the four current pre-existing chapters — exit 0; the protocol body hash before
  any future header is `48dd8342782b32dde49c602ecf57e37dd95a916302bc51d205192a949b5064d8`.
- `test ! -e /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/08.astrobley.return.md` — exit
  0 before this write; the sole target did not exist.

Post-write verification:

- The initial combined validation command was parsed by the default zsh and stopped before its
  assertions with `bad floating point constant` at `$n[.]`; it changed no file. The same checks
  were reissued explicitly under bash with the loop expression corrected to `${n}[.]`.
- The numbered-heading count returned `6`, with each mandatory RETURN heading present exactly
  once.
- The bounded exact-fold count returned `8`; its sorted path list matched the expected current
  paths literally.
- The rebased exact-state assertion enumerated exactly five chapter rows, retained the separate
  one-`raw/`-directory assertion, and bound the protocol row to `GAVELED 2026-09-04`.
- The header/body specification check passed: the protocol header has exactly four YAML lines,
  and the later-write hash command compares everything from line 5 onward with the whole current
  74-line body.
- Both accepted cycle-07 multiline fixed-string clauses remain present byte-for-byte in the
  rebased specification; their blueprint demonstrations exited 0.
- The rebased blueprint scan found no predecessor path, while every path-sensitive list named
  `res/csharp-head-protocol.md` exactly once where required.
- `rg -n '[[:blank:]]+$' <RETURN-08>` — exit 1 with no output, the expected clean result.
- `git diff --no-index --check /dev/null <RETURN-08>` — exit 1 with no diagnostic output, the
  expected new-file result with no whitespace errors.
- Final reposoma HEAD/status remained `a47f4ba` and clean. The pre-write absence check and this
  seat's sole `apply_patch` target establish RETURN 08 as its only write; the pre-existing ia-sync
  session changes remain present and untouched.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md` — accepted
  eight-file blueprint whose seven protocol-slug occurrences are replaced below.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.verdict.md` — cycle-05
  REVISE edge, limited to three verification defects.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/07.astrobley.return.md` and
  `07.cartan-csharp.verdict.md` — accepted fixed-string and exact-state-map repairs retained here.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` and
  `res/csharp-head-protocol.md` — current gaveled protocol pointer, role, dates, and complete
  74-line body at clean reposoma HEAD `a47f4ba`.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` and
  `guide-writing/res/slug-directories.md` — current light-header and Manifest law.
- `/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md`, `res/research.md`, and
  `res/token-economy.md`; `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md`; and
  `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — remaining current surfaces whose accepted
  treatment does not change.

### Patch-ready RETURN-05 replacements

Apply these seven replacements to the later documentation-write blueprint. They identify every
protocol-slug occurrence in RETURN 05; historical receipts themselves remain immutable.

1. Replace the field-2 Manifest outcome sentence at RETURN 05 lines 71–73 with:

> Bounded Manifest counts plus exact-state comparison — five per-file `res/` lines and one
> `raw/` directory line, with cross-vendor-seat/token-economy DRAFT,
> csharp-head-protocol GAVELED 2026-09-04, research GAVELED 2026-09-03, and fanout-turns GAVELED
> `<GAVEL-DATE>`.

2. Replace item 6 of **Exact eight-file fold** with:

> 6. `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md` — four-line header
> prefix only; preserve the complete current protocol body byte-for-byte beneath it.

3. Replace the protocol row in File 1's Manifest with:

```markdown
- `res/csharp-head-protocol.md` · chapter · GAVELED 2026-09-04 — cSharp protocol: head posture, transfer ritual, and letter to successors
```

The 2026-09-04 state is required because that gavel added the protocol framing and successors
letter. The row therefore does not describe the expanded chapter as only the 2026-09-03 posture.

4. Replace the protocol subsection under **Files 5–8 — header-only normalization** with:

~~~~markdown
`raw.guides/runbook/res/csharp-head-protocol.md`:

```yaml
---
title: The cSharp protocol — head posture, transfer ritual, and letter to successors
chapter-of: runbook
---
```

Prepend only those four lines. The existing first line
`# res/csharp-head-protocol — the cSharp protocol` and every byte through the current final line
remain unchanged below the header. In particular, do not rewrite the protocol framing or the
`## To the next cSharp — the short story` successors chapter.
~~~~

5. In the **before editing** `sha256sum` list, replace the protocol operand with:

```bash
  "$repo/raw.guides/runbook/res/csharp-head-protocol.md" \
```

6. In `expected_paths`, replace the protocol path row with:

```bash
  raw.guides/runbook/res/csharp-head-protocol.md \
```

7. If mechanically rebasing RETURN 05's now-superseded Manifest filename loop, replace its loop
header with:

```bash
for chapter in cross-vendor-seat csharp-head-protocol fanout-turns research token-economy; do
```

The later writer must use the stronger cycle-07 exact state comparison below rather than restore
that generic loop. This replacement accounts for the final stale bare-slug occurrence; it does
not add a second Manifest check.

No other RETURN-05 prose or fan-out mechanism changes as a consequence of the rename.

### Exact rebased eight-file fold

The later documentation writer changes exactly these current reposoma paths:

1. `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`
2. `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md`
3. `/home/hruzam/reposoma/raw.guides/status/GUIDE.md`
4. `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md`
5. `/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md`
6. `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md`
7. `/home/hruzam/reposoma/raw.guides/runbook/res/research.md`
8. `/home/hruzam/reposoma/raw.guides/runbook/res/token-economy.md`

All accepted RETURN-05 content for Files 1–5 and 7–8 remains patch-ready as written, subject to
the cycle-07 verification substitutions and the seven corrections above. `<GAVEL-DATE>` remains a
literal placeholder for the later documentation gavel and is not resolved by this RETURN.

### Rebased later-write verification

Record the four complete current bodies before adding headers:

```bash
repo=/home/hruzam/reposoma
body_hashes=/tmp/runbook-upgrade.chapter-bodies.before.sha256
sha256sum \
  "$repo/raw.guides/runbook/res/cross-vendor-seat.md" \
  "$repo/raw.guides/runbook/res/csharp-head-protocol.md" \
  "$repo/raw.guides/runbook/res/research.md" \
  "$repo/raw.guides/runbook/res/token-economy.md" > "$body_hashes"
```

Require exactly the eight current paths after the later write:

```bash
repo=/home/hruzam/reposoma
expected_paths=$(printf '%s\n' \
  raw.guides/bus/GUIDE.md \
  raw.guides/runbook/GUIDE.md \
  raw.guides/runbook/res/cross-vendor-seat.md \
  raw.guides/runbook/res/csharp-head-protocol.md \
  raw.guides/runbook/res/fanout-turns.md \
  raw.guides/runbook/res/research.md \
  raw.guides/runbook/res/token-economy.md \
  raw.guides/status/GUIDE.md | sort)
observed_paths=$(git -C "$repo" status --porcelain=v1 --untracked-files=all |
  sed -E 's/^.. //' | sort)
test "$observed_paths" = "$expected_paths"
```

Preserve every complete pre-existing body below its four-line header:

```bash
repo=/home/hruzam/reposoma
body_hashes=/tmp/runbook-upgrade.chapter-bodies.before.sha256
while read -r old_hash path; do
  new_hash=$(tail -n +5 "$path" | sha256sum | cut -d' ' -f1)
  test "$new_hash" = "$old_hash" || exit 1
done < "$body_hashes"
```

Use cycle 07's accepted exact state assertion with only the protocol row rebased:

```bash
point08_runbook=/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
point08_manifest=$(awk '/^## Manifest$/{p=1; next} p && /^## /{exit} p{print}' \
  "$point08_runbook")
point08_gavel_date=$(sed -nE \
  '1,7 s/^verified: ([0-9]{4}-[0-9]{2}-[0-9]{2})$/\1/p' "$point08_runbook")
test "$(printf '%s\n' "$point08_gavel_date" | wc -l)" -eq 1
printf '%s\n' "$point08_gavel_date" | rg -qx '[0-9]{4}-[0-9]{2}-[0-9]{2}'
point08_expected_chapters=$(printf '%s\n' \
  '- `res/cross-vendor-seat.md` · chapter · DRAFT — cross-vendor seat instrument, tunnel handle, and enable layers' \
  '- `res/csharp-head-protocol.md` · chapter · GAVELED 2026-09-04 — cSharp protocol: head posture, transfer ritual, and letter to successors' \
  "- \`res/fanout-turns.md\` · chapter · GAVELED $point08_gavel_date — full-session fan-out, coordination join, and cold recovery" \
  '- `res/research.md` · chapter · GAVELED 2026-09-03 — big-scope pre-RUNBOOK research gate' \
  '- `res/token-economy.md` · chapter · DRAFT — delegation shape and spend record')
point08_observed_chapters=$(printf '%s\n' "$point08_manifest" |
  rg '^- `res/[^`]+[.]md` · chapter · ')
test "$point08_observed_chapters" = "$point08_expected_chapters"
test "$(printf '%s\n' "$point08_manifest" | rg -c '^- `raw/` · ')" -eq 1
test "$(printf '%s\n' "$point08_manifest" |
  rg -c 'raw/field[.]runbook-check-rules')" -eq 0
```

Retain cycle 07's two multiline fixed-string assertions verbatim:

```bash
point08_bus=/home/hruzam/reposoma/raw.guides/bus/GUIDE.md
point08_late_return_clause=$(printf '%s\n' \
  '> `return: none + reason` is legal only when no RETURN exists and the disposition is STOP or' \
  '> BLOCKED. The VERDICT must verify the absence and state why the cycle cannot receive a RETURN;' \
  '> it never authors a surrogate RETURN. A RETURN that arrives later does not reopen the closed' \
  '> cycle or receive a second VERDICT: its information is assessed in a new numbered cycle.')
rg -UqF "$point08_late_return_clause" "$point08_bus"

point08_chapter=/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
point08_repository_policy_clause=$(printf '%s\n' \
  'Join is not a fourth BUS artifact, a specialized VERDICT, or a requirement that the replacement' \
  'share a repository commit with the receipts. Repository commit policy remains the owning' \
  "repository's separate law.")
rg -UqF "$point08_repository_policy_clause" "$point08_chapter"
```

Finally, prove the retired filename has not leaked into the rebased documentation blueprint while
the current protocol path appears in every path-sensitive list:

```bash
point08_return=/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/08.astrobley.return.md
point08_blueprint=$(awk '
  /^### Exact rebased eight-file fold$/{p=1}
  /^## 4[.] Mismatches discovered$/{p=0}
  p{print}
' "$point08_return")
point08_retired=$(printf '%s%s' 'res/csharp-' 'head.md')
! printf '%s\n' "$point08_blueprint" | rg -nF "$point08_retired"
test "$(printf '%s\n' "$point08_blueprint" |
  rg -c 'raw[.]guides/runbook/res/csharp-head-protocol[.]md')" -eq 3
```

The existing RETURN-05 frontmatter checks, three-kind grammar checks, owner-join assertion,
`carrier:` hold, forbidden invented-kind checks, unresolved-date rejection, and whitespace checks
remain in force exactly as repaired by cycle 07.

## 4. Mismatches discovered

1. RETURN 05's old protocol row is false in three dimensions after commit `862d98e`: filename,
   latest gavel date, and role. A path-only substitution would leave the Manifest claiming the
   expanded chapter contains only head posture and transfer ritual and was fully gaveled on
   2026-09-03.
2. The current RUNBOOK and its prompt/reference prose still contain predecessor-path references.
   POINT 08 explicitly forbids editing session authority, so this RETURN records but does not
   repair that separate session-level drift.
3. Cycle 07's accepted state-map commands contain the predecessor row twice: once in the actual
   later-write replacement and once in its immutable blueprint demonstration. The later writer
   must use the single rebased `point08_expected_chapters` block above; historical RETURN 07 stays
   unchanged.
4. Reposoma is clean at `a47f4ba`; the dirty `raw.guides/sella/**` and runbook/raw conditions
   reported during cycle 07 no longer exist. This removes the clean-baseline hold without changing
   the accepted verification doctrine.
5. The first validation wrapper ran under zsh although its loop expression was bash-oriented;
   zsh rejected `$n[.]` before any assertion. Reissuing the bundle under explicit bash passed.
   This is tooling curvature, not a content failure.

## 5. Recommended next task

Cartan-csharp should independently verify this RETURN against clean reposoma `a47f4ba`, confirm
the seven RETURN-05 substitutions and the rebased cycle-07 assertions, then write VERDICT 08. If
accepted, the next documentation-write POINT may authorize Astrobley to apply exactly the eight
current paths and no others; Oraculum should remain asleep until that actual diff exists.

## 6. Remaining uncertainty

- The documentation gavel date remains unresolved; `<GAVEL-DATE>` is intentionally untouched.
- `carrier:` and the absent candidate-canon sections remain unresolved and outside this fold.
- This read-only seat does not independently accept its own correction. VERDICT 08, the actual
  eight-file documentation write, independent Oraculum diff audit, operator gavel, commit, runtime
  reconciliation, deployment, and fresh-session proofs remain ahead.
