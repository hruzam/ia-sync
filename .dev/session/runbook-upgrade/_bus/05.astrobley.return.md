---
cycle: 05
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.point.md
written: 2026-09-04
host: office
---

# RETURN 05 — gaveled eight-file documentation-fold blueprint

## 1. Files changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md` — added this
  read-only planning RETURN. It instantiates G1A, G2A, G4A, G5B, G6-CONFIRM, and G7A against
  reposoma `b83026b` without editing the documentation it specifies.
- No reposoma guide, RUNBOOK, STATUS, pulse, skill, runtime, deployment, mail, cross-host, index,
  or git-history surface changed.

Implementation position before the cut:

- The smallest coherent fold is now exactly eight reposoma files: the previous three GUIDE maps
  and new chapter, plus header-only normalization of the four existing runbook chapters required
  by G4A. A four-file plan would publish a Manifest whose chapter claims contradict the files'
  missing `chapter-of:` markers.
- G3 is not a choice. Commit `b83026b` makes `raw/` a codified side-directory represented by one
  directory line, never one line per substrate file.
- The missing candidate-canon §§10–12 and the deferred `carrier:` question supply no authority.
  This blueprint neither reconstructs those sections nor introduces `carrier:` into canon.

Success criteria for the later write pass are concrete: only the eight paths named below differ;
all five `res/` chapters have one truthful GAVELED/DRAFT Manifest state; `raw/` occurs once in that
Manifest; the four old chapter bodies survive byte-for-byte after a four-line YAML prefix; BUS
still has only POINT, RETURN, and VERDICT; and a turn joins by verified STATUS replacement rather
than by a commit or new receipt kind.

## 2. Commands and outcomes

- `cat /home/hruzam/.agents/skills/reposoma-surgical-coding/SKILL.md` — exit 0; read the required
  surgical discipline in full before authoring.
- `cat /home/hruzam/ia-sync/AGENTS.md`, `head -80 journal.host-cleanup.md`,
  `git log --oneline -5`, `ls -la _mail/office/inbox/`, and `cat SYNC_DISCIPLINE.md` — exit 0;
  resolved the ia-sync instruction chain and orientation. The inbox was empty.
- `cat /home/hruzam/reposoma/AGENTS.md` — exit 0; resolved the additional instruction governing
  the reposoma evidence paths.
- `cat /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.point.md` — exit 0;
  resolved the nineteen declared read paths, sole write path, gavels, and forbidden surfaces.
- `wc -l` over all nineteen POINT-declared read paths — exit 0; every path resolved, 2,573 lines
  total.
- `cat` or complete bounded `sed -n` reads over all nineteen POINT-declared paths — exit 0; every
  file was read completely. The candidate-canon §§10–12 were not searched for or reconstructed.
- `hostname -s` — exit 0; observed `hruzam-120922` (office).
- `git status --short --untracked-files=all` in `/home/hruzam/ia-sync` before this write — exit 0;
  recorded the pre-existing modified `RUNBOOK.md`, untracked `STATUS.md`, BUS artifacts 01–04,
  POINTs 05–06, and no RETURN 05.
- `git -C /home/hruzam/reposoma status --short --untracked-files=all` — exit 0 with no output;
  reposoma was clean before this write.
- `git -C /home/hruzam/reposoma rev-parse --short HEAD` and
  `git -C /home/hruzam/reposoma show -s --format='%H %s' b83026b` — exit 0; HEAD was `b83026b`,
  the commit canonizing the slug side-directory taxonomy.
- `test ! -e /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md` — exit
  0 before this write; the sole target did not exist.

Post-write verification:

- `awk '/^## [1-6][.] /{n++} END{print n; exit(n==6?0:1)}' <RETURN>` — exit 0 and printed `6`;
  the six mandatory numbered headings occur exactly once.
- A bounded `awk` count over **Exact eight-file fold** — exit 0 and printed `8`; the proposed
  reposoma scope contains exactly eight absolute paths.
- Bounded Manifest counts plus exact-state `rg` checks — exit 0; five per-file `res/` lines and
  one `raw/` directory line were present, with cross-vendor-seat/token-economy DRAFT,
  csharp-head/research GAVELED 2026-09-03, and fanout-turns GAVELED `<GAVEL-DATE>`.
- Content gates for the three-kind grammar, STOP/BLOCKED-only absent-RETURN exception, verified
  STATUS-replacement join, and deferred `carrier:` hold — exit 0.
- `rg -n '[[:blank:]]+$' <RETURN>` — exit 1 with no output, the expected clean result.
- `git diff --no-index --check /dev/null <RETURN>` — exit 1 with no diagnostic output, the
  expected new-file delta with no whitespace errors.
- Final `git status --short --untracked-files=all` in ia-sync — exit 0; RETURN 05 is the only
  post-baseline path attributable to this seat. Concurrent RETURN 06 is present and was not read
  or touched.
- Final `git -C /home/hruzam/reposoma status --short --untracked-files=all` — exit 0 with no
  output; reposoma remains clean.

The commands under **Later write-pass verification** in field 3 are specifications, not commands
run by this read-only seat.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md` and `STATUS.md` — fixed fold,
  participant ownership, turn-01 state, gavel record, and the hold against documentation edits
  before this join.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.astrobley.return.md` — converged
  chapter mechanism, GUIDE seams, and the now-superseded four-file/raw-choice portions.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/03.cartan-csharp.verdict.md` — REVISE
  because `b83026b` dissolved G3 and G4 could expand the file set.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/04.oraculum-audit.return.md` and
  `04.cartan-csharp.verdict.md` — crossed confirmation of G2, G4, G5 and the final ruling sheet.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/raw/T2.assymetry-primary-draft.2026-09-04.md`,
  `T2.assessment.bus-turn-fanout.2026-09-04.md`, and
  `T2.countersign.fanout-turns.2026-09-04.md` — proposal, adversarial assessment, and amendment
  source; all remain experimental rather than canon.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` and
  `res/slug-directories.md` — current Manifest, frontmatter, chapter-marker, raw-directory, and
  resolver law at `b83026b`.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`, its four existing `res/` chapters, and its
  `raw/field.runbook-check-rules.2026-09-03.md` substrate — the exact current runbook topology and
  authority markers the fold must preserve.
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` and `status/GUIDE.md` — the owning artifact
  schemas and the boundaries that receive pointers rather than duplicated fan-out doctrine.

### Exact eight-file fold

The later documentation writer changes exactly these reposoma files and no others:

1. `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — frontmatter, complete Manifest, and two
   general authority sentences plus the chapter pointer.
2. `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — frontmatter and the minimum POINT/VERDICT
   schema and closure seams.
3. `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — frontmatter and optional-field/recovery
   pointers.
4. `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` — new, sole substantive home for
   full-session fan-out operations.
5. `/home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md` — four-line header prefix
   only.
6. `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head.md` — four-line header prefix only.
7. `/home/hruzam/reposoma/raw.guides/runbook/res/research.md` — four-line header prefix only.
8. `/home/hruzam/reposoma/raw.guides/runbook/res/token-economy.md` — four-line header prefix only.

No guide-writing file changes: `b83026b` is consumed as current canon, not amended again.

Use one literal substitution token while preparing the diff: `<GAVEL-DATE>`. Immediately before
the documentation gavel, replace every occurrence in these eight files with the actual
`YYYY-MM-DD` date of that gavel. Do not use this blueprint's date merely because it is available.
Metadata and substance land together.

### File 1 — `raw.guides/runbook/GUIDE.md`

Prepend exactly:

```yaml
---
title: RUNBOOK builder — the session launcher
scope: runbook
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---
```

Keep the existing inline `what:`, `scope:`, `companions:`, both `extends:`, `boundary:`, and
`revised:` authority/provenance lines in their present order and wording beneath the header.

Insert this Manifest after those inline lines and before `## What a RUNBOOK is`:

```markdown
## Manifest

- `res/cross-vendor-seat.md` · chapter · DRAFT — cross-vendor seat instrument, tunnel handle, and enable layers
- `res/csharp-head.md` · chapter · GAVELED 2026-09-03 — cSharp head posture and transfer ritual
- `res/fanout-turns.md` · chapter · GAVELED <GAVEL-DATE> — full-session fan-out, coordination join, and cold recovery
- `res/research.md` · chapter · GAVELED 2026-09-03 — big-scope pre-RUNBOOK research gate
- `res/token-economy.md` · chapter · DRAFT — delegation shape and spend record
- `raw/` · collected runbook substrate reached only by explicit path; not served by the resolver
```

This names each `res/` chapter exactly once with its truthful state and names `raw/` exactly once
as a directory. Do not list `raw/field.runbook-check-rules.2026-09-03.md` separately.

After the participant tuple example, add exactly these two general laws as one sentence each:

> Participant seat names are unique within one RUNBOOK; two incarnations of one agent are two
> seats, and BUS filenames use the declared seat name.

> Only the declared `status_owner` replaces STATUS; a verifier or witness attests the observed
> effect in VERDICT `status_rewritten:` and does not rewrite STATUS unless it is also the
> `status_owner`.

After the existing `head_note:` paragraph, add exactly:

> A cSharp coordinating independent full CLI sessions in one decision uses
> `res/fanout-turns.md`; ordinary in-window subagent delegation remains governed by
> `res/token-economy.md`.

Those are authority and routing seams, not a second operational account. All fan-out mechanics
below live only in the new chapter.

### File 2 — `raw.guides/bus/GUIDE.md`

Prepend exactly:

```yaml
---
title: _bus builder — the session's verified multi-seat exchange
scope: bus
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---
```

Do not change the disk grammar: `<kind>` remains exactly `point`, `return`, or `verdict`.

In the POINT schema, insert after `cycle:`:

```yaml
turn: <optional coordination-turn coordinate; see raw.guides/runbook/res/fanout-turns.md>
```

Do not add `turn:` to RETURN. In the VERDICT schema, insert the same optional `turn:` line after
`cycle:`. Replace only the `return:` and `status_rewritten:` schema lines with:

```yaml
return: <absolute or session-relative RETURN path | none + reason; none is STOP/BLOCKED only>
status_rewritten: <yes | no + reason | deferred: turn NN; see raw.guides/runbook/res/fanout-turns.md>
```

Immediately after the VERDICT schema, add these final clauses:

> `turn:` groups ordinary cycles opened by one head decision; it is not current state and does
> not create a fourth BUS kind. Full-session fan-out operations live in
> `raw.guides/runbook/res/fanout-turns.md`.

> `return: none + reason` is legal only when no RETURN exists and the disposition is STOP or
> BLOCKED. The VERDICT must verify the absence and state why the cycle cannot receive a RETURN;
> it never authors a surrogate RETURN. A RETURN that arrives later does not reopen the closed
> cycle or receive a second VERDICT: its information is assessed in a new numbered cycle.

Replace the first and last bullets in `A cycle is closed only when the VERDICT:` with these
clauses, leaving the middle four bullets unchanged:

```markdown
- checks the RETURN against cited artifacts, or for a STOP/BLOCKED `return: none + reason`
  verifies both the RETURN's absence and the stated reason,
...
- accounts for the STATUS edge in `status_rewritten:`; grouped-cycle deferral, owner discharge,
  and late-artifact rules live in `raw.guides/runbook/res/fanout-turns.md`.
```

The exception is deliberately narrow: ACCEPT and REVISE still require an actual RETURN path.

### File 3 — `raw.guides/status/GUIDE.md`

Prepend exactly:

```yaml
---
title: STATUS builder — the session's bounded recovery snapshot
scope: status
audience: agent + operator + builder
machine: both
verified: <GAVEL-DATE>
---
```

After the mandatory fixed-fields explanation, add exactly:

```markdown
### Optional full-session fan-out fields

`delegated:` — optional only while one full-session coordination turn is open; one row per
current branch. Each row has exactly `cycle`, `to`, `turn`, and `awaiting`; `awaiting` is
`return` or `verdict <seat>`. Semantics: `raw.guides/runbook/res/fanout-turns.md`.

`join_when:` — optional and exactly one while that turn is open; it names the observable cycle
obligations whose verified contents permit the owner to replace STATUS. Semantics:
`raw.guides/runbook/res/fanout-turns.md`.

`next:` remains singular during fan-out and names the next verification or the join, never the
workers' task list.
```

This fold does not add `carrier:` to the row schema. Its deferred disposition is not inferred
from a human-relay example and no alternative field is invented.

After the 03:00 file-presence warning, add exactly:

> When `delegated:` is present, `recovery_probe:` reconciles each row against the corresponding
> POINT, any RETURN, and the contents—not mere presence—of any VERDICT, following
> `raw.guides/runbook/res/fanout-turns.md`.

### File 4 — new `raw.guides/runbook/res/fanout-turns.md`

Create the following complete chapter; do not split any of it into BUS or STATUS:

```markdown
---
title: cSharp fan-out turns — full-session coordination without a fourth BUS kind
chapter-of: runbook
---

# cSharp fan-out turns

Use this chapter only when one persistent cSharp head coordinates at least two independent full
CLI sessions opened from one decision. In-window subagents are not full-session fan-out; their
delegation stays under `res/token-economy.md`.

## Coordinates and authority

`cycle: NN` orders one POINT → RETURN → VERDICT exchange. Optional `turn: NN` groups the cycles
opened by one head decision. Exactly one coordination turn may be open in a session: one open
turn, one `join_when:`, and one singular `next:`. Cycle and turn numbers may interleave because
they order different things; history is never renumbered to make them align.

The RUNBOOK declares the unique seats, head, and `status_owner`. STATUS owns the open turn and
its present obligations. BUS remains past-tense receipts. A human may relay an absolute POINT
path verbatim, but transport supplies no delivery or read claim.

## Open and dispatch a turn

The head writes every POINT for the decision, then replaces STATUS before relay. STATUS carries
one bounded `delegated:` row for each current branch, exactly one `join_when:`, a content-sensitive
`recovery_probe:`, and one `next:`/`expected:` pair.

A `delegated:` row has exactly `cycle`, `to`, `turn`, and `awaiting`. `awaiting` is `return`, which
implies the POINT's `to:` seat, or `verdict <seat>`, which names the witness. The field carries no
timestamps, sent/acknowledged claims, progress notes, or recommendations. Facts that constrain
the gate belong in `holds:`; other narration belongs nowhere in STATUS.

The receiver resolves its declared RUNBOOK seat before work. If POINT `to:` or the seat encoded
by `return_to:` differs from that seat, it stops before work and reports the mismatch; the
outstanding STATUS row remains true. The carrier never repairs or reinterprets the POINT.

## Verify branches

Each RETURN remains bound to its cycle and POINT path; it does not echo a trusted `turn:` claim.
The named witness verifies content and writes the cycle's single VERDICT. If the witness is not
the `status_owner`, `status_rewritten: deferred: turn NN` may name only the currently open turn.
Only the owner replaces STATUS. If the referenced turn has already joined, the VERDICT uses
`no + reason` or defers to the then-open turn; it never points at a closed join.

ACCEPT, STOP, and BLOCKED are terminal branch dispositions. REVISE closes the cycle but does not
satisfy the branch: the head opens one linked corrective cycle in the same turn and replaces
that branch's observable `join_when:` obligation with the corrective cycle. No old receipt is
edited or renumbered.

A STOP or BLOCKED VERDICT may use `return: none + reason` only when no RETURN exists. It verifies
the absence, records why the cycle cannot receive a RETURN, and closes no other branch. ACCEPT
and REVISE require an actual RETURN. If a RETURN later appears for a cycle closed through this
exception, the old cycle remains closed and receives no second VERDICT; the head opens a new
numbered cycle to assess the new information.

## Join

A turn joins only when the `status_owner` checks the current `join_when:` obligations against
the POINT, RETURN where required, and terminal VERDICT contents, then replaces STATUS with the
verified present position. The replacement removes the joined turn's `delegated:` and
`join_when:`, advances `checkpoint:` with evidence pointers, resolves or retypes live `holds:`,
and provides one new `next:`/`expected:` pair. That verified STATUS replacement is the join.

Join is not a fourth BUS artifact, a specialized VERDICT, or a requirement that the replacement
share a repository commit with the receipts. Repository commit policy remains the owning
repository's separate law.

## Cold recovery and late artifacts

During an open turn, `recovery_probe:` reads every STATUS row and reconciles its coordinates and
seats with the POINT, any RETURN, and the VERDICT's `cycle`, `turn`, `return`, `disposition`, and
`status_rewritten:` contents. A VERDICT on disk while STATUS still says `awaiting: verdict` is
ordinary interrupted-head partial state. Disk beats stale STATUS, but the recovering head names
the difference before replacing the snapshot; it does not edit another writer's receipt.

Late artifacts never overwrite history. Information arriving after its turn joined is assessed
through a new numbered cycle and carried into the current STATUS as curvature or a hold when it
still affects the gate. `deferred: turn NN` may refer only to the turn open when that VERDICT is
authored.

## Boundary and retirement

This mechanism introduces no scheduler, inbox, read-state, delivery promise, mutable BUS file,
specialized join receipt, fourth BUS kind, vendor invocation syntax, or runtime implementation.
The chapter is the sole home of full-session fan-out operations; RUNBOOK, BUS, and STATUS expose
only authority, schema, and routing seams.

A native durable cross-session join that preserves equivalent sovereign receipts retires this
file-plane delta. Until then, the file plane coordinates full sessions across windows, hosts, or
vendors without pretending that native subagent fan-out and human relay are the same mechanism.
```

### Files 5–8 — header-only normalization

Prepend the following four-line YAML header to each named file. The existing first `#` heading and
every byte after it remain unchanged; do not reflow, rename, update status prose, or repair any
other legacy metadata.

`raw.guides/runbook/res/cross-vendor-seat.md`:

```yaml
---
title: A cross-vendor seat in the session
chapter-of: runbook
---
```

`raw.guides/runbook/res/csharp-head.md`:

```yaml
---
title: The cSharp head posture
chapter-of: runbook
---
```

`raw.guides/runbook/res/research.md`:

```yaml
---
title: The big-scope research gate
chapter-of: runbook
---
```

`raw.guides/runbook/res/token-economy.md`:

```yaml
---
title: Token economy and delegation shape
chapter-of: runbook
---
```

### Later write-pass verification

Before editing, the documentation writer records the four existing chapter hashes:

```bash
repo=/home/hruzam/reposoma
body_hashes=/tmp/runbook-upgrade.chapter-bodies.before.sha256
sha256sum \
  "$repo/raw.guides/runbook/res/cross-vendor-seat.md" \
  "$repo/raw.guides/runbook/res/csharp-head.md" \
  "$repo/raw.guides/runbook/res/research.md" \
  "$repo/raw.guides/runbook/res/token-economy.md" > "$body_hashes"
```

After editing, prove the exact eight-path delta, including the untracked new chapter:

```bash
repo=/home/hruzam/reposoma
expected_paths=$(printf '%s\n' \
  raw.guides/bus/GUIDE.md \
  raw.guides/runbook/GUIDE.md \
  raw.guides/runbook/res/cross-vendor-seat.md \
  raw.guides/runbook/res/csharp-head.md \
  raw.guides/runbook/res/fanout-turns.md \
  raw.guides/runbook/res/research.md \
  raw.guides/runbook/res/token-economy.md \
  raw.guides/status/GUIDE.md | sort)
observed_paths=$(git -C "$repo" status --porcelain=v1 --untracked-files=all |
  sed -E 's/^.. //' | sort)
test "$observed_paths" = "$expected_paths"
```

Prove the four pre-existing chapter bodies are byte-identical below the new four-line headers:

```bash
repo=/home/hruzam/reposoma
body_hashes=/tmp/runbook-upgrade.chapter-bodies.before.sha256
while read -r old_hash path; do
  new_hash=$(tail -n +5 "$path" | sha256sum | cut -d' ' -f1)
  test "$new_hash" = "$old_hash" || exit 1
done < "$body_hashes"
```

Prove guide and chapter frontmatter, with no unresolved date token at gavel:

```bash
repo=/home/hruzam/reposoma
for slug in runbook bus status; do
  file="$repo/raw.guides/$slug/GUIDE.md"
  sed -n '1,7p' "$file" | rg -Uq \
    '^---\ntitle: .+\nscope: [a-z-]+\naudience: agent \+ operator \+ builder\nmachine: both\nverified: [0-9]{4}-[0-9]{2}-[0-9]{2}\n---$' || exit 1
done
for file in "$repo"/raw.guides/runbook/res/*.md; do
  sed -n '1,4p' "$file" | rg -Uq '^---\ntitle: .+\nchapter-of: runbook\n---$' || exit 1
done
! rg -n '<GAVEL-DATE>' "$repo/raw.guides/runbook" "$repo/raw.guides/bus/GUIDE.md" \
  "$repo/raw.guides/status/GUIDE.md"
```

Prove Manifest completeness and state, isolating the Manifest rather than matching later prose:

```bash
repo=/home/hruzam/reposoma
manifest=$(awk '/^## Manifest$/{p=1; next} p && /^## /{exit} p{print}' \
  "$repo/raw.guides/runbook/GUIDE.md")
test "$(printf '%s\n' "$manifest" | rg -c '^- `res/[^`]+[.]md` · chapter · (GAVELED [0-9]{4}-[0-9]{2}-[0-9]{2}|DRAFT) — ')" -eq 5
test "$(printf '%s\n' "$manifest" | rg -c '^- `raw/` · ')" -eq 1
test "$(printf '%s\n' "$manifest" | rg -c 'raw/field[.]runbook-check-rules')" -eq 0
for chapter in cross-vendor-seat csharp-head fanout-turns research token-economy; do
  test "$(printf '%s\n' "$manifest" | rg -c "res/$chapter[.]md")" -eq 1 || exit 1
done
```

Prove the narrow absent-RETURN exception, three-kind grammar, owner join, and carrier hold:

```bash
repo=/home/hruzam/reposoma
bus="$repo/raw.guides/bus/GUIDE.md"
chapter="$repo/raw.guides/runbook/res/fanout-turns.md"
rg -q '`<kind>` is exactly one of `point`, `return`, or `verdict`' "$bus"
rg -q 'return: <.*none \+ reason; none is STOP/BLOCKED only>' "$bus"
rg -q 'ACCEPT and REVISE.*actual RETURN' "$bus"
rg -q 'later.*new numbered cycle' "$bus"
rg -q 'verified STATUS replacement is the join' "$chapter"
rg -q 'Repository commit policy remains.*separate law' "$chapter"
test "$(rg -c '^turn:' "$bus")" -eq 2
! git -C "$repo" diff -- raw.guides/runbook/GUIDE.md raw.guides/bus/GUIDE.md \
  raw.guides/status/GUIDE.md | rg '^\+.*carrier:'
! rg -n 'carrier:' "$chapter"
! git -C "$repo" diff -- . | rg '^\+.*(§§10|TURN kind|[.]turn[.]md)'
! rg -n '(§§10|TURN kind|[.]turn[.]md)' "$chapter"
```

Finally, check whitespace on tracked changes and the untracked chapter separately:

```bash
git -C /home/hruzam/reposoma diff --check
set +e
chapter_check=$(git diff --no-index --check /dev/null \
  /home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md 2>&1)
chapter_status=$?
set -e
test "$chapter_status" -eq 1
test -z "$chapter_check"
```

The final `git diff --no-index` status is 1 because the new file differs from `/dev/null`; any
diagnostic output or other status is failure. An independent verifier must still inspect the
actual diff rather than treating these commands as self-certification.

## 4. Mismatches discovered

1. **The prior exact-four-file blueprint is obsolete.** G4A necessarily adds the four existing
   `runbook/res/` files, producing the exact eight-file scope above. This is a gavel-driven scope
   correction, not adjacent cleanup.
2. **The prior raw-substrate choice sheet is obsolete.** Reposoma `b83026b` canonized `raw/` as a
   side-directory listed once by directory. Per-file listing would now contradict current law.
3. **G2A moves two general laws out of chapter-only treatment.** Seat uniqueness and
   owner-rewrites/verifier-attests each receive one sentence in RUNBOOK GUIDE; the chapter retains
   only the operational fan-out application.
4. **G5B rejects the assessment's commit-as-JOIN claim.** A commit can durably carry a transition
   under repository policy, but the coordination join itself is the owner's verified STATUS
   replacement.
5. **The carrier question remains genuinely deferred.** The T2 material alternates between a
   `carrier:` row key and the participant tuple. No gavel chose either, so this fold adds neither.
6. **The candidate-canon §§10–12 remain absent.** The new chapter is derived from the declared
   corpus and recorded gavels, not a reconstruction of an unavailable draft.

## 5. Recommended next task

After cycle 06 also reaches a checked terminal VERDICT and cartan-csharp joins turn 01 by replacing
STATUS, issue one reposoma-write POINT to Astrobley for exactly the eight files in field 3. Require
the pre-write body hashes, the content-sensitive checks above, an independent Oraculum audit of the
actual diff, and @majkee's documentation gavel before commit. Do not wake runtime reconciliation
seats from the blueprint alone.

## 6. Remaining uncertainty

- The actual documentation-gavel date is intentionally unresolved until the gavel occurs; every
  `<GAVEL-DATE>` token must be replaced atomically at that time.
- The `carrier:` field versus participant-tuple question remains deferred to later evidence and is
  outside this fold. This blueprint makes no choice by omission or example.
- The unavailable candidate-canon §§10–12 remain unavailable evidence. They are not a blocker to
  applying the recorded gavel, but any later recovered artifact is new evidence for a new cycle.
- This seat does not independently accept its own blueprint. Cycle 05 still needs cartan-csharp's
  claim verification and VERDICT; the documentation diff later needs a crossed Oraculum audit and
  @majkee's gavel.
