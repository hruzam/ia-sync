---
cycle: 07
turn: 01
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/07.cartan-csharp.point.md
written: 2026-09-04
host: office
---

# RETURN 07 — three later-write assertions repaired

## 1. Files changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/07.astrobley.return.md` — added this
  narrow corrective RETURN. No documentation target was edited.
- Position before the cut: the accepted eight-file fold and gaveled prose need no change. The
  smallest coherent correction is three fixed-string/state-map assertions that follow the
  blueprint's literal wrapping and exact chapter states.
- No architectural pushback: this repair stays wholly within POINT 07's granted outcome.

## 2. Commands and outcomes

The following are the patch-ready replacements for the three failed assertions. They are
independent of the current working directory.

Replace `rg -q 'later.*new numbered cycle' "$bus"` with:

```bash
point07_bus=/home/hruzam/reposoma/raw.guides/bus/GUIDE.md
point07_late_return_clause=$(printf '%s\n' \
  '> `return: none + reason` is legal only when no RETURN exists and the disposition is STOP or' \
  '> BLOCKED. The VERDICT must verify the absence and state why the cycle cannot receive a RETURN;' \
  '> it never authors a surrogate RETURN. A RETURN that arrives later does not reopen the closed' \
  '> cycle or receive a second VERDICT: its information is assessed in a new numbered cycle.')
rg -UqF "$point07_late_return_clause" "$point07_bus"
```

Replace `rg -q 'Repository commit policy remains.*separate law' "$chapter"` with:

```bash
point07_chapter=/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md
point07_repository_policy_clause=$(printf '%s\n' \
  'Join is not a fourth BUS artifact, a specialized VERDICT, or a requirement that the replacement' \
  'share a repository commit with the receipts. Repository commit policy remains the owning' \
  "repository's separate law.")
rg -UqF "$point07_repository_policy_clause" "$point07_chapter"
```

Replace the generic five-state count plus filename loop under **Prove Manifest completeness and
state** with this exact chapter-state comparison. Keep RETURN 05's separate `raw/` count and
per-file-raw prohibition unchanged.

```bash
point07_runbook=/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md
point07_manifest=$(awk '/^## Manifest$/{p=1; next} p && /^## /{exit} p{print}' \
  "$point07_runbook")
point07_gavel_date=$(sed -nE \
  '1,7 s/^verified: ([0-9]{4}-[0-9]{2}-[0-9]{2})$/\1/p' "$point07_runbook")
test "$(printf '%s\n' "$point07_gavel_date" | wc -l)" -eq 1
printf '%s\n' "$point07_gavel_date" | rg -qx '[0-9]{4}-[0-9]{2}-[0-9]{2}'
point07_expected_chapters=$(printf '%s\n' \
  '- `res/cross-vendor-seat.md` · chapter · DRAFT — cross-vendor seat instrument, tunnel handle, and enable layers' \
  '- `res/csharp-head.md` · chapter · GAVELED 2026-09-03 — cSharp head posture and transfer ritual' \
  "- \`res/fanout-turns.md\` · chapter · GAVELED $point07_gavel_date — full-session fan-out, coordination join, and cold recovery" \
  '- `res/research.md` · chapter · GAVELED 2026-09-03 — big-scope pre-RUNBOOK research gate' \
  '- `res/token-economy.md` · chapter · DRAFT — delegation shape and spend record')
point07_observed_chapters=$(printf '%s\n' "$point07_manifest" |
  rg '^- `res/[^`]+[.]md` · chapter · ')
test "$point07_observed_chapters" = "$point07_expected_chapters"
```

The date is read from the later write's gaveled RUNBOOK frontmatter. This RETURN does not choose
it.

Demonstration against the literal RETURN-05 blueprint, without creating a scratch file:

```bash
point07_return05=/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md
point07_manifest_blueprint=$(awk '
  /^Insert this Manifest after those inline lines/{want=1; next}
  want && /^```markdown$/{capture=1; next}
  capture && /^```$/{exit}
  capture{print}
' "$point07_return05")
point07_manifest=$(printf '%s\n' "$point07_manifest_blueprint" |
  awk '/^## Manifest$/{p=1; next} p{print}')
point07_bus_blueprint=$(awk '
  index($0, "> `return: none + reason` is legal only") == 1 {capture=1}
  capture{print}
  capture && index($0, "> cycle or receive a second VERDICT:") == 1 {exit}
' "$point07_return05")
point07_chapter_blueprint=$(awk '
  index($0, "Join is not a fourth BUS artifact") == 1 {capture=1}
  capture{print}
  capture && index($0, "repository") == 1 && index($0, "separate law.") > 0 {exit}
' "$point07_return05")
printf '%s\n' "$point07_bus_blueprint" | rg -UqF "$point07_late_return_clause"
printf '%s\n' "$point07_chapter_blueprint" | rg -UqF \
  "$point07_repository_policy_clause"
point07_gavel_date='<GAVEL-DATE>'
point07_expected_chapters=$(printf '%s\n' \
  '- `res/cross-vendor-seat.md` · chapter · DRAFT — cross-vendor seat instrument, tunnel handle, and enable layers' \
  '- `res/csharp-head.md` · chapter · GAVELED 2026-09-03 — cSharp head posture and transfer ritual' \
  "- \`res/fanout-turns.md\` · chapter · GAVELED $point07_gavel_date — full-session fan-out, coordination join, and cold recovery" \
  '- `res/research.md` · chapter · GAVELED 2026-09-03 — big-scope pre-RUNBOOK research gate' \
  '- `res/token-economy.md` · chapter · DRAFT — delegation shape and spend record')
point07_observed_chapters=$(printf '%s\n' "$point07_manifest" |
  rg '^- `res/[^`]+[.]md` · chapter · ')
test "$point07_observed_chapters" = "$point07_expected_chapters"
```

Observed results: all three final checks exited 0; the extracted Manifest contained exactly five
chapter lines. The checks therefore succeed on the exact wrapped clauses and state map proposed
by RETURN 05, rather than on rewritten test fixtures.

Post-write verification:

- `awk` counted each numbered RETURN heading exactly once — exit 0; printed `6`.
- `rg -n '[[:blank:]]+$' <RETURN-07>` — exit 1 with no output, the expected clean result.
- `git diff --no-index --check /dev/null <RETURN-07>` — exit 1 with no diagnostic output, the
  expected new-file result with no whitespace errors.
- A before/after comparison of full ia-sync status, full reposoma status, and hashes for every
  protected POINT-declared authority/documentation path — passed after removing only the new
  RETURN-07 status line. RETURN 07 is the sole delta attributable to this seat.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.astrobley.return.md` — literal
  accepted blueprint used as the demonstration input; SHA-256
  `6c48350045e1be7ce0df932a7eac94ca9bb6b3f41f2643acb5ec755f85566c0f`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/05.cartan-csharp.verdict.md` — identifies
  only the multiline late-RETURN check, multiline repository-policy check, and unbound Manifest
  states as defective.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` and
  `guide-writing/res/slug-directories.md` — current Manifest and side-directory law at
  reposoma `b83026b`.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md`, `bus/GUIDE.md`, and `status/GUIDE.md` —
  current read-only documentation surfaces; none was edited.

## 4. Mismatches discovered

- Reposoma was already dirty in unrelated `raw.guides/sella/**` paths when this seat began.
  Those paths were treated as protected concurrent work and were neither inspected nor changed.
  The runbook, BUS, STATUS, guide-writing, and slug-directory files named by POINT 07 retained
  their pre-write hashes.
- This does not alter VERDICT 05's diagnosis: the three failures are verification-spec defects,
  not defects in the accepted eight-file scope or gaveled prose.

## 5. Recommended next task

Cartan-csharp should independently rerun the three literal-blueprint demonstrations, inspect this
RETURN as the sole cycle-07 write, and issue VERDICT 07. If accepted, the already-planned later
documentation-write POINT may substitute only these three command fragments into RETURN 05's
verification specification.

## 6. Remaining uncertainty

- The actual documentation-gavel date remains unresolved. The later Manifest check binds
  `fanout-turns.md` to the date present in the gaveled RUNBOOK header; it does not supply a date.
- `carrier:` and the absent candidate-canon §§10–12 remain unresolved and untouched.
- Reposoma is dirty-but-unchanged relative to this seat's captured baseline, not absolutely clean.
- Independent acceptance, the eight-file documentation write, diff audit, operator gavel,
  commit, runtime reconciliation, deployment, and fresh-session proof remain outside this cycle.
