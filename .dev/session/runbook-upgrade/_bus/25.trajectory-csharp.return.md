---
cycle: 25
from: trajectory-csharp
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/25.cartan-csharp.point.md
written: 2026-09-09
host: office
reviewed_candidate_sha256: 7627dc2e602e5dbcf9f039234f433b0d7d5fbb1f7f429f3acda2a769388cf41c
verdict: USABLE AS-IS · two bounded wording suggestions, neither blocking
---

# RETURN 25 — client-fit review of the presence-board candidate

## 1. Files changed

- This RETURN only. No board, client, guide, canon, STATUS-of-this-bed, git, or
  deploy change. No file created outside this path; the review harness ran
  in-memory and wrote nothing.

## 2. Commands and outcomes

- Independent extraction: read `24.astrobley.return.md`, matched the single outer
  four-backtick `markdown` fence including final LF with my own regex (not the
  producer's or verifier's code). Result: 119 newline-terminated lines, 5,864
  bytes, SHA-256 `7627dc2e602e5dbcf9f039234f433b0d7d5fbb1f7f429f3acda2a769388cf41c`
  — matches the POINT 25 pin exactly. Cycle 23 was not read as source.
- In-memory client-parser exercise (python3 stdlib, a **third independent
  implementation** distinct from the producer's and the verifier's node harnesses):
  a 14-line regex+assert parser implementing the record grammar as my renderer
  would embed it. Outcomes: 3 valid forms PASS (full, no-note, `Z`-zone);
  8 negative forms rejected (reordered keys, missing timezone, control char in
  note, CRLF, BOM, unknown key, offset `+14:01`, double-quote in note);
  filename/ID mismatch rejected. Exit 0.
- No filesystem, RNG, race, or transport behavior was executed — see §6.

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.astrobley.return.md`
  — reviewed source; candidate pinned above.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.verdict.md`
  — the head's independent ACCEPT this review relies on for provenance only.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/22.trajectory-csharp.review.md`
  — my prior schema requirements; disposition of each is in §4.
- `/home/hruzam/ia-sync/zsh/session/runbook.py` — the renderer this fit is judged
  against (uncommitted client work, untouched this sitting).

## 4. Mismatches discovered

Answering the three POINT-25 questions; findings bounded, with examples.

**Q1 — smallest usable interface: YES for the reader, with one flagged
restriction.** The full grammar parses in 14 stdlib lines (§2) — smaller than my
REVIEW-22 ask; fixed order, mandatory quoting, and byte framing each removed a
reader branch rather than adding one. Accepted as-is, including the opaque
`presence.<32hex>.md` filename: the `ls` ergonomics loss is real but covered by
the one-key-per-line grammar (`grep -h '^seat\|^bed' ~/reposoma/_active/presence.*.md`
is a complete no-client reading), and the ID buys the collision-safety my
REVIEW-22 stamp-based proposal lacked — their trade was right, mine withdrawn.
FLAG: *"allocates 16 bytes from its operating system's cryptographic random
source"* is a normative requirement no reader can verify and that carries no
load — the candidate itself names exclusive creation + fresh-ID retry as the
collision handling, not entropy quality. Example burden: an operator attaching by
hand who types 32 hex from the keyboard produces a byte-valid record
indistinguishable from a compliant one; the MUST is thus unfalsifiable. Minimal
alternative: "The `attachment_id` SHOULD come from a cryptographic random source
and MUST match `[0-9a-f]{32}`; exclusive creation and fresh-ID retry remain the
load-bearing collision handling."

**Q2 — implementable without liveness-guessing or locks: YES, with one missing
sentence.** Attach = POSIX exclusive create (`O_CREAT|O_EXCL`), standard; detach
= unlink of the owner's remembered filename; operator removal of an orphan is the
already-covered human-authority path; reader states (valid / malformed /
conflicted, with filename and reason) map directly onto my TUI's row-state
rendering — no participant is ever guessed, nothing locks. MISSING SENTENCE:
creation is not specified atomic. A reader on its tick can observe a record after
exclusive create but before the write completes; under "after complete creation,
a v1 record is immutable," the owner finishing its initial write sits in an
undefined window. One sentence closes it, e.g.: "Creation should complete in a
single write; a reader observing a malformed record may re-read it once before
reporting it persistently." Not blocking — display-only reporting makes the
flicker harmless — but clients should not bind against an undefined window.

**Q3 — pointers, out-of-workspace bed, display-only age, closing references:
FIT, no new authority needed.** Host-frame resolution maps one-to-one onto my
renderer: records whose `host` equals the local host resolve `bed` against my bed
model for the overlap indicator; foreign-host records render as remote
declarations, never resolved locally — which matches the actual incident class
(same-machine overlap resolves instantly; cross-machine is informational under
acknowledged git lag). The `workspace`/`bed` pair serves my two views
independently (repo-level "someone in this area"; bed-level overlap). Age
thresholds and dim rendering stay my client policy per the contract;
absence/staleness-is-not-closure matches my REVIEW-22 concession verbatim. All
four of my REVIEW-22 schema requirements are satisfied or exceeded (single parse
shape; timezone-carrying ISO-8601; `~/`-anchored pointers; display-only `note`).

## 5. Recommended next task

Cartan integrates or declines the two wording suggestions (Q1 entropy MUST→SHOULD;
Q2 creation-atomicity sentence) on the law side — both are single-sentence edits,
neither changes identity, grammar, lifecycle, or authority, and I bind clients to
the candidate unchanged if both are declined. Then exact filename/schema/lifecycle
wording goes to majkee's gavel. Board clients remain frozen until that gavel, per
the case lock.

## 6. Remaining uncertainty

- My timestamp check validated format and offset range but NOT calendar validity
  (my regex passes `2026-02-30`; the contract correctly requires calendar-valid —
  a real client adds one stdlib date call; the gap is in my check, not the draft).
- Exclusive-create race behavior, RNG sourcing, git-conflict rendering of a
  competing-content record, and cross-host lag were not executed — in-memory
  review only, per the POINT's gates.
- Parser fit is proven against a 14-line reference implementation, not against
  the shipped renderer — the board view does not exist yet and is gavel-gated.
- My client's home-host state remains unverified pending majkee's pull+deploy
  there (carried from REVIEW 22; unchanged).
