---
cycle: 15
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/15.cartan-csharp.point.md
seat_validation: POINT `to: atlas-ui` and `return_to:` seat encoding match my declared
  participant_3 seat; sequential correction linked from REVISE 14, not a fan-out turn.
authored: 2026-09-05 · office · resident
---

# RETURN 15 — reader contract E, corrected

## 1. Files changed

**Executed: none.** This RETURN is the only file written. Sources, guides, deployed trees,
RUNBOOK, STATUS, historical bus artifacts, and git state untouched.

**A–D are preserved by immutable reference, not restated:** RETURN 14
(`/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/14.atlas-ui.return.md`, SHA-256
`18b1de1c99fff2cdd45e6c0f383c4bc365bf8744992ba26563efd8a614358d06`), field 1, items A–D.
The proposed source set stays at three files: Claude `runbook/SKILL.md` (A+B) · Codex
`octopus/SKILL.md` (C+D) · new Codex `guide/SKILL.md` (E below). Optional UI metadata is
withdrawn per POINT 15 gate 3 — not part of the proposal.

### E, corrected — proposed full content of `/home/hruzam/ia-sync/codex/skills/guide/SKILL.md` (NOT executed)

```
---
name: guide
description: Temple guide access over ~/reposoma/raw.guides/ in two modes — consult shared law silently while doing another task, or serve one GUIDE.md or res/ chapter verbatim on an explicit list/read/show request ($guide, "read the runbook guide", "show the fanout chapter"). Not for OpenAI product documentation (openai-docs) and not for writing guides.
---

# Guide — temple guide reader

The guides' single home is `~/reposoma/raw.guides/`: one topic = one slug folder = one
`GUIDE.md`, subchapters beside it in `res/`. Read-only: never edit, create, or reorganize a
guide. Writer law: `~/reposoma/raw.guides/guide-writing/GUIDE.md`.

## Two modes — consult is not display

- **Consult** — this skill was selected while another task depends on shared law: read the
  resolved file, apply it, and cite its path and the specific clause in the answer. Do not
  print guide text the caller never asked to see.
- **Display** — the caller explicitly asks to list, read, show, or print a guide or chapter:
  serve the resolved file verbatim and complete, frontmatter included. The caller wanted the
  law, not a digest.

## Resolution — one rule, only GUIDE.md and res/ resolve

- no argument → the catalog: for every `~/reposoma/raw.guides/*/GUIDE.md`, report the slug,
  its `title:` / `machine:` / `verified:` frontmatter values, and the `res/` chapter
  basenames. Build the catalog from those lines alone; do not read guide bodies.
- `<slug>` → `~/reposoma/raw.guides/<slug>/GUIDE.md`. No such folder → show the catalog;
  resolve nothing else.
- `<slug> <chapter>` → the `res/*.md` file whose **basename contains `<chapter>` as a
  case-insensitive substring** (the one matching rule: `turns` resolves `fanout-turns.md`).
  Exactly one match → resolve it. Several → report the matching basenames and ask; resolve
  nothing. None → show that GUIDE.md's `## Manifest`; resolve nothing.

## Contract (shared law: guide-writing res/slug-directories.md — point, never restate)

- Side-directories (`raw/`, `src/`, `ast/`, `cod/`, and any other) are never resolved here;
  they are reached only by explicit path from a manifest line.
- Manifest check at the gaveled granularity, one warning line per finding, never recursive:
  each `res/*.md` chapter appears per-file in the owning `## Manifest`; each side-directory
  appears as exactly one directory entry, its children never inspected; an undeclared sibling
  file or side-directory is drift.
- Staleness: `verified:` + `half_life_days:` past expiry → prefix one warning naming the
  verified date. A `PUBLISHED MIRROR` banner → state that the owning project is the truth.
```

Resolution of the three REVISE issues, mapped: issue 1 → `## Two modes` + reworded
`description:` (automatic selection preserved; no explicit-only policy, no extra primitive);
issue 2 → single case-insensitive substring rule with explicit one/several/none and
missing-slug behavior, confined to `GUIDE.md` + `res/`; issue 3 → per-file `res/` entries,
one entry per side-directory, children never inspected, undeclared directory = drift.

### Corrected proof design (future, operator-run; NOT run this cycle)

Evidence classes are separated: **catalog** (the client lists the skill) · **selection** (the
model picks it unprompted) · **explicit-path loading** (a supplied file works) · **behavior**
(the contract holds). One probe never certifies another class.

Codex reader (fresh session each):

- **P0 catalog** — ask the client's skill catalog; PASS: `guide` listed. Evidence: catalog.
- **P1 explicit display** — `$guide runbook fanout-turns`; PASS: output is byte-complete
  against `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` **including the
  frontmatter block**, verified by diff/hash against the file, not by eye.
- **P2 matching rule** — `$guide runbook turns`; PASS: same chapter (substring exercised
  where a prefix rule would return nothing). Then the designed-ambiguous probe
  `$guide runbook o`; PASS: reports exactly `cross-vendor-seat.md · csharp-head-protocol.md ·
  fanout-turns.md · token-economy.md` (4 of 5; `research.md` excluded), asks, prints no
  chapter body.
- **P3 consult-not-display** — an unrelated task that depends on the law, e.g. *"here are
  five STATUS lines — do they violate the runbook one-authority rule?"*; PASS: answers from
  the law, cites path + clause, prints no unsolicited chapter. Evidence: selection+behavior.
- **P4 explicit-path diagnostic** (only if P1 fails) — skill-creator eval spelling, "Use
  $guide at /home/hruzam/.agents/skills/guide …". P4 pass + P1 fail narrows the fault to
  {discovery root, selection, stale client state} and no further; discriminate via P0 and a
  client restart. P4 alone diagnoses nothing about discovery.

Producer proofs for A–D (fresh sessions; applicable-read, not name-recall):

- **P5 Codex** — `$octopus` + a fan-out-shaped toy intention (*"one head coordinating two
  independent full CLI sessions from one decision"*); PASS: the session demonstrably reads
  `res/fanout-turns.md` (its draft uses `turn:` / `delegated:` / `join_when:` semantics and
  cites the chapter path) AND ends `OCTOPUS PARKED` with nothing executed or spawned.
- **P6 Claude** — `/runbook` + the same toy intention; PASS: same applicable-read evidence
  AND the `RUNBOOK PARKED` envelope with nothing executed. Merely echoing a chapter name
  fails both.

## 2. Commands and outcomes

| command | outcome |
|---|---|
| `git -C /home/hruzam/reposoma rev-parse HEAD` + `status --short` | still `3feba72791c1550ba993ff7d4e0d57f1bb2b3e7d`, clean — no later drift |
| `sha256sum` recheck: claude guide/runbook skills, codex octopus, fanout chapter | all four match the RETURN-14 frozen values — sources unchanged across cycles 14→15 |
| `ls -la ~/ia-sync/.codex/` · `stat ~/ia-sync/.codex` | **ENOENT in this frame** (see field 4.1 — differs from VERDICT 14's observation) |
| `ls -la ~/ia-sync/.agents/skills/` | exists; contains only `multihost/` (dated 27 Aug) — no guide reader by name or purpose there either |
| `ls /home/hruzam/reposoma/raw.guides/runbook/res/ \| grep o` | ground truth for probe P2: exactly the 4 basenames named above |

## 3. Evidence paths

- RETURN 14 (immutable baseline for A–D): path + SHA-256 in field 1.
- VERDICT 14: `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/14.cartan-csharp.verdict.md`
  — source of the three E issues and the proof-design critique this RETURN answers.
- Discovery-root evidence: official Build-skills documentation
  (`learn.chatgpt.com/docs/build-skills#where-codex-loads-local-skills`) supporting the user
  `.agents/skills` source — **fetched by cartan-csharp 2026-09-05 and cited in VERDICT 14; I
  had no network tools this cycle and attribute the fetch to the head, not myself.**
  Consequence held: deployment target unchanged; no migration, no duplicate install.
- Gaveled law E conforms to: `/home/hruzam/reposoma/raw.guides/guide-writing/res/slug-directories.md`
  (SHA-256 unchanged from RETURN 14 field 3).

## 4. Mismatches discovered

1. **`/home/hruzam/ia-sync/.codex/` flickers.** RETURN 14 (04:22): absent. VERDICT 14:
   empty read-only directory, birth 04:34. This frame: absent again. Two seats' observations
   disagree over time — reported as live frame drift, origin unestablished, no fault
   assigned, nothing created or removed by me, no source fix proposed.
2. **Carriage self-report, unchanged:** this incarnation runs `claude-fable-5` against the
   tuple's fixed `claude-opus-4-8`. Actual carriage reported as observed from inside; still
   no independent transcript — remains UNVERIFIED self-report by nature. Roster untouched.
3. Pre-existing Claude guide-skill drifts (recursive fallback into substrate; pre-taxonomy
   sibling wording) remain disclosed-and-held per POINT 15 — outside this source cut, not
   silently repaired, not expanded into the proposal.

## 5. Recommended next task

Verify this corrected E against the three REVISE issues and the proof-design critique; on
ACCEPT, relay the three-file source set (A+B · C+D · E) to @majkee for exact source-scope
approval, then open the authoring cycle — author on the ia-sync table, validate
(skill-creator `quick_validate` on E, `deploy.sh --dry-run`), stop before deploy; deploy and
P0–P6 remain the operator's hands.

## 6. Remaining uncertainty

- Every invocation spelling and all of P0–P6 remain unrun; nothing this cycle proves
  discovery, selection, or behavior — the contract is only textually tightened.
- Whether P3's consult mode holds under implicit selection is the least predictable proof:
  it tests model judgment against the two-mode text, not a mechanical rule. If P3 fails
  after deploy, the fix is wording iteration in E, not a policy change or new primitive.
- The `.codex` flicker (field 4.1) has no explanation in my frame; if it recurs during the
  authoring cycle it belongs in STATUS `holds:` before any Codex-side write.
- Office-local only; home parity arrives via commit + pull + deploy, outside this cycle.
