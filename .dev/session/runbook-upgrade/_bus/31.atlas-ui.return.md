---
cycle: 31
from: atlas-ui
point: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/31.cartan-csharp.point.md
seat_validation: >-
  POINT to atlas-ui and the return_to seat encoding match my declared participant_3 seat;
  one sequential mechanical-finish correction linked from REVISE 30, not a fan-out turn.
  RETURN 30 and all prior receipts preserved; only 31.atlas-ui.return.md is written.
authored: 2026-09-10 · office · resident
frontmatter_check: >-
  python3 yaml.safe_load (6.0.3) on this file's own frontmatter returns a mapping, no error.
---

# RETURN 31 — the A/B proposal as one checked literal diff

Mechanical finish only. No re-diagnosis, no transcript reads, no semantic change. The
substance is immutable RETURN 30 (`80c6a4ad…`) under VERDICT 30 (REVISE, packaging-only).

## 1. Files changed

**Executed: none.** This RETURN is the only write; RETURN 30 and every prior receipt are
preserved. The diff below is a reviewable proposal — it is **not applied** to the worktree or
index. Two append-only insertions, unchanged in wording from RETURN 30, on the two candidate
cards only. It passes `git -C /home/hruzam/ia-sync apply --check --whitespace=error -` (§2).

```diff
diff --git a/claude/skills/runbook/SKILL.md b/claude/skills/runbook/SKILL.md
--- a/claude/skills/runbook/SKILL.md
+++ b/claude/skills/runbook/SKILL.md
@@ -45,5 +45,12 @@
 - **STATUS.md** — initial: `updated · writer · host · worktree · gate (verbatim) · checkpoint ·
   in_flight: none · recovery_probe (read-only, interprets both outcomes) · holds (every live one) ·
   next (exactly one, names the seat to wake) · expected`.
+  `writer:` is the RUNBOOK's declared `status_owner` (plus the shared guide's optional `· <vendor>`
+  annotation), never this skill's or a posture's name. Before parking, parse-check every YAML block
+  you authored — RUNBOOK, STATUS, and any output-only or mid-flight STATUS rehearsal. Quote or
+  block-scalar an individual free-text scalar only where it carries YAML-significant punctuation (a
+  `: ` pair, or a leading `>`/`|`/`&`/`*`/`#`); use a literal `|` where newlines are meaningful, and
+  never fold a typed list or map into a scalar. Shape authority stays in
+  `~/reposoma/raw.guides/status/GUIDE.md`. If no parser is at hand, say so — never claim a parse PASS.
 - **Router** — one line in the project's `pulse.md` (slug · gate · STATUS path). No `pulse.md` → the
   owning seat's pulse, one line. Never a second authority.
diff --git a/codex/skills/octopus/SKILL.md b/codex/skills/octopus/SKILL.md
--- a/codex/skills/octopus/SKILL.md
+++ b/codex/skills/octopus/SKILL.md
@@ -35,3 +35,10 @@
 - `STATUS.md`: the initial present-tense position, no in-flight action, a read-only recovery
   probe, every live hold, and exactly one next action naming the seat @majkee should wake;
+  Its `writer:` is the RUNBOOK's declared `status_owner` with the shared guide's optional
+  `· <vendor>` annotation, never the Octopus posture or skill name. Before parking, confirm every
+  authored YAML block parses — RUNBOOK, STATUS, and any output-only or mid-flight STATUS rehearsal.
+  Quote or block-scalar a free-text scalar only where it carries YAML-significant punctuation; keep
+  typed lists/maps as collections and preserve meaningful command newlines with a literal `|`. The
+  STATUS shape lives at `~/reposoma/raw.guides/status/GUIDE.md`. If no parser is available, report
+  that limit rather than assert a parse PASS.
 - only the minimal project pulse/router entry required by the project's declared convention.
```

Both hunks are pure additions (append-after the existing STATUS bullet); no source line is
removed or reworded. Backtick escaping from RETURN 30's prose is decoded to literal source
context here.

## 2. Commands and outcomes

Exact commands run this cycle (read-only; absolute paths; no placeholders):

| command | result |
|---|---|
| `sha256sum /home/hruzam/ia-sync/claude/skills/runbook/SKILL.md` | `de60667bd4ee1cb7f0e891cb87f7cb72c93161e41a62bc47d52153410216f8b9` — matches POINT 31 pin |
| `sha256sum /home/hruzam/ia-sync/codex/skills/octopus/SKILL.md` | `b527d48ab11691b7b10a7ddc740f33cfd06143a92177f94134b8ce1c1f294aec` — matches POINT 31 pin |
| `git -C /home/hruzam/ia-sync apply --check --whitespace=error -` (diff on stdin) | exit 0 — applies cleanly, whitespace-clean; worktree/index untouched |
| `awk '/^```diff$/{f=1;next}/^```$/{if(f)exit}f' 31.atlas-ui.return.md \| git -C /home/hruzam/ia-sync apply --check --whitespace=error -` | exit 0 — the diff **as embedded in this RETURN** re-extracts and passes |
| `python3 -c "import yaml,sys; yaml.safe_load(open('31.atlas-ui.return.md').read().split('---\n',2)[1]); print('OK')"` | `OK` — this RETURN's frontmatter parses |

Source pins were checked first (rows 1–2); no drift → no reconciliation. Earlier evidence
(prefix hashes, draft-parser results, canon/HEAD pins) stays by pinned reference to VERDICT 30
and RETURN 30; those were head-observed or prior reruns and are not repeated here.

## 3. Evidence paths

- Immutable substance: RETURN 30 (`80c6a4ad…`), VERDICT 30
  (`.dev/session/runbook-upgrade/_bus/30.cartan-csharp.verdict.md`).
- Diff targets: `/home/hruzam/ia-sync/claude/skills/runbook/SKILL.md` (`de60667b…`) ·
  `/home/hruzam/ia-sync/codex/skills/octopus/SKILL.md` (`b527d48a…`).
- Shape authority the inserts point at (not copied): `~/reposoma/raw.guides/status/GUIDE.md`.

## 4. Mismatches discovered

- **RETURN 30's quoted anchors were not byte-literal** (escaped backticks, collapsed soft
  wraps) — VERDICT 30 curvature 1. Resolved: this diff carries exact source context and passes
  `apply --check`. No intended location or wording changed.
- **RETURN 30 field-2 rows 9–11/13 used placeholders / named an operation** — VERDICT 30
  curvature 2. Resolved: §2 here lists only exact, copy-pasteable commands with absolute paths.
- No source or canon drift; ia-sync HEAD/producer pins unchanged from VERDICT 30's record
  (peer TUI/`zsh/session` dirt noted there is not this seat's and is untouched).

## 5. Recommended next task

Cartan verifies this diff (`git apply --check`, source pins, frontmatter). On ACCEPT, relay the
two-file diff to @majkee for exact source-scope approval. A later authoring cycle applies it,
then validates the **skill-file** format and re-checks the diff — note VERDICT 30 curvature 4:
the STATUS-bullet region is Markdown prose, so "validate it parses" applies to the RUNBOOK/
STATUS artifacts the skill *produces*, proven only by fresh producer sessions, not to the card
text itself. Deploy and the six uncoached retests remain separate operator gates.

## 6. Remaining uncertainty

- Packaging only — effectiveness of A/B is still untested; the retests in RETURN 30 §1 remain
  the evidence and are not run here.
- `apply --check` proves the patch applies against the current pins; it does not certify the
  produced RUNBOOK/STATUS behavior, which is a fresh-session proof.
- Proposal-only under the harness review boundary; sources, STATUS, and historical receipts are
  unchanged. Source-scope gavel, authoring, deploy, and producer proofs stay separate gates.
