---
cycle: 24
from: astrobley
to: cartan-csharp
kind: return
answers: /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.point.md
written: 2026-09-06
host: office
execution_topology: operator-designated Astrobley posture in a separate top-level window; no delegation
---

# RETURN 24 — direct-session presence-board candidate

## 1. Files changed

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.astrobley.return.md` — added
  this corrective RETURN and complete candidate. No behavior or canon changed.

The complete proposed `runbook/res/presence-board.md` bytes are inside this single extractable
fence:

````markdown
---
title: Presence board — advisory cross-session attachment records
chapter-of: runbook
---

# Presence board

> **DRAFT — not gaveled.** This candidate has no canonical or client-binding authority.

Use the presence board when an active session wants to declare its attachment to a workspace so
people and clients can notice possible overlap. Attach by creating one complete record; detach by
removing it under the ownership rule below. The board is a shared display surface, not doing-state.

## Advisory boundary

Presence may inform a person or seat that coordination could help. It never commands or authorizes
work, wake, delivery, permission, scope, resource lock, schedule, takeover, prune, or workspace access.

Readers do not treat presence, absence, age, or damage as a behavioral gate. They may display a
record and its uncertainty, then leave any action to the applicable session and human authority.

## Home and attachment identity

The proposed home is `~/reposoma/_active/`, with one file per attachment. Here `~` belongs to the
host carrying the shared reposoma checkout.

Each attaching incarnation allocates 16 bytes from its operating system's cryptographic random
source and encodes them as 32 lowercase hexadecimal characters: the `attachment_id`. The filename is:

```text
presence.<attachment_id>.md
```

The filename grammar is `^presence\.[0-9a-f]{32}\.md$`. Its hexadecimal component must equal the
record's `attachment_id` byte-for-byte.

Create the target with an exclusive, create-without-replacement operation. If it exists, do not read,
replace, amend, or delete it as collision handling; allocate a fresh ID and try a new filename. This
is collision-resistant allocation with explicit conflict handling, not mathematical uniqueness.

The ID identifies only this attachment and grants no rights over a workspace, bed, seat, or record.
No registry or lock service participates. The ID and `attached_at` stay stable; v1 has no refresh.

## Record grammar

A record is UTF-8 without BOM, LF-only, ending in one LF. It has one frontmatter document and no
Markdown body. Exact `---` lines enclose one flat `key: "value"` scalar per line in the order below.
Blank lines, comments, nesting, lists, multiline values, aliases, tags, duplicate keys, and unknown
keys are invalid. Every value uses ASCII double quotes and contains no ASCII control character,
double quote, or backslash; escapes are not interpreted. This is a small grammar, not general YAML.

Required keys, in order:

- `schema` — exactly `presence-board/v1`.
- `attachment_id` — exactly 32 lowercase hexadecimal characters.
- `seat` — the declaring seat; 1–64 ASCII letters, digits, `.`, `_`, or `-`, starting alphanumeric.
- `host` — the declaring host label under the same 1–64 character grammar.
- `workspace` — a whole quoted pointer beginning `~/`, scoped to `host`.
- `bed` — a whole quoted pointer beginning `~/`, scoped to `host`; it may be outside `workspace`.
- `attached_at` — a calendar-valid `YYYY-MM-DDTHH:MM:SSZ` or
  `YYYY-MM-DDTHH:MM:SS±HH:MM`. Fractions and missing zones are invalid; offsets range through
  `14:00`, with `14` permitting only `00` minutes.

One optional final key is allowed:

- `note` — one line for display only. A reader never parses it as policy, state, instruction, scope,
  or authority.

Missing, reordered, duplicate, or extra keys, bad quoting, filename/ID mismatch, or conflict markers
make a record malformed. Competing contents for one filename make it conflicted. Readers report the
state, filename, and reason; they do not guess, choose, hide the row, or infer a live participant.

## Valid record

Filename: `presence.4f8c2d7a9b1e6c03d5a7f9218b4e0c6d.md`

```yaml
---
schema: "presence-board/v1"
attachment_id: "4f8c2d7a9b1e6c03d5a7f9218b4e0c6d"
seat: "astrobley"
host: "office"
workspace: "~/ia-sync"
bed: "~/ia-sync/.dev/session/runbook-upgrade"
attached_at: "2026-09-06T14:05:09+02:00"
note: "Drafting the presence-board contract"
---
```

The filename and ID map exactly. The timestamp is a declaration, not a heartbeat or liveness claim.

## Ownership and lifecycle

The incarnation that exclusively created a record is its single writer and attachment owner. Reusable
seat, host, workspace, and bed labels do not let another incarnation edit or delete it.

After complete creation, a v1 record is immutable. Its owner detaches by removing exactly the file
named by its owned ID; reattachment gets a new ID, filename, and time. Only explicit human authority
may remove an orphan. Readers never auto-delete, refresh, repair, or infer takeover.

Detaching ends only the board declaration; it neither closes the bed nor proves gate, receipt,
evidence, promotion, or transfer completion. The RUNBOOK GUIDE **On gate closure** still governs.
The cSharp protocol still requires committed receipts, EVERY raw keeper in the promotion manifest,
and experience transfer. `res/fanout-turns.md` **Cold recovery and late artifacts** still applies.

Route any owed overlap handoff through its BUS or transfer authority before the responsible session
closes. Board detachment neither satisfies that handoff nor suppresses a late artifact.

## Reading and display

Resolve `workspace` and `bed` only in the declaring `host` frame. Remote `~/` remains a quoted remote
pointer, not the reader's home, a verified local path, remote-access permission, or relocation.

Use `attached_at`, never mtime. Age thresholds and dim rendering are client policy. Old proves
neither death nor activity; invalid, ambiguous, or future/skewed renders unknown. Transport may lag,
so the board is not a real-time census.

Absence, staleness, damage, conflict, or a TUI unknown row is not closure evidence. Plain-file attach
and detach are the contract; commands, clients, universal validators, daemons, and heartbeats are out.
````

Prospective RUNBOOK GUIDE Manifest line:

```markdown
- `res/presence-board.md` · chapter · DRAFT — advisory cross-session attachment records and detach boundary
```

Compared with cycle 23, the candidate retains the opaque 128-bit attachment ID, exclusive
no-replacement creation, immutable single-owner record, timezone-bearing attachment declaration,
host-scoped pointers, advisory boundary and existing closure references. It tightens the record byte
grammar to UTF-8 without BOM, LF-only with one final LF, and forbids ASCII control characters in all
quoted values.

## 2. Commands and outcomes

- Direct execution provenance: Majkee designated this existing top-level window as the Astrobley
  posture and relayed absolute POINT 24 here. No `spawn_agent`, collaboration delegate, child task,
  parent verification, tunnel or second seat was invoked during cycle 24. This is a transcript/process
  claim for Cartan to assess; filename, seat label and hash do not independently prove it.
- Before POINT 24 arrived, this window completed Majkee's separate request for
  `.dev/session/codex-identity-resolution/raw/draft.codex-session-identity-resolution.2026-09-06.md`.
  That path was already present in the cycle-24 baseline and was not edited after POINT 24 resolution.
- `cat /home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.point.md` — exit 0;
  resolved `to: astrobley`, cycle 24, the exact `return_to`, canonical headings and sole write scope.
- Complete `cat` reads of POINT 24's explicit paths and POINT 23's inherited law/peer paths — exit 0.
  Current STATUS names cycle 24 awaiting this independent-session RETURN; no cycle-23 acceptance was
  inherited.
- A fresh in-memory `node - <<'NODE'` harness extracted the proposed candidate, strictly parsed the
  example and filename/ID mapping, exercised timestamp and byte framing, and wrote no files. Exit 0:
  119 newline-terminated lines, 5,864 bytes, SHA-256
  `7627dc2e602e5dbcf9f039234f433b0d7d5fbb1f7f429f3acda2a769388cf41c`.
- The same harness selected distinct filenames for two same-seat/same-host/same-second attachments
  and repeated bed slugs in different workspaces. It rejected missing timezone, invalid calendar
  date, ASCII control in `note`, duplicate key, BOM and CRLF framing. Semantic checks confirmed
  foreign-label non-ownership, human-only orphan removal, remote `~/` scoping, visible malformed
  reporting, display-only age and non-liveness timestamp wording.
- Post-write commands and results are appended below after running against this disk artifact; they
  are the reproducible final checks, not a claim about an unbuilt client.

Final artifact checks:

```text
node - <<'NODE' … NODE
  exit 0
  result: PASS
  candidate: 119 newline-terminated lines · 5,864 bytes
  candidate_sha256: 7627dc2e602e5dbcf9f039234f433b0d7d5fbb1f7f429f3acda2a769388cf41c
  canonical_headings: 6
  example: presence.4f8c2d7a9b1e6c03d5a7f9218b4e0c6d.md
  negative_cases: 6
  identity_cases: 2
  semantic_clauses: 7

rg -n '^(## [1-6]\. |Prospective RUNBOOK GUIDE Manifest line:)' \
  .dev/session/runbook-upgrade/_bus/24.astrobley.return.md
  exit 0 · fields 1–6 found once in order; prospective Manifest marker found once

rg -n '[[:blank:]]+$' \
   .dev/session/runbook-upgrade/_bus/24.astrobley.return.md
  exit 1 as expected · no trailing-whitespace match

git diff --cached --name-only
  exit 0 · no output · index empty

git status --short -- .dev/session/codex-identity-resolution \
  .dev/session/runbook-upgrade/_bus/24.cartan-csharp.point.md \
  .dev/session/runbook-upgrade/_bus/24.astrobley.return.md
  exit 0 · the pre-POINT identity-resolution draft and Cartan POINT 24 remain untracked;
  RETURN 24 is the only new cycle-24 writer path
```

## 3. Evidence paths

- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.point.md` — corrective
  authority and provenance ruling; SHA-256 before work
  `4573ed8c5551320528ecf4d0b7ec1d155bbdd2fc8b2ea356706581e6c5115a88`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/24.astrobley.return.md` — this complete
  candidate and direct execution report.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.astrobley.return.md` — untrusted input,
  full-file SHA-256 `53cef527bf0160e283c362c2b764885a13c98085708f3a8645454135b1ddda1d`.
- `/home/hruzam/ia-sync/.dev/session/runbook-upgrade/_bus/23.cartan-csharp.verdict.md` — preserved
  inoperative receipt, SHA-256 `c6b45986b3856c4bfb24947edd88941d044b0001cb21fa303208129f87285848`.
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — canonical six-field RETURN envelope and
  evidence-before-verdict boundary.
- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — Manifest, sole STATUS ownership and
  **On gate closure**.
- `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md` — navigator/writer
  separation, crossed witness and operator transport.
- `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` — full-session versus subagent
  distinction, recovery, late artifacts and head-owned join.
- `/home/hruzam/reposoma/raw.guides/guide-writing/GUIDE.md` — chapter sip unit, light header,
  Manifest registration and point-never-copy law.

## 4. Mismatches discovered

1. Cycle 23 used a spawned Astrobley child although POINT 23 prohibited native-subagent
   substitution. Its root then wrote the Cartan VERDICT/STATUS transition. Cycle 24 preserves those
   bytes as failure evidence and does not inherit their acceptance.
2. RETURN 23 claimed POINT 23 explicitly requested the Codex role's `implementation_return` fields.
   The pinned POINT instead requested a six-field RETURN and cited the BUS GUIDE; POINT 24 removes
   any remaining ambiguity by spelling the six canonical headings. This RETURN follows them.
3. This cycle executed directly in Majkee's operator-designated Astrobley-posture window, but the
   window was not originally launched with an Astrobley profile because that profile does not yet
   exist. The designation plus direct execution satisfies POINT 24's case-recovery instruction; it
   does not prove the proposed future profile mechanism or alter the repository's default identity.
4. The cycle-23 candidate specified UTF-8 and line-oriented values but left BOM, CRLF, final-newline
   and other ASCII-control handling implicit. The complete candidate above pins those bytes without
   changing its identity, authority or lifecycle design.
5. Two post-write orchestration wrappers failed before invoking the shell because literal Markdown
   backticks terminated the JavaScript command container. The first shell-executed harness then found
   a defect in its prose needle: it expected `same writer` while the candidate says `single writer`.
   Correcting only that needle produced the complete PASS above; the RETURN candidate was unchanged.

## 5. Recommended next task

The original `cartan-csharp` head independently verifies this RETURN, its sole write boundary, direct
execution disclosure, exact extracted bytes and fresh harness results. If its VERDICT accepts cycle
24, Majkee relays the exact candidate hash to Trajectory for client-fit review. No client or board
work should unfreeze before that review and Majkee's gavel.

## 6. Remaining uncertainty

- The disk artifact cannot independently prove which UI window or runtime process authored it.
  Majkee observed/designated the window; Cartan must preserve that provenance as operator-relayed
  evidence rather than upgrading it from the filename.
- No Astrobley top-level profile exists or was tested. This cycle proves direct operator-designated
  posture and write separation only.
- The proposed grammar has been exercised by an in-memory harness, not by Trajectory's client or a
  live board. Parser fit, UX and transport behavior remain unverified.
- A 128-bit random ID is collision-resistant rather than mathematically unique; exclusive creation
  and fresh-ID retry remain load-bearing.
- Guide installation, Manifest editing, client work, board creation, runtime proofs and canon gavel
  remain outside this RETURN and unperformed.
