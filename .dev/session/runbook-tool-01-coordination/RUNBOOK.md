# RUNBOOK — runbook-tool-01-coordination

```yaml
goal: >-
  Qualify a lighter coordination workflow and an office runbook-tool attention view,
  using runbook-upgrade observations without repeating its expensive head ceremony.
gate: >-
  After codex-identity-resolution is closed with evidence, majkee accepts one bounded
  office pilot whose attention view correctly exposes declared outstanding work without
  false completion or duplicate dispatch, with witnessed Y/N decisions on bed-local
  entry cards, economical helpers and Vara's bounded fit recorded in the closing VERDICT.
participant_0:
  seat: cartan-coordination
  runtime: codex
  carriage: operator-selected; Terra high recommended for the working head
  instrument: resident
  role: cSharp and sole STATUS owner; escalate consequential design instead of retaining a premium model for routine carriage
participant_1:
  seat: majkee
  role: operator, gavel, session activation and observed-use acceptance
participant_2:
  seat: trajectory-tool
  runtime: claude
  carriage: operator-selected
  role: existing session-browser owner; bounded tool design and implementation
participant_3:
  seat: atlas-ui
  runtime: claude
  carriage: operator-selected
  role: existing harness builder; entry-card and Vara contract assessment, not a second head
participant_4:
  seat: oraculum-review
  runtime: claude
  carriage: operator-selected
  role: independent consequential-boundary and final pilot witness, called when evidence is ready
status_owner: cartan-coordination
head_note: >-
  cSharp — cartan-coordination owns this launcher and stays as navigator and STATUS owner
  through the pilot; bounded builders do the body of work and return evidence.
schema_note: >-
  runbook/GUIDE.md, status/GUIDE.md and the gaveled cSharp/fanout chapters;
  token-economy and cross-vendor-seat remain DRAFT references, not newly promoted law.
```

## Entry and authority

This is the successor pilot, not a reopening of runbook-upgrade's accepted limited STOP.
It does not inherit the other beds' state ownership. Planning Cartan authors this initial
package; majkee appoints the continuing Cartan to the same declared head seat. No new
global persona is created. Recommended carriage is not an attestation of this author's model.

First read current STATUS, then this launcher and
`/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/raw/seed.csharp.2026-09-16.md`.
Shared law remains at `~/reposoma/raw.guides/{runbook,status,bus}/GUIDE.md` and
`runbook/res/{csharp-head-protocol,fanout-turns,presence-board}.md`.
Absolute paths in the seed resolve those documents. Do not load the old whole BUS by default.

Only this bed and its one pulse router row are authorized by this planning cut. Future
source edits, runtime experiments and deployment require the exact scoped proposal below.
No AGENTS.md, CLAUDE.md, agent definition or fixture is installed by opening this bed.

## Ordered work — one pilot outcome, earlier prerequisites first

0. **Finish identity first, in its existing bed.**
   `/home/hruzam/ia-sync/.dev/session/codex-identity-resolution/STATUS.md` owns that work.
   Its owner refreshes the existing Stage A brief only where current evidence requires,
   obtains the outstanding gavel, and follows its author/deploy/fresh-proof gates. This
   head consumes the closing evidence, never copies its doing-state or declares it closed.
   An accepted STOP there is not permission to bypass this dependency; return to majkee
   for a successor scope decision. Native mixed-model children do not technically require
   the sovereign Astrobley profile; this ordering is majkee's explicit dependency choice.
1. **Design and preregister the smallest trial.**
   Consume the targeting preflight in raw/. Agree exact source ownership with Trajectory;
   existing `runbook-tool-00` obligations are not automatically transferred. Atlas assesses
   entry cards and Vara. Name the fixtures, candidate helper/model, comparison baseline,
   observation fields, write paths and stopping condition. Present one scoped package to
   majkee, including any required canon decision; do not invent a general roster framework.
2. **Build only the approved delta.**
   Start from existing `zsh/session/runbook.py` live refresh, cycle view and copy actions.
   Prefer a deterministic read-only attention view over an LLM polling loop. Build/code
   belongs to Trajectory or one explicitly assigned implementer, not both on the same files.
   Bed-local card trials stay in explicitly approved isolated fixture directories; never
   put experimental AGENTS/CLAUDE instructions at `.dev/` or `.dev/session/` scope.
3. **Run bounded capability and entry tests.**
   Qualify one economical candidate against one stronger baseline before widening the
   matrix. Haiku/Luna are narrow-task candidates; Sonnet/Terra are escalation candidates,
   not certified equivalents. Source/live/model/topology evidence and actual behavior are
   separate. Entry-card verdict is Y/N, not assumed Y because a file exists.
4. **Witness, operator use and disposition.**
   Oraculum reviews the consequential contract and actual pilot evidence once ready.
   Majkee tries the approved office tool cut after scoped deployment approval. Record
   results, limits, Y/N decisions and a short experience transfer. No repeated probe loop:
   one bounded correction/retest requires a named hypothesis; persistent failure goes to
   STOP/re-scope, not a broader unbounded experiment. Promote keepers before retirement.

## Pilot acceptance and discrimination

- Tool reports facts and uncertainty, never infers delivery, liveness or successful closure
  from filenames, time, board presence or an arbitrary ACCEPT. Unsupported legacy shapes
  render explicitly unclassified with a path; they are not silently healthy.
- Fixtures cover: new/partially written RETURN; landed terminal VERDICT with stale STATUS;
  REVISE plus corrective cycle; wrong seat/cycle/path; unknown execution; informational
  peer POINT owing no RETURN; duplicate/changed receipt; stopped bed with retained files.
- Checks cover only declared obligations. Questions left implicit in prose cannot be
  certified as “nothing forgotten.” The head must surface consequential unclassified items.
- Notification v0 is operator-facing, on the running tool, with artifact links and optional
  deduplicated alerts. No daemon, auto-wake, auto-send, retry, STATUS write or shared unread state.
- Entry test contrasts root launch, bed launch and explicit-path fallback; includes a child
  starting at the same folder level and a reviewer that must not inherit head authority.
  Record actual working directory, loaded guidance and behavior. Same folder is a condition
  to test, not proof all runtimes/fork modes load identical instructions.
- Keep model request, effective configuration, observed runtime metadata and self-report
  separate. Missing metadata is unknown, not a passed model check. A child never becomes an
  independent full-session witness merely because it has another model or a separate thread.
- Measure correct next actions, missed declared obligations, false alerts, unsafe duplicate
  dispatch, operator interventions, duration and usage where available. No savings percentage
  or general reliability claim from a single bounded pilot. No safety miss is averaged away.

## Team and ownership — activate only when needed

| Seat | Owns | Does not own |
|---|---|---|
| Identity Cartan in its existing window | Its existing identity bed and approved cut | This pilot's STATUS |
| cartan-coordination, economical Cartan/Medusa | This bed's navigation, integration and STATUS | Canon gavel; another bed's state |
| trajectory-tool | Approved `zsh/session/` tool delta and tests | Harness cards or automatic transport |
| atlas-ui | Exact approved harness proposal/cut, assessed with native Codex support as needed | Tool edits or a second STATUS |
| oraculum-review | Its own independent review receipt | Fixing the candidate or replacing STATUS |
| Native Codex children | A brief's disjoint files/read scope; return to parent | Sovereign seat identity or head authority |

Reuse global Codex `implementer`, `researcher`, `architect`, `harness_builder` and `verifier`
as appropriate; use a built-in worker for a permitted explicit Luna selection. Never fork
the Claude roster into project-local Codex roles. Astrobley/Sol is for difficult execution,
not the default messenger. Vara is a candidate pre-routed walker, not the BUS itself.

## prompt-0 — continuing cartan-coordination (after dependency closure)

Read `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/STATUS.md`,
`/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/RUNBOOK.md`,
`/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/raw/seed.csharp.2026-09-16.md`
and `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/raw/codex-targeting.preflight.2026-09-16.md`.
Verify the identity bed's actual closing evidence first. If missing, report that one boundary
and park; do not reproduce its work. Resolve the current host, tree, board and tool owner.
Prepare the smallest stage-1 proposal and operator team assignment; no source edits or
test-agent launches before its exact scope is approved. Keep STATUS a short present snapshot.
Use strong help only for consequential uncertainty. Never spawn a proxy for a sovereign seat.

## prompt-1 — majkee's immediate relay to identity Cartan

Use the already-running identity window if one exists; otherwise wake one unprofiled Cartan
in `/home/hruzam/ia-sync` on the existing bed's recommended Sol/high carriage. Do not launch
`--profile astrobley` yet: its source and deployed profile were absent at this preflight.
Paste this task (not a command to run in a shell):

> Resume `/home/hruzam/ia-sync/.dev/session/codex-identity-resolution` as its existing Cartan
> owner. Read STATUS first, then RUNBOOK and `raw/brief.stage-a.identity-decisions.2026-09-09.md`.
> Majkee wants this prerequisite finished before runbook-tool-01-coordination begins. Read
> `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/raw/codex-targeting.preflight.2026-09-16.md`
> as advisory input. Reconcile the current frame and any actual later gavel. If the five
> decisions and exact AGENTS paragraph remain ungaveled, give me one concise decision card;
> do not infer approval from this wake. If already gaveled, proceed only within your existing
> stages and authority. Explain root/profile/child identity separately from model selection.
> Preserve the pilot bed and other work; do not spawn a same-seat substitute or create another
> identity bed. Leave your result in your own STATUS and evidence, with one next operator action.

## prompt-2 — trajectory-tool (only when assigned)

Read `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/RUNBOOK.md` and STATUS.md
in that bed, then the exact POINT majkee relays. Without a matching assignment, do not start.
Reconcile your existing runbook-tool-00 ownership before proposing the tool delta. Use current
`/home/hruzam/ia-sync/zsh/session/runbook.py`, not the old feature checklist, as the baseline.
You are not alone in this worktree: preserve peer edits. Return the scoped design/diff/checks
to the POINT's return_to; no STATUS, harness, deployment or transport authority is implied.

## prompt-3 — atlas-ui (only when assigned)

Read `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/RUNBOOK.md`, its STATUS.md
and raw/codex-targeting.preflight.2026-09-16.md, then the exact POINT majkee relays. Assess
Vara's current contract and the smallest entry-card Y/N test, including same-folder children
and non-head visitors. Preserve peer work. Proposal first: no new card, activated deferred
feature or runtime write before the exact harness scope is approved. Return only as assigned.

## prompt-4 — oraculum-review (only when evidence is ready)

Read `/home/hruzam/ia-sync/.dev/session/runbook-tool-01-coordination/RUNBOOK.md`, its STATUS.md
and the exact review POINT majkee relays. Independently check the candidate diff and decisive
fixtures, especially false completion, duplicate dispatch and ownership. Do not repair the
artifact or write STATUS. Preserve uncertain model/topology claims as uncertain; return one
bounded review in the assigned path. This is not another audit of every predecessor receipt.

## Known holds

- Majkee activates independent sessions and owns the gavel. No automatic mesh, live-session
  steering or relay connector is authorized. Nablarva's current relay work remains its owners'.
- The existing cSharp/fanout law applies if this head opens full-session branches. Use ordinary
  POINT/RETURN/VERDICT, one turn and one STATUS owner; no empty BUS is pre-created at planning.
- No global/system card edits, parent-directory experiment cards, new model aliases, generic
  profiles, cloned rosters or canon changes under a “tool improvement” label.
- No stage, commit, push, deploy, home carry, detach or prune in this planning cut. Subsequent
  Git scope should be agreed once; re-confirm only when its scope/risk/preconditions change.
- Do not repair stale/invalid peer RUNBOOK/STATUS files here. Surface them to their owner.
- Host-local identities, histories, auth and endpoint handles do not enter portable source.
- `runbook-tool-00` home/examples/retirement debt and incarnations work remain separately owned.
  Capability economy draft thresholds are not newly gaveled acceptance criteria.
