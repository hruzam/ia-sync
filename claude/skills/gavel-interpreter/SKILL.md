---
name: gavel-interpreter
description: Invoke as /gavel-interpreter when an operator gavel has just landed. I interpret the decision into the temple gavel bed per the format law that raw.therapy/README.md owns, and append it as the next G-NN. Preproduction collector — capture only; ID-wiring protocol and frequency rule stay tabled. NOT /gavel-ballot (that clears a pending queue to flag.md).
---

I interpret a landed gavel into the bed. A gavel is a decision folded into a question that still
reads true after the path it was about is forgotten.

## Where the truth lives (I read it, I do not restate it)

- **Format law — kinds — wiring:** `raw.therapy/README.md` → `## Gavels`. It owns the rule
  (never name the path/task · Socratic-question OR seven associations · therapy-origin vs
  independent · frequency). Single source of truth. I follow it; I never copy it here.
- **The bed:** `raw.therapy/gavels/gavels.md` — the entries and the next `G-NN`.

## On invoke

1. Read `raw.therapy/README.md` `## Gavels` for the current format law.
2. Read the bed → highest `G-NN` + 1 = my id.
3. **Interpret** the landed decision into that format — I draft, @majkee reshapes before it locks.
4. Classify + tag per the README's two-kinds rule; append to the bed (oldest at top).
5. Surface the draft for reshape. I interpret and record — I do not gavel. @majkee is the bed-maker.

## Scope (preproduction)

Capture + interpret only. The ID-wiring protocol and the frequency/cadence rule (both defined in
`raw.therapy/README.md` `## Gavels`) stay tabled with the full gavel-format skill until Nabla
settles the mental-map seed (`nabla-lab/drafts/mental-map-collective-mind.md`). I am the collector
that gives that later work real data to design from.
