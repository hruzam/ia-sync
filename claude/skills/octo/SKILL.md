---
name: octo
description: Invoke as /octo. The octopus regime card (EXPERIMENTAL) — a pluggable head over persistent tentacles held on line via SendMessage, blind drafters, hydrated cables, two buffers (stream.md manned · program.pulse.md batch), gavel visibility, circuit breakers. The head is a seat, not a name — whoever the operator launches wears it. Run at session start to see what's on the line.
---

I am the octopus regime card. **Big brain, thin state · long tentacles, deep context.**
Status: EXPERIMENTAL — proving ground: applications-in-common. The regime is the
invariant; heads are pluggable.

## Shape

```
HEAD       a seat, not a name — whoever the operator launches wears it:
           Medusa (project sessions) · Oraculum/Houston (strategy arcs) ·
           Flight (tactical). No head-finding mechanism: the operator's
           launch IS the selection.
TENTACLES  held ON LINE across loops (SendMessage) — spawn once, probe, continue:
           Eagle    pathfinder · canon-holder · task buffer (contamination shield —
                    the head stays blind to legacy wiring in early phase)
           coders   Delta · Vector · Trajectory — blind drafters, one clean brief each
           Assay    fresh-eyes PASS/FAIL gate (stands in for the operator's eyes)
CABLES     !-hydrated skills — briefing assembled before the first token.
           Truth lives in files (README.md · stream.md) · history sinks to journal.
```

## Buffers — two regimes, one board

- **MANNED** (operator in the chair): buffer = `.dev/session/<slug>/stream.md`
  (TASK · LIVE · GAVELS · CLOSE). Direct head→coder, no gate — the operator is the eyes.
- **UNMANNED batch**: buffer = `program.pulse.md`, Vara flat bus, @Assay gate per line.
  Authority: decision 0012 — the law for batches, untouched by this card.

## ⚠ Bazooka rule — don't deploy the full loop for a fly

The full loop (buffer · bus · gate) earns its cost only on a batch of non-trivial work.
Small task, operator present → dispatch a coder directly. No buffer, no ceremony.

## Gavel visibility

Gaveling events park in the stream's GAVELS section — never out of sight range.
At close, gavels still in force carry into CLOSE `gates:`. A gavel leaves the stream
only by canonization (flag / decisions ledger) — not by scrolling away.

## Probes — the wanderer test

Every tentacle gets a scope card: touch this · never that · report in this shape.
Probe via SendMessage; an answer off the scope card = lost → recall, no debate.

## Rules I do not cross (0012 L8)

- `blocked` waits for brain/human — never auto-proceeds on a guess.
- Never auto-commit to `core` — load-bearing takes a human gate.
- Tentacles do not spawn tentacles — the head routes.

*Octo kinds: if terrain shows distinct head-shapes need distinct cards, split then —
not before. One card until repeated pain justifies more.*
