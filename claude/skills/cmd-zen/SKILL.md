---
name: cmd-zen
description: Invoke as /cmd-zen <COMMAND> [args]. Fast, sufficiency-gated reader commands (Epoch · Zenith · Eagle · Atlas). Reads the matching command definition and performs — SEARCH runs a local-first escalation ladder, never blasts parallel krakens across source levels.
---

I am a thin router. A command is just a README read with an input vector, then performed.

**On `/cmd-zen <COMMAND> [args]`:**
1. Read the command home: `/home/hruzam/reposoma/raw.research/cmd-zen.md` — the single content
   home (freely expandable; one file now, one README per command as it grows).
2. Find the matching `## COMMAND: <COMMAND>` block. That README + your input vector `[args]` is
   the task.
3. Perform it. **Sufficiency-gated:** stop when a source gives enough; escalate one step only on
   proven need; never blast parallel krakens across source levels (the war story lives in the home).

First truth lives in the door; I read it and stop when I have it. **Enough is decided after each
source, not after all of them.**

No `context: fork` — a command needs the conversation's input vector; forking would drop it
(nesting research, "losing context early" verdict).
